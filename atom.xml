<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>个人随想</title>
  
  <subtitle>fly97&#39;s Blogs</subtitle>
  <link href="https://fly97.cn/atom.xml" rel="self"/>
  
  <link href="https://fly97.cn/"/>
  <updated>2022-05-16T15:39:16.818Z</updated>
  <id>https://fly97.cn/</id>
  
  <author>
    <name>fly97</name>
    
  </author>
  
  <generator uri="https://hexo.io/">Hexo</generator>
  
  <entry>
    <title>Kubernetes集群实践（四）| 如何在集群中使用网络存储系统NFS</title>
    <link href="https://fly97.cn/p/how-to-use-nfs-in-k8s/"/>
    <id>https://fly97.cn/p/how-to-use-nfs-in-k8s/</id>
    <published>2022-05-16T15:39:16.818Z</published>
    <updated>2022-05-16T15:39:16.818Z</updated>
    
    <content type="html"><![CDATA[<p>由于需要在Pods中共享文件，直接挂载到宿主机肯定不行的。因此需要有一套网络存储系统。</p><p>本文主要介绍如何在K8s中使用网络存储系统NFS。</p><p>关键词：<strong>k8s</strong></p><a id="more"></a><h3 id="准备工作">准备工作</h3><ul><li>安装好Kubernetes的集群</li><li>Ubuntu 20.04 LTS</li></ul><h3 id="安装服务端">安装服务端</h3><p>一般在集群外部安装服务端，这样可以降低集群的负载。这里只是测试用，因此<strong>在master节点</strong>上安装服务端。</p><h4 id="安装">安装</h4><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">sudo apt-get install nfs-kernel-server</span><br></pre></td></tr></table></figure><h4 id="创建要共享的目录">创建要共享的目录</h4><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">mkdir /data/nfsshare -p</span><br></pre></td></tr></table></figure><h4 id="编辑nfs配置">编辑nfs配置</h4><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">sudo vim /etc/exports</span><br></pre></td></tr></table></figure><p><img data-src="image-20220512133120131.png" /></p><p>其中IP是允许访问的网段，</p><ul><li>rw：访问到此目录的服务器都具备读写权限</li><li>sync：数据同步写入内存和硬盘，与其相对的是<code>async</code>（只在必要的时候写入内存和硬盘）</li><li>all_squash：共享文件的UID和GID映射匿名用户anonymous，适合公用目录</li><li>no_subtree_check：不检查父目录的权限</li><li>anonuid=1000：指定nfs服务器<code>/etc/passwd</code>文件中匿名用户的UID</li><li>anongid=1000：指定nfs服务器/<code>etc/passwd</code>文件中匿名用户的GID</li></ul><p>使用映射匿名用户UID和GID，同时设置匿名用户的UID和GID，可以避免<strong>NFS服务器的权限问题。</strong></p><h4 id="载入配置">载入配置</h4><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">sudo exportfs -rv</span><br></pre></td></tr></table></figure><h4 id="启动服务">启动服务</h4><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">sudo systemctl <span class="built_in">enable</span> nfs-kernel-server --now </span><br></pre></td></tr></table></figure><h4 id="服务检查">服务检查</h4><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">cat /var/lib/nfs/etab</span><br></pre></td></tr></table></figure><p><img data-src="image-20220512112029175.png" /></p><p>查看NFS共享情况</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="variable">$showmount</span> -e 192.168.15.201</span><br><span class="line">/home/wf09/nfsfiles 192.168.15.0/24</span><br></pre></td></tr></table></figure><p>其中IP是nfs-server的IP。</p><h3 id="安装客户端">安装客户端</h3><p>集群中需要使用NFS存储的机器<strong>都需要安装客户端！</strong>不然使用NFS作为存储的Pod就会一直提示<code>Init:0/1</code></p><p><img data-src="image-20220512112529311.png" /></p><p>这里使用之前配置好的xcall脚本去进行调用。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">sudo apt install nfs-common -y<span class="string">&quot;</span></span><br></pre></td></tr></table></figure><p><img data-src="image-20220512112427702.png" /></p><h3 id="使用nfs作为存储卷">使用nfs作为存储卷</h3><p>使用NFS作为存储方式有以下几种</p><h4 id="在deploymentstatefulset中直接使用">在deployment/statefulset中直接使用</h4><h5 id="创建一个deployment资源清单">创建一个deployment资源清单</h5><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">apiVersion:</span> <span class="string">apps/v1</span></span><br><span class="line"><span class="attr">kind:</span> <span class="string">Deployment</span></span><br><span class="line"><span class="attr">metadata:</span></span><br><span class="line">  <span class="attr">name:</span> <span class="string">backend-deployment</span></span><br><span class="line"><span class="attr">spec:</span></span><br><span class="line">  <span class="attr">selector:</span></span><br><span class="line">    <span class="attr">matchLabels:</span></span><br><span class="line">      <span class="attr">app:</span> <span class="string">backend</span></span><br><span class="line">  <span class="attr">replicas:</span> <span class="number">3</span></span><br><span class="line">  <span class="attr">template:</span></span><br><span class="line">    <span class="attr">metadata:</span></span><br><span class="line">      <span class="attr">labels:</span></span><br><span class="line">        <span class="attr">app:</span> <span class="string">backend</span></span><br><span class="line">    <span class="attr">spec:</span></span><br><span class="line">      <span class="attr">volumes:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">jar</span></span><br><span class="line">        <span class="attr">emptyDir:</span> &#123;&#125;</span><br><span class="line">      <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">nfsfiles</span>              <span class="comment"># 卷名称</span></span><br><span class="line">        <span class="attr">nfs:</span>                        <span class="comment"># 使用NFS作为存储卷</span></span><br><span class="line">          <span class="attr">server:</span> <span class="number">192.168</span><span class="number">.15</span><span class="number">.201</span>    <span class="comment"># NFS服务地址</span></span><br><span class="line">          <span class="attr">path:</span> <span class="string">/home/wf09/nfsfiles</span> <span class="comment"># NFS服务器共享的目录</span></span><br><span class="line">          <span class="attr">readOnly:</span> <span class="literal">false</span>           <span class="comment"># 是否为只读</span></span><br><span class="line">      <span class="attr">initContainers:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">tool</span></span><br><span class="line">        <span class="attr">image:</span> <span class="string">harbor.local/zheshiyou/backend</span></span><br><span class="line">        <span class="attr">command:</span> [<span class="string">&quot;cp&quot;</span>, <span class="string">&quot;/app.jar&quot;</span>, <span class="string">&quot;/jar&quot;</span>]</span><br><span class="line">        <span class="attr">volumeMounts:</span></span><br><span class="line">        <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">jar</span></span><br><span class="line">          <span class="attr">mountPath:</span> <span class="string">/jar</span></span><br><span class="line">      <span class="attr">containers:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">backend</span></span><br><span class="line">        <span class="attr">image:</span> <span class="string">harbor.local/zheshiyou/jre:latest</span></span><br><span class="line">        <span class="attr">command:</span> [<span class="string">&quot;java&quot;</span>, <span class="string">&quot;-jar&quot;</span>, <span class="string">&quot;/jar/app.jar&quot;</span>]</span><br><span class="line">        <span class="comment">#command: [&quot;bash&quot;,&quot;-c&quot;, &quot;while true ; do continue ; done&quot;]</span></span><br><span class="line">        <span class="attr">imagePullPolicy:</span> <span class="string">Always</span></span><br><span class="line">        <span class="attr">ports:</span></span><br><span class="line">        <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">server</span></span><br><span class="line">          <span class="attr">containerPort:</span> <span class="number">8080</span></span><br><span class="line">        <span class="attr">volumeMounts:</span></span><br><span class="line">        <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">jar</span></span><br><span class="line">          <span class="attr">mountPath:</span> <span class="string">/jar</span></span><br><span class="line">        <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">nfsfiles</span>                <span class="comment"># 卷挂载到容器中的目录</span></span><br><span class="line">          <span class="attr">mountPath:</span> <span class="string">/share</span>             <span class="comment"># 卷名称</span></span><br></pre></td></tr></table></figure><p>上面的示例定义在资源配置文件deployment.yaml中，其中的Pod资源拥有一个关联至NFS服务器192.168.15.201的存储卷，backend容器将其挂载到容器中的/share目录上，它是backend容器中的需要持久化的目录。</p><h5 id="创建deployment并查看配置信息">创建deployment并查看配置信息</h5><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">kubectl apply -f deployment.yml</span><br></pre></td></tr></table></figure><p>查看配置信息</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">kubectl get pods -o wide -l app=backend</span><br></pre></td></tr></table></figure><p><img data-src="image-20220512113720297.png" /></p><p>查看deployment中某一个pod的调度信息</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">kubectl describe pods/backend-deployment-7b44ccc689-bvdm6</span><br></pre></td></tr></table></figure><p>可知调度成功</p><p><img data-src="image-20220512114009179.png" /></p><h4 id="查看容器挂载情况">查看容器挂载情况</h4><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">kubectl <span class="built_in">exec</span> -it backend-deployment-7b44ccc689-bvdm6 -- df -hT</span><br></pre></td></tr></table></figure><p><img data-src="image-20220512114335873.png" /></p><h4 id="测试数据持久化访问">测试数据持久化访问</h4><p>模拟正常资源调度情况：在某一Pod存放文件，然后登录到其他的Pod查看文件是否共享成功。</p><ol type="1"><li><p>登录到某一Pod</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">kubectl <span class="built_in">exec</span> -ti backend-deployment-7b44ccc689-bvdm6 bash</span><br></pre></td></tr></table></figure></li><li><p>生成10M文件，模拟正常PDF大小</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">dd <span class="keyword">if</span>=/dev/zero of=<span class="built_in">test</span> bs=1M count=10</span><br></pre></td></tr></table></figure></li><li><p>登录到其他Pod，查看文件是否存在</p><p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">kubectl <span class="built_in">exec</span> backend-deployment-7b44ccc689-l949v -- ls -lh /share</span><br></pre></td></tr></table></figure></p></li></ol><p><img data-src="image-20220512133823379.png" /></p><h3 id="附录">附录</h3><p>NFS共享参数说明</p><table><thead><tr class="header"><th>参数</th><th>说明</th></tr></thead><tbody><tr class="odd"><td>ro</td><td>只读访问</td></tr><tr class="even"><td>rw</td><td>读写访问</td></tr><tr class="odd"><td>sync</td><td>同步写入，同时将数据写入内存和硬盘中，保证不丢失数据</td></tr><tr class="even"><td>async</td><td>异步写入，优先将数据写入内存，然后再写入到硬盘，可能会丢失数据</td></tr><tr class="odd"><td>secure</td><td>nfs通过1024以下的安全TCP端口发送</td></tr><tr class="even"><td>insecure</td><td>nfs通过1024以上的端口发送</td></tr><tr class="odd"><td>wdelay</td><td>如果多个用户要写入NFS目录，则归组写入<strong>（默认）</strong></td></tr><tr class="even"><td>no_wdelay</td><td>如果多个用户要写入NFS目录，则立即写入。当使用async时，无需此设置</td></tr><tr class="odd"><td>hide</td><td>不共享nfs目录的子目录</td></tr><tr class="even"><td>no_hide</td><td>共享nfs目录的子目录</td></tr><tr class="odd"><td>subtree_check</td><td>如果共享/usr/bin之类的子目录时，强制nfs检查父目录的权限<strong>（默认）</strong></td></tr><tr class="even"><td>no_subtree_check</td><td>不检查父目录的权限</td></tr><tr class="odd"><td>all_squash</td><td>共享文件的UID和GID映射匿名用户anonymous，适合公用目录</td></tr><tr class="even"><td>no_all_squash</td><td>保留共享文件的UID和GID<strong>（默认）</strong></td></tr><tr class="odd"><td>root_squash</td><td>root用户的所有请求映射成如anonymous用户一样的权限<strong>（默认）</strong></td></tr><tr class="even"><td>no_root_squash</td><td>root用户具有根目录的完全管理访问权限</td></tr><tr class="odd"><td>anonuid=xxx</td><td>指定nfs服务器/etc/passwd文件中匿名用户的UID</td></tr><tr class="even"><td>anongid=xxx</td><td>指定nfs服务器/etc/passwd文件中匿名用户的GID</td></tr></tbody></table>]]></content>
    
    
    <summary type="html">&lt;p&gt;由于需要在Pods中共享文件，直接挂载到宿主机肯定不行的。因此需要有一套网络存储系统。&lt;/p&gt;
&lt;p&gt;本文主要介绍如何在K8s中使用网络存储系统NFS。&lt;/p&gt;
&lt;p&gt;关键词：&lt;strong&gt;k8s&lt;/strong&gt;&lt;/p&gt;</summary>
    
    
    
    
    <category term="k8s" scheme="https://fly97.cn/tags/k8s/"/>
    
  </entry>
  
  <entry>
    <title>大话数据结构（二） | 栈和队列</title>
    <link href="https://fly97.cn/p/stack-and-ground/"/>
    <id>https://fly97.cn/p/stack-and-ground/</id>
    <published>2022-05-15T01:41:00.000Z</published>
    <updated>2022-05-15T01:41:00.000Z</updated>
    
    <content type="html"><![CDATA[<p>本文主要介绍栈和队列。</p><p><strong>关键词：数据结构</strong></p><a id="more"></a><h3 id="栈的定义">栈的定义</h3><p>栈（stack）是限定仅仅在栈尾进行插入和删除操作的线性表。</p><ul><li>允许插入和删除的一端称为栈顶</li><li>另一端被称为栈底</li><li>不含任何元素的栈称为空栈</li><li>栈被称为后进先出（LIFO，Last In First Out）的线性表</li><li>栈的插入操作叫做进栈，栈的删除操作叫做出栈</li></ul><h4 id="进栈出栈变化形式">进栈出栈变化形式</h4><p><strong>最先进栈的元素不一定最后出栈。</strong></p><p>栈对线性表的插入和删除的位置进行了限制，并没有对元素进出的时间进行限制。在不是所有元素都进栈的情况下事先进入的元素也可以出栈，只要<strong>保证栈顶元素出栈</strong>即可。</p><p>举例：假设有三个元素1、2、3依次进栈，出栈次序如何？</p><ol type="1"><li>1、2、3进，3、2、1出。出栈次序为321。</li><li>1进，1出，2进，2出，3进，3出。此时出栈次序为123。</li><li>1进，2进，2出，1出，3进，3出。此时出栈次序为213。</li><li>1进，1出，2进，3进，3出，2出。此时出栈次序为132。</li><li>1进，2进，2出，3进，3出，1出。此时出栈次序为231。</li></ol><p>不可能出现312这样的答案，因为进栈的顺序是123，因此<strong>2一定先于1出栈</strong>。</p><h3 id="抽象数据类型">抽象数据类型</h3><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line">DATA</span><br><span class="line">元素具有相同的类型，相邻具有前驱和后继的关系</span><br><span class="line">Operation</span><br><span class="line">InitStack(*S): 初始化操作，建立一个空栈S</span><br><span class="line">DestroyStack(*S): 若栈存在，则销毁它</span><br><span class="line">  ClearStack(*S): 将栈清空</span><br><span class="line">  StackEmpty(*S): 若栈为空，返回True；否则返回False</span><br><span class="line">  GetTop(S, *e): 若栈存在且非空，用e返回S的栈顶元素</span><br><span class="line">  Push(*S, e): 若栈存在，插入新元素e到栈并成为栈顶元素</span><br><span class="line">  Pop(*S, *e): 删除栈S中栈顶元素, 并用e返回其值</span><br><span class="line">  StackLength(S): 返回栈S的元素个数</span><br><span class="line">endADT</span><br></pre></td></tr></table></figure>]]></content>
    
    
    <summary type="html">&lt;p&gt;本文主要介绍栈和队列。&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;关键词：数据结构&lt;/strong&gt;&lt;/p&gt;</summary>
    
    
    
    
    <category term="数据结构" scheme="https://fly97.cn/tags/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84/"/>
    
  </entry>
  
  <entry>
    <title>初探docker（四）| docker容器退出码</title>
    <link href="https://fly97.cn/p/docker-tutorial-04/"/>
    <id>https://fly97.cn/p/docker-tutorial-04/</id>
    <published>2022-05-14T01:41:00.000Z</published>
    <updated>2022-05-14T01:41:00.000Z</updated>
    
    <content type="html"><![CDATA[<p>本文主要介绍Docker容器退出状态码。</p><p><strong>关键词：Docker</strong></p><a id="more"></a><h3 id="常见容器退出码解释">常见容器退出码解释</h3><h4 id="exit-0">Exit 0</h4><ul><li>退出状态码表示特定容器没有附加前台进程</li><li>该状态码表示<strong>容器正常退出</strong></li><li>某些情况下，容器中的进程成功捕获到了外部收到的信号，并成功进程了处理，此时容器退出的状态码也是0。</li></ul><h4 id="exit-1">Exit 1</h4><ul><li>程序错误，例如Dockerfile引用了不存在的文件，如entrypoint引用了错误的包；</li><li>程序错误可以很简单比如除以0，也可很复杂比如空引用和其他程序crash；</li></ul><h4 id="exit-137">Exit 137</h4><ul><li>表明容器收到了<code>SIGKILL</code>信号，表示进程被杀掉，对应<code>kill -9</code>；</li><li>引发<code>SIGKILL</code>的通常是用户手动执行的<code>docker kill</code>或者由docker守护进程发起的；</li><li>在k8s中，如果Pod的limit资源设置比较小，引发运行内存不足导致 <code>OOMKillered</code>，此时<code>state</code>的 <code>OOMKilled</code>设置为<code>true</code>。通过<code>dmesg -T</code>也可看到oom日志</li></ul><h4 id="exit-139">Exit 139</h4><ul><li>表明容器收到了<code>SIGENV</code>信号，表示无效的内存引用，对应<code>kill -11</code>；</li><li>一般是代码或者基础镜像有问题</li></ul><h4 id="exit-143">Exit 143</h4><ul><li>表明容器收到了 <code>SIGTERM</code>信号，表示终端被关闭，对应<code>kill -15</code>；</li><li>引发<code>SIGTERM</code>一般对应用户手动执行<code>docker stop</code>或者由docker守护进程发起的；</li><li>有时<code>docker stop</code>命令也可能会导致<strong>Exit 137</strong>。在发生代码无法处理<code>SIGTERM</code>的情况下，docker守护进程等待10秒钟后发出<code>SIGKILL</code>强制退出。</li></ul><h4 id="不常用的一些-exit-code">不常用的一些 Exit Code</h4><ul><li><code>Exit 126</code>: 权限问题或命令不可执行</li><li><code>Exit 127</code>: Shell脚本中可能出现错字且字符无法识别的情况</li><li><code>Exit 1 或 255</code>：因为很多程序员写异常退出时习惯用 exit(1) 或 exit(-1)，-1 会根据转换规则转成 255。这个一般是自定义 code，要看具体逻辑。</li></ul><h3 id="附录具有特殊含义的退出代码">附录：具有特殊含义的退出代码</h3><table><colgroup><col style="width: 9%" /><col style="width: 25%" /><col style="width: 20%" /><col style="width: 45%" /></colgroup><thead><tr class="header"><th>退出状态代码</th><th>含义</th><th>例子</th><th>解释</th></tr></thead><tbody><tr class="odd"><td>1</td><td>捕获一般错误</td><td>let "var1 = 1 / 0"</td><td>各种各样的错误，例如“除以零”和其他不允许的操作</td></tr><tr class="even"><td>2</td><td>滥用shell内置函数（根据bash文档）</td><td>empty_function() {}</td><td>缺少关键字或命令，或权限问题（以及二进制文件比较失败的差异返回代码）</td></tr><tr class="odd"><td>126</td><td>调用的命令无法执行</td><td>/dev/null</td><td>权限问题或命令不是可执行文件</td></tr><tr class="even"><td>127</td><td>调用的命令找不到</td><td>illegal_command</td><td><span class="math inline">\(PATH 拼写错误或者可能有问题 | | 128 | 退出参数无效 | exit 3.14159 | exit 只接受 0 - 255 范围内的整数参数 | | 128+n | 致命错误信号 | *kill -9* `\)</span>PPID` of script</td></tr><tr class="odd"><td>130</td><td>由 Ctrl-C 终止的脚本</td><td>Ctrl-C</td><td>Ctrl-C 是致命错误，错误信号是2</td></tr><tr class="even"><td>255*</td><td>退出状态超出范围</td><td>exit -1</td><td>exit 只接受 0 - 255 范围内的整数</td></tr></tbody></table><ul><li>根据上表，退出代码 1 - 2、126 - 165 和 255 [1] 具有特殊含义，因此对于用户指定的退出参数应避免使用。 以退出 127 结束脚本肯定会在故障排除时引起混淆（错误代码是“找不到命令”还是用户定义的？）。 但是，许多脚本使用退出 1 作为一般的错误救助。 <strong>由于退出代码 1 表示许多可能的错误，因此它在调试中并不是特别有用。</strong></li></ul>]]></content>
    
    
    <summary type="html">&lt;p&gt;本文主要介绍Docker容器退出状态码。&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;关键词：Docker&lt;/strong&gt;&lt;/p&gt;</summary>
    
    
    
    
    <category term="docker" scheme="https://fly97.cn/tags/docker/"/>
    
  </entry>
  
  <entry>
    <title>Kubernetes集群实践（三）| 集群外如何访问K8s内的Service</title>
    <link href="https://fly97.cn/p/service-access-application-cluster/"/>
    <id>https://fly97.cn/p/service-access-application-cluster/</id>
    <published>2022-05-10T06:37:00.000Z</published>
    <updated>2022-05-10T06:37:00.000Z</updated>
    
    <content type="html"><![CDATA[<p>本文主要介绍<strong>集群外如何访问K8s内的Service</strong>。</p><p>关键词：<strong>k8s</strong></p><a id="more"></a><h3 id="准备工作">准备工作</h3><p>这里是使用的开启了k8s集群的<strong>Docker Desktop For Mac</strong>。在Windows/Mac上使用K8s in Docker请查看上一篇文章。</p><p><img data-src="image-20220501231242091.png" /></p><h3 id="创建developments">创建Developments</h3><p>Deployment 是在 Pod 上层的一个抽象，它可以定义一组 Pod 的副本数目、以及这个 Pod 的版本。一般用 Deployment 这个抽象来做应用的真正的管理，而 Pod 是组成 Deployment 最小的单元。</p><p>Kubernetes 是通过 Controller，也就是我们刚才提到的控制器去维护 Deployment 中 Pod 的数目，它也会去帮助 Deployment 自动恢复失败的 Pod。</p><p>这里的给出一个Development的定义：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">apiVersion:</span> <span class="string">apps/v1</span></span><br><span class="line"><span class="attr">kind:</span> <span class="string">Deployment</span></span><br><span class="line"><span class="attr">metadata:</span></span><br><span class="line">  <span class="attr">name:</span> <span class="string">jixiao-deployment</span></span><br><span class="line"><span class="attr">spec:</span></span><br><span class="line">  <span class="attr">replicas:</span> <span class="number">3</span></span><br><span class="line">  <span class="attr">selector:</span></span><br><span class="line">    <span class="attr">matchLabels:</span></span><br><span class="line">      <span class="attr">app:</span> <span class="string">jixiao</span></span><br><span class="line">  <span class="attr">template:</span></span><br><span class="line">    <span class="attr">metadata:</span></span><br><span class="line">      <span class="attr">labels:</span></span><br><span class="line">        <span class="attr">app:</span> <span class="string">jixiao</span></span><br><span class="line">    <span class="attr">spec:</span></span><br><span class="line">      <span class="attr">containers:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">jixiao</span></span><br><span class="line">        <span class="attr">image:</span> <span class="string">jixiao-jar</span></span><br><span class="line">        <span class="attr">imagePullPolicy:</span> <span class="string">Never</span></span><br><span class="line">        <span class="attr">ports:</span></span><br><span class="line">        <span class="bullet">-</span> <span class="attr">containerPort:</span> <span class="number">8080</span></span><br></pre></td></tr></table></figure><p>TIPS：</p><ul><li>这里使用的是本地镜像，因此<code>imagePullPolicy</code>设置为<code>Never</code></li><li>containerPort指的是Pod向外部暴露的端口</li></ul><h3 id="创建service">创建Service</h3><p>Service 提供了一个或者多个 Pod 实例的稳定访问地址。</p><p>可以理解为Service 是 Pod和外部访问的桥梁。</p><p>通过Servide访问Pod应用有多种方式，这里给出<strong>通过Service 连接到Pod，并通过端口转发访问的方式</strong>。</p><p>这里创建一个Service：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">apiVersion:</span> <span class="string">v1</span></span><br><span class="line"><span class="attr">kind:</span> <span class="string">Service</span></span><br><span class="line"><span class="attr">metadata:</span></span><br><span class="line">  <span class="attr">name:</span> <span class="string">jixiao</span></span><br><span class="line">  <span class="attr">labels:</span></span><br><span class="line">    <span class="attr">app:</span> <span class="string">jixiao</span></span><br><span class="line"><span class="attr">spec:</span></span><br><span class="line">  <span class="attr">ports:</span></span><br><span class="line">  <span class="bullet">-</span> <span class="attr">port:</span> <span class="number">80</span></span><br><span class="line">    <span class="attr">targetPort:</span> <span class="number">8080</span></span><br><span class="line">    <span class="attr">protocol:</span> <span class="string">TCP</span></span><br><span class="line">  <span class="attr">selector:</span></span><br><span class="line">    <span class="attr">app:</span> <span class="string">jixiao</span></span><br></pre></td></tr></table></figure><p>TIPS：</p><ul><li><p>这里通过<code>.spec.selector.app</code>的Value访问刚刚部署的<code>Developments</code>，<strong>即对应的Value要完全相同才行</strong>，简单理解为<code>app: jixiao</code>要出现在两个配置文件中即可。</p></li><li><p><code>targetPort</code>指的是Pod暴露的端口，<code>port</code>指的是在Service中使用的端口。</p></li></ul><h3 id="集群外访问集群内service的方式">集群外访问集群内Service的方式</h3><h4 id="端口转发">端口转发</h4><p>转发一个本地端口到Pod端口</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">kubectl port-forward service/jixiao 7777:80</span><br></pre></td></tr></table></figure><p>TIPS：</p><ul><li>这里的80端口是Service中使用的端口，而不是Pod向外暴露的端口。</li></ul><p><img data-src="image-20220501234222534.png" /></p><h3 id="测试">测试</h3><p>新打开一个终端</p><p><img data-src="image-20220501234321909.png" /></p><h4 id="externalips">externalIPs</h4><p>如果外部的 IP 路由到集群中一个或多个 Node 上，Kubernetes Service 会被暴露给这些 <strong>externalIPs</strong>。 通过外部 IP（作为目的 IP 地址）进入到集群，打到 Service 的端口上的流量， 将会被路由到 Service 的 Endpoint 上。</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">apiVersion:</span> <span class="string">v1</span></span><br><span class="line"><span class="attr">kind:</span> <span class="string">Service</span></span><br><span class="line"><span class="attr">metadata:</span></span><br><span class="line">  <span class="attr">name:</span> <span class="string">server</span></span><br><span class="line"><span class="attr">spec:</span></span><br><span class="line">  <span class="attr">selector:</span></span><br><span class="line">    <span class="attr">app:</span> <span class="string">server</span></span><br><span class="line">  <span class="attr">ports:</span></span><br><span class="line">  <span class="bullet">-</span> <span class="attr">protocol:</span> <span class="string">TCP</span></span><br><span class="line">    <span class="attr">port:</span> <span class="number">8888</span></span><br><span class="line">    <span class="attr">targetPort:</span> <span class="string">http</span></span><br><span class="line">  <span class="attr">externalIPs:</span></span><br><span class="line">  <span class="bullet">-</span> <span class="number">192.168</span><span class="number">.15</span><span class="number">.202</span></span><br></pre></td></tr></table></figure><p>TIPS：</p><p>如上配置可以通过<code>192.168.15.202:8888</code>访问到Service后面的Pods应用。</p><h4 id="nodeport">NodePort</h4><p>NodePort 服务是引导外部流量到你的服务的最原始方式。NodePort，正如这个名字所示，在所有节点（虚拟机）上开放一个特定端口，任何发送到该端口的流量都被转发到对应服务。</p><p>然而，<a href="https://kuboard.cn/learning/k8s-intermediate/service/service-types.html#nodeport">NodePort</a> 默认范围是30000-32767。这个端口号太大，不适合对外提供服务。</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">apiVersion:</span> <span class="string">v1</span></span><br><span class="line"><span class="attr">kind:</span> <span class="string">Service</span></span><br><span class="line"><span class="attr">metadata:</span></span><br><span class="line">  <span class="attr">name:</span> <span class="string">www2</span></span><br><span class="line"><span class="attr">spec:</span></span><br><span class="line">  <span class="attr">type:</span> <span class="string">NodePort</span></span><br><span class="line">  <span class="attr">selector:</span></span><br><span class="line">    <span class="attr">app:</span> <span class="string">www</span></span><br><span class="line">  <span class="attr">ports:</span></span><br><span class="line">  <span class="bullet">-</span> <span class="attr">protocol:</span> <span class="string">TCP</span></span><br><span class="line">    <span class="attr">port:</span> <span class="number">80</span></span><br><span class="line">    <span class="attr">nodePort:</span> <span class="number">30000</span></span><br><span class="line">    <span class="attr">targetPort:</span> <span class="string">http</span></span><br></pre></td></tr></table></figure>]]></content>
    
    
    <summary type="html">&lt;p&gt;本文主要介绍&lt;strong&gt;集群外如何访问K8s内的Service&lt;/strong&gt;。&lt;/p&gt;
&lt;p&gt;关键词：&lt;strong&gt;k8s&lt;/strong&gt;&lt;/p&gt;</summary>
    
    
    
    
    <category term="k8s" scheme="https://fly97.cn/tags/k8s/"/>
    
  </entry>
  
  <entry>
    <title>使用Docker Compose搭建Harbor镜像仓库</title>
    <link href="https://fly97.cn/p/build-harbor-mirror-warehouse-with-docker-compose/"/>
    <id>https://fly97.cn/p/build-harbor-mirror-warehouse-with-docker-compose/</id>
    <published>2022-05-08T09:26:00.000Z</published>
    <updated>2022-05-08T09:26:00.000Z</updated>
    
    <content type="html"><![CDATA[<p>Docker容器应用的开发和运行离不开可靠的镜像管理，虽然docker官方提供了公共的镜像仓库（Docker Hub），但是从安全和效率等方面考虑，部署我们私有环境内的Registry也是非常必要的。Harbor是由VMware公司开源的企业级的Docker Registry管理项目，它包括权限管理（RBAC）、LDAP、日志审核、界面管理、自我注册、镜像复制和中文支持等功能。</p><p>本文主要介绍如何使用Docker Compose搭建Harbor镜像仓库。</p><p>关键词：<strong>docker，harbor</strong></p><a id="more"></a><h3 id="准备工作">准备工作</h3><ul><li>Mac mini</li><li>Docker Desktop For Mac</li><li>docker-compose</li></ul><h3 id="下载安装文件">下载安装文件</h3><h4 id="选择离线安装文件">选择离线安装文件</h4><blockquote><p>https://github.com/goharbor/harbor/releases/download/v2.5.0/harbor-offline-installer-v2.5.0.tgz</p></blockquote><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">wget https://github.com/goharbor/harbor/releases/download/v2.5.0/harbor-offline-installer-v2.5.0.tgz</span><br></pre></td></tr></table></figure><p>解压文件</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">tar zxvf harbor-offline-installer-v2.5.0.tgz</span><br></pre></td></tr></table></figure><h3 id="修改配置">修改配置</h3><h4 id="配置hosts">配置hosts</h4><p>harbor要求使用https，因此需要生成本地可信的https证书。这里已经生成好了。</p><p>因为是本地使用，所以无需修改DNS，只需要修改hosts。</p><p><img data-src="image-20220508201150693.png" /></p><h4 id="修改配置文件">修改配置文件</h4><p>将模版配置文件修改为配置文件</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">cp harbor.yml.tmpl harbor.yml</span><br></pre></td></tr></table></figure><p>修改配置文件，主要是修改https和持久化目录，其他的还没研究。</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br><span class="line">84</span><br><span class="line">85</span><br><span class="line">86</span><br><span class="line">87</span><br><span class="line">88</span><br><span class="line">89</span><br><span class="line">90</span><br><span class="line">91</span><br><span class="line">92</span><br><span class="line">93</span><br><span class="line">94</span><br><span class="line">95</span><br><span class="line">96</span><br><span class="line">97</span><br><span class="line">98</span><br><span class="line">99</span><br><span class="line">100</span><br><span class="line">101</span><br><span class="line">102</span><br><span class="line">103</span><br><span class="line">104</span><br><span class="line">105</span><br><span class="line">106</span><br><span class="line">107</span><br><span class="line">108</span><br><span class="line">109</span><br><span class="line">110</span><br><span class="line">111</span><br><span class="line">112</span><br><span class="line">113</span><br><span class="line">114</span><br><span class="line">115</span><br><span class="line">116</span><br><span class="line">117</span><br><span class="line">118</span><br><span class="line">119</span><br><span class="line">120</span><br><span class="line">121</span><br><span class="line">122</span><br><span class="line">123</span><br><span class="line">124</span><br><span class="line">125</span><br><span class="line">126</span><br><span class="line">127</span><br><span class="line">128</span><br><span class="line">129</span><br><span class="line">130</span><br><span class="line">131</span><br><span class="line">132</span><br><span class="line">133</span><br><span class="line">134</span><br><span class="line">135</span><br><span class="line">136</span><br><span class="line">137</span><br><span class="line">138</span><br><span class="line">139</span><br><span class="line">140</span><br><span class="line">141</span><br><span class="line">142</span><br><span class="line">143</span><br><span class="line">144</span><br><span class="line">145</span><br><span class="line">146</span><br><span class="line">147</span><br><span class="line">148</span><br><span class="line">149</span><br><span class="line">150</span><br><span class="line">151</span><br><span class="line">152</span><br><span class="line">153</span><br><span class="line">154</span><br><span class="line">155</span><br><span class="line">156</span><br><span class="line">157</span><br><span class="line">158</span><br><span class="line">159</span><br><span class="line">160</span><br><span class="line">161</span><br><span class="line">162</span><br><span class="line">163</span><br><span class="line">164</span><br><span class="line">165</span><br><span class="line">166</span><br><span class="line">167</span><br><span class="line">168</span><br><span class="line">169</span><br><span class="line">170</span><br><span class="line">171</span><br><span class="line">172</span><br><span class="line">173</span><br><span class="line">174</span><br><span class="line">175</span><br><span class="line">176</span><br><span class="line">177</span><br><span class="line">178</span><br><span class="line">179</span><br><span class="line">180</span><br><span class="line">181</span><br><span class="line">182</span><br><span class="line">183</span><br><span class="line">184</span><br><span class="line">185</span><br><span class="line">186</span><br><span class="line">187</span><br><span class="line">188</span><br><span class="line">189</span><br><span class="line">190</span><br><span class="line">191</span><br><span class="line">192</span><br><span class="line">193</span><br><span class="line">194</span><br><span class="line">195</span><br><span class="line">196</span><br><span class="line">197</span><br><span class="line">198</span><br><span class="line">199</span><br><span class="line">200</span><br><span class="line">201</span><br><span class="line">202</span><br><span class="line">203</span><br><span class="line">204</span><br><span class="line">205</span><br><span class="line">206</span><br><span class="line">207</span><br><span class="line">208</span><br><span class="line">209</span><br><span class="line">210</span><br><span class="line">211</span><br><span class="line">212</span><br><span class="line">213</span><br><span class="line">214</span><br><span class="line">215</span><br><span class="line">216</span><br><span class="line">217</span><br><span class="line">218</span><br><span class="line">219</span><br><span class="line">220</span><br><span class="line">221</span><br><span class="line">222</span><br><span class="line">223</span><br><span class="line">224</span><br><span class="line">225</span><br><span class="line">226</span><br><span class="line">227</span><br><span class="line">228</span><br><span class="line">229</span><br><span class="line">230</span><br><span class="line">231</span><br><span class="line">232</span><br><span class="line">233</span><br><span class="line">234</span><br><span class="line">235</span><br><span class="line">236</span><br><span class="line">237</span><br><span class="line">238</span><br><span class="line">239</span><br><span class="line">240</span><br><span class="line">241</span><br><span class="line">242</span><br><span class="line">243</span><br><span class="line">244</span><br><span class="line">245</span><br><span class="line">246</span><br><span class="line">247</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># Configuration file of Harbor</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># The IP address or hostname to access admin UI and registry service.</span></span><br><span class="line"><span class="comment"># DO NOT use localhost or 127.0.0.1, because Harbor needs to be accessed by external clients.</span></span><br><span class="line"><span class="attr">hostname:</span> <span class="string">harbor.local</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># http related config</span></span><br><span class="line"><span class="comment"># http:</span></span><br><span class="line"><span class="comment">#   # port for http, default is 80. If https enabled, this port will redirect to https port</span></span><br><span class="line"><span class="comment">#   port: 80</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># https related config</span></span><br><span class="line"><span class="attr">https:</span></span><br><span class="line">  <span class="comment"># https port for harbor, default is 443</span></span><br><span class="line">  <span class="attr">port:</span> <span class="number">443</span></span><br><span class="line">  <span class="comment"># The path of cert and key files for nginx</span></span><br><span class="line">  <span class="attr">certificate:</span> <span class="string">harbor.crt</span></span><br><span class="line">  <span class="attr">private_key:</span> <span class="string">harbor.key</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># # Uncomment following will enable tls communication between all harbor components</span></span><br><span class="line"><span class="comment"># internal_tls:</span></span><br><span class="line"><span class="comment">#   # set enabled to true means internal tls is enabled</span></span><br><span class="line"><span class="comment">#   enabled: true</span></span><br><span class="line"><span class="comment">#   # put your cert and key files on dir</span></span><br><span class="line"><span class="comment">#   dir: /etc/harbor/tls/internal</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Uncomment external_url if you want to enable external proxy</span></span><br><span class="line"><span class="comment"># And when it enabled the hostname will no longer used</span></span><br><span class="line"><span class="comment"># external_url: https://reg.mydomain.com:8433</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># The initial password of Harbor admin</span></span><br><span class="line"><span class="comment"># It only works in first time to install harbor</span></span><br><span class="line"><span class="comment"># Remember Change the admin password from UI after launching Harbor.</span></span><br><span class="line"><span class="attr">harbor_admin_password:</span> <span class="string">Harbor12345</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Harbor DB configuration</span></span><br><span class="line"><span class="attr">database:</span></span><br><span class="line">  <span class="comment"># The password for the root user of Harbor DB. Change this before any production use.</span></span><br><span class="line">  <span class="attr">password:</span> <span class="string">root123</span></span><br><span class="line">  <span class="comment"># The maximum number of connections in the idle connection pool. If it &lt;=0, no idle connections are retained.</span></span><br><span class="line">  <span class="attr">max_idle_conns:</span> <span class="number">100</span></span><br><span class="line">  <span class="comment"># The maximum number of open connections to the database. If it &lt;= 0, then there is no limit on the number of open connections.</span></span><br><span class="line">  <span class="comment"># <span class="doctag">Note:</span> the default number of connections is 1024 for postgres of harbor.</span></span><br><span class="line">  <span class="attr">max_open_conns:</span> <span class="number">900</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># The default data volume</span></span><br><span class="line"><span class="attr">data_volume:</span> <span class="string">/Users/wf09/Desktop/docker/harbor/data</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Harbor Storage settings by default is using /data dir on local filesystem</span></span><br><span class="line"><span class="comment"># Uncomment storage_service setting If you want to using external storage</span></span><br><span class="line"><span class="comment"># storage_service:</span></span><br><span class="line"><span class="comment">#   # ca_bundle is the path to the custom root ca certificate, which will be injected into the truststore</span></span><br><span class="line"><span class="comment">#   # of registry&#x27;s and chart repository&#x27;s containers.  This is usually needed when the user hosts a internal storage with self signed certificate.</span></span><br><span class="line"><span class="comment">#   ca_bundle:</span></span><br><span class="line"></span><br><span class="line"><span class="comment">#   # storage backend, default is filesystem, options include filesystem, azure, gcs, s3, swift and oss</span></span><br><span class="line"><span class="comment">#   # for more info about this configuration please refer https://docs.docker.com/registry/configuration/</span></span><br><span class="line"><span class="comment">#   filesystem:</span></span><br><span class="line"><span class="comment">#     maxthreads: 100</span></span><br><span class="line"><span class="comment">#   # set disable to true when you want to disable registry redirect</span></span><br><span class="line"><span class="comment">#   redirect:</span></span><br><span class="line"><span class="comment">#     disabled: false</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Trivy configuration</span></span><br><span class="line"><span class="comment">#</span></span><br><span class="line"><span class="comment"># Trivy DB contains vulnerability information from NVD, Red Hat, and many other upstream vulnerability databases.</span></span><br><span class="line"><span class="comment"># It is downloaded by Trivy from the GitHub release page https://github.com/aquasecurity/trivy-db/releases and cached</span></span><br><span class="line"><span class="comment"># in the local file system. In addition, the database contains the update timestamp so Trivy can detect whether it</span></span><br><span class="line"><span class="comment"># should download a newer version from the Internet or use the cached one. Currently, the database is updated every</span></span><br><span class="line"><span class="comment"># 12 hours and published as a new release to GitHub.</span></span><br><span class="line"><span class="attr">trivy:</span></span><br><span class="line">  <span class="comment"># ignoreUnfixed The flag to display only fixed vulnerabilities</span></span><br><span class="line">  <span class="attr">ignore_unfixed:</span> <span class="literal">false</span></span><br><span class="line">  <span class="comment"># skipUpdate The flag to enable or disable Trivy DB downloads from GitHub</span></span><br><span class="line">  <span class="comment">#</span></span><br><span class="line">  <span class="comment"># You might want to enable this flag in test or CI/CD environments to avoid GitHub rate limiting issues.</span></span><br><span class="line">  <span class="comment"># If the flag is enabled you have to download the `trivy-offline.tar.gz` archive manually, extract `trivy.db` and</span></span><br><span class="line">  <span class="comment"># `metadata.json` files and mount them in the `/home/scanner/.cache/trivy/db` path.</span></span><br><span class="line">  <span class="attr">skip_update:</span> <span class="literal">false</span></span><br><span class="line">  <span class="comment">#</span></span><br><span class="line">  <span class="comment"># The offline_scan option prevents Trivy from sending API requests to identify dependencies.</span></span><br><span class="line">  <span class="comment"># Scanning JAR files and pom.xml may require Internet access for better detection, but this option tries to avoid it.</span></span><br><span class="line">  <span class="comment"># For example, the offline mode will not try to resolve transitive dependencies in pom.xml when the dependency doesn&#x27;t</span></span><br><span class="line">  <span class="comment"># exist in the local repositories. It means a number of detected vulnerabilities might be fewer in offline mode.</span></span><br><span class="line">  <span class="comment"># It would work if all the dependencies are in local.</span></span><br><span class="line">  <span class="comment"># This option doesn’t affect DB download. You need to specify &quot;skip-update&quot; as well as &quot;offline-scan&quot; in an air-gapped environment.</span></span><br><span class="line">  <span class="attr">offline_scan:</span> <span class="literal">false</span></span><br><span class="line">  <span class="comment">#</span></span><br><span class="line">  <span class="comment"># insecure The flag to skip verifying registry certificate</span></span><br><span class="line">  <span class="attr">insecure:</span> <span class="literal">false</span></span><br><span class="line">  <span class="comment"># github_token The GitHub access token to download Trivy DB</span></span><br><span class="line">  <span class="comment">#</span></span><br><span class="line">  <span class="comment"># Anonymous downloads from GitHub are subject to the limit of 60 requests per hour. Normally such rate limit is enough</span></span><br><span class="line">  <span class="comment"># for production operations. If, for any reason, it&#x27;s not enough, you could increase the rate limit to 5000</span></span><br><span class="line">  <span class="comment"># requests per hour by specifying the GitHub access token. For more details on GitHub rate limiting please consult</span></span><br><span class="line">  <span class="comment"># https://developer.github.com/v3/#rate-limiting</span></span><br><span class="line">  <span class="comment">#</span></span><br><span class="line">  <span class="comment"># You can create a GitHub token by following the instructions in</span></span><br><span class="line">  <span class="comment"># https://help.github.com/en/github/authenticating-to-github/creating-a-personal-access-token-for-the-command-line</span></span><br><span class="line">  <span class="comment">#</span></span><br><span class="line">  <span class="comment"># github_token: xxx</span></span><br><span class="line"></span><br><span class="line"><span class="attr">jobservice:</span></span><br><span class="line">  <span class="comment"># Maximum number of job workers in job service</span></span><br><span class="line">  <span class="attr">max_job_workers:</span> <span class="number">10</span></span><br><span class="line"></span><br><span class="line"><span class="attr">notification:</span></span><br><span class="line">  <span class="comment"># Maximum retry count for webhook job</span></span><br><span class="line">  <span class="attr">webhook_job_max_retry:</span> <span class="number">10</span></span><br><span class="line"></span><br><span class="line"><span class="attr">chart:</span></span><br><span class="line">  <span class="comment"># Change the value of absolute_url to enabled can enable absolute url in chart</span></span><br><span class="line">  <span class="attr">absolute_url:</span> <span class="string">disabled</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Log configurations</span></span><br><span class="line"><span class="attr">log:</span></span><br><span class="line">  <span class="comment"># options are debug, info, warning, error, fatal</span></span><br><span class="line">  <span class="attr">level:</span> <span class="string">info</span></span><br><span class="line">  <span class="comment"># configs for logs in local storage</span></span><br><span class="line">  <span class="attr">local:</span></span><br><span class="line">    <span class="comment"># Log files are rotated log_rotate_count times before being removed. If count is 0, old versions are removed rather than rotated.</span></span><br><span class="line">    <span class="attr">rotate_count:</span> <span class="number">50</span></span><br><span class="line">    <span class="comment"># Log files are rotated only if they grow bigger than log_rotate_size bytes. If size is followed by k, the size is assumed to be in kilobytes.</span></span><br><span class="line">    <span class="comment"># If the M is used, the size is in megabytes, and if G is used, the size is in gigabytes. So size 100, size 100k, size 100M and size 100G</span></span><br><span class="line">    <span class="comment"># are all valid.</span></span><br><span class="line">    <span class="attr">rotate_size:</span> <span class="string">200M</span></span><br><span class="line">    <span class="comment"># The directory on your host that store log</span></span><br><span class="line">    <span class="attr">location:</span> <span class="string">/var/log/harbor</span></span><br><span class="line"></span><br><span class="line">  <span class="comment"># Uncomment following lines to enable external syslog endpoint.</span></span><br><span class="line">  <span class="comment"># external_endpoint:</span></span><br><span class="line">  <span class="comment">#   # protocol used to transmit log to external endpoint, options is tcp or udp</span></span><br><span class="line">  <span class="comment">#   protocol: tcp</span></span><br><span class="line">  <span class="comment">#   # The host of external endpoint</span></span><br><span class="line">  <span class="comment">#   host: localhost</span></span><br><span class="line">  <span class="comment">#   # Port of external endpoint</span></span><br><span class="line">  <span class="comment">#   port: 5140</span></span><br><span class="line"></span><br><span class="line"><span class="comment">#This attribute is for migrator to detect the version of the .cfg file, DO NOT MODIFY!</span></span><br><span class="line"><span class="attr">_version:</span> <span class="number">2.5</span><span class="number">.0</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Uncomment external_database if using external database.</span></span><br><span class="line"><span class="comment"># external_database:</span></span><br><span class="line"><span class="comment">#   harbor:</span></span><br><span class="line"><span class="comment">#     host: harbor_db_host</span></span><br><span class="line"><span class="comment">#     port: harbor_db_port</span></span><br><span class="line"><span class="comment">#     db_name: harbor_db_name</span></span><br><span class="line"><span class="comment">#     username: harbor_db_username</span></span><br><span class="line"><span class="comment">#     password: harbor_db_password</span></span><br><span class="line"><span class="comment">#     ssl_mode: disable</span></span><br><span class="line"><span class="comment">#     max_idle_conns: 2</span></span><br><span class="line"><span class="comment">#     max_open_conns: 0</span></span><br><span class="line"><span class="comment">#   notary_signer:</span></span><br><span class="line"><span class="comment">#     host: notary_signer_db_host</span></span><br><span class="line"><span class="comment">#     port: notary_signer_db_port</span></span><br><span class="line"><span class="comment">#     db_name: notary_signer_db_name</span></span><br><span class="line"><span class="comment">#     username: notary_signer_db_username</span></span><br><span class="line"><span class="comment">#     password: notary_signer_db_password</span></span><br><span class="line"><span class="comment">#     ssl_mode: disable</span></span><br><span class="line"><span class="comment">#   notary_server:</span></span><br><span class="line"><span class="comment">#     host: notary_server_db_host</span></span><br><span class="line"><span class="comment">#     port: notary_server_db_port</span></span><br><span class="line"><span class="comment">#     db_name: notary_server_db_name</span></span><br><span class="line"><span class="comment">#     username: notary_server_db_username</span></span><br><span class="line"><span class="comment">#     password: notary_server_db_password</span></span><br><span class="line"><span class="comment">#     ssl_mode: disable</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Uncomment external_redis if using external Redis server</span></span><br><span class="line"><span class="comment"># external_redis:</span></span><br><span class="line"><span class="comment">#   # support redis, redis+sentinel</span></span><br><span class="line"><span class="comment">#   # host for redis: &lt;host_redis&gt;:&lt;port_redis&gt;</span></span><br><span class="line"><span class="comment">#   # host for redis+sentinel:</span></span><br><span class="line"><span class="comment">#   #  &lt;host_sentinel1&gt;:&lt;port_sentinel1&gt;,&lt;host_sentinel2&gt;:&lt;port_sentinel2&gt;,&lt;host_sentinel3&gt;:&lt;port_sentinel3&gt;</span></span><br><span class="line"><span class="comment">#   host: redis:6379</span></span><br><span class="line"><span class="comment">#   password: </span></span><br><span class="line"><span class="comment">#   # sentinel_master_set must be set to support redis+sentinel</span></span><br><span class="line"><span class="comment">#   #sentinel_master_set:</span></span><br><span class="line"><span class="comment">#   # db_index 0 is for core, it&#x27;s unchangeable</span></span><br><span class="line"><span class="comment">#   registry_db_index: 1</span></span><br><span class="line"><span class="comment">#   jobservice_db_index: 2</span></span><br><span class="line"><span class="comment">#   chartmuseum_db_index: 3</span></span><br><span class="line"><span class="comment">#   trivy_db_index: 5</span></span><br><span class="line"><span class="comment">#   idle_timeout_seconds: 30</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Uncomment uaa for trusting the certificate of uaa instance that is hosted via self-signed cert.</span></span><br><span class="line"><span class="comment"># uaa:</span></span><br><span class="line"><span class="comment">#   ca_file: /path/to/ca</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Global proxy</span></span><br><span class="line"><span class="comment"># Config http proxy for components, e.g. http://my.proxy.com:3128</span></span><br><span class="line"><span class="comment"># Components doesn&#x27;t need to connect to each others via http proxy.</span></span><br><span class="line"><span class="comment"># Remove component from `components` array if want disable proxy</span></span><br><span class="line"><span class="comment"># for it. If you want use proxy for replication, MUST enable proxy</span></span><br><span class="line"><span class="comment"># for core and jobservice, and set `http_proxy` and `https_proxy`.</span></span><br><span class="line"><span class="comment"># Add domain to the `no_proxy` field, when you want disable proxy</span></span><br><span class="line"><span class="comment"># for some special registry.</span></span><br><span class="line"><span class="attr">proxy:</span></span><br><span class="line">  <span class="attr">http_proxy:</span></span><br><span class="line">  <span class="attr">https_proxy:</span></span><br><span class="line">  <span class="attr">no_proxy:</span></span><br><span class="line">  <span class="attr">components:</span></span><br><span class="line">    <span class="bullet">-</span> <span class="string">core</span></span><br><span class="line">    <span class="bullet">-</span> <span class="string">jobservice</span></span><br><span class="line">    <span class="bullet">-</span> <span class="string">trivy</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># metric:</span></span><br><span class="line"><span class="comment">#   enabled: false</span></span><br><span class="line"><span class="comment">#   port: 9090</span></span><br><span class="line"><span class="comment">#   path: /metrics</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Trace related config</span></span><br><span class="line"><span class="comment"># only can enable one trace provider(jaeger or otel) at the same time,</span></span><br><span class="line"><span class="comment"># and when using jaeger as provider, can only enable it with agent mode or collector mode.</span></span><br><span class="line"><span class="comment"># if using jaeger collector mode, uncomment endpoint and uncomment username, password if needed</span></span><br><span class="line"><span class="comment"># if using jaeger agetn mode uncomment agent_host and agent_port</span></span><br><span class="line"><span class="comment"># trace:</span></span><br><span class="line"><span class="comment">#   enabled: true</span></span><br><span class="line"><span class="comment">#   # set sample_rate to 1 if you wanna sampling 100% of trace data; set 0.5 if you wanna sampling 50% of trace data, and so forth</span></span><br><span class="line"><span class="comment">#   sample_rate: 1</span></span><br><span class="line"><span class="comment">#   # # namespace used to differenciate different harbor services</span></span><br><span class="line"><span class="comment">#   # namespace:</span></span><br><span class="line"><span class="comment">#   # # attributes is a key value dict contains user defined attributes used to initialize trace provider</span></span><br><span class="line"><span class="comment">#   # attributes:</span></span><br><span class="line"><span class="comment">#   #   application: harbor</span></span><br><span class="line"><span class="comment">#   # # jaeger should be 1.26 or newer.</span></span><br><span class="line"><span class="comment">#   # jaeger:</span></span><br><span class="line"><span class="comment">#   #   endpoint: http://hostname:14268/api/traces</span></span><br><span class="line"><span class="comment">#   #   username:</span></span><br><span class="line"><span class="comment">#   #   password:</span></span><br><span class="line"><span class="comment">#   #   agent_host: hostname</span></span><br><span class="line"><span class="comment">#   #   # export trace data by jaeger.thrift in compact mode</span></span><br><span class="line"><span class="comment">#   #   agent_port: 6831</span></span><br><span class="line"><span class="comment">#   # otel:</span></span><br><span class="line"><span class="comment">#   #   endpoint: hostname:4318</span></span><br><span class="line"><span class="comment">#   #   url_path: /v1/traces</span></span><br><span class="line"><span class="comment">#   #   compression: false</span></span><br><span class="line"><span class="comment">#   #   insecure: true</span></span><br><span class="line"><span class="comment">#   #   timeout: 10s</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># enable purge _upload directories</span></span><br><span class="line"><span class="attr">upload_purging:</span></span><br><span class="line">  <span class="attr">enabled:</span> <span class="literal">true</span></span><br><span class="line">  <span class="comment"># remove files in _upload directories which exist for a period of time, default is one week.</span></span><br><span class="line">  <span class="attr">age:</span> <span class="string">168h</span></span><br><span class="line">  <span class="comment"># the interval of the purge operations</span></span><br><span class="line">  <span class="attr">interval:</span> <span class="string">24h</span></span><br><span class="line">  <span class="attr">dryrun:</span> <span class="literal">false</span></span><br></pre></td></tr></table></figure><h3 id="安装运行">安装运行</h3><p>harbor目录下执行</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">./install.sh</span><br></pre></td></tr></table></figure><p>有下图所示说明启动成功</p><p><img data-src="image-20220508212557479.png" /></p><h3 id="排错">排错</h3><h4 id="filenotfounderror-errno-2-no-such-file-or-directory-hostfsharbor.key">FileNotFoundError: [Errno 2] No such file or directory: '/hostfs/harbor.key'</h4><p><img data-src="image-20220508205215581.png" /></p><p>好像是<code>./prepare</code>脚本编写错误，按照下面issus提到的修改</p><blockquote><p>https://github.com/goharbor/harbor/issues/13465</p></blockquote><p><img data-src="image-20220508205501816.png" /></p><p>再次运行<code>./prepare</code>，成功</p><p><img data-src="image-20220508205609714.png" /></p><h4 id="浏览器访问过慢">浏览器访问过慢</h4><p>考虑和SSL证书的OCSP装订有关。</p>]]></content>
    
    
    <summary type="html">&lt;p&gt;Docker容器应用的开发和运行离不开可靠的镜像管理，虽然docker官方提供了公共的镜像仓库（Docker Hub），但是从安全和效率等方面考虑，部署我们私有环境内的Registry也是非常必要的。Harbor是由VMware公司开源的企业级的Docker Registry管理项目，它包括权限管理（RBAC）、LDAP、日志审核、界面管理、自我注册、镜像复制和中文支持等功能。&lt;/p&gt;
&lt;p&gt;本文主要介绍如何使用Docker Compose搭建Harbor镜像仓库。&lt;/p&gt;
&lt;p&gt;关键词：&lt;strong&gt;docker，harbor&lt;/strong&gt;&lt;/p&gt;</summary>
    
    
    
    
    <category term="docker" scheme="https://fly97.cn/tags/docker/"/>
    
    <category term="harbor" scheme="https://fly97.cn/tags/harbor/"/>
    
  </entry>
  
  <entry>
    <title>安装配置Jenkins</title>
    <link href="https://fly97.cn/p/install-and-config-jenkins/"/>
    <id>https://fly97.cn/p/install-and-config-jenkins/</id>
    <published>2022-05-08T09:26:00.000Z</published>
    <updated>2022-05-08T09:26:00.000Z</updated>
    
    <content type="html"><![CDATA[<p>本文主要介绍安装和配置Jenkins。</p><p>关键词：<strong>jenkins</strong></p><a id="more"></a><h3 id="准备工作">准备工作</h3><ul><li>Ubuntu 20.04 LTS</li></ul><h4 id="安装openjdk">安装OpenJDK</h4><h5 id="下载jdk">下载jdk</h5><blockquote><p>https://www.injdk.cn/</p></blockquote><p>这里使用Zulu打包的openjdk 11。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">wget https://d6.injdk.cn/openjdk/zulu/11/zulu11.43.55-ca-jdk11.0.9.1-linux_amd64.deb</span><br></pre></td></tr></table></figure><h5 id="安装">安装</h5><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">sudo apt install ./zulu11.43.55-ca-jdk11.0.9.1-linux_amd64.deb -y</span><br></pre></td></tr></table></figure><h4 id="安装jenkins">安装jenkins</h4><h5 id="下载jenkins">下载jenkins</h5><p>由于众所周知的原因，在国内无法快速访问jenkins.io，这里使用USTC提供的镜像。</p><blockquote><p>https://mirrors.ustc.edu.cn/jenkins/debian-stable/</p></blockquote><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">wget https://mirrors.ustc.edu.cn/jenkins/debian-stable/jenkins_2.332.3_all.deb</span><br></pre></td></tr></table></figure><h5 id="安装-1">安装</h5><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">sudo apt-get install fontconfig -y</span><br><span class="line">sudo apt install ./jenkins_2.332.3_all.deb -y</span><br></pre></td></tr></table></figure><h3 id="配置jenkins">配置jenkins</h3><p>使用systemd，配置文件地址在</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">/lib/systemd/system/jenkins.service</span><br></pre></td></tr></table></figure><h4 id="修改监听端口">修改监听端口</h4><p><img data-src="安装配置Jenkins/image-20220509131641758.png" /></p><h4 id="启用反向代理">启用反向代理</h4><p>这里使用前缀<code>/jenkins</code>进行反向代理</p><figure><img data-src="安装配置Jenkins/image-20220509131731389.png" alt="image-20220509131731389" /><figcaption aria-hidden="true">image-20220509131731389</figcaption></figure><h5 id="反向代理设置">反向代理设置</h5><figure class="highlight nginx"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br></pre></td><td class="code"><pre><span class="line"></span><br><span class="line"><span class="attribute">upstream</span> jenkins&#123;</span><br><span class="line">    <span class="attribute">server</span> <span class="number">127.0.0.1:8080</span>;                   <span class="comment"># 后端jenkins地址</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="section">server</span> &#123;</span><br><span class="line">    <span class="attribute">listen</span>       <span class="number">443</span>    ssl;</span><br><span class="line">    <span class="attribute">server_name</span>  jenkins.local;</span><br><span class="line"></span><br><span class="line">    <span class="attribute">access_log</span>  /var/log/nginx/jenkins.access.log  main;</span><br><span class="line">    <span class="attribute">ssl_session_cache</span> shared:SSL:<span class="number">10m</span>;</span><br><span class="line">    <span class="attribute">ssl_certificate</span>     /usr/local/ssl/jenkins.crt;  <span class="comment"># pem文件的路径</span></span><br><span class="line">    <span class="attribute">ssl_certificate_key</span>  /usr/local/ssl/jenkins.key; <span class="comment"># key文件的路径</span></span><br><span class="line">    <span class="attribute">ssl_trusted_certificate</span> /usr/local/ssl/ca.crt;</span><br><span class="line">    <span class="comment">#ssl_reject_handshake on;</span></span><br><span class="line">    <span class="comment"># ssl验证相关配置</span></span><br><span class="line">    <span class="attribute">ssl_session_timeout</span>  <span class="number">10m</span>;    <span class="comment">#缓存有效期</span></span><br><span class="line">    <span class="attribute">ssl_ciphers</span> ECDHE-RSA-AES128-GCM-SHA256:ECDHE:ECDH:AES:HIGH:!NULL:!aNULL:!MD5:!ADH:!RC4;    <span class="comment">#加密算法</span></span><br><span class="line">    <span class="attribute">ssl_protocols</span> TLSv1 TLSv1.<span class="number">1</span> TLSv1.<span class="number">2</span>;    <span class="comment">#安全链接可选的加密协议</span></span><br><span class="line">    <span class="attribute">ssl_prefer_server_ciphers</span> <span class="literal">on</span>;   <span class="comment">#使用服务器端的首选算法</span></span><br><span class="line">    <span class="attribute">ssl_stapling</span> <span class="literal">on</span>;</span><br><span class="line">    <span class="attribute">ssl_stapling_verify</span> <span class="literal">on</span>;</span><br><span class="line"></span><br><span class="line">    <span class="attribute">location</span> / &#123;</span><br><span class="line">        <span class="attribute">proxy_pass</span> http://jenkins;</span><br><span class="line">        <span class="attribute">proxy_set_header</span> Host <span class="variable">$host</span>;</span><br><span class="line">        <span class="attribute">proxy_set_header</span> X-Forwarded-Proto <span class="variable">$scheme</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><h4 id="修改插件地址">修改插件地址</h4><blockquote><p>https://www.jenkins-zh.cn/tutorial/management/plugin/update-center/</p></blockquote><p>修改地址，<strong>Dashboard&gt;&gt;&gt;插件管理&gt;&gt;&gt;Plugin Manager&gt;&gt;&gt;高级</strong></p><p><img data-src="安装配置Jenkins/image-20220509135702702.png" /></p><h4 id="添加ssh密钥">添加SSH密钥</h4>]]></content>
    
    
    <summary type="html">&lt;p&gt;本文主要介绍安装和配置Jenkins。&lt;/p&gt;
&lt;p&gt;关键词：&lt;strong&gt;jenkins&lt;/strong&gt;&lt;/p&gt;</summary>
    
    
    
    
    <category term="jenkins" scheme="https://fly97.cn/tags/jenkins/"/>
    
  </entry>
  
  <entry>
    <title>使用OpenSSL创建自签名ecc证书</title>
    <link href="https://fly97.cn/p/create-a-self-signed-certificate-with-openSSL/"/>
    <id>https://fly97.cn/p/create-a-self-signed-certificate-with-openSSL/</id>
    <published>2022-05-08T07:26:00.000Z</published>
    <updated>2022-05-08T07:26:00.000Z</updated>
    
    <content type="html"><![CDATA[<p>在搭建服务时，时不时需要使用https证书。而内网环境最好是使用自签名证书，便于管理。</p><p>至于为什么要生成ECC证书而不是用RSA证书：ECC证书相对于RSA证书来说，体积小且速度快，而且内网使用不需要考虑兼容性。</p><p>本文主要介绍如何使用OpenSSL创建自签名ecc证书。</p><p>关键词：<strong>openssl，https</strong></p><a id="more"></a><h3 id="准备工作">准备工作</h3><ul><li>Mac mini</li><li>LibreSSL 2.8.3</li></ul><p>这里还需要修改一下openssl的默认配置，使其可以生成版本v3的证书：</p><p><img data-src="image-20220508170638980.png" /></p><h3 id="生成ca根证书">生成CA根证书</h3><p>为什么要使用CA根证书呢？</p><p>使用自定义CA签发本地使用的https证书，便于管理：<strong>只需要把导入CA根证书，主机就可以正常使用自签名的证书。</strong></p><h4 id="生成ca根证书的私钥">生成CA根证书的私钥</h4><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">openssl ecparam -genkey -name prime256v1 -out ca.key</span><br></pre></td></tr></table></figure><h4 id="生成ca根证书-1">生成CA根证书</h4><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">openssl req -new -x509 -subj <span class="string">&quot;/C=CN/CN=Root\ CA&quot;</span>  -days 7305 -key ca.key -out ca.crt -extensions v3_ca </span><br></pre></td></tr></table></figure><p>这里的C=CN指的是国别代码，CN=Root CA的含义是CA根证书的名称是Root CA。</p><h4 id="查看根证书详细信息">查看根证书详细信息</h4><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">openssl x509 -<span class="keyword">in</span> ca.crt -text -noout</span><br></pre></td></tr></table></figure><p><img data-src="image-20220508171315027.png" /></p><h3 id="生成域名证书">生成域名证书</h3><h4 id="生成域名证书的私钥">生成域名证书的私钥</h4><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">openssl ecparam -genkey -name prime256v1 -out harbor.key</span><br></pre></td></tr></table></figure><h4 id="生成域名证书签名请求csr">生成域名证书签名请求CSR</h4><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">openssl req -new -sha256 -subj <span class="string">&quot;/C=CN/CN=harbor.local&quot;</span> -key harbor.key -out harbor.local.csr</span><br></pre></td></tr></table></figure><h4 id="生成域名证书-1">生成域名证书</h4><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">openssl x509 -req -days 7305 -sha256 -CA ca.crt -CAkey ca.key -CAcreateserial -<span class="keyword">in</span> harbor.local.csr -out harbor.crt -extensions v3_ca</span><br></pre></td></tr></table></figure><h4 id="查看域名证书详细信息">查看域名证书详细信息</h4><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">openssl x509 -<span class="keyword">in</span> harbor.crt -text -noout</span><br></pre></td></tr></table></figure><p><img data-src="image-20220508172238988.png" /></p><p>此时发现生成的证书版本并不是V3，需要一些信息修正。</p><p>创建<code>v3.ext</code>文件，输入以下内容</p><figure class="highlight ini"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">authorityKeyIdentifier</span>=keyid,issuer</span><br><span class="line"><span class="attr">basicConstraints</span>=CA:<span class="literal">FALSE</span></span><br><span class="line"><span class="attr">keyUsage</span> = digitalSignature, nonRepudiation, keyEncipherment, dataEncipherment</span><br><span class="line"><span class="attr">subjectAltName</span> = @alt_names</span><br><span class="line"></span><br><span class="line"><span class="section">[alt_names]</span></span><br><span class="line"><span class="attr">DNS.1</span> = localhost</span><br></pre></td></tr></table></figure><h4 id="再次生成域名证书">再次生成域名证书</h4><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">openssl x509 -req -days 7305 -sha256 -CA ca.crt -CAkey ca.key -CAcreateserial -<span class="keyword">in</span> harbor.local.csr -out harbor.crt -extfile v3.ext</span><br></pre></td></tr></table></figure><h4 id="再次查看生成域名证书的详细信息">再次查看生成域名证书的详细信息</h4><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">openssl x509 -<span class="keyword">in</span> harbor.crt -text -noout</span><br></pre></td></tr></table></figure><p><img data-src="image-20220508172652252.png" /></p><p>此时成功创建版本v3的ecc域名证书。</p><h3 id="导入ca证书">导入CA证书</h3><h4 id="mac-os">Mac OS</h4><ol type="1"><li>打开钥匙串，找到证书</li></ol><p><img data-src="image-20220508173309850.png" /></p><ol start="2" type="1"><li>将需要导入的CA证书移动到空白处，此时会发现证书未被信任</li></ol><p><img data-src="image-20220508173356932.png" /></p><ol start="3" type="1"><li>右键此证书，查看简介，选择信任，修改选项使用此证书为<strong>始终信任</strong></li></ol><p><img data-src="image-20220508173609105.png" /></p><ol start="4" type="1"><li>在钥匙串打开该CA签发域名证书的证书，会发现<strong>域名证书已经被信任</strong>。</li></ol><p><img data-src="image-20220508173744194.png" /></p><h4 id="windows">Windows</h4><ol type="1"><li>右键需要导入的根证书，选择安装，选择<strong>导入本地计算机</strong></li></ol><p><img data-src="image-20220508174220492.png" /></p><ol start="2" type="1"><li>选择<strong>将证书放入下列存储</strong>，选择<strong>受信任的证书颁发机构</strong></li></ol><p><img data-src="image-20220508174340109.png" /></p><ol start="3" type="1"><li>完成向导</li></ol><p><img data-src="image-20220508174403758.png" /></p><ol start="4" type="1"><li>右键该CA签发的证书，此时发现证书没有问题。</li></ol><p><img data-src="image-20220508174651903.png" /></p><h4 id="linux">Linux</h4><p>大部分情况下我们都是在Linux系统上操作导入根证书的。</p><p>这里以Ubuntu为例</p><ol type="1"><li><p>在目录<code>/usr/share/ca-certificates</code>下创建一个存放额外证书的文件夹</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">sudo mkdir -p /usr/share/ca-certificates/<span class="built_in">local</span></span><br></pre></td></tr></table></figure></li><li><p>拷贝证书到证书文件夹</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">sudo cp ca.crt /usr/share/ca-certificates/<span class="built_in">local</span>/</span><br></pre></td></tr></table></figure></li><li><p>执行以下命令</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">sudo dpkg-reconfigure ca-certificates</span><br></pre></td></tr></table></figure><p><img data-src="image-20220508175418532.png" /></p></li></ol><p>​ 选择yes，然后会提示选择证书，敲一下回车，ca证书就被选中</p><p>​ <img data-src="image-20220508175641724.png" /></p><ol start="4" type="1"><li><p>导入成功</p><p><img data-src="image-20220508175724957.png" /></p></li></ol>]]></content>
    
    
    <summary type="html">&lt;p&gt;在搭建服务时，时不时需要使用https证书。而内网环境最好是使用自签名证书，便于管理。&lt;/p&gt;
&lt;p&gt;至于为什么要生成ECC证书而不是用RSA证书：ECC证书相对于RSA证书来说，体积小且速度快，而且内网使用不需要考虑兼容性。&lt;/p&gt;
&lt;p&gt;本文主要介绍如何使用OpenSSL创建自签名ecc证书。&lt;/p&gt;
&lt;p&gt;关键词：&lt;strong&gt;openssl，https&lt;/strong&gt;&lt;/p&gt;</summary>
    
    
    
    
    <category term="openssl" scheme="https://fly97.cn/tags/openssl/"/>
    
    <category term="https" scheme="https://fly97.cn/tags/https/"/>
    
  </entry>
  
  <entry>
    <title>使用Helm中部署Harbor</title>
    <link href="https://fly97.cn/p/deploy-harbor-on-K8s-with-helm/"/>
    <id>https://fly97.cn/p/deploy-harbor-on-K8s-with-helm/</id>
    <published>2022-05-07T07:26:00.000Z</published>
    <updated>2022-05-07T07:26:00.000Z</updated>
    
    <content type="html"><![CDATA[<p>Harbor 是一个开源的企业级私有 Docker 镜像仓库，我们可以利用 Harbor 在本地安全地存储和管理 Docker 镜像，而不需要将镜像上传到 Docker Hub 或其他第三方镜像仓库。</p><p>关键词：<strong>Harbor</strong></p><a id="more"></a><h3 id="安装helm">安装Helm</h3><p>我们将 Helm 客户端安装在 k8s-master 节点上</p><h4 id="用二进制版本安装">用二进制版本安装</h4><ol type="1"><li>下载 <a href="https://github.com/helm/helm/releases">需要的版本</a></li><li>解压(<code>tar -zxvf helm-v3.0.0-linux-amd64.tar.gz</code>)</li><li>在解压目中找到<code>helm</code>程序，移动到需要的目录中(<code>mv linux-amd64/helm /usr/local/bin/helm</code>)</li></ol><p>然后就可以执行客户端程序并 <a href="https://helm.sh/zh/docs/intro/quickstart/#初始化">添加稳定仓库</a>: <code>helm help</code>.</p><p><img data-src="image-20220507135650409.png" /></p><h4 id="查看版本号">查看版本号</h4><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">helm version</span><br></pre></td></tr></table></figure><h4 id="安装命令补全">安装命令补全</h4><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">echo</span> <span class="string">&#x27;source &lt;(helm completion bash)&#x27;</span> &gt;&gt; ~/.bashrc</span><br><span class="line"><span class="built_in">source</span> ~/.bashrc</span><br></pre></td></tr></table></figure><h4 id="修改helm仓库镜像">修改Helm仓库镜像</h4><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">helm repo add aliyun  https://kubernetes.oss-cn-hangzhou.aliyuncs.com/charts</span><br><span class="line">helm repo update</span><br></pre></td></tr></table></figure><p><img data-src="image-20220507135947826.png" /></p><p>查看仓库列表</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">helm repo list</span><br></pre></td></tr></table></figure><h3 id="安装harbor">安装Harbor</h3><h4 id="添加harbor仓库">添加Harbor仓库</h4><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">helm repo add harbor https://helm.goharbor.io</span><br><span class="line">helm repo update</span><br></pre></td></tr></table></figure><h4 id="搜索harbor">搜索Harbor</h4><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">helm search repo harbor/harbor</span><br></pre></td></tr></table></figure><p><img data-src="image-20220507140154997.png" /></p><h4 id="将harbor下载到本地">将Harbor下载到本地</h4><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">helm fetch harbor/harbor</span><br></pre></td></tr></table></figure><h4 id="解压缩">解压缩</h4><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">tar xvf harbor-1.9.0.tgz</span><br><span class="line"><span class="built_in">cd</span> harbor</span><br></pre></td></tr></table></figure><h4 id="修改配置">修改配置</h4><p>修改配置文件<code>values.yaml</code>，具体查看<a href="https://github.com/goharbor/harbor-helm/blob/master/README.md#configuration">GIthub配置列表</a>。</p><p>这里修改了以下几个配置：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">expose:</span></span><br><span class="line">  <span class="attr">type:</span> <span class="string">nodePort</span></span><br><span class="line">  <span class="attr">tls:</span></span><br><span class="line">    <span class="attr">enabled:</span> <span class="literal">false</span></span><br><span class="line">  <span class="attr">nodePort:</span></span><br><span class="line">    <span class="attr">ports:</span></span><br><span class="line">      <span class="attr">http:</span></span><br><span class="line">        <span class="attr">nodePort:</span> <span class="number">30004</span></span><br><span class="line">      <span class="attr">https:</span></span><br><span class="line">        <span class="attr">nodePort:</span> <span class="number">30005</span></span><br><span class="line">      <span class="attr">notary:</span></span><br><span class="line">        <span class="attr">nodePort:</span> <span class="number">30006</span></span><br><span class="line"><span class="attr">externalURL:</span> <span class="string">http://192.168.232.167:30004</span></span><br><span class="line"><span class="attr">persistence:</span></span><br><span class="line">  <span class="attr">persistentVolumeClaim:</span></span><br><span class="line">    <span class="attr">registry:</span></span><br><span class="line">      <span class="attr">existingClaim:</span> <span class="string">&quot;pvc-harbor&quot;</span></span><br><span class="line">      <span class="attr">storageClass:</span> <span class="string">&quot;-&quot;</span></span><br><span class="line">      <span class="attr">subPath:</span> <span class="string">&quot;registry&quot;</span></span><br><span class="line">    <span class="attr">chartmuseum:</span></span><br><span class="line">      <span class="attr">existingClaim:</span> <span class="string">&quot;pvc-harbor&quot;</span></span><br><span class="line">      <span class="attr">storageClass:</span> <span class="string">&quot;-&quot;</span></span><br><span class="line">      <span class="attr">subPath:</span> <span class="string">&quot;chartmuseum&quot;</span></span><br><span class="line">    <span class="attr">jobservice:</span></span><br><span class="line">      <span class="attr">existingClaim:</span> <span class="string">&quot;pvc-harbor&quot;</span></span><br><span class="line">      <span class="attr">storageClass:</span> <span class="string">&quot;-&quot;</span></span><br><span class="line">      <span class="attr">subPath:</span> <span class="string">&quot;jobservice&quot;</span></span><br><span class="line">    <span class="attr">database:</span></span><br><span class="line">      <span class="attr">existingClaim:</span> <span class="string">&quot;pvc-harbor&quot;</span></span><br><span class="line">      <span class="attr">storageClass:</span> <span class="string">&quot;-&quot;</span></span><br><span class="line">      <span class="attr">subPath:</span> <span class="string">&quot;database&quot;</span></span><br><span class="line">    <span class="attr">redis:</span></span><br><span class="line">      <span class="attr">existingClaim:</span> <span class="string">&quot;pvc-harbor&quot;</span></span><br><span class="line">      <span class="attr">storageClass:</span> <span class="string">&quot;-&quot;</span></span><br><span class="line">      <span class="attr">subPath:</span> <span class="string">&quot;redis&quot;</span></span><br></pre></td></tr></table></figure><p>其中 <code>pvc-harbor</code> 是预先创建好的 PVC，创建过程略。</p><p>部署 Harbor：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line">$ helm install -g harbor/harbor -f values.yaml .</span><br><span class="line"></span><br><span class="line">NAME: harbor-1589352564</span><br><span class="line">LAST DEPLOYED: Wed May 13 14:49:25 2020</span><br><span class="line">NAMESPACE: default</span><br><span class="line">STATUS: deployed</span><br><span class="line">REVISION: 1</span><br><span class="line">TEST SUITE: None</span><br><span class="line">NOTES:</span><br><span class="line">Please <span class="built_in">wait</span> <span class="keyword">for</span> several minutes <span class="keyword">for</span> Harbor deployment to complete.</span><br><span class="line">Then you should be able to visit the Harbor portal at http://192.168.232.167:30004.</span><br><span class="line">For more details, please visit https://github.com/goharbor/harbor.</span><br></pre></td></tr></table></figure><p>部署成功，等待 Harbor 启动完成，然后就可以通过<code>http://192.168.232.167:30004</code> 访问Harbor，默认用户名是admin，密码是Harbor12345。</p><p>由于 Docker 自从 1.3.x 之后，docker registry 交互默认使用的是HTTPS，而我们搭建的 Harbor 使用的是HTTP，所以为了避免 pull/push 镜像时得到错误：<code>http: server gave HTTP response to HTTPS client</code>，需要修改 docker 的配置文件 <code>/etc/docker/daemon.json</code>，加入以下配置：</p><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">&#123;</span><br><span class="line">    <span class="attr">&quot;insecure-registries&quot;</span>: [<span class="string">&quot;192.168.232.167:30004&quot;</span>]</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>保存后重启 docker 服务：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">sudo systemctl restart docker</span><br></pre></td></tr></table></figure><h3 id="上传第一个镜像">上传第一个镜像</h3><p>下载一个 busybox 镜像：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">docker pull busybox:latest</span><br></pre></td></tr></table></figure><p>修改 tag：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">docker tag busybox:latest 192.168.232.167:30004/library/busybox:latest</span><br></pre></td></tr></table></figure><p>library 是 Harbor 的默认项目地址，也可以登录 Harbor 自行新建一个项目。</p><p>使用 docker login 登录到 Harbor，并输入用户名和密码：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">docker login 192.168.232.167:30004</span><br></pre></td></tr></table></figure><p>登录成功后，上传镜像到 Harbor：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">docker push 192.168.232.167:30004/library/busybox:latest</span><br></pre></td></tr></table></figure><h3 id="后记">后记</h3><p>上面的软件没有部署完，<strong>例如PVC和helm的架构还不懂，</strong>因此先告一段落。</p><p>决定先使用<code>docker-compose</code>先部署<strong>Harbor</strong> 。</p><h3 id="参考文档">参考文档</h3><p>https://helm.sh/docs/using_helm/#installing-helm</p><p>https://github.com/goharbor/harbor-helm</p><p>https://www.cnblogs.com/dukuan/p/9963744.html</p><p>https://blog.51cto.com/wangpengtai/2418636?source=dra</p><p>http://kpali.me/2020/05/13/deploy-harbor-in-kubernetes.html</p>]]></content>
    
    
    <summary type="html">&lt;p&gt;Harbor 是一个开源的企业级私有 Docker 镜像仓库，我们可以利用 Harbor 在本地安全地存储和管理 Docker 镜像，而不需要将镜像上传到 Docker Hub 或其他第三方镜像仓库。&lt;/p&gt;
&lt;p&gt;关键词：&lt;strong&gt;Harbor&lt;/strong&gt;&lt;/p&gt;</summary>
    
    
    
    
    <category term="k8s" scheme="https://fly97.cn/tags/k8s/"/>
    
    <category term="harbor" scheme="https://fly97.cn/tags/harbor/"/>
    
  </entry>
  
  <entry>
    <title>应用编排与管理：核心原理</title>
    <link href="https://fly97.cn/p/core-concept-about-resource-orchestration-and-management/"/>
    <id>https://fly97.cn/p/core-concept-about-resource-orchestration-and-management/</id>
    <published>2022-05-07T07:26:00.000Z</published>
    <updated>2022-05-07T07:26:00.000Z</updated>
    
    <content type="html"><![CDATA[<p>本文是CNCF × Alibaba 云原生技术公开课的<strong>第五章应用编排与管理：核心原理</strong>学习笔记。</p><p>关键词：<strong>k8s</strong></p><a id="more"></a><h3 id="资源元信息">资源元信息</h3><p>kubernetes的资源组成主要包括<strong>Spec、Status两部分</strong>。其中Spec部分用来描述期望状态，Status部分用来表述观测状态。</p><p>Kubernetes的元数据部分主要包括</p><ul><li>用来识别资源的<strong>标签：Labels</strong></li><li>用来描述资源的<strong>注解：Annotation</strong></li><li>用来描述多个资源关系之间相互部分的<strong>OwnerReference</strong></li></ul><h4 id="labels">Labels</h4><p>资源标签是一<strong>种标识型的Key：Value</strong>元数据。</p><p>以下是几个例子</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">enviroment:</span> <span class="string">production</span></span><br><span class="line"><span class="attr">release:</span> <span class="string">stable</span></span><br><span class="line"><span class="attr">app.kubernetes.io/version:</span> <span class="number">5.7</span><span class="number">.21</span></span><br><span class="line"><span class="attr">failure-domain.beta.kubernetes.io/region:</span> <span class="string">cn-hangzhou</span></span><br></pre></td></tr></table></figure><p>前三个label都打在了Pod对象上，分别标识了<strong>对应的应用环境</strong>、<strong>发布的成熟度</strong>、<strong>和应用的版本</strong>。</p><p>最后一个标签打在Node对象上，并在域名前面加了版本的标识和beta字符串。</p><p>标签主要用来筛选资源和组合资源，可以使用<strong>类似SQL查询Select，来根据Label查询和相关的资源</strong>。</p><p><img data-src="image-20220507170230377.png" /></p><h4 id="selector">Selector</h4><p>Selector可以用来匹配Labels的资源。</p><p>假设系统有以下Pod：</p><h5 id="相等型selector">相等型Selector</h5><p><img data-src="image-20220507170750082.png" /></p><p>通过<code>Tie=front</code>可以匹配左边栏的Pod。</p><p>相等型的Selector可以包括<strong>多个相等条件即逻辑与</strong>的关系。</p><p>通过<code>Tie=front, Env=dev</code>的Selector，可以筛选出左上角的Pod。</p><h5 id="集合型selector">集合型Selector</h5><p><img data-src="image-20220507171530472.png" /></p><ul><li><strong>in操作</strong><ul><li>通过<code>Env in (test, gray)</code>可以<code>tie</code> 是<code>front</code>或者<code>back</code>的Pod</li><li>通过<code>release</code>可以筛选出存在 <code>release</code>标签的Pod</li></ul></li><li><strong>notin操作</strong>：<ul><li>通过<code>tie notin(front,back)</code>可以筛选出所有<code>tie</code> 不是<code>front</code>且不是<code>back</code>的Pod；</li><li>通过<code>!release</code>可以筛选不存在 <code>release</code>标签的Pod</li></ul></li></ul><p>Tips：集合型和相等型的Selector<strong>可以用<code>,</code>来连接，表示逻辑与</strong>的关系</p><h4 id="annotations">Annotations</h4><p>Annotations（注解）一般是系统或者工具用来存储资源的非标识信息，可以用来拓展资源的<strong>spec/status</strong>的描述。</p><p>以下是几个例子</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">deployment.kubernetes.io/revision:</span> <span class="string">&quot;1&quot;</span></span><br><span class="line"><span class="attr">kubectl.kubernetes.io/last-applied-configuration:</span> <span class="string">|</span></span><br><span class="line">&#123;<span class="string">&quot;apiVersion&quot;</span><span class="string">:&quot;apps/v1&quot;</span>,<span class="string">&quot;kind&quot;</span><span class="string">:&quot;Deployment&quot;</span>,<span class="string">&quot;metadata&quot;</span><span class="string">:</span>&#123;<span class="string">&quot;annotations&quot;</span><span class="string">:</span>&#123;&#125;,<span class="string">&quot;name&quot;</span><span class="string">:&quot;busybox-deployment&quot;</span>,<span class="string">&quot;namespace&quot;</span><span class="string">:&quot;default&quot;</span>&#125;,<span class="string">&quot;spec&quot;</span><span class="string">:</span>&#123;<span class="string">&quot;replicas&quot;</span><span class="string">:2</span>,<span class="string">&quot;selector&quot;</span><span class="string">:</span>&#123;<span class="string">&quot;matchLabels&quot;</span><span class="string">:</span>&#123;<span class="string">&quot;app&quot;</span><span class="string">:&quot;busybox&quot;</span>&#125;&#125;,<span class="string">&quot;template&quot;</span><span class="string">:</span>&#123;<span class="string">&quot;metadata&quot;</span><span class="string">:</span>&#123;<span class="string">&quot;labels&quot;</span><span class="string">:</span>&#123;<span class="string">&quot;app&quot;</span><span class="string">:&quot;busybox&quot;</span>&#125;&#125;,<span class="string">&quot;spec&quot;</span><span class="string">:</span>&#123;<span class="string">&quot;containers&quot;</span><span class="string">:</span>[&#123;<span class="string">&quot;args&quot;</span><span class="string">:</span>[<span class="string">&quot;-c&quot;</span>,<span class="string">&quot;while true ; do continue ; done&quot;</span>],<span class="string">&quot;command&quot;</span><span class="string">:</span>[<span class="string">&quot;sh&quot;</span>],<span class="string">&quot;image&quot;</span><span class="string">:&quot;busybox&quot;</span>,<span class="string">&quot;name&quot;</span><span class="string">:&quot;busybox&quot;</span>,<span class="string">&quot;ports&quot;</span><span class="string">:</span>[&#123;<span class="string">&quot;containerPort&quot;</span><span class="string">:80</span>&#125;]&#125;]&#125;&#125;&#125;&#125;</span><br></pre></td></tr></table></figure><p><img data-src="image-20220507173513904.png" /></p><h4 id="ownereference">Ownereference</h4><p>Ownereference（所有者）一般是指集合类的资源，比如说Pod集合，比如<strong>replicaset、statefulset</strong>等。</p><p>集合类资源的控制器会创建对应的归属资源。</p><p>比如：<strong>replicaset</strong>控制器在操作中会创建Pod，则被创建Pod的Ownerefence就指向了创建了Pod的replicaset。</p><p>Ownereference可以使得用户可以<strong>方便的查找一个创建资源的对象</strong>，还可以<strong>实现级联删除</strong>的效果。</p><p><img data-src="image-20220507174246402.png" /></p><h3 id="操作演示">操作演示</h3><h4 id="查看pod">查看Pod</h4><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">kubectl get pods</span><br></pre></td></tr></table></figure><h4 id="创建pod">创建Pod</h4><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">kubectl apply -f pod1.yaml</span><br></pre></td></tr></table></figure><h4 id="查看标签">查看标签</h4><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">kubectl get pods --show-labels</span><br></pre></td></tr></table></figure><h4 id="查看具体的资源信息">查看具体的资源信息</h4><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">kubectl get pods nginx1 -o yaml | less</span><br></pre></td></tr></table></figure><h4 id="修改已有的资源标签">修改已有的资源标签</h4><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">kubectl label pods nginx1 env=<span class="built_in">test</span></span><br></pre></td></tr></table></figure><p>如果想覆盖已有的标签信息需要加上<code>--overwide</code></p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">kubectl label pods nginx1 env=<span class="built_in">test</span> --overwide</span><br></pre></td></tr></table></figure><h4 id="去掉已有的标签">去掉已有的标签</h4><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">kubectl label pods nginx tie-</span><br></pre></td></tr></table></figure><h4 id="使用selector对pod进行筛选">使用Selector对Pod进行筛选</h4><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">kubectl get pods --show-labels -l env=<span class="built_in">test</span></span><br><span class="line">kubectl get pods --show-labels -l env=test1,env=test2 <span class="comment"># 一个也筛选不出来</span></span><br><span class="line">kubectl get pods --show-labels -l env=dev,tie=front</span><br><span class="line">kubectl get pods --show-labels -l <span class="string">&#x27;env in (dev,test)&#x27;</span></span><br></pre></td></tr></table></figure><h4 id="增加注解">增加注解</h4><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">kubectl annotate pods nginx1 my-annotate=<span class="string">&#x27;my annotate,ok&#x27;</span></span><br></pre></td></tr></table></figure><h3 id="控制器模式">控制器模式</h3><h4 id="控制循环">控制循环</h4><p>控制型模式最核心的就是控制循环的概念。在控制循环中包括了控制器，被控制的系统，以及能够观测系统的传感器，三个逻辑组件。</p><p>这些<strong>组件都是逻辑</strong>的，外界通过修改资源 spec 来控制资源，控制器比较资源 spec 和 status，从而计算一个 diff，diff 最后会用来决定执行对系统进行什么样的控制操作，控制操作会使得系统产生新的输出，并被传感器以资源 status 形式上报，控制器的各个组件将都会是独立自主地运行，不断使系统向 spec 表示终态趋近。</p><p><img data-src="image-20220508160446196.png" /></p><h4 id="传感器">传感器</h4><p>控制循环中逻辑的传感器主要由 Reflector、Informer、Indexer 三个组件构成。</p><ul><li>Reflector 通过 List 和 Watch K8s server 来获取资源的数据；<ul><li>List 用来在 Controller 重启以及 Watch 中断的情况下，进行系统资源的全量更新；</li><li>Watch 则在多次 List 之间进行增量的资源更新；</li></ul></li><li>Reflector 在获取新的资源数据后，会在 Delta 队列中塞入一个包括资源对象信息本身以及资源对象事件类型的 Delta 记录；</li><li>Delta 队列中可以保证同一个对象在队列中仅有一条记录，从而避免 Reflector 重新 List 和 Watch 的时候产生重复的记录。</li><li>Informer 组件不断地从 Delta 队列中弹出 delta 记录，然后把资源对象交给 indexer，让 indexer 把资源记录在一个缓存中，缓存在默认设置下是用资源的命名空间来做索引的，并且可以被 Controller Manager 或多个 Controller 所共享。之后，再把这个事件交给事件的回调函数</li></ul><p><img data-src="image-20220508161637650.png" /></p><p>控制循环中的控制器组件主要由事件处理函数以及 worker 组成，事件处理函数之间会相互关注资源的新增、更新、删除的事件，并根据控制器的逻辑去决定是否需要处理。对需要处理的事件，会把事件关联资源的命名空间以及名字塞入一个工作队列中，并且由后续的 worker 池中的一个 Worker 来处理，工作队列会对存储的对象进行去重，从而避免多个 Woker 处理同一个资源的情况。</p><p>Worker 在处理资源对象时，一般需要用资源的名字来重新获得最新的资源数据，用来创建或者更新资源对象，或者调用其他的外部服务，Worker 如果处理失败的时候，一般情况下会把资源的名字重新加入到工作队列中，从而方便之后进行重试。</p><p><img data-src="image-20220508161830611.png" /></p><h3 id="控制器模式总结">控制器模式总结</h3><h4 id="两种api设计方法">两种API设计方法</h4><p>Kubernetes 控制器模式<strong>依赖声明式的 API</strong>。另外一种常见的 API 类型是命令式 API。</p><p><img data-src="image-20220508161904833.png" /></p><p><img data-src="image-20220508161848698.png" /></p>]]></content>
    
    
    <summary type="html">&lt;p&gt;本文是CNCF × Alibaba 云原生技术公开课的&lt;strong&gt;第五章应用编排与管理：核心原理&lt;/strong&gt;学习笔记。&lt;/p&gt;
&lt;p&gt;关键词：&lt;strong&gt;k8s&lt;/strong&gt;&lt;/p&gt;</summary>
    
    
    
    
    <category term="k8s" scheme="https://fly97.cn/tags/k8s/"/>
    
  </entry>
  
  <entry>
    <title>Kubernetes集群实践（二）| 如何编写k8s资源清单yaml文件</title>
    <link href="https://fly97.cn/p/how-to-write-k8s-resource-manifest-yaml/"/>
    <id>https://fly97.cn/p/how-to-write-k8s-resource-manifest-yaml/</id>
    <published>2022-05-06T07:26:00.000Z</published>
    <updated>2022-05-06T07:26:00.000Z</updated>
    
    <content type="html"><![CDATA[<p>本文主要介绍如何编写k8s资源清单yaml文件。</p><p>在K8s集群上进行资源配置通常都是通过yaml文件进行的，因此如何快速编写yaml文件就变得十分重要。</p><p>关键词：<strong>k8s</strong></p><a id="more"></a><h3 id="yaml文件格式">yaml文件格式</h3><p>这个可以参考我的上一篇文章。</p><h3 id="yaml资源清单基本格式">yaml资源清单基本格式</h3><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">apiVersion:</span> <span class="string">apps/v1</span> <span class="comment"># API版本</span></span><br><span class="line"><span class="attr">kind:</span> <span class="string">Deployment</span>    <span class="comment"># 资源类型</span></span><br><span class="line"><span class="attr">metadata:</span>           <span class="comment"># 资源元数据</span></span><br><span class="line"> <span class="attr">name:</span> <span class="string">web</span></span><br><span class="line"> <span class="attr">namespace:</span> <span class="string">default</span></span><br><span class="line"><span class="attr">spec:</span>               <span class="comment"># 资源的期望值</span></span><br><span class="line"> <span class="attr">replicas:</span> <span class="number">3</span>        <span class="comment"># 副本实例数目</span></span><br><span class="line"> <span class="attr">selector:</span>          <span class="comment"># 资源选择器，与下面的metadata.labels保持一致</span></span><br><span class="line">  <span class="attr">matchLabels:</span></span><br><span class="line">   <span class="attr">app:</span> <span class="string">web</span></span><br><span class="line"> <span class="attr">template:</span>          <span class="comment"># Pod模板</span></span><br><span class="line">  <span class="attr">metadata:</span>         <span class="comment"># Pod元数据</span></span><br><span class="line">  <span class="attr">labels:</span>         </span><br><span class="line">   <span class="attr">app:</span> <span class="string">web</span></span><br><span class="line">  <span class="attr">spec:</span>             <span class="comment"># Pod资源的期望值</span></span><br><span class="line">   <span class="attr">containers:</span>      <span class="comment"># 容器配置</span></span><br><span class="line">   <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">web</span></span><br><span class="line">   <span class="bullet">-</span> <span class="attr">image:</span> <span class="string">busybox</span> </span><br></pre></td></tr></table></figure><h3 id="kubernetes-的-api"><strong>Kubernetes</strong> 的 <strong>API</strong></h3><p><img data-src="image-20220506105442472.png" /></p><p>当我们去提交一个 Pod，或者 get 一个 Pod 的时候，它的 content 内容都是用 JSON 或者是 YAML 表达的。上图中有个 yaml 的例子，在这个 yaml file 中，对 Pod 资源的描述也分为几个部分。</p><p>第一个部分，一般来讲会是 API 的 <strong>version</strong>。比如在这个例子中是 V1，它也会描述我在操作哪个资源；比如说我的 <strong>kind</strong> 如果是 pod，在 Metadata 中，就写上这个 Pod 的名字；比如说 nginx，我们也会给它打一些 <strong>label</strong>，我们等下会讲到 label 的概念。在 Metadata 中，有时候也会去写 <strong>annotation</strong>，也就是对资源的额外的一些用户层次的描述。</p><p>比较重要的一个部分叫做 <strong>Spec</strong>，<strong>Spec 也就是我们希望 Pod 达到的一个预期的状态</strong>。比如说它内部需要有哪些 container 被运行；比如说这里面有一个 nginx 的 container，它的 image 是什么？它暴露的 port 是什么？</p><p>当我们从 Kubernetes API 中去获取这个资源的时候，一般来讲在 Spec 下面会有一个项目叫 <strong>status</strong>，它表达了这个资源当前的状态；比如说一个 Pod 的状态可能是正在被调度、或者是已经 running、或者是已经被 terminates，就是被执行完毕了。</p><p>刚刚在 API 之中，我们讲了一个比较有意思的 metadata 叫做“<strong>label</strong>”，这个 label 可以是一组<strong>键值对</strong>。</p><p>比如下图的第一个 pod 中，label 就可能是一个 color 等于 red，即它的颜色是红颜色。当然你也可以加其他 label，比如说 size: big 就是大小，定义为大的，它可以是一组 label。</p><p>这些 label 是可以被 selector，也就是选择器所查询的。这个能力实际上跟我们的 sql 类型的 select 语句是非常相似的，比如下图中的三个 Pod 资源中，我们就可以进行 select。name color 等于 red，就是它的颜色是红色的，我们也可以看到，只有两个被选中了，因为只有他们的 label 是红色的，另外一个 label 中写的 color 等于 yellow，也就是它的颜色是黄色，是不会被选中的。</p><p><img data-src="image-20220506110134831.png" /></p><p>通过 label，kubernetes 的 API 层就可以对这些资源进行一个筛选，那这些筛选也是 kubernetes 对资源的集合所表达默认的一种方式。</p><h3 id="使用yaml创建资源对象deployment">使用yaml创建资源对象Deployment</h3><p>资源清单可以通过以下命令导出，会有很多冗余信息</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">kubectl get 资源类型 -n 命名空间 -o yaml</span><br></pre></td></tr></table></figure><p>省略<code>-n</code>参数将<strong>导出默认命名空间<code>default</code></strong>的资源信息</p><blockquote><p>以上名字在查看<strong>配置文件其他字段含义</strong>时十分有用！</p></blockquote><p>使用以下命令可查看所有pod的命名空间</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">kubectl get pods -o wide --all-namespaces</span><br></pre></td></tr></table></figure><p>apiVersion字段可以通过以下命令查询</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">kubectl api-resources |grep 资源类型</span><br></pre></td></tr></table></figure><h4 id="资源对象文件">资源对象文件</h4><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">apiVersion:</span> <span class="string">apps/v1</span><span class="comment"># API版本【必需字段】</span></span><br><span class="line"><span class="attr">Kind:</span> <span class="string">Deployment</span>        <span class="comment"># 资源对象【必需字段】</span></span><br><span class="line"><span class="attr">metedata:</span>               <span class="comment"># 资源元数据【必须字段】</span></span><br><span class="line">  <span class="attr">name:</span> <span class="string">web</span>             <span class="comment"># 资源名称</span></span><br><span class="line">  <span class="attr">namespace:</span> <span class="string">default</span>    <span class="comment"># 资源所在的命名空间</span></span><br><span class="line"><span class="attr">spec:</span>                   <span class="comment"># deployment的期望状态</span></span><br><span class="line">  <span class="attr">replicas:</span> <span class="number">1</span>           <span class="comment"># 副本实例数量</span></span><br><span class="line">  <span class="attr">selector:</span>      <span class="comment"># 标签选择器，与下文中的metadata.labels保持一致</span></span><br><span class="line">    <span class="attr">matchLabels:</span>                </span><br><span class="line">      <span class="attr">app:</span> <span class="string">web</span></span><br><span class="line">  <span class="attr">template:</span>   <span class="comment"># Pod模板</span></span><br><span class="line">    <span class="attr">metedata:</span> <span class="comment"># Pod元数据</span></span><br><span class="line">      <span class="attr">labels:</span> <span class="comment"># 定义标签</span></span><br><span class="line">      <span class="comment"># 标签一般根据项目属性去写</span></span><br><span class="line">      <span class="comment"># 例如 project: ms</span></span><br><span class="line">      <span class="attr">app:</span> <span class="string">web</span> <span class="comment"># 标签</span></span><br><span class="line">    <span class="attr">spec:</span>    <span class="comment"># Pod规格</span></span><br><span class="line">      <span class="attr">containers:</span>  <span class="comment"># 容器配置</span></span><br><span class="line">      <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">web</span>  <span class="comment"># 容器名</span></span><br><span class="line">        <span class="attr">image:</span> <span class="string">busybox</span>  <span class="comment"># 镜像名</span></span><br><span class="line">        <span class="comment">#command: [&quot;sh&quot;]     # 容器执行命令</span></span><br><span class="line">        <span class="comment">#args: [&quot;-c&quot;, &quot;while true ; do continue ; done&quot;] # 命令执行的参数</span></span><br></pre></td></tr></table></figure><p>以上资源清单等同于</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">kubectl create deployment web --image=busybox --replicas=3 -n default</span><br></pre></td></tr></table></figure><h3 id="使用yaml创建资源对象service">使用yaml创建资源对象Service</h3><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">apiVersion:</span> <span class="string">v1</span>              <span class="comment"># api版本【必需字段】</span></span><br><span class="line"><span class="attr">kind:</span> <span class="string">Service</span>               <span class="comment"># 资源类型【必需字段】</span></span><br><span class="line"><span class="attr">metedata:</span>                   <span class="comment"># 资源元数据【必需字段】</span></span><br><span class="line">  <span class="attr">name:</span> <span class="string">web</span></span><br><span class="line">  <span class="attr">namespace:</span> <span class="string">default</span>        <span class="comment"># 资源所在namespace</span></span><br><span class="line"><span class="attr">spec:</span>                       <span class="comment"># 期望的资源配置</span></span><br><span class="line">  <span class="attr">ports:</span> <span class="number">80</span>                 <span class="comment"># Service端口，通过Cluster IP访问时使用的端口</span></span><br><span class="line">  <span class="bullet">-</span> <span class="attr">protocol:</span> <span class="string">TCP</span>             <span class="comment"># 通信协议</span></span><br><span class="line">    <span class="attr">targetPort:</span> <span class="number">8080</span>          <span class="comment"># 容器内部的通信端口</span></span><br><span class="line">  <span class="attr">selector:</span> </span><br><span class="line">    <span class="attr">app:</span> <span class="string">web</span>                  <span class="comment"># 标签</span></span><br><span class="line">  <span class="attr">type:</span> <span class="string">NodePort</span>            <span class="comment"># Service暴露类型，NodePort允许集群外部访问</span></span><br></pre></td></tr></table></figure><p>以上资源清单等同于：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">kubectl expose sevice web --port=80 --target-port=8080 --<span class="built_in">type</span>=NodePort -n default</span><br></pre></td></tr></table></figure><p>可以将以上两个资源清单合并成一个yaml文件，两个文件中间需要有<code>---</code>分割线，否则<strong>kubectl只识别最后一个yaml的内容</strong>。</p><h3 id="快速编写k8s资源清单">快速编写K8s资源清单</h3><h4 id="用create命令生成deployment的yaml">用create命令生成deployment的yaml</h4><blockquote><p>导出的yaml文件有很多冗余字段，不需要可以删除</p><p>如：creationTimestamp和status可以删除</p></blockquote><p>使用<code>--dry-run</code>可以检查是否有错误</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">kubectl create deployment nginx --image=nginx -o yaml --dry-run=client &gt; deployment.yaml</span><br></pre></td></tr></table></figure><h4 id="用expose命令生成service的yaml">用expose命令生成service的yaml</h4><blockquote><p>导出的yaml文件有很多冗余字段，不需要可以删除</p><p>如：creationTimestamp和status可以删除</p></blockquote><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">kubectl expose servcie nginx --port=80 --target-port=8080 --<span class="built_in">type</span>=NodePort -o yaml --dry-run=client &gt; service.yaml</span><br></pre></td></tr></table></figure><h4 id="用get命令导出yaml">用get命令导出yaml</h4><blockquote><p>这种方式导出的yaml存在很多冗余</p></blockquote><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">kubectl get deployment nginx -o yaml &gt; deployment.yml</span><br></pre></td></tr></table></figure><h3 id="怎么查询资源清单文件的指定字段">怎么查询资源清单文件的指定字段</h3><p>以下命令能查到pod.spec.containers下的所有字段</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">kubectl explain pods.spec.containers</span><br></pre></td></tr></table></figure><h3 id="如何查询资源所有字段">如何查询资源所有字段</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">kubectl explain deployment</span><br></pre></td></tr></table></figure><h3 id="模糊查询资源类型">模糊查询资源类型</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">kubectl api-resources | grep 资源类型的模糊字母</span><br></pre></td></tr></table></figure><h3 id="检查yaml语法是否有误">检查yaml语法是否有误</h3><p>可以使用<strong>kube-linter</strong>来检查yaml语法是否有误</p><blockquote><p>https://github.com/stackrox/kube-linter</p></blockquote><h4 id="执行检测">执行检测</h4><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">kube-linter lint pod.yaml</span><br></pre></td></tr></table></figure><h3 id="参考">参考</h3><ul><li>https://blog.csdn.net/omaidb/article/details/121855353</li></ul>]]></content>
    
    
    <summary type="html">&lt;p&gt;本文主要介绍如何编写k8s资源清单yaml文件。&lt;/p&gt;
&lt;p&gt;在K8s集群上进行资源配置通常都是通过yaml文件进行的，因此如何快速编写yaml文件就变得十分重要。&lt;/p&gt;
&lt;p&gt;关键词：&lt;strong&gt;k8s&lt;/strong&gt;&lt;/p&gt;</summary>
    
    
    
    
    <category term="k8s" scheme="https://fly97.cn/tags/k8s/"/>
    
  </entry>
  
  <entry>
    <title>YAML简明教程</title>
    <link href="https://fly97.cn/p/yaml-concise-tutorial/"/>
    <id>https://fly97.cn/p/yaml-concise-tutorial/</id>
    <published>2022-05-06T02:26:00.000Z</published>
    <updated>2022-05-06T02:26:00.000Z</updated>
    
    <content type="html"><![CDATA[<p>本文主要介绍YAML 。</p><p>YAML 是 "YAML Ain't a Markup Language"（YAML 不是一种标记语言）的递归缩写。在开发的这种语言时，YAML 的意思其实是："Yet Another Markup Language"（仍是一种标记语言）。</p><p>YAML 的语法和其他高级语言类似，并且可以简单表达清单、散列表，标量等数据形态。它使用空白符号缩进和大量依赖外观的特色，特别适合用来表达或编辑数据结构、各种配置文件、倾印调试内容、文件大纲（例如：许多电子邮件标题格式和YAML非常接近）。</p><p>YAML 的配置文件后缀为 <strong>yml</strong>或<strong>yaml</strong>。</p><p>关键词：<strong>YAML</strong></p><a id="more"></a><h3 id="yaml文件格式">yaml文件格式</h3><h4 id="基本语法">基本语法</h4><ul><li>大小写敏感</li><li>使用缩进表示层级关系</li><li>缩进<strong>不允许使用Tab，只允许空格</strong></li><li>缩进的空格数不重要，只要相同层级的元素左对齐即可</li><li><code>#</code>表示注释</li><li><code>---</code>表示yaml格式文件的开始</li></ul><h4 id="数据类型">数据类型</h4><p>YAML支持以下数据类型</p><ul><li>对象：键值对的几个，又称为映射（mapping）/ 哈希（hashes）/ 字典（dictionary）</li><li>数组：一组按次序排列的值，又称为序列（sequence）/ 列表（list）</li><li>标量（scalars）：单个的、不可再分的值</li></ul><h4 id="yaml对象">YAML对象</h4><p>对象键值对使用冒号结构表示key-value，<strong>冒号后面要加一个空格。</strong></p><p>也可以使用<code>key:&#123;key1: value, key2: value, ...&#125;</code></p><p>也可以使用缩进表示层级关系：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">key:</span></span><br><span class="line">    <span class="attr">key1:</span> <span class="string">value</span></span><br><span class="line">    <span class="attr">key2:</span> <span class="string">value</span></span><br></pre></td></tr></table></figure><p>对于较为复杂的对象格式，可以使用<strong>问号加一个空格表示一个复杂的key</strong>，配合<strong>一个冒号加一个空格表示一个value</strong></p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="string">?</span></span><br><span class="line">    <span class="bullet">-</span> <span class="string">complexkey1</span></span><br><span class="line">    <span class="bullet">-</span> <span class="string">complexkey2</span></span><br><span class="line"><span class="string">:</span></span><br><span class="line"><span class="bullet">-</span> <span class="string">complexvalue1</span></span><br><span class="line"><span class="bullet">-</span> <span class="string">complexvalue2</span></span><br></pre></td></tr></table></figure><p>即对象的属性是一个数组<code>[complexkey1,complexkey2]</code>，对应的值也是一个数组<code>[complexvalue1, complexvalue2]</code></p><h4 id="yaml数组">YAML数组</h4><p>以<code>-</code>开头的行表示构成一个数组</p><p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="bullet">-</span> <span class="string">A</span></span><br><span class="line"><span class="bullet">-</span> <span class="string">B</span></span><br><span class="line"><span class="bullet">-</span> <span class="string">C</span></span><br></pre></td></tr></table></figure></p><p>支持多维数组，可以使用<strong>行内表示</strong></p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">key:</span> [<span class="string">value1</span>, <span class="string">value2</span>, <span class="string">...</span>]</span><br></pre></td></tr></table></figure><p>如果子成员是一个数组，则可以在下面缩进一个空格</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="bullet">-</span> </span><br><span class="line"> <span class="bullet">-</span> <span class="string">A</span></span><br><span class="line"> <span class="bullet">-</span> <span class="string">B</span></span><br><span class="line"> <span class="bullet">-</span> <span class="string">C</span></span><br></pre></td></tr></table></figure><p>一个复杂的例子：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">companies:</span></span><br><span class="line">    <span class="bullet">-</span></span><br><span class="line">        <span class="attr">id:</span> <span class="number">1</span></span><br><span class="line">        <span class="attr">name:</span> <span class="string">company1</span></span><br><span class="line">        <span class="attr">price:</span> <span class="string">200W</span></span><br><span class="line">    <span class="bullet">-</span></span><br><span class="line">        <span class="attr">id:</span> <span class="number">2</span></span><br><span class="line">        <span class="attr">name:</span> <span class="string">company2</span></span><br><span class="line">        <span class="attr">price:</span> <span class="string">500W</span></span><br></pre></td></tr></table></figure><p>即<code>companies</code>属性是一个数组，每一个数组元素又是由<code>id/name/price</code>三个属性组成。</p><p>数组也可以使用<strong>行内表示</strong></p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">companies:</span> [&#123;<span class="attr">id:</span> <span class="number">1</span>,<span class="attr">name:</span> <span class="string">company1</span>,<span class="attr">price:</span> <span class="string">200W</span>&#125;,&#123;<span class="attr">id:</span> <span class="number">2</span>,<span class="attr">name:</span> <span class="string">company2</span>,<span class="attr">price:</span> <span class="string">500W</span>&#125;]</span><br></pre></td></tr></table></figure><h4 id="复合结构">复合结构</h4><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">languages:</span></span><br><span class="line">  <span class="bullet">-</span> <span class="string">Ruby</span></span><br><span class="line">  <span class="bullet">-</span> <span class="string">Perl</span></span><br><span class="line">  <span class="bullet">-</span> <span class="string">Python</span> </span><br><span class="line"><span class="attr">websites:</span></span><br><span class="line">  <span class="attr">YAML:</span> <span class="string">yaml.org</span> </span><br><span class="line">  <span class="attr">Ruby:</span> <span class="string">ruby-lang.org</span> </span><br><span class="line">  <span class="attr">Python:</span> <span class="string">python.org</span> </span><br><span class="line">  <span class="attr">Perl:</span> <span class="string">use.perl.org</span></span><br></pre></td></tr></table></figure><p>转换为json</p><figure class="highlight"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">&#123; </span><br><span class="line">  languages: [ &#x27;Ruby&#x27;, &#x27;Perl&#x27;, &#x27;Python&#x27;],</span><br><span class="line">  websites: &#123;</span><br><span class="line">    YAML: &#x27;yaml.org&#x27;,</span><br><span class="line">    Ruby: &#x27;ruby-lang.org&#x27;,</span><br><span class="line">    Python: &#x27;python.org&#x27;,</span><br><span class="line">    Perl: &#x27;use.perl.org&#x27; </span><br><span class="line">  &#125; </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="标量">标量</h4><p>标量是不可再分的值，包括</p><ul><li>字符串</li><li>布尔值</li><li>整数</li><li>浮点数</li><li>NULL</li><li>时间</li><li>日期</li></ul><p>通过一个例子来了解纯量的基本使用</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">boolean:</span> </span><br><span class="line">    <span class="bullet">-</span> <span class="literal">TRUE</span>  <span class="comment">#true,True都可以</span></span><br><span class="line">    <span class="bullet">-</span> <span class="literal">FALSE</span>  <span class="comment">#false，False都可以</span></span><br><span class="line"><span class="attr">float:</span></span><br><span class="line">    <span class="bullet">-</span> <span class="number">3.14</span></span><br><span class="line">    <span class="bullet">-</span> <span class="number">6.8523015e+5</span>  <span class="comment">#可以使用科学计数法</span></span><br><span class="line"><span class="attr">int:</span></span><br><span class="line">    <span class="bullet">-</span> <span class="number">123</span></span><br><span class="line">    <span class="bullet">-</span> <span class="string">0b1010_0111_0100_1010_1110</span>    <span class="comment">#二进制表示</span></span><br><span class="line"><span class="attr">null:</span></span><br><span class="line">    <span class="attr">nodeName:</span> <span class="string">&#x27;node&#x27;</span></span><br><span class="line">    <span class="attr">parent:</span> <span class="string">~</span>  <span class="comment">#使用~表示null</span></span><br><span class="line"><span class="attr">string:</span></span><br><span class="line">    <span class="bullet">-</span> <span class="string">哈哈</span></span><br><span class="line">    <span class="bullet">-</span> <span class="string">&#x27;Hello world&#x27;</span>  <span class="comment">#可以使用双引号或者单引号包裹特殊字符</span></span><br><span class="line">    <span class="bullet">-</span> <span class="string">newline</span></span><br><span class="line">      <span class="string">newline2</span>    <span class="comment">#字符串可以拆成多行，每一行会被转化成一个空格</span></span><br><span class="line"><span class="attr">date:</span></span><br><span class="line">    <span class="bullet">-</span> <span class="number">2018-02-17</span>    <span class="comment">#日期必须使用ISO 8601格式，即yyyy-MM-dd</span></span><br><span class="line"><span class="attr">datetime:</span> </span><br><span class="line">    <span class="bullet">-</span>  <span class="number">2018-02-17T15:02:31+08:00</span>    <span class="comment">#时间使用ISO 8601格式，时间和日期之间使用T连接，最后使用+代表时区</span></span><br></pre></td></tr></table></figure><h4 id="引用">引用</h4><p><code>&amp;</code>锚点和<code>*</code>别名，可以用来引用</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">defaults:</span> <span class="meta">&amp;defaults</span></span><br><span class="line">  <span class="attr">adapter:</span>  <span class="string">postgres</span></span><br><span class="line">  <span class="attr">host:</span>     <span class="string">localhost</span></span><br><span class="line"></span><br><span class="line"><span class="attr">development:</span></span><br><span class="line">  <span class="attr">database:</span> <span class="string">myapp_development</span></span><br><span class="line">  <span class="string">&lt;&lt;:</span> <span class="meta">*defaults</span></span><br><span class="line"></span><br><span class="line"><span class="attr">test:</span></span><br><span class="line">  <span class="attr">database:</span> <span class="string">myapp_test</span></span><br><span class="line">  <span class="string">&lt;&lt;:</span> <span class="meta">*defaults</span></span><br></pre></td></tr></table></figure><p>相当于</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">defaults:</span></span><br><span class="line">  <span class="attr">adapter:</span>  <span class="string">postgres</span></span><br><span class="line">  <span class="attr">host:</span>     <span class="string">localhost</span></span><br><span class="line"></span><br><span class="line"><span class="attr">development:</span></span><br><span class="line">  <span class="attr">database:</span> <span class="string">myapp_development</span></span><br><span class="line">  <span class="attr">adapter:</span>  <span class="string">postgres</span></span><br><span class="line">  <span class="attr">host:</span>     <span class="string">localhost</span></span><br><span class="line"></span><br><span class="line"><span class="attr">test:</span></span><br><span class="line">  <span class="attr">database:</span> <span class="string">myapp_test</span></span><br><span class="line">  <span class="attr">adapter:</span>  <span class="string">postgres</span></span><br><span class="line">  <span class="attr">host:</span>     <span class="string">localhost</span></span><br></pre></td></tr></table></figure><p><code>&amp;</code>用来建立锚点，<code>&lt;&lt;</code>表示合并到当前数据，<code>*</code>用来引用锚点。</p><blockquote><p>此处的锚点有点类似于编程语言【指针】的含义。</p></blockquote><p>下面是另一个例子：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">- &amp;showell Steve </span><br><span class="line">- Clark </span><br><span class="line">- Brian </span><br><span class="line">- Oren </span><br><span class="line">- *showell </span><br></pre></td></tr></table></figure><p>转换为JavaScript代码如下</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">[ <span class="string">&#x27;Steve&#x27;</span>, <span class="string">&#x27;Clark&#x27;</span>, <span class="string">&#x27;Brian&#x27;</span>, <span class="string">&#x27;Oren&#x27;</span>, <span class="string">&#x27;Steve&#x27;</span> ]</span><br></pre></td></tr></table></figure><h3 id="参考地址">参考地址</h3><ul><li>https://www.runoob.com/w3cnote/yaml-intro.html</li><li>https://www.ruanyifeng.com/blog/2016/07/yaml.html</li><li>https://www.jianshu.com/p/97222440cd08</li><li>https://daihainidewo.github.io/blog/yaml%E6%95%99%E7%A8%8B/</li></ul>]]></content>
    
    
    <summary type="html">&lt;p&gt;本文主要介绍YAML 。&lt;/p&gt;
&lt;p&gt;YAML 是 &quot;YAML Ain&#39;t a Markup Language&quot;（YAML 不是一种标记语言）的递归缩写。在开发的这种语言时，YAML 的意思其实是：&quot;Yet Another Markup Language&quot;（仍是一种标记语言）。&lt;/p&gt;
&lt;p&gt;YAML 的语法和其他高级语言类似，并且可以简单表达清单、散列表，标量等数据形态。它使用空白符号缩进和大量依赖外观的特色，特别适合用来表达或编辑数据结构、各种配置文件、倾印调试内容、文件大纲（例如：许多电子邮件标题格式和YAML非常接近）。&lt;/p&gt;
&lt;p&gt;YAML 的配置文件后缀为 &lt;strong&gt;yml&lt;/strong&gt;或&lt;strong&gt;yaml&lt;/strong&gt;。&lt;/p&gt;
&lt;p&gt;关键词：&lt;strong&gt;YAML&lt;/strong&gt;&lt;/p&gt;</summary>
    
    
    
    
    <category term="yaml" scheme="https://fly97.cn/tags/yaml/"/>
    
  </entry>
  
  <entry>
    <title>应用编排与管理：Deployment</title>
    <link href="https://fly97.cn/p/core-concept-about-resource-orchestration-and-management/"/>
    <id>https://fly97.cn/p/core-concept-about-resource-orchestration-and-management/</id>
    <published>2022-05-05T07:26:00.000Z</published>
    <updated>2022-05-05T07:26:00.000Z</updated>
    
    <content type="html"><![CDATA[<p>本文是CNCF × Alibaba 云原生技术公开课的<strong>第七章应用编排与管理：Deployment</strong>学习笔记。</p><p>关键词：<strong>k8s</strong></p><a id="more"></a><h3 id="需求来源">需求来源</h3><p>假如我们直接管理集群中所有的Pod，应用A、B、C的Pod，其实是散乱的分布在集群中。</p><p><img data-src="image-20220513133334099.png" /></p><p>而且在使用过程中存在以上问题。然后就引入了今天课程的主题。</p><h4 id="deployment管理部署发布的控制器">Deployment：管理部署发布的控制器</h4><p>首先明确<strong>Doployment是管理部署发布的控制器</strong></p><p>Doployment将应用A、B、C分别规划到不同的Deployment中，<strong>每个Deployment是管理一组相同的应用Pod，这组Pod是相同的副本</strong>。</p><ol type="1"><li>Deployment定义了一种Pod期望数量。例如对于应用A，我们期望Pod的数量是四个，<code>controller</code>会持续<strong>维持Pod数量为期望的数量</strong>。当Pod出现了网络问题或者宿主机出现了问题时，<code>controller</code>可以帮我们恢复也就是新起来对应的Pod，来；</li><li>配置Pod发布方式。<code>controller</code>会<strong>按照用户给定的策略来更新Pod</strong>，<strong>更新过程中可以设定不可用Pod的数量在多少范围</strong>内。</li><li>如果更新过程中发生问题，支持一键回滚。<strong>Deployment在更新应用时不是一次性更新全部Pod，而是一个Pod一个Pod的进行更新</strong>。如果新的Pod无法起来，这样也不影响旧版本的Pod正常运行。</li></ol><p><img data-src="image-20220513134455213.png" /></p><h3 id="用法用例">用法用例</h3><h4 id="deployment语法">Deployment语法</h4><p><img data-src="image-20220513134618739.png" /></p><ul><li>apiVersion：当前Deployment所属的组是apps，版本是v1</li><li>metadata：当前Deployment的元信息。<code>.metedata.name</code>是nginx-deployment，<code>.metadata.labels</code>是app=nginx</li><li>spec：期望状态。核心字段是<code>replicas</code>，即<strong>期望的Pod数量，<code>controller</code>会一直控制Pod在这个数量</strong>；selector是Pod选择器，这里<strong>label的要与下文中Pod模板的label一致</strong>；template是Pod的模板，包含了<strong>Pod的元信息，这里是app=nginx</strong>，还<strong>包含了Pod的期望状态，这里有容器的一些如镜像版本、容器名称、端口映射</strong>等信息。</li></ul><h4 id="查看deployment状态">查看Deployment状态</h4><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="variable">$kubectl</span> get deployment </span><br></pre></td></tr></table></figure><p><img data-src="image-20220513135710806.png" /></p><ul><li>READY：期望的Pod数量/实际Pod的数量</li><li>UP-TO-DATE：达到最新期望版本的Pod数量</li><li>AVAILABLE：运行过程中的可用Pod数量。这里Pod并不简单是可用的（Ready状态），其实包含了一些<strong>可用超过一定时间长度的Pod</strong></li><li>AGE：deployment创建的时长，如上图Deployment就已经创建了80分钟</li></ul><h4 id="查看pod状态">查看Pod状态</h4><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="variable">$kubectl</span> get pods</span><br></pre></td></tr></table></figure><p><img data-src="image-20220513140522034.png" /></p><ul><li>Pod的名字格式是<code>&#123;deployment-name&#125;-&#123;pod模板的hash&#125;-&#123;随机字符串&#125;</code>，Pod的<strong>ownerReferences</strong>即<strong>Pod所属的controller资源不是Deployment而是ReplicaSet</strong>。这个<ul><li>ReplicaSet的name其实是<strong>nginx-deployment加上pod.template-hash</strong></li><li>所有的Pod都是ReplicaSet创建出来的，ReplicaSet对应的某个Deployment.template版本</li></ul></li></ul><h4 id="更新镜像">更新镜像</h4><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="variable">$kubectl</span> <span class="built_in">set</span> image deployment.v1.apps/nginx-deployment nginx=nginx:1.9.1</span><br></pre></td></tr></table></figure><p><img data-src="image-20220513145414786.png" /></p><h4 id="快速回滚">快速回滚</h4><p>如果在发布过程中遇到了问题，支持快速回滚。</p><p>快速回滚到Deployment上一个版本</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="variable">$kubectl</span> rollout undo deployment/nginx-deployment</span><br></pre></td></tr></table></figure><p>回滚Deployment到上一个版本，需要先查询版本列表</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="variable">$kubectl</span> rollout <span class="built_in">history</span> deployment/nginx=deployment</span><br></pre></td></tr></table></figure><p><img data-src="image-20220513150000845.png" /></p><p>这里的CHANGE-CAUSE列全都是<code>&lt;none&gt;</code>，在应用资源清单时可以<strong>使用如下命令</strong>使得api-server记录：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="variable">$kubectl</span> apply -f nginx-deployment.yml --record</span><br></pre></td></tr></table></figure><h5 id="设置历史版本保留">设置历史版本保留</h5><p>通过<code>.spec.revisionHistoryLimit</code>字段可以设置保留历史版本的<code>replicaset</code>的数量，默认值是10</p><h3 id="deploymentstatus">DeploymentStatus</h3><p>每一个资源都有它的spec.Status，这里deploymentStatus中描述的是它的转换状态：也就是<strong>Processing、Complete、和Failed。</strong></p><p><img data-src="image-20220513151104847.png" /></p><ul><li>以Processing为例：Processing指的是Deployment正在处于<strong>扩容和发布状态</strong>。当Processing状态的deployment所有的ReplicasSet以及Pod副本全部达到最新版本，而且是avilable，这样就会进入Complete状态。而Complete状态如果发生了一些扩缩容的话，也会进入Processing的状态。</li><li>如果在处理过程中遇到一些问题：比如说<strong>拉取镜像失败了</strong>，或者<strong>就绪探针检查失败了</strong>，就会进入failed状态；如果在运行过程中即complete状态，中间运行时发生了一些<strong>探针检查失败</strong>，此时deployment也会进入failed状态。进入failed状态后，除非所有副本均变成available，而且是update到最新版本，deployment才会进入Complete状态。</li></ul><h3 id="架构设计">架构设计</h3><h4 id="管理模式">管理模式</h4><p><img data-src="image-20220513165107394.png" /></p><ul><li>Deployment负责管理不同版本的ReplicaSet，由ReplicaSet来管理具体的Pod副本数</li><li>每个ReplicaSet对应Deployment template的一个版本。每修改一次template，都会生成一个新的ReplocaSet，这个ReplicaSet下的Pod其实是相同版本。</li></ul><h4 id="deployment控制器">Deployment控制器</h4><p>控制器实现原理</p><p>所有的控制器都是通过Informer中的<strong>一些事件做处理和监听</strong>。Deployment控制器关注Deployment和ReplicaSet的事件，收到事件后加入队列。控制器从队列中取出来后，进行check paused的处理，paused其实是检查deployment是否需要新的发布，如果paused设置为true，就表示Deployment只会做副本数量上的维持而不是发布。</p><p><img data-src="image-20220513165749153.png" /></p><ul><li>当check paused为yes，那么控制器只会做<strong>同步副本</strong>，也就是说把副本同步到对应的ReplicaSet中，最后在更新Deployment的状态。</li><li>当check paused为false，那么控制器会跟<strong>更新副本</strong>，也就是通过Create或者Rolling的方式来更新，更新方式也是通过Create/Update/Delete ReplicaSet来实现的</li></ul><h4 id="replicaset控制器">ReplicaSet控制器</h4><p>当Deployment分配了ReplicaSet之后，ReplicaSet控制器也是通过Informer中监听一些事件，这些事件包含了ReplicaSet和Pod的事件。ReplicaSet控制器管理副本数，使得状态向spec规定的副本数无限靠近。</p><p><img data-src="image-20220513171158658.png" /></p><p>Deployment控制器做了更复杂的时区，包含了版本管理。Deployment控制器把每一个版本的数量维持工作交给ReplicaSet控制器来做。</p><h4 id="扩缩容模拟">扩/缩容模拟</h4><p><img data-src="image-20220513171437987.png" /></p><h4 id="发布模拟">发布模拟</h4><p><img data-src="image-20220513172101061.png" /></p><ul><li>若修改template中的一个容器的image，Deployment控制器会新建一个对应template2中的ReplicaSet，创建出来之后会<strong>逐渐增加ReplicaSet2 中 Pod 的数量，同时逐渐减少 ReplicaSet1 中的 Pod 数量。</strong></li><li>最终新版本的Pod为Pod4、Pod5和Pod6，旧版本的Pod被删除，这里就完成了一次发布。</li></ul><h4 id="回滚模拟">回滚模拟</h4><p><img data-src="image-20220513175011929.png" /></p><ul><li>如果此时发现当前业务版本使用有问题时，此时需要回滚，不管是通过<code>rollout</code>命令还是通过回滚修改template，都是把当前的templete回滚为旧版本的templete</li><li>此时Deployment会修改ReplicationSet1中Pod的期望数量，把期望数量修改为3个，而且会逐渐减少新版本 ReplicaSet2 中的 replica 数量，最终的效果就是把 Pod 从旧版本重新创建出来。<strong>回滚并不是把之前的 Pod 重新找出来，而是说重新创建出符合旧版本 template 的 Pod。</strong></li></ul><h4 id="spec字段解析">spec字段解析</h4><ul><li><p>MinReadySeconds：Deployment会根据Pod ready查看Pod是否可用，默认值是0。如果设置MinReadySeconds=30（秒），Deployment会等到超过30秒以后才认为Pod是avaliable的。Pod available的前提条件是Pod ready，但ready 的Pod不一定是available的。<strong>一定要超过MinReadySeconds之后才会判断为available</strong>；</p></li><li><p>revisionHistoryLimit：保留历史revision，即保留历史ReplicaSet的数量，默认值为10个，这里可以设置为一个或者两个，如果回滚可能性比较大的话可以设置数量超过10；</p></li><li><p>progressDeadlineSecond：当Deployment处于扩容或者发布状态时，pod会处于一个processing的状态，可以设置这个状态的超时时间，如果超过了这个超过时间还处于processing，那么可以认为这个Pod进入了failed的状态。</p></li></ul><h4 id="升级策略字段解析">升级策略字段解析</h4><p>可以在<code>.spec.strategy</code>字段下设置升级策略。</p><p><img data-src="image-20220513220124878.png" /></p><ul><li>maxUnavailable：滚动升级过程中最多有多少个Pod不可用；</li><li>maxSurge：滚动升级过程中最多存在多少个Pod超过预期replica数量。</li></ul><p>当<code>.spec.replicas</code>=3的Deployment在发布时可能存在一种情况：新版本的ReplicaSet和旧版本的ReplicaSet加起来是4个，超过了期望数3个。这是因为<strong>默认的Max Unavailable和MaxSurge都是25%，</strong>默认Deployment在发布过程中，可能有25%的replicas是不可用的，也可能会有25%的replica是可用的，因此最高可以达到125%的replica数量。</p><p>这里可以根据用户实际场景来设置：当用户的资源足够时，且更注重发布过程中的可用性，<strong>此时可以设置maxUnavailable较小，maxSurge较大</strong>。但是如果用户的资源比较紧张，此时可以设置MaxSurge较小，甚至设置为0。</p><p><strong>maxUnavailable和maxSurge不能同时为0：</strong>当MaxSurge为0时，必须要删除Pod，才能扩容Pod；当maxUnavailable为0时，限制了升级过程中无法删除Pod，因为一旦删除就会有Pod不可用。两者产生了冲突。</p><h3 id="小结">小结</h3><ul><li>Deployment时K8s中一种常见的工作负载，支持部署多版本的Pod</li><li>Deployment时管理多版本的方式，是针对每个版本的模版创建一个ReplicaSet，由ReplicaSet维护一盒一定数量的Pod副本，而Deployment只需要关心不同版本的ReplicaSet要制定多少数量的Pod；</li><li>因此Deployment发布部署的根本原理，就是Deployment调整不同版本的ReplicaSet的中期望副本数，以此来达到多版本Pod的升级和回滚</li></ul>]]></content>
    
    
    <summary type="html">&lt;p&gt;本文是CNCF × Alibaba 云原生技术公开课的&lt;strong&gt;第七章应用编排与管理：Deployment&lt;/strong&gt;学习笔记。&lt;/p&gt;
&lt;p&gt;关键词：&lt;strong&gt;k8s&lt;/strong&gt;&lt;/p&gt;</summary>
    
    
    
    
    <category term="k8s" scheme="https://fly97.cn/tags/k8s/"/>
    
  </entry>
  
  <entry>
    <title>理解Pod和容器设计模式</title>
    <link href="https://fly97.cn/p/container-design-patterns/"/>
    <id>https://fly97.cn/p/container-design-patterns/</id>
    <published>2022-05-05T07:26:00.000Z</published>
    <updated>2022-05-05T07:26:00.000Z</updated>
    
    <content type="html"><![CDATA[<p>本文是CNCF × Alibaba 云原生技术公开课的<strong>第四章理解 Pod 和容器设计模式</strong>学习笔记。</p><p>关键词：<strong>k8s</strong></p><a id="more"></a><h3 id="为什么需要pod">为什么需要Pod</h3><h4 id="容器的基本概念">容器的基本概念</h4><ul><li>容器的本质是一个进程，是一个<strong>视图被隔离，资源受限</strong>的进程。</li></ul><p>容器里面PID=1的进程就是容器本身，这意味着<strong>管理容器等于直接管理应用本身</strong>。</p><p>如果将Kubernetes类比于云时代的操作系统，那么容器的镜像就是这个操作系统的软件安装包。</p><p><img data-src="image-20220505153649015.png" /></p><h4 id="真实操作系统的例子">真实操作系统的例子</h4><p>假如有一个程序叫做helloworld，这个程序是由一组进程组成，这里说的进程等同于Linux线程。</p><p>因为Linux线程是轻量级进程，所以如果从Linux中看helloword的pstree，会看到这个helloword程序由四个线程组成。四个线程<strong>相互协作</strong>，共享helloword程序的资源，组成了helloworld程序的<strong>真实工作情况</strong>。</p><p>以上实际上是一个<strong>进程组</strong>的概念。</p><p><img data-src="image-20220505154210244.png" /></p><p>真实的操作系统中，一个程序往往是根据进程组来进行管理的。类比到Kubernetes，由于Pod是Kubernetes进行资源调度的最小单位，那么<strong>Pod就可类比成操作系统的进程组</strong>。</p><p><img data-src="image-20220505161515178.png" /></p><h4 id="进程组的概念">进程组的概念</h4><p>还是原来的例子：hellowolrd程序由四个进程组成，这些进程会共享一些资源和文件。如果现在<strong>把helloworld程序用容器跑起来</strong>，应该怎么做？</p><p>一个自然的想法是，启动一个docker容器，里面运行四个进程。</p><p>但是容器的设计本身是一种<strong>单进程</strong>的模型。不是说容器内只能起一个进程，由于容器的应用等于进程，所以只能去管理PID=1的这个进程，其他再起来的进程实际上一个<strong>托管状态</strong>，此时的PID=1的进程具有<strong>进程管理</strong>的能力。</p><p>因为容器中PID=1的进程需要具有进程管理的能力，因此需要PID=1的进程改为<strong>systemd</strong>，否则这个应用或者容器没有办法去管理多个进程。因为PID=1的进程是应用本身，如果此时该进程被杀死，<strong>那么剩下三个进程的资源就没有人回收了</strong>。</p><p>但是如果真的将这个应用本身改为<strong>systemd</strong>，或者直接把容器里PID=1的进程直接修改为<strong>systemd</strong>。这会导致<strong>无法获得这个应用的状态</strong>，因为容器里运行的是<strong>systemd</strong>，而不是<strong>应用本身</strong>。</p><p>简单梳理下：</p><ul><li><p>由于容器实际上是一个单进程模型，所以如果在容器里启动多个进程，只有一个可以作为PID=1的进程。如果这个PID=1的进程挂了，其他的进程会成为<strong>孤儿</strong>，<strong>操作系统无法回收这一部分资源。</strong></p><blockquote><p>此时的孤儿进程由宿主机的操作系统的systemd或者init进程管理。</p></blockquote></li><li><p>如果在容器里运行一个systemd管理其他进程，这样会导<strong>致管理容器无法直接管理应用</strong>。应用被systemd接管了，此时<strong>应用状态的生命周期不等于容器的生命周期</strong>。</p></li></ul><p><img data-src="image-20220505161527738.png" /></p><h4 id="pod进程组">Pod=进程组</h4><p>Pod是Kubernetes抽象出来的一个可以类比为进程组的概念。</p><p>前文提到的这四个进程共同组成的一个应用helloworld，在kubenetes里面实际上会被定义<strong>为拥有四个容器的Pod</strong>。</p><p>因为在一个容器里启动多个进程会遇到上面的问题，<strong>所以Kubernetes把四个相互协作的进程用四个独立的容器启动起来，然后定义在一个Pod里。</strong></p><p>当Kubernetes把helloword给拉起来的时候，实际上会看到四个容器，他们共享了某些资源，这些<strong>资源都属于Pod</strong>，所以Pod在Kubernetes里只是一个<strong>逻辑单位</strong>。多个容器的组合就叫做Pod。<strong>Pod是Kubernetes资源分配的原子调度单位。</strong></p><p><img data-src="image-20220505162400647.png" /></p><h4 id="为什么pod必须是原子调度单位">为什么Pod必须是原子调度单位</h4><p>为什么要把Pod作为一个概念抽象出来呢？为什么不通过调度等方式把Pod这个抽象的概念去掉？为什么Pod必须是Kubernetes里的原子调度单位？</p><p>通过一个例子来解释。</p><p>比如现在有两个容器，他们是紧密协作的，所以他们应该被部署在同一个pod里。具体来说，第一个容器是App也就是业务容器，他会<strong>写日志</strong>文件；第二个容器是LogCollector，他会<strong>转发业务容器写的日志文件</strong>到其他组件。</p><p>两个容器的资源需求如下：</p><ul><li>App需要1G内存</li><li>LogCollector需要0.5G内存</li></ul><p>而当前集群环境的可用内存：</p><ul><li>Node_A可用内存为1.25G</li><li>Node_B可用内存为2G</li></ul><p>比如现在没有Pod的概念，就只有两个容器，需要这两个容器<strong>运行在同一台机器上</strong>。如果调度器先把App调度到了Node_A上面，由于内存不足以再运行LogCollector，<strong>调度失败</strong>，必须重新调度。</p><p>以上是一个非常典型的<strong>成组调度失败</strong>的例子，Task co-scheduling问题不是说不能解，在很多项目里，都有类似的解法。</p><ul><li><p>比如在Mesos中，他会进行<strong>资源囤积</strong>：即当资源满足了所有约束的任务时，才会开始调度，这是一个<strong>典型的成组调度</strong>的解法。在Mesos中，上文提到的两个"容器"不会立刻调度。<strong>但是这样的话调度效率会损失，因为需要等待；其次会因为互相等待而产生死锁。</strong></p></li><li><p>另一种是Google的解法，他在Omega 系统中做了一个非常复杂且非常厉害的解法，被称为<strong>乐观调度</strong>：即不管冲突的异常情况，先调度，同时设置一个回滚机制，如果在调度过程中出现了冲突，即通过回滚来解决问题。这个方式来说更加优雅，也更加高效。<strong>但是实现机制非常复杂</strong>，因为<strong>悲观锁的设置一定要比乐观锁要简单</strong>。</p></li></ul><p>而像这样一个调度问题，在Kubernetes里，直接通过<strong>Pod</strong>一个概念解决了，因为在Kubernetes里，App容器和LogCollector容器是属于一个Pod的，在调度时必然是以一个Pod为单位进行调度，<strong>因此不存在这个问题</strong>。</p><p><img data-src="image-20220505171036279.png" /></p><h4 id="再次理解pod">再次理解Pod</h4><p>首先Pod里面的容器是<strong>超亲密关系</strong>。和超亲密关系相比，还有一种关系叫<strong>亲密关系</strong>，这个亲密关系一定是可用通过调度来解决的。</p><p>比如现在有两个Pod，<strong>需要运行在同一台宿主机上</strong>，那么就属于亲密关系，调度器是可以去做的。</p><p>对于超亲密关系来说。必须通过Pod来解决，如果超亲密关系赋予不了，那么<strong>整个Pod或者整个应用</strong>都无法启动。</p><p>什么是超亲密关系呢？</p><ul><li>比如两个进程会<strong>发生文件交换</strong>，例如一个写日志，一个读取日志；</li><li>两个进程之间<strong>通过localhost或者本地的socket</strong>去通信；</li><li>两个容器或者微服务之间，会发生非常频繁的RPC调用，出于性能的考虑，也应该是超亲密关系；</li><li>两个容器或者应用，他们需要<strong>共享某些Linux NameSpace</strong>。最常见的例子就是一个容器需要加入另外一个容器的Network Namespace，这样就可以看到另外一个容器的网络设备信息；</li></ul><p><img data-src="image-20220505193202869.png" /></p><p>Pod解决了两个问题</p><ol type="1"><li>怎么去描述<strong>超亲密关系</strong>；</li><li>怎么去对<strong>超亲密关系的容器或者说是业务去做统一调度</strong>。</li></ol><h3 id="pod的实现机制">Pod的实现机制</h3><h4 id="pod要解决的问题">Pod要解决的问题</h4><p>容器之间原本说被Linux Namespace和cgroups隔开的，所以现在实际要解决的事怎么打破隔离。</p><p>具体解法分两个部分：<strong>网络和存储</strong>。</p><h5 id="共享网络">共享网络</h5><p>现在有一个Pod，其中包含了容器A和容器B，他们两个需要共享Network Namespace。</p><p>Kuberetes会在某个Pod里，额外起一个<strong>Infra Container容器</strong>来共享整个Pod的Network Namespace。</p><p>Infra Container是一个非常小的镜像，是一个汇编语言写的、永远处于暂停状态的容器。由于有了这样一个Infra Container之后，<strong>其他所有容器都会通过加入Namespace的方式加入到Infra Container中的Network Namespace中。</strong></p><p>因此一个Pod里面的所有容器网络视图是完全一样的，<strong>即他们看到的网络设备、IP地址、MAC地址等其他网络信息其实完全是一样的</strong>，这些都来自于Pod第一次创建这个Infra Container。</p><p>在Pod里面一定有一个IP地址，是这个Pod的Network Namespace 对应的地址，也是Infra Container的IP地址。这个IP地址被Pod中的容器共享。这就是Pod的网络实现方式。</p><p>在整个Pod里，必然是Infra Container第一个启动，而且整个Pod的生命周期等同于Infra Container的生命周期，与容器A和容器B是无关的。<strong>因此Kubernetes允许单独更新Pod里的某一个镜像，且更新时整个Pod也不会重建，也不会重启。</strong></p><p><img data-src="image-20220505193219913.png" /></p><h5 id="共享存储">共享存储</h5><p>现在有两个容器，一个是容器A，另一个是容器B。两个容器需要进行文件共享。</p><p><strong>在一个Pod里，声明一个Volume，它是属于Pod的，同一个Pod访问的Volume是同一份Volume。</strong></p><p>之前的例子，应用容器App写了日志，只要这个日志是在一个Volume中，只要声明挂载了同一个Volume，这个Volume就可以被另外一个LogCollector容器看到。</p><p><img data-src="image-20220505194156672.png" /></p><h3 id="详解容器设计模式">详解容器设计模式</h3><p>举个例子，现在需要发布一个应用，这个应用是用JAVA写的，打包完了以后需要把他放在Tomcat的web APP目录下面，这样就可以启动起来了。</p><p>如果需要打包成镜像，通常有几种做法：</p><ul><li>把WAR包和Tomcat打包进一个镜像里。<strong>此时无论要更新WAR包还是更新Tomcat都要重新做一个镜像。</strong></li><li>镜像里只打包Tomcat。他就是一个Tomcat，但是需要使用数据卷的方式，从宿主机把WAR包挂载进Tomcat中，挂载到web APP目录下。<strong>此时需要维护一套分布式存储系统</strong>，因为master分配负载到工作节点是不一定的，有可能这次分配到节点A下一次就分配到节点B，所以必须有一套分布式存储系统，使得容器无论在A还是在B都可以找到这个WAR包。</li></ul><blockquote><p>即使有了分布式存储系统作Volume，还需要负责维护Volume里的WAR包，需要单独写一套Kubernetes Volume 插件，用于每次Pod启动之前，把应用所需要的WAR包下载到Volume里，最后才能被应用挂载到。</p></blockquote><h5 id="init-container">Init Container</h5><p>Kubernetes提供了一种在没有分布式存储的情况下也能发布应用的方式。</p><p>首先定义一个Init Container，就是把WAR包从镜像里拷贝到一个Volume里。完成以后就退出。<strong>Init Container会比用户容器先启动，而且严格按照定义顺序来执行。</strong></p><p>由于一个Pod里面的容器是共享Volume的，tomcat容器启动的时候把Init Container操作的Volume挂载为自己的APP目录，<strong>此时APP目录下一定会找到Init Contaier操作的WAR包。</strong></p><p>有了Init Container，这个Pod就是一个自包含的“容器组”。不用担心没有分布式存储，Volume不是持久化的，它一定是可以公布的。</p><p>通过组合两种不同角色的容器，并且按照像Init Container 这样的一种编排方式，统一的去打包应用。像这样的一个概念，在Kubernetes里就是一个非常经典的容器设计模式，叫做：Sidecar</p><p><img data-src="image-20220505203809412.png" /></p><h5 id="容器设计模式sidecar">容器设计模式：Sidecar</h5><p><strong>在Pod里，定义一些专门的容器，用来执行主业务容器的一些辅助工作，这种容器设计模式被称为Sidecar。</strong></p><p>还有一些其他操作：</p><ul><li><p>原本需要在<strong>容器里执行SSH需要干的事情</strong>，比如执行脚本，判断一些前置条件，其实都可以像Init Container或者Sidecar的方式去解决；</p></li><li><p><strong>日志收集</strong>，日志收集本身是一个进程，就可以打包作为pod的一部分；</p></li><li><p><strong>Debug应用</strong>，现在Debug整个应用都可以在应用Pod里面再次定义一个小的额外的Container，因为在同一个Pod里，共享相同的Namespace</p></li><li><p>查看其他容器的工作状态，不需要登陆到容器里去看，只要把监控组件装到额外的小容器里就行了。<strong>业务监控</strong>也可以通过Sidecar方式来做。</p></li></ul><p><strong>通过Sidecar可以把辅助功能从业务容器解耦，这个能力还可以重用</strong>，即同样的一个Sidecar容器可以被全公司的人共用。这就是设计模式的威力。</p><p><img data-src="image-20220505203837861.png" /></p><h4 id="sidecar应用与日志收集">Sidecar：应用与日志收集</h4><p>前面提到的应用日志收集，业务容器将日志写在一个 Volume 里面，而由于 Volume 在 Pod 里面是被共享的，所以日志容器 —— 即 Sidecar 容器一定可以通过共享该 Volume，直接把日志文件读出来，然后存到远程存储里面，或者转发到另外一个例子。<strong>现在业界常用的 Fluentd 日志进程或日志组件，基本上都是这样的工作方式。</strong></p><p><img data-src="image-20220505203853464.png" /></p><h4 id="sidecar代理容器">Sidecar：代理容器</h4><p>假如现在有个 Pod 需要访问一个外部系统，或者一些外部服务，但是这些外部系统是一个集群，那么这个时候如何通过一个统一的、简单的方式，用一个 IP 地址，就把这些集群都访问到？有一种方法就是：修改代码。因为代码里记录了这些集群的地址；<strong>另外还有一种解耦的方法，即通过 Sidecar 代理容器。</strong></p><p>简单说，<strong>单独写一个这么小的 Proxy，用来处理对接外部的服务集群，它对外暴露出来只有一个 IP 地址就可以了</strong>。所以接下来，业务容器主要访问 Proxy，然后由 Proxy 去连接这些服务集群，这里的关键在于 Pod 里面多个容器是通过 localhost 直接通信的，因为它们同属于一个 network Namespace，网络视图都一样，所以它们俩通信 localhost，并没有性能损耗。</p><p><img data-src="image-20220505203959014.png" /></p><h4 id="sidecar适配器容器">Sidecar：适配器容器</h4><p>现在业务暴露出来的 API，比如说有个 API 的一个格式是 A，但是现在有一个外部系统要去访问我的业务容器，它只知道的一种格式是 API B ,所以要做一个工作，就是把业务容器怎么想办法改掉，要去改业务代码。但实际上，你可以<strong>通过一个 Adapter 帮你来做这层转换。</strong></p><p>现在有个例子：现在业务容器暴露出来的监控接口是 /metrics，访问这个这个容器的 metrics 的这个 URL 就可以拿到了。可是现在，这个监控系统升级了，它访问的 URL 是 /health，metrics 不认识。那这个怎么办？<strong>可以不去改代码，而是额外写一个 Adapter，用来把所有对 health 的这个请求转发给 metrics</strong>，所以这个 Adapter 对外暴露的是 health 这样一个监控的 URL，这就可以了，你的业务就又可以工作了。</p><p>这样的关键还在于 Pod 之中的容器是通过 localhost 直接通信的，所以没有性能损耗，并且这样一个 <strong>Adapter 容器可以被全公司重用起来</strong>，这些都是设计模式给我们带来的好处。</p><h3 id="总结">总结</h3><p><img data-src="image-20220505204158684.png" /></p>]]></content>
    
    
    <summary type="html">&lt;p&gt;本文是CNCF × Alibaba 云原生技术公开课的&lt;strong&gt;第四章理解 Pod 和容器设计模式&lt;/strong&gt;学习笔记。&lt;/p&gt;
&lt;p&gt;关键词：&lt;strong&gt;k8s&lt;/strong&gt;&lt;/p&gt;</summary>
    
    
    
    
    <category term="k8s" scheme="https://fly97.cn/tags/k8s/"/>
    
  </entry>
  
  <entry>
    <title>K8s核心概念</title>
    <link href="https://fly97.cn/p/core-concept-about-k8s/"/>
    <id>https://fly97.cn/p/core-concept-about-k8s/</id>
    <published>2022-05-05T05:25:00.000Z</published>
    <updated>2022-05-05T05:25:00.000Z</updated>
    
    <content type="html"><![CDATA[<p>本文是CNCF × Alibaba 云原生技术公开课的<strong>第三章Kubernetes 核心概念</strong>学习笔记。</p><p>关键词：<strong>k8s</strong></p><a id="more"></a><h3 id="k8s的架构">k8s的架构</h3><p>k8s的架构是比较典型的二层架构和server-client架构。Master作为中央的管控节点，会与Node进行一个链接。</p><p>所有用户侧的组件，只会和Master进行连接，把希望的状态或者想执行的命令发给Master，Master会把这些命令发给相应的节点。</p><p><img data-src="kubernetes-high-level-component-archtecture.jpg" /></p><h4 id="master">Master</h4><p>Master节点主要包含四个主要的组件：API Server、Controller、Scheduler以及etcd：</p><p><img data-src="image-20220429212540202.png" /></p><ul><li>API Server：用来处理API操作，k8s的所有组件都会和API Server进行连接，组件和组件之间一般不进行独立链接，都要依赖API Server进行消息的传送。</li><li>Controller：控制器。用来完成对集群状态的一些管理。例如<strong>自动对容器进行修复，或者自动进行水平扩张</strong>。</li><li>Scheduler：调度器。顾名思义就是完成调度的操作。例如把一个用户提交的Container，依赖于它对CPU、对memory请求大小，找一台合适的节点，进行放置。</li><li>etcd：分布式存储系统。API Server中所需要的原信息存在etcd中，etcd本身是一个高可用系统，通过etcd保证整个k8s Master节点的高可用性。</li></ul><h4 id="node">Node</h4><p>Node是真正运行业务负载的，每个业务负载以<strong>Pod</strong>的形式运行。一个Pod中运行一个或者多个容器，真正运行这些Pod组件的是<strong>kubelet</strong>，也就是Node上面最关键的组件，它通过API Server接收到需要Pod运行的状态，然后提交到Container Runtime组件中，在OS上创建容器所需要的环境，最终把容器或者Pod运行起来。Node需要对存储和网络进行管理。K8s不直接管理存储和网络，K8s通过Storage Plugin或者Network Plugin进行管理，完成存储和网络操作。</p><p><img data-src="image-20220429213903847.png" /></p><p>在K8s自己的环境里，也会有k8s的Network，他是为了提供Service Network来进行组网。真正完成组网的组件是Kube-proxy。Kube-proxy利用iptable的能力来组建k8s集群的网络。以上就是Node上面的四个组件（Container Runtime、Storage Plugin、Network Plugin、Kube-proxy）。</p><p>注意：K8s上的Node不会直接和用户进行交互，Node和用户之间的交互只会通过Master节点。用户通过Master节点下发这些信息。K8s上的每个Node，都会运行上面的这几个组件。</p><h5 id="例子">例子</h5><p>通过一个例子看K8s架构中的这些组件是如何进行交互的。</p><p><img data-src="image-20220429220138689.png" /></p><p>用户通过UI或者CLI提交一个Pod到K8s进行部署，这个Pod请求会首先通过UI或者CLI提交给K8s API Server，下一步API Server会把这个信息写入到存储系统etcd，之后Scheduler会通过API Server的watch机制得到这个信息：有一个Pod需要被调度。</p><p>此时Scheduler会根据这个内存状态进行一次调度决策，在完成这次调度以后，Scheduler向API Server报告。此时API Server接收到消息以后，把这次结果写入etcd，然后API Server会通知相应的节点。相应节点的Kubelet会得到这个通知，Kubelet调用Container Runtime来启动配置这个容器和这个容器的运行环境，去调度Storage Plugin来配置存储，Network Plugin去配置网络。</p><h3 id="核心概念">核心概念</h3><h4 id="pod">Pod</h4><p>Pod是K8s的一个最小调度和属性单元，用户可以通过K8s的Pod API来生产一个Pod，让K8s对这个Pod进行调度，也就是把他放到另一个K8s管理的节点上运行起来。</p><p>一个Pod简单来说是一组容器的抽象，它包含一个或者多个容器。</p><p>Pod包含一些其他所需要的资源，比如卷。</p><p><img data-src="image-20220430130241945.png" /></p><p>Pod的共享上下文包括一组Linux命名空间，控制组和可能一些其他的隔离方面，这些都是用来隔离Docker容器的技术。在Pod的上下文中，每个独立的应用可能会进一步实施隔离。</p><p>就Docker使用的概念术语来说，<strong>Pod类似于共享名字空间和文件系统卷的一组Docker容器</strong>。</p><p>Pod给这些容器提供了一个共享的运行环境，他们会共享同一个网络环境，这些容器可以用localhost来进行直接的连接。</p><h5 id="使用pod">使用Pod</h5><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">apiVersion:</span> <span class="string">v1</span></span><br><span class="line"><span class="attr">kind:</span> <span class="string">Pod</span></span><br><span class="line"><span class="attr">metadata:</span></span><br><span class="line">  <span class="attr">name:</span> <span class="string">nginx</span></span><br><span class="line"><span class="attr">spec:</span></span><br><span class="line">  <span class="attr">containers:</span></span><br><span class="line">  <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">nginx</span></span><br><span class="line">    <span class="attr">image:</span> <span class="string">nginx:1.14.2</span></span><br><span class="line">    <span class="attr">ports:</span></span><br><span class="line">    <span class="bullet">-</span> <span class="attr">containerPort:</span> <span class="number">80</span></span><br></pre></td></tr></table></figure><p>要创建上面的Pod，请运行以下命令</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">kubectl apply -f simple-pod.yaml</span><br></pre></td></tr></table></figure><p>注意：Pod一般不是直接创建的，而是使用<strong>工作负载资源</strong>创建的，</p><h4 id="volume">Volume</h4><p>Volume用来管理K8s存储，Volume声明容器中的容器可以访问文件的目录。</p><p>一个卷可以被挂载Pod中一个或者多个容器指定的路径下面。</p><p>Volume本身是一个<strong>抽象</strong>的概念，一个Volume可以支持多种后端存储。K8s的Volume就支持了很多存储插件，可以支持<strong>本地的存储</strong>，也可以支持<strong>分布式存储</strong>。它也可以支持云存储，例如阿里云上的云盘、AWS上的云盘、Google上的云盘等。</p><p><img data-src="image-20220430132644760.png" /></p><p>Docker 也有 <a href="https://docs.docker.com/storage/">卷（Volume）</a> 的概念，但对它只有少量且松散的管理。 Docker 卷是磁盘上或者另外一个容器内的一个目录。 Docker 提供卷驱动程序，但是其功能非常有限。</p><p>Kubernetes 支持很多类型的卷。 <a href="https://kubernetes.io/docs/concepts/workloads/pods/pod-overview/">Pod</a> 可以同时使用任意数目的卷类型。 临时卷类型的生命周期与 Pod 相同，但持久卷可以比 Pod 的存活期长。 当 Pod 不再存在时，Kubernetes 也会销毁临时卷；不过 Kubernetes 不会销毁<strong>持久卷</strong>。 对于给定 Pod 中任何类型的卷，在容器重启期间数据都不会丢失。</p><h5 id="挂载本地卷">挂载本地卷</h5><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">apiVersion:</span> <span class="string">v1</span></span><br><span class="line"><span class="attr">kind:</span> <span class="string">Pod</span></span><br><span class="line"><span class="attr">metadata:</span></span><br><span class="line">  <span class="attr">name:</span> <span class="string">nginx</span></span><br><span class="line"><span class="attr">spec:</span></span><br><span class="line">  <span class="attr">containers:</span></span><br><span class="line">  <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">nginx</span></span><br><span class="line">    <span class="attr">image:</span> <span class="string">nginx:1.14.2</span></span><br><span class="line">    <span class="attr">ports:</span></span><br><span class="line">    <span class="bullet">-</span> <span class="attr">containerPort:</span> <span class="number">80</span></span><br><span class="line">    <span class="attr">volumeMounts:</span></span><br><span class="line">    <span class="bullet">-</span> <span class="attr">mountPath:</span> <span class="string">/mnt/logs</span></span><br><span class="line">      <span class="attr">name:</span> <span class="string">logs</span></span><br><span class="line">    <span class="attr">volumes:</span></span><br><span class="line">    <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">go-logs</span></span><br><span class="line">        <span class="attr">hostPath:</span></span><br><span class="line">        <span class="attr">path:</span> <span class="string">/logs</span></span><br></pre></td></tr></table></figure><h5 id="挂载网络卷nfs">挂载网络卷NFS</h5><p>待补充</p><h4 id="deployment">Deployment</h4><p>Deployment为<strong>Pod和ReplicaSet（副本）</strong>提供声明式的更新能力。</p><p>Deployment是在Pod上层的抽象，用Deployment这个抽象来做应用的真正管理，而Pod是组成Deployment的最小单元。</p><p>K8s通过Controller也就是控制器去维护Deployment中Pod的数目，帮助Deployment自动恢复失败的Pod。</p><p><img data-src="image-20220430135024552.png" /></p><p>例如可以定义一个Deployment，这个Deployment需要两个Pod，当一个Pod失败后，控制器会检测到，他会重新把Deployment中的Pod数据从一个恢复到两个，然后再去重新生成一个Pod。</p><p>通过控制器，还可以完成发布的策略，例如进行滚动升级，或者版本回滚等。</p><h5 id="创建deployment">创建Deployment</h5><p>下面是一个 Deployment 示例。其中创建了一个 ReplicaSet，负责启动三个 <code>nginx</code> Pods：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">apiVersion:</span> <span class="string">apps/v1</span></span><br><span class="line"><span class="attr">kind:</span> <span class="string">Deployment</span></span><br><span class="line"><span class="attr">metadata:</span></span><br><span class="line">  <span class="attr">name:</span> <span class="string">nginx-deployment</span></span><br><span class="line">  <span class="attr">labels:</span></span><br><span class="line">    <span class="attr">app:</span> <span class="string">nginx</span></span><br><span class="line"><span class="attr">spec:</span></span><br><span class="line">  <span class="attr">replicas:</span> <span class="number">3</span></span><br><span class="line">  <span class="attr">selector:</span></span><br><span class="line">    <span class="attr">matchLabels:</span></span><br><span class="line">      <span class="attr">app:</span> <span class="string">nginx</span></span><br><span class="line">  <span class="attr">template:</span></span><br><span class="line">    <span class="attr">metadata:</span></span><br><span class="line">      <span class="attr">labels:</span></span><br><span class="line">        <span class="attr">app:</span> <span class="string">nginx</span></span><br><span class="line">    <span class="attr">spec:</span></span><br><span class="line">      <span class="attr">containers:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">nginx</span></span><br><span class="line">        <span class="attr">image:</span> <span class="string">nginx:1.14.2</span></span><br><span class="line">        <span class="attr">ports:</span></span><br><span class="line">        <span class="bullet">-</span> <span class="attr">containerPort:</span> <span class="number">80</span></span><br></pre></td></tr></table></figure><p>在该例中：</p><ul><li>创建名为<code>nginx-deployment</code>的Deployment</li><li>该Deployment创建<code>3</code>个（由replicas字段标明）Pod副本</li><li><code>selector</code>字段定义了Deployment如何查找要管理的Pods。在这里选择Pod模板中定义的标签（app：nginx）。</li></ul><blockquote><p><code>spec.selector.matchLabels</code> 字段是key-Value键值对映射，在matchLabels映射的每个Key-Value映射等效于<code>matchExpressions</code>中的一个元素，即key字段是<code>key</code>，操作符是<code>In</code>，<code>values</code>数组仅包含<code>value</code>。在<code>matchLabels</code>和<code>matchExpressions</code>中给出的所有条件都满足才能匹配。</p></blockquote><ul><li>template包含以下子字段：<ul><li>Pod被使用<code>.metadata.labels</code>字段打上<code>app:nginx</code>标签</li><li>Pod模板规约即<code>.template.spec</code>字段指示Pods运行一个nginx容器，该容器运行<code>nginx:1.14.2</code>的Docker Hub 镜像</li><li>创建一个容器并使用<code>.spec.template.spec.containers[0].name</code>字段并将其命名为<code>nginx</code></li></ul></li></ul><h4 id="service">Service</h4><p>Service提供了一个或者多个Pod实例的稳定访问地址。</p><p>从上文中可以看到，一个Deployment可以有两个甚至更多个完全相同的Pod，对于一个外部的用户来说，访问哪一个Pod其实都属于一样的，所以他希望做一次负载均衡，在做负载均衡的同时，只想访问某一个固定的<strong>VIP，VirtualIP</strong>地址，而不希望得到一个具体的Pod的IP地址。</p><p>由于Pod本身可能会终止，如果一个Pod失败了，可能会换成另外一个新的。</p><p>对于外部用户来说，提供了多个具体的Pod地址，这个用户需要不停的去更新Pod地址。<strong>K8s的Service把所有Pod的访问能力抽象成一个第三方的IP地址。</strong></p><p>实现Service有多种方式，K8s支持Cluster IP，上面讲过的kuber-proxy组网，也支持nodePort，LoadBalancer等其他的一些访问能力。</p><p><img data-src="image-20220430153116204.png" /></p><h5 id="使用service">使用Service</h5><p>例如，假定有一组Pod，他们对外暴露了9376端口，同时还被打上了app=Myapp标签</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">apiVersion:</span> <span class="string">v1</span></span><br><span class="line"><span class="attr">kind:</span> <span class="string">Service</span></span><br><span class="line"><span class="attr">metadata:</span></span><br><span class="line">  <span class="attr">name:</span> <span class="string">my-service</span></span><br><span class="line"><span class="attr">spec:</span></span><br><span class="line">  <span class="attr">selector:</span></span><br><span class="line">    <span class="attr">app:</span> <span class="string">MyApp</span></span><br><span class="line">  <span class="attr">ports:</span></span><br><span class="line">    <span class="bullet">-</span> <span class="attr">protocol:</span> <span class="string">TCP</span></span><br><span class="line">      <span class="attr">port:</span> <span class="number">80</span></span><br><span class="line">      <span class="attr">targetPort:</span> <span class="number">9376</span></span><br></pre></td></tr></table></figure><p>上述配置创建了一个名为<code>my-service</code>的对象，它会将请求代理到TCP端口9376，并且具有标签<code>app=MyApp</code>的Pod上。</p>]]></content>
    
    
    <summary type="html">&lt;p&gt;本文是CNCF × Alibaba 云原生技术公开课的&lt;strong&gt;第三章Kubernetes 核心概念&lt;/strong&gt;学习笔记。&lt;/p&gt;
&lt;p&gt;关键词：&lt;strong&gt;k8s&lt;/strong&gt;&lt;/p&gt;</summary>
    
    
    
    
    <category term="k8s" scheme="https://fly97.cn/tags/k8s/"/>
    
  </entry>
  
  <entry>
    <title>Kubernetes集群实践（一）| 使用Kubeadm创建一个K8s集群</title>
    <link href="https://fly97.cn/p/create-a-k8s-cluster-with-kubeadm/"/>
    <id>https://fly97.cn/p/create-a-k8s-cluster-with-kubeadm/</id>
    <published>2022-05-04T05:25:00.000Z</published>
    <updated>2022-05-04T05:25:00.000Z</updated>
    
    <content type="html"><![CDATA[<p>本文主要介绍如何使用一个kubeadm创建一个K8s集群。</p><p>关键词：<strong>docker，k8s</strong></p><a id="more"></a><h3 id="准备工作">准备工作</h3><p>首先确保你至少有两台机器，这里准备了四台机器：一台master节点，三台node节点，主机名分别为node1、node2、node3。</p><p>master节点配置了管理node集群的脚本，分别是批量执行命令的<strong>xcall</strong>脚本和批量拷贝文件的<strong>xsync</strong>脚本。</p><p>安装xcall和xsync脚本的机器需要先配置<strong>免密码登录</strong>，并安装<strong>rsync服务</strong>。</p><h4 id="xcall">xcall</h4><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#!/bin/bash</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 获取控制台指令</span></span><br><span class="line"></span><br><span class="line">cmd=$*</span><br><span class="line"></span><br><span class="line"><span class="comment"># 判断指令是否为空</span></span><br><span class="line"><span class="keyword">if</span> [ ! -n <span class="string">&quot;<span class="variable">$cmd</span>&quot;</span> ]</span><br><span class="line"><span class="keyword">then</span></span><br><span class="line">        <span class="built_in">echo</span> <span class="string">&quot;command can not be null !&quot;</span></span><br><span class="line">        <span class="built_in">exit</span></span><br><span class="line"><span class="keyword">fi</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 获取当前登录用户</span></span><br><span class="line">user=`whoami`</span><br><span class="line"></span><br><span class="line"><span class="comment"># 在从机执行指令,这里需要根据你具体的集群情况配置，host与具体主机名一致，同上</span></span><br><span class="line"><span class="keyword">for</span> host <span class="keyword">in</span> node1 node2 node3</span><br><span class="line"><span class="keyword">do</span></span><br><span class="line">        <span class="built_in">echo</span> =============== <span class="variable">$host</span> ===============</span><br><span class="line">        <span class="built_in">echo</span> <span class="string">&quot;--&gt; excute command \&quot;<span class="variable">$cmd</span>\&quot;&quot;</span></span><br><span class="line">        ssh <span class="variable">$host</span> <span class="variable">$cmd</span></span><br><span class="line"><span class="keyword">done</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">echo</span> <span class="string">&quot;excute successfully !&quot;</span></span><br></pre></td></tr></table></figure><h4 id="xsync">xsync</h4><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#!/bin/bash</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 获取输出参数，如果没有参数则直接返回</span></span><br><span class="line">pcount=<span class="variable">$#</span></span><br><span class="line"><span class="keyword">if</span> [ <span class="variable">$pcount</span> -eq 0 ]</span><br><span class="line"><span class="keyword">then</span></span><br><span class="line">        <span class="built_in">echo</span> <span class="string">&quot;no parameter find !&quot;</span>;</span><br><span class="line">        <span class="built_in">exit</span>;</span><br><span class="line"><span class="keyword">fi</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 获取传输文件名</span></span><br><span class="line">p1=<span class="variable">$1</span></span><br><span class="line">filename=`basename <span class="variable">$p1</span>`</span><br><span class="line"><span class="built_in">echo</span> <span class="string">&quot;load file <span class="variable">$p1</span> success !&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 获取文件的绝对路径</span></span><br><span class="line">pdir=`<span class="built_in">cd</span> -P $(dirname <span class="variable">$p1</span>); <span class="built_in">pwd</span>`</span><br><span class="line"><span class="built_in">echo</span> <span class="string">&quot;file path is <span class="variable">$pdir</span>&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 获取当前用户（如果想使用root用户权限拷贝文件，在命令后加入-root参数即可）</span></span><br><span class="line">user=<span class="variable">$2</span></span><br><span class="line"><span class="keyword">case</span> <span class="string">&quot;<span class="variable">$user</span>&quot;</span> <span class="keyword">in</span></span><br><span class="line"><span class="string">&quot;-root&quot;</span>)</span><br><span class="line">        user=<span class="string">&quot;root&quot;</span>;;</span><br><span class="line"><span class="string">&quot;&quot;</span>)</span><br><span class="line">        user=`whoami`;;</span><br><span class="line">*)</span><br><span class="line">        <span class="built_in">echo</span> <span class="string">&quot;illegal parameter <span class="variable">$user</span>&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">esac</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">echo</span> <span class="variable">$user</span></span><br><span class="line"><span class="comment"># 拷贝文件到从机(这里注意主机的host需要根据你的实际情况配置，要与你具体的主机名对应)</span></span><br><span class="line"><span class="keyword">for</span> host <span class="keyword">in</span> node1 node2 node3</span><br><span class="line"><span class="keyword">do</span></span><br><span class="line">        <span class="built_in">echo</span> <span class="string">&quot;================<span class="variable">$host</span>=================&quot;</span></span><br><span class="line">        ssh <span class="variable">$host</span> <span class="string">&quot;mkdir -p <span class="variable">$pdir</span>&quot;</span></span><br><span class="line">        rsync -av <span class="variable">$pdir</span>/<span class="variable">$filename</span> <span class="variable">$host</span>:<span class="variable">$pdir</span>/<span class="variable">$filename</span></span><br><span class="line"><span class="keyword">done</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">echo</span> <span class="string">&quot;complate !&quot;</span></span><br></pre></td></tr></table></figure><h4 id="设置主机公钥确认">设置主机公钥确认</h4><p>SSH 公钥检查是一个重要的安全机制，可以防范中间人劫持等黑客攻击。但是在特定情况下，严格的 SSH 公钥检查会破坏一些依赖 SSH 协议的自动化任务，就需要一种手段能够<strong>绕过 SSH 的公钥检查</strong>。</p><p>编辑<code>/etc/ssh/ssh_config</code>文件，修改<code>StrictHostKeyChecking=no</code>.</p><ol type="1"><li><code>StrictHostKeyChecking=no</code> 最不安全的级别，当然也没有那么多烦人的提示了，相对安全的内网<strong>测试</strong>时建议使用。如果连接<strong>server</strong>的key在本地不存在，那么就自动添加到文件中（默认是known_hosts），并且给出一个警告。</li><li><code>StrictHostKeyChecking=ask</code> 默认的级别，就是出现刚才的提示了。如果连接和key不匹配，给出提示，并拒绝登录。</li><li><code>StrictHostKeyChecking=yes</code> 最安全的级别，如果连接与key不匹配，就拒绝连接，不会提示详细信息。</li></ol><h4 id="将两个脚本所在的目录添加到环境变量">将两个脚本所在的目录添加到环境变量</h4><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">echo</span> <span class="string">&quot;export PATH=<span class="variable">$PATH</span>:~/.bin&quot;</span> &gt;&gt; ~/.bashrc</span><br></pre></td></tr></table></figure><h4 id="检查脚本是否正常运行">检查脚本是否正常运行</h4><p><img data-src="image-20220504134400786.png" /></p><h3 id="安装kubeadm">安装kubeadm</h3><h4 id="开始之前">开始之前</h4><p>你需要准备：</p><ul><li>一台兼容的 Linux 主机。Kubernetes 项目为基于 Debian 和 Red Hat 的 Linux 发行版以及一些不提供包管理器的发行版提供通用的指令</li><li>每台机器 2 GB 或更多的 RAM （如果少于这个数字将会影响你应用的运行内存)</li><li>2 CPU 核或更多</li><li>集群中的所有机器的网络彼此均能相互连接(公网和内网都可以)</li><li>节点之中不可以有重复的主机名、MAC 地址或 product_uuid。请参见<a href="https://kubernetes.io/zh/docs/setup/production-environment/tools/kubeadm/install-kubeadm/#verify-mac-address">这里</a>了解更多详细信息。</li><li>开启机器上的某些端口。请参见<a href="https://kubernetes.io/zh/docs/setup/production-environment/tools/kubeadm/install-kubeadm/#check-required-ports">这里</a> 了解更多详细信息。</li><li><strong>禁用交换分区</strong>。为了保证 kubelet 正常工作，你 <strong>必须</strong> 禁用交换分区。</li></ul><h5 id="禁用交换分区">禁用交换分区</h5><p>临时关闭</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">swapoff -a</span><br></pre></td></tr></table></figure><p>永久关闭</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">vim /etc/fstab</span><br></pre></td></tr></table></figure><p>注释掉最后一行的swap</p><p><img data-src="image-20220504135341965.png" /></p><h5 id="确保每个节点上-mac-地址和-product_uuid-的唯一性">确保每个节点上 MAC 地址和 product_uuid 的唯一性</h5><p>这里直接使用xcall脚本执行命令。</p><p>一般来讲，硬件设备会拥有唯一的地址，但是有些虚拟机的地址可能会重复。 Kubernetes 使用这些值来唯一确定集群中的节点。 如果这些值在每个节点上不唯一，可能会导致<strong>安装失败</strong>。</p><p>检查mac地址</p><p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">ip link</span><br></pre></td></tr></table></figure></p><p><img data-src="image-20220504140036574.png" /></p><p>检查product_uuid</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">sudo cat /sys/class/dmi/id/product_uuid</span><br></pre></td></tr></table></figure><p><img data-src="image-20220504140019711.png" /></p><h5 id="检查网络适配器">检查网络适配器</h5><p>如果<strong>有一个以上的网络适配器</strong>，同时K8s通过默认路由不可达，此时需要先添加默认IP路由规则。</p><p>这里使用的是同一局域网的虚拟机，默认符合规则。</p><h4 id="允许-iptables-检查桥接流量">允许 iptables 检查桥接流量</h4><p>检查<code>br_netfilter</code>模块是否被加载</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">lsmod | grep br_netfilter</span><br></pre></td></tr></table></figure><p><img data-src="image-20220504140607226.png" /></p><p>这里没有加载，需要设置加载</p><p>显式加载</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">sudo modprobe br_netfilter</span><br></pre></td></tr></table></figure><p>为了让Linux开机时就加载，需要修改<code>sysctl</code> 配置</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">cat &lt;&lt;EOF | sudo tee /etc/modules-load.d/k8s.conf</span><br><span class="line">br_netfilter</span><br><span class="line">EOF</span><br><span class="line"></span><br><span class="line">cat &lt;&lt;EOF | sudo tee /etc/sysctl.d/k8s.conf</span><br><span class="line">net.bridge.bridge-nf-call-ip6tables = 1</span><br><span class="line">net.bridge.bridge-nf-call-iptables = 1</span><br><span class="line">EOF</span><br><span class="line">sudo sysctl --system</span><br></pre></td></tr></table></figure><h4 id="检查所需端口">检查所需端口</h4><p>为了和K8s组件通信，需要检查特定端口</p><p>具体请查看：https://kubernetes.io/zh/docs/reference/ports-and-protocols/</p><h4 id="安装容器运行时">安装容器运行时</h4><h5 id="设置运行时">设置运行时</h5><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line">cat &lt;&lt;EOF | sudo tee /etc/modules-load.d/containerd.conf</span><br><span class="line">overlay</span><br><span class="line">br_netfilter</span><br><span class="line">EOF</span><br><span class="line"></span><br><span class="line">sudo modprobe overlay</span><br><span class="line">sudo modprobe br_netfilter</span><br><span class="line"></span><br><span class="line"><span class="comment"># 设置必需的 sysctl 参数，这些参数在重新启动后仍然存在。</span></span><br><span class="line">cat &lt;&lt;EOF | sudo tee /etc/sysctl.d/99-kubernetes-cri.conf</span><br><span class="line">net.bridge.bridge-nf-call-iptables  = 1</span><br><span class="line">net.ipv4.ip_forward                 = 1</span><br><span class="line">net.bridge.bridge-nf-call-ip6tables = 1</span><br><span class="line">EOF</span><br><span class="line"></span><br><span class="line"><span class="comment"># 应用 sysctl 参数而无需重新启动</span></span><br><span class="line">sudo sysctl --system</span><br></pre></td></tr></table></figure><p>K8s支持以下运行时</p><table><thead><tr class="header"><th>运行时</th><th>域套接字</th></tr></thead><tbody><tr class="odd"><td>Docker Engine</td><td><code>/var/run/dockershim.sock</code></td></tr><tr class="even"><td>containerd</td><td><code>/run/containerd/containerd.sock</code></td></tr><tr class="odd"><td>CRI-O</td><td><code>/var/run/crio/crio.sock</code></td></tr></tbody></table><p>由于在<strong>1.24之后的版本不在支持Dockershim </strong>，因此不推荐使用Docker Engine作为K8s容器运行时，这里使用<strong>containerd</strong>。</p><p>方法：</p><ul><li><p>按照安装docker的方法，安装docker需要的apt源；</p></li><li><p>执行以下命令</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">sudo apt-get update &amp;&amp; sudo apt-get install containerd.io</span><br></pre></td></tr></table></figure></li></ul><p>配置运行时</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">sudo mkdir -p /etc/containerd</span><br><span class="line">containerd config default | sudo tee /etc/containerd/config.toml</span><br></pre></td></tr></table></figure><p>重新启动 containerd</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">sudo systemctl restart containerd</span><br></pre></td></tr></table></figure><p><img data-src="image-20220504143744362.png" /></p><h5 id="设置docker-hub镜像源">设置Docker Hub镜像源</h5><p>默认containerd在Docker Hub拉取镜像，这样速度很慢</p><p><img data-src="image-20220505115520415.png" /></p><p>在<code>/etc/containerd</code>创建mirrors.d文件夹</p><p><img data-src="image-20220505115710174.png" /></p><p>编辑hosts.toml，这里使用daocloud的源</p><figure class="highlight toml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">server</span> = <span class="string">&quot;https://docker.io&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="section">[host.&quot;http://f1361db2.m.daocloud.io&quot;]</span></span><br><span class="line">  <span class="attr">capabilities</span> = [<span class="string">&quot;pull&quot;</span>, <span class="string">&quot;resolve&quot;</span>]</span><br></pre></td></tr></table></figure><h5 id="使用-systemd-cgroup-驱动程序">使用 <code>systemd</code> cgroup 驱动程序</h5><p>结合 <code>runc</code> 使用 <code>systemd</code> cgroup 驱动，在 <code>/etc/containerd/config.toml</code> 中设置</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">[plugins.&quot;io.containerd.grpc.v1.cri&quot;.containerd.runtimes.runc]</span><br><span class="line">  ...</span><br><span class="line">  [plugins.&quot;io.containerd.grpc.v1.cri&quot;.containerd.runtimes.runc.options]</span><br><span class="line">    SystemdCgroup &#x3D; true</span><br></pre></td></tr></table></figure><p>如果您应用此更改，请确保再次重新启动 containerd：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">sudo systemctl restart containerd</span><br></pre></td></tr></table></figure><p><a href="https://kubernetes.io/zh/docs/setup/production-environment/container-runtimes/#containerd">参考</a></p><h4 id="安装-kubeadmkubelet-和-kubectl">安装 kubeadm、kubelet 和 kubectl</h4><p>这里使用基于Debian的发行版</p><ol type="1"><li>更新 <code>apt</code>包索引并安装使用 Kubernetes <code>apt</code> 仓库所需要的包</li></ol><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">sudo apt-get update</span><br><span class="line">sudo apt-get install -y apt-transport-https ca-certificates curl</span><br></pre></td></tr></table></figure><ol start="2" type="1"><li>安装仓库密钥，这里使用<strong>阿里云的源</strong></li></ol><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">curl https://mirrors.aliyun.com/kubernetes/apt/doc/apt-key.gpg | sudo apt-key add - </span><br><span class="line">cat &lt;&lt; EOF &gt;/etc/apt/sources.list.d/kubernetes.list</span><br><span class="line">deb https://mirrors.aliyun.com/kubernetes/apt/ kubernetes-xenial main</span><br><span class="line">EOF</span><br></pre></td></tr></table></figure><ol start="3" type="1"><li>更新apt包索引，安装 kubelet、kubeadm 和 kubectl，并<strong>锁定其版本</strong>：</li></ol><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">sudo apt-get update</span><br><span class="line">sudo apt-get install -y kubelet kubeadm kubectl</span><br><span class="line">sudo apt-mark hold kubelet kubeadm kubectl</span><br></pre></td></tr></table></figure><p>kubelet 现在每隔几秒就会重启，因为它陷入了一个等待 kubeadm 指令的死循环。</p><p><img data-src="image-20220504145739260.png" /></p><h4 id="配置容器运行时-cgroup-驱动">配置容器运行时 cgroup 驱动</h4><p>由于 kubeadm 把 kubelet 视为一个系统服务来管理，所以对基于 kubeadm 的安装， <strong>推荐使用 <code>systemd</code> 驱动</strong>，不推荐 <code>cgroupfs</code> 驱动。</p><p>一个示例：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># kubeadm-config.yaml</span></span><br><span class="line">kind: ClusterConfiguration</span><br><span class="line">apiVersion: kubeadm.k8s.io/v1beta3</span><br><span class="line">kubernetesVersion: v1.23.6</span><br><span class="line">imageRepository: registry.cn-hangzhou.aliyuncs.com/google_containers</span><br><span class="line">networking:</span><br><span class="line">  podSubnet: <span class="string">&quot;10.244.0.0/16&quot;</span> <span class="comment"># --pod-network-cidr # 网络插件地址</span></span><br><span class="line">---</span><br><span class="line">kind: KubeletConfiguration</span><br><span class="line">apiVersion: kubelet.config.k8s.io/v1beta1</span><br><span class="line">cgroupDriver: systemd</span><br></pre></td></tr></table></figure><h3 id="启动控制节点">启动控制节点</h3><p>启用控制平面前应先启动containerd服务</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">sudo systemctl <span class="built_in">enable</span> containerd --now</span><br></pre></td></tr></table></figure><p>修改<code>containerd.sock</code>权限，不使用sudo也可以拉取镜像</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">sudo chown -R wf09:wf09 /run/containerd/</span><br></pre></td></tr></table></figure><p>运行master节点之前可以先拉取本地镜像</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">kubeadm config images pull --config kubeadm-config.yaml</span><br></pre></td></tr></table></figure><p>在master节点运行以下命令，同时传递上文中说到的kubeadm-config.yaml</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">kubeadm init --config kubeadm-config.yaml</span><br></pre></td></tr></table></figure><h3 id="删除控制节点">删除控制节点</h3><p>如果在启动后控制平面以后出现错误，必须先<strong>删除控制平面</strong></p><p>更详细的可以参考<a href="https://kubernetes.io/zh/docs/setup/production-environment/tools/kubeadm/create-cluster-kubeadm/#tear-down">卸载集群</a></p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">sudo kubeadm reset</span><br></pre></td></tr></table></figure><p><img data-src="image-20220505095239821.png" /></p><h3 id="控制节点启动成功">控制节点启动成功</h3><p><img data-src="image-20220505101003030.png" /></p><p>如果使用非root用户运行kubectl，需要运行以下命令</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">mkdir -p <span class="variable">$HOME</span>/.kube</span><br><span class="line">sudo cp -i /etc/kubernetes/admin.conf <span class="variable">$HOME</span>/.kube/config</span><br><span class="line">sudo chown $(id -u):$(id -g) <span class="variable">$HOME</span>/.kube/config</span><br></pre></td></tr></table></figure><p>如果使用root用户，则可以运行</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">export</span> KUBECONFIG=/etc/kubernetes/admin.conf</span><br></pre></td></tr></table></figure><p>还需要记录一下token，后面的节点<strong>加入集群</strong>需要这个</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">kubeadm join 192.168.15.201:6443 --token 57hnhv.sy8wb7c1gsrzbotp \</span><br><span class="line">        --discovery-token-ca-cert-hash sha256:15cb23455b3cb846261b535dcbbf73ea36e3f089cf4b8b36623bf36b461b522d</span><br></pre></td></tr></table></figure><h3 id="安装-pod-网络附加组件">安装 Pod 网络附加组件</h3><p>必须部署一个基于Pod网络插件的容器网络接口，以便的Pod可以相互通信</p><p>安装之前需要先设置<strong>网络插件地址</strong>，在kubeadm-config.yaml添加以下配置</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">networking:</span></span><br><span class="line">  <span class="attr">podSubnet:</span> <span class="string">&quot;10.244.0.0/16&quot;</span> <span class="comment"># --pod-network-cidr # 网络插件地址</span></span><br></pre></td></tr></table></figure><p>这里使用<strong>flannel</strong></p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">kubectl apply -f https://raw.githubusercontent.com/flannel-io/flannel/master/Documentation/kube-flannel.yml</span><br></pre></td></tr></table></figure><p><img data-src="image-20220505112506790.png" /></p><p>检查节点状态</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">kubectl get nodes</span><br></pre></td></tr></table></figure><p><img data-src="image-20220505112608594.png" /></p><p>当STATUS状态为Ready时说明集群已经启动成功了。</p><h3 id="设置master为可调度节点">设置Master为可调度节点</h3><p>默认情况下，<code>Master</code> 不参与 <code>Pod</code> 调度，也就是说不会在 <code>Master</code> 节点上部署其他非系统 <code>Pod</code>。可以使用以下命令调整这个策略</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 允许 Master 部署 Pod</span></span><br><span class="line">kubectl taint nodes master node-role.kubernetes.io/master- --overwrite</span><br><span class="line"></span><br><span class="line"><span class="comment"># 禁止 Master 部署 Pod</span></span><br><span class="line">kubectl taint nodes master node-role.kubernetes.io/master=:NoSchedule --overwrite</span><br><span class="line"></span><br><span class="line"><span class="comment"># 其中 localhost.master 是操作的节点名称</span></span><br></pre></td></tr></table></figure><h3 id="启动工作节点">启动工作节点</h3><p>准备工作首先也需要完成<strong>安装kubeadm</strong>的步骤。</p><p>剩下的步骤就比较简单了，找到上文中<strong>成功启动K8s控制节点</strong>中的token，在工作节点执行</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">sudo kubeadm join 192.168.15.201:6443 --token 57hnhv.sy8wb7c1gsrzbotp \</span><br><span class="line">--discovery-token-ca-cert-hash sha256:15cb23455b3cb846261b535dcbbf73ea36e3f089cf4b8b36623bf36b461b522d</span><br></pre></td></tr></table></figure><p><img data-src="image-20220505133657174.png" /></p><p>如果有类似的回显说明已经加入成功了。</p><h3 id="集群启动成功">集群启动成功</h3><p><img data-src="image-20220505141228217.png" /></p><h3 id="kubernetes排错">Kubernetes排错</h3><h4 id="wait-control-plane-waiting-for-the-kubelet-to-boot-up-the-control-plane-as-static-pods-from-directory-etckubernetesmanifests.-this-can-take-up-to-4m0s">[wait-control-plane] Waiting for the kubelet to boot up the control plane as static Pods from directory “/etc/kubernetes/manifests”. This can take up to 4m0s</h4><p>查看kubelet日志</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">journalctl -xeu kubelet</span><br></pre></td></tr></table></figure><p>出现频率较高的就是<strong>sandbox</strong>，应该和这个镜像有关</p><p><img data-src="image-20220505094125196.png" /></p><p>查看<code>containerd</code>默认配置</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">cat /etc/containerd/config.toml</span><br></pre></td></tr></table></figure><p><img data-src="image-20220505094253375.png" /></p><p>发现默认配置是<code>k8s.gcr.io</code>域的镜像，国内拉取不到。</p><p>上文中已经拉取了阿里云的镜像，执行以下命令可以查看镜像的拉取地址</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">crictl images</span><br></pre></td></tr></table></figure><p>如果报错应该是权限问题，<strong>即<code>containerd</code> 的创建的unix socket文件权限是root</strong>，可以试试加上<code>sudo</code></p><p><img data-src="image-20220505094820005.png" /></p><h5 id="解决方案">解决方案</h5><p>将<code>k8s.gcr.io/pause</code>镜像更改为本地拉取的阿里云镜像即可。</p><h4 id="加入节点时提示getting-status-of-runtime-rpc-error-code-unimplemented-desc-unknown-service-runtime.v1alpha2.runtimeservice">加入节点时，提示"getting status of runtime: rpc error: code = Unimplemented desc = unknown service runtime.v1alpha2.RuntimeService"</h4><h5 id="解决方案-1">解决方案</h5><p>检查<code>/etc/containerd/config.toml</code>文件，将SystemdCgroup设置为true</p><p><img data-src="image-20220505140624811.png" /></p><p>重启containerd服务</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">sudo systemctl restart containerd</span><br></pre></td></tr></table></figure><h3 id="参考">参考</h3><ul><li><p>https://github.com/flannel-io/flannel</p></li><li><p>https://kubernetes.io/zh/docs/setup/production-environment/tools/kubeadm/create-cluster-kubeadm/#pod-network</p></li><li><p>https://github.com/containerd/containerd/blob/main/docs/cri/registry.md</p></li></ul>]]></content>
    
    
    <summary type="html">&lt;p&gt;本文主要介绍如何使用一个kubeadm创建一个K8s集群。&lt;/p&gt;
&lt;p&gt;关键词：&lt;strong&gt;docker，k8s&lt;/strong&gt;&lt;/p&gt;</summary>
    
    
    
    
    <category term="k8s" scheme="https://fly97.cn/tags/k8s/"/>
    
  </entry>
  
  <entry>
    <title>在Docker Destop启用k8s服务</title>
    <link href="https://fly97.cn/p/enable-k8s-in-docker-desktop/"/>
    <id>https://fly97.cn/p/enable-k8s-in-docker-desktop/</id>
    <published>2022-04-29T06:37:00.000Z</published>
    <updated>2022-04-29T06:37:00.000Z</updated>
    
    <content type="html"><![CDATA[<p>作为目前事实上的容器编排系统标准，K8s 无疑是现代应用的基石。</p><p>本文主要是开启Docker Desktop for Win/Mac 的K8s服务。</p><p>关键词：<strong>docker，k8s</strong></p><a id="more"></a><h3 id="确定k8s版本">确定k8s版本</h3><ul><li>需安装 Docker Desktop 的 Mac 或者 Windows 版本，如果没有请下载<a href="https://store.docker.com/search?type=edition&amp;offering=community">下载 Docker CE最新版本</a></li><li>当前 master 分支已经在 Docker for Mac/Windows 4.4.2 (包含 Docker CE 20.10.12 和 Kubernetes 1.22.5) 版本测试通过</li><li>如果需要测试其他版本，请查看 Docker Desktop版本，Docker -&gt; About Docker Desktop</li></ul><p><img data-src="image-20220429163406392.png" /></p><h3 id="切换到对应分支">切换到对应分支</h3><p>在任意目录下clone<a href="https://github.com/AliyunContainerService/k8s-for-docker-desktop">k8s-for-docker-desktop</a></p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git <span class="built_in">clone</span> https://github.com/AliyunContainerService/k8s-for-docker-desktop.git</span><br></pre></td></tr></table></figure><p>这里的Kubernetes版本为 v1.22.5, 故使用 <a href="https://github.com/AliyunContainerService/k8s-for-docker-desktop/tree/v1.22.5">v1.22.5 分支</a> <code>git checkout v1.22.5</code></p><h3 id="开启k8s">开启K8s</h3><p>为 Docker daemon 配置镜像加速，参考<a href="https://cr.console.aliyun.com/cn-hangzhou/instances/mirrors">阿里云镜像服务</a> 或中科大镜像加速地址<code>https://docker.mirrors.ustc.edu.cn</code></p><p><img data-src="image-20220429163857048.png" /></p><p>从阿里云镜像服务下载 Kubernetes 所需要的镜像</p><p>在 Mac 上执行如下脚本</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">.&#x2F;load_images.sh</span><br></pre></td></tr></table></figure><p>在Windows上，使用 PowerShell</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">.\load_images.ps1</span><br></pre></td></tr></table></figure><p>说明:</p><ul><li>如果因为安全策略无法执行 PowerShell 脚本，请在 “以管理员身份运行” 的 PowerShell 中执行 <code>Set-ExecutionPolicy RemoteSigned</code> 命令。</li><li>如果需要，可以通过修改 <code>images.properties</code> 文件自行加载你自己需要的镜像</li></ul><p><img data-src="image-20220429164553018.png" /></p><p>开启 Kubernetes，并等待 Kubernetes 开始运行</p><p><img data-src="image-20220429164629527.png" /></p><p>当左下角出现k8s的图标时，说明开启成功</p><p><img data-src="image-20220429165336573.png" /></p><p>TIPS：</p><p>在Mac上:</p><p>如果在Kubernetes部署的过程中出现问题，可以通过docker desktop应用日志获得实时日志信息：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">pred&#x3D;&#39;process matches &quot;.*(ocker|vpnkit).*&quot;</span><br><span class="line">  || (process in &#123;&quot;taskgated-helper&quot;, &quot;launchservicesd&quot;, &quot;kernel&quot;&#125; &amp;&amp; eventMessage contains[c] &quot;docker&quot;)&#39;</span><br><span class="line">&#x2F;usr&#x2F;bin&#x2F;log stream --style syslog --level&#x3D;debug --color&#x3D;always --predicate &quot;$pred&quot;</span><br></pre></td></tr></table></figure><p>在Windows上:</p><p>如果在Kubernetes部署的过程中出现问题，可以在 <code>C:\ProgramData\DockerDesktop</code>下的service.txt 查看Docker日志, 在 <code>C:\Users\yourUserName\AppData\Local\Docker</code>下的log.txt 查看Kubernetes日志。</p><h3 id="配置k8s">配置K8s</h3><p>验证k8s集群状态</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">kubectl cluster-info</span><br><span class="line">kubectl get nodes</span><br></pre></td></tr></table></figure><p><img data-src="image-20220429170726815.png" /></p><p>配置K8s控制台</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">kubectl create -f kubernetes-dashboard.yaml</span><br></pre></td></tr></table></figure><p><img data-src="image-20220429170904942.png" /></p><p>检查 kubernetes-dashboard 应用状态</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">kubectl get pod -n kubernetes-dashboard</span><br></pre></td></tr></table></figure><p><img data-src="image-20220429171026647.png" /></p><p>开启API Server 访问代理</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">kubectl proxy</span><br></pre></td></tr></table></figure><p><img data-src="image-20220429171129620.png" /></p><p>通过如下URL访问kubernetes-dashboard</p><p>http://localhost:8001/api/v1/namespaces/kubernetes-dashboard/services/https:kubernetes-dashboard:/proxy/</p><p>配置Mac环境</p><p>配置控制台访问令牌</p><p>对于Mac环境</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">TOKEN&#x3D;$(kubectl -n kube-system describe secret default| awk &#39;$1&#x3D;&#x3D;&quot;token:&quot;&#123;print $2&#125;&#39;)</span><br><span class="line">kubectl config set-credentials docker-desktop --token&#x3D;&quot;$&#123;TOKEN&#125;&quot;</span><br><span class="line">echo $TOKEN</span><br></pre></td></tr></table></figure><p>对于Windows环境</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">$TOKEN&#x3D;((kubectl -n kube-system describe secret default | Select-String &quot;token:&quot;) -split &quot; +&quot;)[1]</span><br><span class="line">kubectl config set-credentials docker-desktop --token&#x3D;&quot;$&#123;TOKEN&#125;&quot;</span><br><span class="line">echo $TOKEN</span><br></pre></td></tr></table></figure><p><img data-src="image-20220429171807293.png" /></p><p>登录dashboard的时候选择令牌，输入上文控制台输出中的内容。</p><p>或者选择kubeconfig，路径如下</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">Mac: <span class="variable">$HOME</span>/.kube/config</span><br><span class="line">Win: %UserProfile%\.kube\config</span><br></pre></td></tr></table></figure><p>点击登陆，进入Kubernetes Dashboard</p>]]></content>
    
    
    <summary type="html">&lt;p&gt;作为目前事实上的容器编排系统标准，K8s 无疑是现代应用的基石。&lt;/p&gt;
&lt;p&gt;本文主要是开启Docker Desktop for Win/Mac 的K8s服务。&lt;/p&gt;
&lt;p&gt;关键词：&lt;strong&gt;docker，k8s&lt;/strong&gt;&lt;/p&gt;</summary>
    
    
    
    
    <category term="docker" scheme="https://fly97.cn/tags/docker/"/>
    
  </entry>
  
  <entry>
    <title>progit学习笔记</title>
    <link href="https://fly97.cn/p/study-notebooks-for-pro-git/"/>
    <id>https://fly97.cn/p/study-notebooks-for-pro-git/</id>
    <published>2022-04-28T06:37:00.000Z</published>
    <updated>2022-04-28T06:37:00.000Z</updated>
    
    <content type="html"><![CDATA[<p>本文是progit的学习记录。</p><p>关键词：<strong>git</strong></p><a id="more"></a><h2 id="介绍">介绍</h2><p>略。</p><h2 id="安装">安装</h2><p>略。</p><h2 id="初次运行git前的配置">初次运行Git前的配置</h2><p><code>git config</code>用来帮助设置控制Git的外观和行为的控制变量。这些变量存在三个不同位置：</p><ol type="1"><li><code>/etc/gitconfig</code>：包含系统上每一个用户以及他们的仓库的通用配置。传递<code>--system</code>选项让Git读写此文件。</li><li><code>~/.gitconfig</code>或<code>~/.config/git/config</code>：只针对当前用户。传递<code>--global</code>选项让Git读写此文件。</li><li>当前使用仓库的Git目录的<code>.git/config</code>文件，只针对该仓库。</li></ol><p>每一个级别覆盖上一级别的配置。即当<code>.git/config</code>文件存在时，不读取其他文件，以此类推。</p><p>window中会查找 <code>$HOME</code>目录下（一般是<code>C:\\Users\\$USER</code>）的<code>.gitconfig</code>文件。Git同样会寻找 <code>/etc/gitconfig</code>文件，但是只限于安装Git时所选的目标位置。</p><h3 id="用户信息">用户信息</h3><p>设置用户名和邮件地址</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">git config --global user.name <span class="string">&quot;wf09&quot;</span></span><br><span class="line">git config --global user.email wf.09@qq.com</span><br></pre></td></tr></table></figure><p>使用了<code>--global</code>选项则该命令只需要运行一次。</p><h3 id="文本编辑器">文本编辑器</h3><p>默认使用操作系统默认的文本编辑器，通常是Vim</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git config --global core.editer vim</span><br></pre></td></tr></table></figure><h3 id="设置代理">设置代理</h3><p>这里的代理分为两种，一种是https/http</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 全局</span></span><br><span class="line">git config --global https.proxy socks5://127.0.0.1:7891</span><br><span class="line">git config --global https.proxy http://127.0.0.1:7890</span><br><span class="line"></span><br><span class="line"><span class="comment"># 只对github进行代理</span></span><br><span class="line">git config --global https.https://github.com.proxy socks5://127.0.0.1:7891</span><br><span class="line">git config --global https.https://github.com.proxy socks5://127.0.0.1:7890</span><br><span class="line"></span><br><span class="line"><span class="comment"># 取消</span></span><br><span class="line">git config --global --<span class="built_in">unset</span> https.proxy</span><br></pre></td></tr></table></figure><p>另外一种是ssh协议的代理</p><p>Linux/Unix环境下，在<code>~/.ssh/config</code>文件下加入如下行</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">Host github.com *.github.com</span><br><span class="line">    User git</span><br><span class="line">    Port 22</span><br><span class="line">    HostName %h</span><br><span class="line">    IdentityFile ~/.ssh/id_ed25519</span><br><span class="line">    ProxyCommand nc -x 127.0.0.1:7890 %h %p%</span><br></pre></td></tr></table></figure><p>其中<code>nc -x</code>后面跟的是socks5的代理地址</p><p>Windows环境下，在 <code>C:\User\$USER\.ssh\config</code>文件下加入如下行</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">Host github.com *.github.com *coding.net</span><br><span class="line">    User git</span><br><span class="line">    Port 22</span><br><span class="line">    HostName %h</span><br><span class="line">    IdentityFile /c/Users/wf09/.ssh/id_ed25519</span><br><span class="line">    ProxyCommand connect -S 127.0.0.1:10809 %h %p</span><br></pre></td></tr></table></figure><p>其中<code>connect -S</code>后面跟的是socks5的代理地址</p><h3 id="检查配置信息">检查配置信息</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br></pre></td><td class="code"><pre><span class="line">$ git config --list</span><br><span class="line">core.excludesfile=~/.gitignore</span><br><span class="line">core.legacyheaders=<span class="literal">false</span></span><br><span class="line">core.quotepath=<span class="literal">false</span></span><br><span class="line">mergetool.keepbackup=<span class="literal">true</span></span><br><span class="line">push.default=simple</span><br><span class="line">color.ui=auto</span><br><span class="line">color.interactive=auto</span><br><span class="line">repack.usedeltabaseoffset=<span class="literal">true</span></span><br><span class="line">alias.s=status</span><br><span class="line">alias.a=!git add . &amp;&amp; git status</span><br><span class="line">alias.au=!git add -u . &amp;&amp; git status</span><br><span class="line">alias.aa=!git add . &amp;&amp; git add -u . &amp;&amp; git status</span><br><span class="line">alias.c=commit</span><br><span class="line">alias.cm=commit -m</span><br><span class="line">alias.ca=commit --amend</span><br><span class="line">alias.ac=!git add . &amp;&amp; git commit</span><br><span class="line">alias.acm=!git add . &amp;&amp; git commit -m</span><br><span class="line">alias.l=<span class="built_in">log</span> --graph --all --pretty=format:<span class="string">&#x27;%C(yellow)%h%C(cyan)%d%Creset %s %C(white)- %an, %ar%Creset&#x27;</span></span><br><span class="line">alias.ll=<span class="built_in">log</span> --<span class="built_in">stat</span> --abbrev-commit</span><br><span class="line">alias.lg=<span class="built_in">log</span> --color --graph --pretty=format:<span class="string">&#x27;%C(bold white)%h%Creset -%C(bold green)%d%Creset %s %C(bold green)(%cr)%Creset %C(bold blue)&lt;%an&gt;%Creset&#x27;</span> --abbrev-commit --date=relative</span><br><span class="line">alias.llg=<span class="built_in">log</span> --color --graph --pretty=format:<span class="string">&#x27;%C(bold white)%H %d%Creset%n%s%n%+b%C(bold blue)%an &lt;%ae&gt;%Creset %C(bold green)%cr (%ci)&#x27;</span> --abbrev-commit</span><br><span class="line">alias.d=diff</span><br><span class="line">alias.master=checkout master</span><br><span class="line">alias.spull=svn rebase</span><br><span class="line">alias.spush=svn dcommit</span><br><span class="line">alias.alias=!git config --list | grep <span class="string">&#x27;alias\.&#x27;</span> | sed <span class="string">&#x27;s/alias\.\([^=]*\)=\(.*\)/\1\ =&gt; \2/&#x27;</span> | sort</span><br><span class="line">include.path=~/.gitcinclude</span><br><span class="line">include.path=.githubconfig</span><br><span class="line">include.path=.gitcredential</span><br><span class="line">diff.exif.textconv=exif</span><br><span class="line">credential.helper=osxkeychain</span><br></pre></td></tr></table></figure><p>可能会看到重复的用户名，因为Git会在不同的文件中读取同一个配置（例如：<code>/etc/gitconfig</code>和<code>~/.gitconfig</code>。后一个配置会覆盖前一个配置。</p><p>可以用过<code>git config &lt;key&gt;</code>来检查Git的某一项配置。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git config user.name</span><br></pre></td></tr></table></figure><h2 id="git基础">git基础</h2><h3 id="获取git仓库">获取Git仓库</h3><h4 id="初始化仓库">初始化仓库</h4><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git init</span><br></pre></td></tr></table></figure><h4 id="克隆现有仓库">克隆现有仓库</h4><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">git <span class="built_in">clone</span> https://github.com/git/git.git</span><br><span class="line"><span class="comment"># 自定义本地仓库名</span></span><br><span class="line">git <span class="built_in">clone</span> https://github.com/git/git.git git-repo</span><br></pre></td></tr></table></figure><h3 id="记录每次更新到仓库">记录每次更新到仓库</h3><p>请注意，工作目录下的每一个文件不外乎两种状态：<strong>已跟踪或未跟踪</strong>。其中<strong>已跟踪的文件</strong>是指那些被纳入了版本控制的文件，在<strong>上一次快照</strong>中有他们的记录。他们的状态可能处于<strong>未修改，已修改或已放入暂存区</strong>。工作目录中除了已跟踪文件目录以外的其他所有文件都属于<strong>未跟踪文件</strong>，他们既不存在于上次快照的记录中，也没有放入暂存区。初次克隆某个仓库时，工作目录中的文件都属于已跟踪文件，并且处于未修改状态。</p><p>编辑过某些文件后，由于自上次提交后你对他们做了修改，Git将他们标记为已修改文件。将这些修改过的文件放入暂存区，然后提交所有暂存了的修改，如此反复，所以使用GIT时文件的生命周期如下：<img data-src="lifecycle.png" /></p><h4 id="检查当前文件状态">检查当前文件状态</h4><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">$ git status</span><br><span class="line">On branch main</span><br><span class="line">Your branch is up to date with <span class="string">&#x27;origin/main&#x27;</span>.</span><br><span class="line"></span><br><span class="line">Untracked files:</span><br><span class="line">  (use <span class="string">&quot;git add &lt;file&gt;...&quot;</span> to include <span class="keyword">in</span> what will be committed)</span><br><span class="line"></span><br><span class="line">        <span class="built_in">source</span>/_posts/progit学习笔记.md</span><br><span class="line">        <span class="built_in">source</span>/_posts/progit学习笔记/</span><br><span class="line"></span><br><span class="line">nothing added to commit but untracked files present (use <span class="string">&quot;git add&quot;</span> to track)</span><br></pre></td></tr></table></figure><p>在状态报告中可以看到新建的<code>progit学习笔记.md文件</code>和<code>progit学习笔记/</code>出现在<code>Untracked files</code>下面。未跟踪的文件意味着Git在之前的快照（提交）中没有这些文件；Git不会自动将其纳入跟踪范围，除非你明明白白的告诉他需要跟踪该文件。</p><h4 id="跟踪新文件">跟踪新文件</h4><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">$ git add <span class="built_in">source</span>/_posts/progit学习笔记.md</span><br></pre></td></tr></table></figure><p>此时在运行<code>git status</code>命令，会看到上面的文件<strong>已被跟踪</strong>，并处于<strong>暂存</strong>的状态</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line">$ git status</span><br><span class="line">On branch main</span><br><span class="line">Your branch is up to date with <span class="string">&#x27;origin/main&#x27;</span>.</span><br><span class="line"></span><br><span class="line">Changes to be committed:</span><br><span class="line">  (use <span class="string">&quot;git reset HEAD &lt;file&gt;...&quot;</span> to unstage)</span><br><span class="line"></span><br><span class="line">        new file:   <span class="built_in">source</span>/_posts/progit学习笔记.md</span><br><span class="line"></span><br><span class="line">Changes not staged <span class="keyword">for</span> commit:</span><br><span class="line">  (use <span class="string">&quot;git add &lt;file&gt;...&quot;</span> to update what will be committed)</span><br><span class="line">  (use <span class="string">&quot;git checkout -- &lt;file&gt;...&quot;</span> to discard changes <span class="keyword">in</span> working directory)</span><br><span class="line"></span><br><span class="line">        modified:   <span class="built_in">source</span>/_posts/progit学习笔记.md</span><br><span class="line"></span><br><span class="line">Untracked files:</span><br><span class="line">  (use <span class="string">&quot;git add &lt;file&gt;...&quot;</span> to include <span class="keyword">in</span> what will be committed)</span><br><span class="line"></span><br><span class="line">        <span class="built_in">source</span>/_posts/progit学习笔记/</span><br></pre></td></tr></table></figure><p>只要在<code>Changes to be committed</code>这一行下面的，说明是已暂存的状态，如果此时提交，那么该文件此时的版本将会被留在历史记录中。</p><p><code>git add</code>命令使用文件或者目录的路径作为参数，如果是目录的参数，该命令递归的跟踪该目录下的所有文件。</p><h4 id="暂存已修改文件">暂存已修改文件</h4><p>修改一个已经被跟踪的文件，然后运行命令，就会看到上面所示的内容：</p><p>文件<code>source/_posts/progit学习笔记.md</code>同时出现在出现在暂存区和非暂存区。此时Git只暂存了你运行的<code>git add</code>命令似的版本，如果现在提交，<code>source/_posts/progit学习笔记.md</code>版本是最后一次运行<code>git add</code>命令似的版本，而不是执行<code>git commit</code>时工作目录的版本。需要继续运行<code>git add</code>把最新版本重新暂存起来：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="variable">$git</span> add .</span><br></pre></td></tr></table></figure><p><code>git add .</code>表示将所有修改过的文件移动到暂存区。</p><p><code>git add</code>是一个多功能命令：</p><ul><li>可以开始跟踪新文件</li><li>可以把已跟踪的文件放到暂存区</li><li>合并时把有冲突的文件修改为已解决状态等。</li></ul><p>应该将<code>git add</code>命令应理解为<strong>添加内容到下一次提交中</strong>而不是<strong>将一个文件添加到项目中</strong>。</p><h4 id="状态简览">状态简览</h4><p><code>git status</code>命令的输出十分详细。使用<code>git status -s</code>命令或者<code>git status --short</code>命令将得到一种更为紧凑的格式输出：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="variable">$git</span> status -s</span><br><span class="line">AM <span class="built_in">source</span>/_posts/progit学习笔记.md</span><br><span class="line">A  <span class="built_in">source</span>/_posts/progit学习笔记/lifecycle.png</span><br></pre></td></tr></table></figure><p>未被跟踪的文件有<code>??</code>标记，新添加到暂存区中文件有<code>A</code>标记，修改过的文件有<code>M</code>标记。</p><p>出现靠左边的M表示该文件被修改了并放入到了暂存区；出现在靠右边的M表示该文件被修改了但是还没放到ss暂存区。</p><blockquote><p>和我实际操作的不一致。貌似版本原因。如果一个文件在工作区中被修改并提交到暂存区后又在工作区中被修改，文件前面的标记应该是AM，而不是上文中的MM。</p></blockquote><h4 id="忽略文件">忽略文件</h4><p>创建一个<code>.gitignore</code>的文件，列出要忽略的文件模式</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">$ cat .gitignore</span><br><span class="line">*.[oa]</span><br><span class="line">*~</span><br></pre></td></tr></table></figure><p>第一行告诉Git忽略所有<code>.o</code>或者<code>.a</code>结尾的文件。一般这类对象文件和存档文件都是编译过程中出现的。</p><p>第二行告诉Git忽略所有以波浪符号<code>~</code>为结尾的文件。通常这是用来保存文件副本的。</p><p>此外可能还需要需要忽略log，tmp或者pid目录，以及自动生成的文档等。</p><p><code>.gitignore</code>的格式规范如下：</p><ul><li>所有空行或以<code>#</code>开头的行都会被git忽略；</li><li>可以是由标准的<code>glob</code>模式进行匹配；</li><li>匹配模式可以以<code>/</code>开头防止递归；</li><li>匹配模式可以以<code>/</code>结尾制定目录</li><li>要忽略制定模式意外的文件或目录，可以在模式前面加上 感叹号(!)并取反。</li></ul><blockquote><p>glob模式是shell所使用的简化的正则表达式：</p><ul><li>星号（*）匹配零个或者多个任意字符；</li><li>[abc]匹配任意一个在方括号中的字符，这个例子要么匹配一个a，要么匹配一个b，要么匹配一个c；</li><li>问号（?）只匹配一个任意字符；</li><li>如果在方括号内使用短划线分割两个字符，表示所有在这两个字符范围内的都可以匹配（比如[0-9]匹配所有0到的数字）；</li><li>使用两个星号（*）表示匹配任意中间目录，如<code>a/**z</code>可以匹配<code>a/z,a/b/z,a/b/c/z</code>等</li></ul></blockquote><p>一个<code>.gitignore</code>文件的例子：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># no .a files</span></span><br><span class="line">*.a</span><br><span class="line"></span><br><span class="line"><span class="comment"># but do track lib.a, even though you&#x27;re ignoring .a files above</span></span><br><span class="line">!lib.a</span><br><span class="line"></span><br><span class="line"><span class="comment"># only ignore the TODO file in the current directory, not subdir/TODO</span></span><br><span class="line">/TODO</span><br><span class="line"></span><br><span class="line"><span class="comment"># ignore all files in the build/ directory</span></span><br><span class="line">build/</span><br><span class="line"></span><br><span class="line"><span class="comment"># ignore doc/notes.txt, but not doc/server/arch.txt</span></span><br><span class="line">doc/*.txt</span><br><span class="line"></span><br><span class="line"><span class="comment"># ignore all .pdf files in the doc/ directory</span></span><br><span class="line">doc/**/*.pdf</span><br></pre></td></tr></table></figure><h4 id="查看已暂存和未暂存的修改">查看已暂存和未暂存的修改</h4><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git status</span><br></pre></td></tr></table></figure><p><img data-src="image-20220504184931180.png" /></p><p>要查看尚未暂存的文件更新了那些部分，直接输入</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git diff</span><br></pre></td></tr></table></figure><p><img data-src="image-20220504185045124.png" /></p><p>此命令用来比较工作目录中当前文件和暂存区域快照之间的差异，也就是修改之后还没有暂存起来的变化内容。</p><p>若要查看已暂存的将要添加到下次提交里的内容，可以用如下命令，该条命令将比对已暂存的和最后一次提交的文件差异：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git diff --staged</span><br></pre></td></tr></table></figure><p><code>git diff</code>本身<strong>只显示尚未暂存的改动</strong>，而不是自上次提交以来所做的所有改动。</p><h4 id="提交更新">提交更新</h4><p>交互方式提交更新</p><p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git commit </span><br></pre></td></tr></table></figure></p><p><img data-src="image-20220504190419351.png" /></p><p>将提交信息和命令放在同一行</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git commit -m <span class="string">&quot;update progit学习笔记&quot;</span></span><br></pre></td></tr></table></figure><p><img data-src="image-20220504190442634.png" /></p><p>可以看到，当前是在那个分支（main）提交的，本次提交的完整的SHA-1校验和<code>5f66f52</code> ，有三个文件修订过，有35行添加并且有一行删改。</p><p>提交时记录的<strong>是放在暂存区的快照</strong>，任何还未暂存的文件仍然保持已修改状态，可以在下次提交的时候纳入版本管理。</p><p>每提交一次，都是对项目做一次快照，以后可以<strong>回到这个状态</strong>，或者进行<strong>比较</strong></p><h4 id="跳过使用暂存区">跳过使用暂存区</h4><p>自动把已经跟踪过的文件暂存起来一并提交</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git commit -m <span class="string">&quot;update message&quot;</span> -a</span><br></pre></td></tr></table></figure><h4 id="移除文件">移除文件</h4><p>从暂存区移除，并<strong>连带从工作目录中删除</strong>指定的文件</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git rm file</span><br></pre></td></tr></table></figure><p>从暂存区移除，<strong>但仍保留在当前目录</strong></p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git rm --cached file</span><br></pre></td></tr></table></figure><p><code>git rm</code>命令后面可以列出文件或者目录的名字，也可以使用glob模式。注意<code>*</code>号后面的反斜杠<code>\</code>.</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git rm <span class="built_in">log</span>/\*<span class="built_in">log</span></span><br></pre></td></tr></table></figure><p>以上命令将删除<code>log/</code> 目录下所有拓展名为<code>.log</code>的所有文件。</p><h4 id="移动文件">移动文件</h4><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git mv file_from file_to</span><br></pre></td></tr></table></figure><p>相当于运行了以下命令</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">mv file_from file_to</span><br><span class="line">git rm README.md</span><br><span class="line">git add README</span><br></pre></td></tr></table></figure><h3 id="查看提交历史">查看提交历史</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git <span class="built_in">log</span></span><br></pre></td></tr></table></figure><p><img data-src="image-20220504192609395.png" /></p><ul><li>在不传任何参数的默认情况下， <code>git log</code>会按照时间先后顺序列出所有的提交，最近的更新排在最上面。</li></ul><h4 id="按补丁的格式显示最近1次提交引入的差异">按补丁的格式显示<strong>最近1次</strong>提交引入的差异</h4><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git <span class="built_in">log</span> -p -1</span><br></pre></td></tr></table></figure><p><img data-src="image-20220504193112496.png" /></p><h4 id="查看每次提交的简略统计信息">查看每次提交的简略统计信息</h4><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git <span class="built_in">log</span> --<span class="built_in">stat</span></span><br></pre></td></tr></table></figure><p><img data-src="image-20220504193301800.png" /></p><ul><li><code>--stats</code>选项在每次提交的下面列出了所有被修改过的文件、有多少文件被修改了一集被修改文件的那些行被移除了或者添加了。</li></ul><h4 id="将commit消息显示为一行">将commit消息显示为一行</h4><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git <span class="built_in">log</span> --pretty=oneline</span><br></pre></td></tr></table></figure><p><img data-src="image-20220504193659106.png" /></p><p>也可以只显示sha1校验和的前几位</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git <span class="built_in">log</span> --oneline</span><br></pre></td></tr></table></figure><p><img data-src="image-20220504193756345.png" /></p><h4 id="定制格式显示提交历史">定制格式显示提交历史</h4><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git <span class="built_in">log</span> --pretty=format:<span class="string">&quot;%h - %an, %ar : %s&quot;</span></span><br></pre></td></tr></table></figure><p><img data-src="image-20220504194214332.png" /></p><p>以下是<code>--pretty</code>的常用选项</p><table><thead><tr class="header"><th style="text-align: left;">选项</th><th style="text-align: left;">说明</th></tr></thead><tbody><tr class="odd"><td style="text-align: left;"><code>%H</code></td><td style="text-align: left;">提交对象（commit）的完整哈希字串</td></tr><tr class="even"><td style="text-align: left;"><code>%h</code></td><td style="text-align: left;">提交对象的简短哈希字串</td></tr><tr class="odd"><td style="text-align: left;"><code>%T</code></td><td style="text-align: left;">树对象（tree）的完整哈希字串</td></tr><tr class="even"><td style="text-align: left;"><code>%t</code></td><td style="text-align: left;">树对象的简短哈希字串</td></tr><tr class="odd"><td style="text-align: left;"><code>%P</code></td><td style="text-align: left;">父对象（parent）的完整哈希字串</td></tr><tr class="even"><td style="text-align: left;"><code>%p</code></td><td style="text-align: left;">父对象的简短哈希字串</td></tr><tr class="odd"><td style="text-align: left;"><code>%an</code></td><td style="text-align: left;">作者（author）的名字</td></tr><tr class="even"><td style="text-align: left;"><code>%ae</code></td><td style="text-align: left;">作者的电子邮件地址</td></tr><tr class="odd"><td style="text-align: left;"><code>%ad</code></td><td style="text-align: left;">作者修订日期（可以用 --date= 选项定制格式）</td></tr><tr class="even"><td style="text-align: left;"><code>%ar</code></td><td style="text-align: left;">作者修订日期，按多久以前的方式显示</td></tr><tr class="odd"><td style="text-align: left;"><code>%cn</code></td><td style="text-align: left;">提交者（committer）的名字</td></tr><tr class="even"><td style="text-align: left;"><code>%ce</code></td><td style="text-align: left;">提交者的电子邮件地址</td></tr><tr class="odd"><td style="text-align: left;"><code>%cd</code></td><td style="text-align: left;">提交日期</td></tr><tr class="even"><td style="text-align: left;"><code>%cr</code></td><td style="text-align: left;">提交日期，按多久以前的方式显示</td></tr><tr class="odd"><td style="text-align: left;"><code>%s</code></td><td style="text-align: left;">提交说明</td></tr></tbody></table><ul><li><strong>作者</strong>是实际作出修改的人，<strong>提交者</strong>指的是最后将此工作成果提交到仓库的人</li></ul><p><code>online</code>或<code>format</code>与另一个选项<code>--graph</code>结合使用，可以清晰的展示你的分支、合并历史</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git <span class="built_in">log</span> --pretty=format:<span class="string">&quot;%h %s&quot;</span> --graph</span><br></pre></td></tr></table></figure><p><img data-src="image-20220504195321176.png" /></p><p>以下是<code>git log</code>的常用选项</p><table><colgroup><col style="width: 22%" /><col style="width: 77%" /></colgroup><thead><tr class="header"><th><code>-p</code></th><th>按补丁格式显示每个更新之间的差异。</th></tr></thead><tbody><tr class="odd"><td><code>--stat</code></td><td>显示每次更新的文件修改统计信息。</td></tr><tr class="even"><td><code>--shortstat</code></td><td>只显示 --stat 中最后的行数修改添加移除统计。</td></tr><tr class="odd"><td><code>--name-only</code></td><td>仅在提交信息后显示已修改的文件清单。</td></tr><tr class="even"><td><code>--name-status</code></td><td>显示新增、修改、删除的文件清单。</td></tr><tr class="odd"><td><code>--abbrev-commit</code></td><td>仅显示 SHA-1 的前几个字符，而非所有的 40 个字符。</td></tr><tr class="even"><td><code>--relative-date</code></td><td>使用较短的相对时间显示（比如，“2 weeks ago”）。</td></tr><tr class="odd"><td><code>--graph</code></td><td>显示 ASCII 图形表示的分支合并历史。</td></tr><tr class="even"><td><code>--pretty</code></td><td>使用其他格式显示历史提交信息。可用的选项包括 oneline，short，full，fuller 和 format（后跟指定格式）。</td></tr></tbody></table><h4 id="限制输出长度">限制输出长度</h4><p><code>-&lt;n&gt;</code> 选项的写法，其中的 <code>n</code> 可以是任何整数，表示仅显示最近的若干条提交。</p><h4 id="限制输出时间">限制输出时间</h4><p>列出所有<strong>最近两周内的</strong>提交</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git <span class="built_in">log</span> --since=2.weeks</span><br></pre></td></tr></table></figure><p>列出5天之前的提交</p><p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git <span class="built_in">log</span> --until 5.days</span><br></pre></td></tr></table></figure></p><p>时间可以是<strong>具体的某一天 <code>"2008-01-15"</code></strong>，也可以是<strong>相对时间</strong> <code>"2 years 1 day 3 minutes ago"</code>。</p><h4 id="筛选commit作者">筛选commit作者</h4><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git <span class="built_in">log</span> --author wf09</span><br></pre></td></tr></table></figure><h4 id="筛选commit关键字">筛选commit关键字</h4><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git <span class="built_in">log</span> --grep update</span><br></pre></td></tr></table></figure><h4 id="筛选添加或移除了某些字符串的提交">筛选添加或移除了某些字符串的提交</h4><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git <span class="built_in">log</span> -Supdate</span><br></pre></td></tr></table></figure><h4 id="筛选某些文件和目录的历史提交">筛选某些文件和目录的历史提交</h4><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git <span class="built_in">log</span> &lt;file_name&gt;</span><br></pre></td></tr></table></figure><p>以下是限制<code>git log</code>输出的选项</p><table><thead><tr class="header"><th style="text-align: left;">选项</th><th style="text-align: left;">说明</th></tr></thead><tbody><tr class="odd"><td style="text-align: left;"><code>-(n)</code></td><td style="text-align: left;">仅显示最近的 n 条提交</td></tr><tr class="even"><td style="text-align: left;"><code>--since</code>, <code>--after</code></td><td style="text-align: left;">仅显示指定时间之后的提交。</td></tr><tr class="odd"><td style="text-align: left;"><code>--until</code>, <code>--before</code></td><td style="text-align: left;">仅显示指定时间之前的提交。</td></tr><tr class="even"><td style="text-align: left;"><code>--author</code></td><td style="text-align: left;">仅显示指定作者相关的提交。</td></tr><tr class="odd"><td style="text-align: left;"><code>--committer</code></td><td style="text-align: left;">仅显示指定提交者相关的提交。</td></tr><tr class="even"><td style="text-align: left;"><code>--grep</code></td><td style="text-align: left;">仅显示含指定关键字的提交</td></tr><tr class="odd"><td style="text-align: left;"><code>-S</code></td><td style="text-align: left;">仅显示添加或移除了某个关键字的提交</td></tr></tbody></table><h3 id="撤销操作">撤销操作</h3><p>提交后发现忘记了暂存某些需要的修改，可以像下面这样操作</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">git commit -m <span class="string">&quot;commit message&quot;</span></span><br><span class="line">git add forgotten_file</span><br><span class="line">git commit --amend</span><br></pre></td></tr></table></figure><p>最终会只有一个提交，第二次提交的结果会代替第一次提交的结果。</p><h4 id="取消暂存的文件">取消暂存的文件</h4><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git reset HEAD [filename]</span><br></pre></td></tr></table></figure><p>忽略文件名就是取消所有暂存的文件</p><p><img data-src="image-20220504204117234.png" /></p><ul><li>不加选项的执行<code>git reset</code><strong>只会修改暂存区域</strong></li><li><code>--hard</code>选项<strong>会丢失工作区</strong>的更改！</li></ul><h4 id="撤销对文件的更改">撤销对文件的更改</h4><p>还原到上次提交的状态</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git checkout -- [filename]</span><br></pre></td></tr></table></figure><p>注意：<code>git checkout --</code>会<strong>完全撤销对当前文件的更改</strong>，和 <code>git reset --hard</code>选项类似</p>]]></content>
    
    
    <summary type="html">&lt;p&gt;本文是progit的学习记录。&lt;/p&gt;
&lt;p&gt;关键词：&lt;strong&gt;git&lt;/strong&gt;&lt;/p&gt;</summary>
    
    
    
    
    <category term="git" scheme="https://fly97.cn/tags/git/"/>
    
  </entry>
  
  <entry>
    <title>WebRTC学习笔记(一)|基于开源WebRTC，从0到1实现实时音视频聊天功能</title>
    <link href="https://fly97.cn/p/WebRTC-study-notes-01/"/>
    <id>https://fly97.cn/p/WebRTC-study-notes-01/</id>
    <published>2022-04-27T05:55:00.000Z</published>
    <updated>2022-04-27T05:55:00.000Z</updated>
    
    <content type="html"><![CDATA[<p>实习公司一直开展的项目突然停滞了，因为一个老板的需求我们一直无法满足：</p><ul><li>老板想用现有的WebRTC技术，我们使用了现有的技术，但是效果不够好；</li><li>使用别的公司的技术但是收费又太贵了，初创公司没有那么多钱；</li><li>初创公司没有技术leader，第一批员工大部分都是实习员工；</li></ul><p>别问我为啥不跑路，说多了都是泪。</p><p>借着这个机会，好好学习一下webRTC技术，同时学会怎么阅读golang库的源码。</p><a id="more"></a><h2 id="零基础入门基于开源webrtc从0到1实现实时音视频聊天功能">零基础入门：<strong>基于开源WebRTC，从0到1实现实时音视频聊天功能</strong></h2><h3 id="知识准备">知识准备</h3><h4 id="音视频理论基础">音视频理论基础</h4><h4 id="什么是webrtc">什么是WebRTC</h4><h4 id="webrtc学习资源">WebRTC学习资源</h4><h3 id="技术组成">技术组成</h3><p>简单来说，WebRTC 是一个可以在 Web 应用程序中实现音频，视频和数据的实时通信的开源项目。在实时通信中，音视频的采集和处理是一个很复杂的过程。比如音视频流的编解码、降噪和回声消除等，但是在 WebRTC 中，这一切都交由浏览器的底层封装来完成。我们可以直接拿到优化后的媒体流，然后将其输出到本地屏幕和扬声器，或者转发给其对等端。</p><figure><img data-src="WebRTC学习笔记(一)/webp.webp" alt="img" /><figcaption aria-hidden="true">img</figcaption></figure><ul><li>WebAPI层：面向开发者提供标准API（javascirpt），前端应用通过这一层接入使用WebRTC能力。</li><li>C++ API层：面向浏览器开发者，使浏览器制造商能够轻松地实现Web API方案。</li><li>音频引擎（VoiceEngine）：音频引擎是一系列音频多媒体处理的框架，包括从视频采集卡到网络传输端等整个解决方案。<ul><li>iSAC/iLBC/Opus等编解码</li><li>NetEQ语音信号处理</li><li>回声消除和降噪</li></ul></li><li>视频引擎（VideoEngine）： 是一系列视频处理的整体框架，从摄像头采集视频、视频信息网络传输到视频显示整个完整过程的解决方案。<ul><li>VP8编解码</li><li>jitter buffer：动态抖动缓冲</li><li>Image enhancements：图像增益</li></ul></li><li>传输（Transport)：传输 / 会话层，会话协商 + NAT穿透组件<ul><li>RTP 实时协议</li><li>P2P传输 STUN+TRUN+ICE实现的网络穿越</li></ul></li></ul><p>虽然浏览器给我们解决了大部分音视频处理问题，但是从浏览器请求音频和视频时，我们还是需要特别注意流的大小和质量。因为即便硬件能够捕获高清质量流，CPU 和带宽也不一定可以跟上，这也是我们在建立多个对等连接时，不得不考虑的问题。</p><h3 id="webrtc的p2p通信原理">WebRTC的P2P通信原理</h3><h4 id="技术难点">技术难点</h4><p>P2P即点对点通信。</p><p>要实现两个不同的网络环境（具有麦克风、摄像头设备）的客户端（可能是不同的Web浏览器或者手机APP）之间的音视频通信的问题：</p><ol type="1"><li>怎么知道彼此的存在也就是如何发现对方？</li><li>彼此音视频编码能力如何沟通？</li><li>音视频数据如何传输，怎么让对方看得到自己？</li></ol><h4 id="怎么知道彼此的存在">怎么知道彼此的存在</h4><p>WebRTC虽然支持端对端通信，但是这不意味着WebRTC不再需要服务器。</p><p>P2P通信过程中，双方需要交换一些元数据如媒体信息、网络数据等信息，通常把这一过程叫做信令（signaling）。</p><p>对应的服务器既为信令服务器，也被称为房间服务器。因为它不仅可以交换彼此的媒体信息和网络信息，也可以管理网络信息。</p><p>如：</p><ol type="1"><li>通知彼此who加入了房间</li><li>who离开了房间</li><li>告诉第三方房间人数是否已满可以加入房间。</li></ol><p>为了避免出现冗余，并最大限度提高与已有技术的兼容性，WebTRTC并没有规定信令和协议。在本文后面的实践章节会基于Node.js实现一个信令服务器。</p><h4 id="彼此音视频编码能力如何沟通">彼此音视频编码能力如何沟通</h4><p>不同浏览器对于音视频的编解码能力是不同的。为了保证双方都可以正确的编解码，最简单的方法就是使用<strong>客户端支持编码方式的交集</strong>。</p><p>在WebRTC中有一个专门的协议，<strong>Session Description Protocol（SDP）</strong>，可以用于描述上述这类信息。</p><p>参与音视频通讯的双方想要了解对方支持的媒体格式，必须交换SDP信息，而交换SDP的过程，通常被称为媒体协商。</p><h4 id="音视频数据如何传输怎么能让对方看见自己">音视频数据如何传输，怎么能让对方看见自己</h4><p>其问题的本质是网络协商的过程，即参与音视频实时通信的双方要了解彼此之间的网络状况，这样才能找到一条互相通讯的链路。</p><p>理想的网络情况是每个浏览器都有自己的公网IP地址，这样的话就可以直接进行点对点连接。</p><p>然而大部分情况是我们的设备基本上都是处于某个局域网内，需要<strong>Network Address Translation（NAT），网络地址转换</strong>才能和外部的ip地址通信。</p><p>什么是ICE？</p><p><strong>Interactive Connectivity Establishment（ICE），交互式连接建立</strong>。ICE不是一种协议，而是整合了STUN和TURN两种协议的框架。</p><p>其中：<strong>STUN（Session Traversal Utilities for NAT，NAT会话穿越应用程序）</strong>，它允许位于NAT或多重NAT后的客户端找出自己对应的公网IP地址和端口，也就是俗称的P2P打洞。</p><p>但是，如果NAT类型对称型的话，那么就无法打动成功。这是TURN就派上用场了。<strong>TURN（Traversal Using Replays around NAT）</strong>是STUN/RFC5389的一个扩展协议在其基础上添加了Replay（中继）功能。</p><p>简单说，其目的就是解决对称型NAT无法穿越的问题，在STUN分配公网失败以后，可以通过TURN服务器请求公网IP地址作为中继地址。</p><p>在WebRCT中有三种类型的ICE候选者，分别是：</p><ul><li>主机候选者：表示的是本地局域网内的IP地址和端口，他是三个候选者中优先级最高的，也就是WebRTC底层首先会尝试本地局域网建立连接。</li><li>反射候选者：表示的是获取NAT内主机的外网IP地址和端口，其优先级低于主机候选者。也就是说当WebRTC尝试本地连接不通时，会尝试通过反射候选者获得的IP地址和端口进行连接；</li><li>中级候选者：表示的是中继服务器的IP地址和端，即通过服务器中转媒体数据，当WebRTC客户端通信双方无法穿越P2P时，为了保证双方可以通讯，此时只能通过服务器中转来保证服务质量。</li></ul><p><img data-src="WebRTC学习笔记(一)/110413q4zdfkzfemkmmfhk.png" /></p><p>从上图可以看出：在非本地局域网内WebRTC通过STUN Server获得自己的外网IP和端口，然后通过信令服务器与远端的WebRTC交换网络信息，之后双方就可以尝试建立P2P连接了。当NAT穿越不成功时，则会通过Relay Server（TURN）中转。</p><p>值得一提的是：在WebRTC中网络信息通常用candidate来描述，而上图中的STUN server和Replay server也可以同一个server。本文使用继承了STUN（打洞）和TURN（中继）的开源项目coturn。</p><p>综上对三个问题的解释，我们可以用下图来说明WebRTC实现点对点的通信的基本原理。</p><p><img data-src="WebRTC学习笔记(一)/110424jt8q1v49mqki3128.png" /></p><p>简而言之：就是通过WebRTC提供的API获取各段的媒体信息SDP以及网络信息candidate，并通过信令服务器交换，进而建立了两端的连接通道完成实时视频语音通话。</p><h3 id="webrtc几个重要api">WebRTC几个重要API</h3><h4 id="音视频采集api">音视频采集API</h4><p>音视频采集 API，即 MediaDevices.<a href="https://developer.mozilla.org/zh-CN/docs/Web/API/MediaDevices/getDisplayMedia">getUserMedia()</a>。</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> constraints = &#123;</span><br><span class="line">        video: <span class="literal">true</span>,</span><br><span class="line">        audio: <span class="literal">true</span></span><br><span class="line">     </span><br><span class="line">&#125;;</span><br><span class="line"><span class="comment">//   非安全模式（非https/localhost）下 navigator.mediaDevices 会返回 undefined</span></span><br><span class="line"><span class="keyword">try</span> &#123;</span><br><span class="line">        <span class="keyword">const</span> stream = <span class="keyword">await</span> navigator.mediaDevices.getUserMedia(constraints);</span><br><span class="line">        <span class="built_in">document</span>.querySelector(<span class="string">&#x27;video&#x27;</span>).srcObject = stream;</span><br><span class="line">    &#125;   <span class="keyword">catch</span> (error) &#123;</span><br><span class="line">        <span class="built_in">console</span>.error(error);</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure><h4 id="获取音视频设备输入输出列表">获取音视频设备输入输出列表</h4><p>获取音视频设备输入输出列表API，即 MediaDevices.<a href="https://developer.mozilla.org/zh-CN/docs/Web/API/MediaDevices/enumerateDevices">enumerateDevices()</a>。</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">try</span> &#123;</span><br><span class="line">        <span class="keyword">const</span> devices = <span class="keyword">await</span> navigator.mediaDevices.enumerateDevices();</span><br><span class="line">        <span class="built_in">this</span>.videoinputs = devices.filter(<span class="function"><span class="params">device</span> =&gt;</span> device.kind === <span class="string">&#x27;videoinput&#x27;</span>);</span><br><span class="line">        <span class="built_in">this</span>.audiooutputs = devices.filter(<span class="function"><span class="params">device</span> =&gt;</span> device.kind === <span class="string">&#x27;audiooutput&#x27;</span>);</span><br><span class="line">        <span class="built_in">this</span>.audioinputs = devices.filter(<span class="function"><span class="params">device</span> =&gt;</span> device.kind === <span class="string">&#x27;audioinput&#x27;</span>);</span><br><span class="line">      &#125; <span class="keyword">catch</span> (error) &#123;</span><br><span class="line">        <span class="built_in">console</span>.error(error);</span><br><span class="line">      &#125;</span><br></pre></td></tr></table></figure><h4 id="创建点对点连接">创建点对点连接</h4><p><a href="https://developer.mozilla.org/zh-CN/docs/Web/API/RTCPeerConnection">RTCPeerConnection</a> 作为创建点对点连接的 API,是我们实现音视频实时通信的关键。</p><p>在本文的实践章节中，主要运用到了以下方法。</p><p><strong>媒体协商方法：</strong></p><ul><li><a href="https://developer.mozilla.org/zh-CN/docs/Web/API/RTCPeerConnection/createOffer">createOffer</a></li><li><a href="https://developer.mozilla.org/en-US/docs/Web/API/RTCPeerConnection/createAnswer">createAnswer</a></li><li><a href="https://developer.mozilla.org/en-US/docs/Web/API/RTCPeerConnection/localDescription">localDesccription</a></li><li><a href="https://developer.mozilla.org/zh-CN/docs/Web/API/RTCPeerConnection/remoteDescription">remoteDesccription</a></li></ul><p><strong>重要事件：</strong></p><ul><li><a href="https://developer.mozilla.org/zh-CN/docs/Web/API/RTCPeerConnection/onicecandidate">onicecandidate</a></li><li><a href="https://developer.mozilla.org/zh-CN/docs/Web/API/RTCPeerConnection/onaddstream">onaddstream</a></li></ul><p>P2P通信中一个最重要的环节就是交换媒体信息。</p><p>原理图：</p><p><img data-src="WebRTC学习笔记(一)/115037opmgs8r3s5c5zycp.png" /></p><p>从上图不难发现，整个媒体协过程也可以简化为三个步骤对应上述四个媒体协商方法。</p><ul><li>呼叫端Amy<strong>创建Offer（createOffer）</strong>并将offer消息（内容是呼叫段Amy的SDP信息）通过信令服务器传送给接收端Bob，同时调用<strong>setLocalDescription</strong>将含有本地SDP的Offer保存起来；</li><li>接收端Bob收到对端的Offer信息调用<strong>setRemoteDescription</strong>方法将含有对端SDP信息的Offer保存起来，并<strong>创建Answer（createAnswer）</strong>并将Answer消息（内容是接受端Bob的SDP消息）通过信令服务器传送给呼叫端Amy；</li><li>呼叫端Amy收到对端的Answer消息后调用<strong>setRemoteDescription</strong>方法将对端SDP信息的Answer保存起来。</li></ul><p>经过上述三个步骤，则完成了P2P通信过程中的媒体协商部分。</p><p>实际上：在呼叫端以及接收端调用<strong>setLocalDescription</strong>他同时也开始了收集各端自己的<strong>网络信息（candidate）</strong>，然后各端<strong>通过监听事件（onicecandidate）</strong>收集到了各自的<strong>candidate</strong>并通过信令服务器传输给对端，进而打通了P2P的网络通道，并通过<strong>监听onaddstream事件</strong>拿到了对方的视频流进而完成了整个视频通话过程。</p><h3 id="动手编码实践">动手编码实践</h3><p>代码操作的部分可以可参考下文中的原文链接</p><h2 id="参考">参考</h2><p>http://www.52im.net/thread-3680-1-1.html#2</p>]]></content>
    
    
    <summary type="html">&lt;p&gt;实习公司一直开展的项目突然停滞了，因为一个老板的需求我们一直无法满足：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;老板想用现有的WebRTC技术，我们使用了现有的技术，但是效果不够好；&lt;/li&gt;
&lt;li&gt;使用别的公司的技术但是收费又太贵了，初创公司没有那么多钱；&lt;/li&gt;
&lt;li&gt;初创公司没有技术leader，第一批员工大部分都是实习员工；&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;别问我为啥不跑路，说多了都是泪。&lt;/p&gt;
&lt;p&gt;借着这个机会，好好学习一下webRTC技术，同时学会怎么阅读golang库的源码。&lt;/p&gt;</summary>
    
    
    
    
    <category term="WebRTC" scheme="https://fly97.cn/tags/WebRTC/"/>
    
  </entry>
  
  <entry>
    <title>kafaka学习笔记之快速入门</title>
    <link href="https://fly97.cn/p/introduction-to-kafaka-study-notes-02/"/>
    <id>https://fly97.cn/p/introduction-to-kafaka-study-notes-02/</id>
    <published>2022-04-19T02:03:00.000Z</published>
    <updated>2022-04-19T02:03:00.000Z</updated>
    
    <content type="html"><![CDATA[<p>业务需要，最近在看尚硅谷的教程，本着好记性不如烂笔头的原则，有了这一系列的文章。</p><p>关键词：<strong>kafaka</strong></p><a id="more"></a><h2 id="安装部署">安装部署</h2><h3 id="docker安装">docker安装</h3><p>这里使用docker的安装方式，这里贴出使用的镜像地址：https://hub.docker.com/r/bitnami/kafka/</p><p>docker安装方式十分简单，这里贴出使用的docker-compose.yml文件：</p><figure class="highlight yml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">version:</span> <span class="string">&quot;3&quot;</span></span><br><span class="line"><span class="attr">services:</span></span><br><span class="line">  <span class="attr">zookeeper:</span></span><br><span class="line">    <span class="attr">image:</span> <span class="string">&#x27;bitnami/zookeeper:latest&#x27;</span></span><br><span class="line">    <span class="attr">ports:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">&#x27;2181:2181&#x27;</span></span><br><span class="line">    <span class="attr">environment:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">ALLOW_ANONYMOUS_LOGIN=yes</span></span><br><span class="line">  <span class="attr">kafka:</span></span><br><span class="line">    <span class="attr">image:</span> <span class="string">&#x27;bitnami/kafka:latest&#x27;</span></span><br><span class="line">    <span class="attr">ports:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">&#x27;9092:9092&#x27;</span></span><br><span class="line">    <span class="attr">environment:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">KAFKA_BROKER_ID=1</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">KAFKA_CFG_LISTENERS=PLAINTEXT://:9092</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">KAFKA_CFG_ADVERTISED_LISTENERS=PLAINTEXT://127.0.0.1:9092</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">KAFKA_CFG_ZOOKEEPER_CONNECT=zookeeper:2181/kafka</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">ALLOW_PLAINTEXT_LISTENER=yes</span></span><br><span class="line">    <span class="attr">depends_on:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">zookeeper</span></span><br></pre></td></tr></table></figure><p>有几点需要注意：</p><ul><li><p><code>KAFKA_BROKER_ID</code>：设置<code>broker.id</code>，每一个broker必须设置不同的值，否则会启动失败；</p></li><li><p><code>KAFKA_CFG_LISTENERS</code>：设置kafka服务端监听的网络地址，这里是默认值0.0.0.0；</p></li><li><p><code>KAFKA_CFG_ADVERTISED_LISTENERS</code>：向外通告kafka实例的网络地址，一般使用宿主机的局域网地址或者公网地址；</p></li><li><p><code>KAFKA_CFG_ZOOKEEPER_CONNECT</code>：设置zookeeper的地址，这里最好是加上<code>/kafka</code>后缀，不然导致zookeeper的一级目录看起来混乱；</p></li></ul><h3 id="配置文件">配置文件</h3><p>可以通过环境变量快速设置几个关键选项，其他选项可以通过挂载配置文件进行修改：</p><ul><li>环境变量设置</li></ul><blockquote><p>The configuration can easily be setup with the Bitnami Apache Kafka Docker image using the following environment variables:</p><ul><li><code>ALLOW_PLAINTEXT_LISTENER</code>: Allow to use the PLAINTEXT listener. Default: <strong>no</strong>.</li><li><code>KAFKA_INTER_BROKER_USER</code>: Apache Kafka inter broker communication user. Default: admin. Default: <strong>user</strong>.</li><li><code>KAFKA_INTER_BROKER_PASSWORD</code>: Apache Kafka inter broker communication password. Default: <strong>bitnami</strong>.</li><li><code>KAFKA_CERTIFICATE_PASSWORD</code>: Password for certificates. No defaults.</li><li><code>KAFKA_HEAP_OPTS</code>: Apache Kafka's Java Heap size. Default: <strong>-Xmx1024m -Xms1024m</strong>.</li><li><code>KAFKA_ZOOKEEPER_PROTOCOL</code>: Authentication protocol for Zookeeper connections. Allowed protocols: <strong>PLAINTEXT</strong>, <strong>SASL</strong>, <strong>SSL</strong>, and <strong>SASL_SSL</strong>. Defaults: <strong>PLAINTEXT</strong>.</li><li><code>KAFKA_ZOOKEEPER_USER</code>: Apache Kafka Zookeeper user for SASL authentication. No defaults.</li><li><code>KAFKA_ZOOKEEPER_PASSWORD</code>: Apache Kafka Zookeeper user password for SASL authentication. No defaults.</li><li><code>KAFKA_ZOOKEEPER_TLS_KEYSTORE_PASSWORD</code>: Apache Kafka Zookeeper keystore file password and key password. No defaults.</li><li><code>KAFKA_ZOOKEEPER_TLS_TRUSTSTORE_PASSWORD</code>: Apache Kafka Zookeeper truststore file password. No defaults.</li><li><code>KAFKA_ZOOKEEPER_TLS_VERIFY_HOSTNAME</code>: Verify Zookeeper hostname on TLS certificates. Defaults: <strong>true</strong>.</li><li><code>KAFKA_ZOOKEEPER_TLS_TYPE</code>: Choose the TLS certificate format to use. Allowed values: <code>JKS</code>, <code>PEM</code>. Defaults: <strong>JKS</strong>.</li><li><code>KAFKA_CFG_SASL_ENABLED_MECHANISMS</code>: Allowed mechanism when using SASL either for clients, inter broker, or zookeeper comunications. Allowed values: <code>PLAIN</code>, <code>SCRAM-SHA-256</code>, <code>SCRAM-SHA-512</code> or a comma separated combination of those values. Default: <strong>PLAIN,SCRAM-SHA-256,SCRAM-SHA-512</strong></li><li><code>KAFKA_CFG_SASL_MECHANISM_INTER_BROKER_PROTOCOL</code>: SASL mechanism to use for inter broker communications. No defaults.</li><li><code>KAFKA_TLS_CLIENT_AUTH</code>: Configures kafka brokers to request client authentication. Allowed values: <code>required</code>, <code>requested</code>, <code>none</code>. Defaults: <strong>required</strong>.</li><li><code>KAFKA_TLS_TYPE</code>: Choose the TLS certificate format to use. Allowed values: <code>JKS</code>, <code>PEM</code>. Defaults: <strong>JKS</strong>.</li><li><code>KAFKA_CLIENT_USERS</code>: Users that will be created into Zookeeper when using SASL for client communications. Separated by commas. Default: <strong>user</strong></li><li><code>KAFKA_CLIENT_PASSWORDS</code>: Passwords for the users specified at<code>KAFKA_CLIENT_USERS</code>. Separated by commas. Default: <strong>bitnami</strong></li><li><code>KAFKA_CFG_MAX_PARTITION_FETCH_BYTES</code>: The maximum amount of data per-partition the server will return. Default: 1048576</li><li><code>KAFKA_CFG_MAX_REQUEST_SIZE</code>: The maximum size of a request in bytes. Default: 1048576</li></ul><p>Additionally, any environment variable beginning with <code>KAFKA_CFG_</code> will be mapped to its corresponding Apache Kafka key. For example, use <code>KAFKA_CFG_BACKGROUND_THREADS</code> in order to set <code>background.threads</code> or <code>KAFKA_CFG_AUTO_CREATE_TOPICS_ENABLE</code> in order to configure <code>auto.create.topics.enable</code>.</p></blockquote><ul><li>挂载配置文件</li></ul><figure class="highlight yml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="string">...</span></span><br><span class="line"><span class="attr">services:</span></span><br><span class="line">  <span class="attr">kafka:</span></span><br><span class="line">    <span class="string">...</span></span><br><span class="line">    <span class="attr">volumes:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">&#x27;kafka_data:/bitnami&#x27;</span></span><br><span class="line"><span class="string">+</span>     <span class="bullet">-</span> <span class="string">/path/to/server.properties:/bitnami/kafka/config/server.properties</span></span><br></pre></td></tr></table></figure><p>一些推荐配置</p><figure class="highlight ini"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">#broker 的全局唯一编号，不能重复，只能是数字。 </span></span><br><span class="line"><span class="attr">broker.id</span>=<span class="number">0</span></span><br><span class="line"><span class="comment">#处理网络请求的线程数量</span></span><br><span class="line"><span class="attr">num.network.threads</span>=<span class="number">3</span></span><br><span class="line"><span class="comment">#用来处理磁盘 IO 的线程数量</span></span><br><span class="line"><span class="attr">num.io.threads</span>=<span class="number">8</span> <span class="comment">#发送套接字的缓冲区大小</span></span><br><span class="line"><span class="attr">socket.send.buffer.bytes</span>=<span class="number">102400</span></span><br><span class="line"><span class="comment">#接收套接字的缓冲区大小 </span></span><br><span class="line"><span class="attr">socket.receive.buffer.bytes</span>=<span class="number">102400</span></span><br><span class="line"><span class="comment">#请求套接字的缓冲区大小</span></span><br><span class="line"><span class="attr">socket.request.max.bytes</span>=<span class="number">104857600</span></span><br><span class="line"><span class="comment">#kafka 运行日志(数据)存放的路径，路径不需要提前创建，kafka 自动帮你创建，可以 配置多个磁盘路径，路径与路径之间可以用&quot;，&quot;分隔 </span></span><br><span class="line"><span class="attr">log.dirs</span>=/opt/module/kafka/datas</span><br><span class="line"><span class="comment">#topic 在当前 broker 上的分区个数</span></span><br><span class="line"><span class="attr">num.partitions</span>=<span class="number">1</span></span><br><span class="line"><span class="comment">#用来恢复和清理 data 下数据的线程数量 </span></span><br><span class="line"><span class="attr">num.recovery.threads.per.data.dir</span>=<span class="number">1</span></span><br><span class="line"><span class="comment"># 每个topic创建时的副本数，默认时1个副本 </span></span><br><span class="line"><span class="attr">offsets.topic.replication.factor</span>=<span class="number">1</span></span><br><span class="line"><span class="comment">#segment 文件保留的最长时间，超时将被删除</span></span><br><span class="line"><span class="attr">log.retention.hours</span>=<span class="number">168</span></span><br><span class="line"><span class="comment">#每个 segment 文件的大小，默认最大 1G </span></span><br><span class="line"><span class="attr">log.segment.bytes</span>=<span class="number">1073741824</span></span><br><span class="line"><span class="comment"># 检查过期数据的时间，默认5分钟检查一次是否数据过期 </span></span><br><span class="line"><span class="attr">log.retention.check.interval.ms</span>=<span class="number">300000</span></span><br><span class="line"><span class="comment">#配置连接 Zookeeper 集群地址(在 zk 根目录下创建/kafka，方便管理)</span></span><br><span class="line"><span class="attr">zookeeper.connect</span>=hadoop102:<span class="number">2181</span>,hadoop103:<span class="number">2181</span>,hadoop104:<span class="number">2181</span>/kafka</span><br></pre></td></tr></table></figure><h2 id="kafka命令行操作">kafka命令行操作</h2><p><img data-src="image-20220419214422212.png" /></p><h3 id="主题命令行参数">主题命令行参数</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">bin/kafka-topics.sh</span><br></pre></td></tr></table></figure><table><thead><tr class="header"><th>参数</th><th>描述</th></tr></thead><tbody><tr class="odd"><td>--bootstrap-server &lt;String: server toconnect to&gt;</td><td>链接kafka broker主机名称</td></tr><tr class="even"><td>--topic &lt;String: topic&gt;</td><td>操作的 topic 名称</td></tr><tr class="odd"><td>--create</td><td>创建主题</td></tr><tr class="even"><td>--alter</td><td>修改主题</td></tr><tr class="odd"><td>--delete</td><td>删除主题</td></tr><tr class="even"><td>--list</td><td>查看所有主题</td></tr><tr class="odd"><td>--describe</td><td>查看主题详细描述</td></tr><tr class="even"><td>--partitions &lt;Integer: # of partitions&gt;</td><td>设置分区数</td></tr><tr class="odd"><td>--replication-factor&lt;Integer: replication factor&gt;</td><td>设置分区副本</td></tr><tr class="even"><td>--config &lt;String: name=value&gt;</td><td>更新系统默认的配置</td></tr></tbody></table><h3 id="主题命令行相关操作">主题命令行相关操作</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 查看当前服务器所有topic</span></span><br><span class="line">bin/kafka-topics.sh --bootstrap-server hadoop102:9092 --list</span><br><span class="line"><span class="comment"># 指定topic名、定义副本数、分区数，创建topic</span></span><br><span class="line">bin/kafka-topics.sh --bootstrap-server hadoop102:9092 --create --partitions 1 --replication-factor 3 --topic first</span><br><span class="line"><span class="comment"># 查看first主题的详情</span></span><br><span class="line">bin/kafka-topics.sh --bootstrap-server hadoop102:9092 --describe --topic first</span><br><span class="line"><span class="comment"># 修改分区数（分区数只能增加不能减少）</span></span><br><span class="line">bin/kafka-topics.sh --bootstrap-server hadoop102:9092 --alter --topic first --partitions 3</span><br><span class="line"><span class="comment"># 删除主题（需要将server.properties 中设置 delete.topic.enable=true，否则只是标记删除）</span></span><br><span class="line">bin/kafka-topics.sh --bootstrap-server hadoop102:9092 --delete --topic first</span><br></pre></td></tr></table></figure><h2 id="生产者命令行操作">生产者命令行操作</h2><h3 id="生产者命令行参数">生产者命令行参数</h3><table><colgroup><col style="width: 57%" /><col style="width: 42%" /></colgroup><thead><tr class="header"><th>参数</th><th>描述</th></tr></thead><tbody><tr class="odd"><td>--bootstrap-server &lt;String: server toconnect to&gt;</td><td>连接的 Kafka Broker 主机名称和端口号</td></tr><tr class="even"><td>--topic &lt;String: topic&gt;</td><td>操作的 topic 名称</td></tr></tbody></table><h3 id="发送消息">发送消息</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">bin/kafka-console-producer.sh --bootstrap-server hadoop102:9092 --topic first</span><br></pre></td></tr></table></figure><h2 id="消费者命令行参操作">消费者命令行参操作</h2><h3 id="消费者命令行参数">消费者命令行参数</h3><table><thead><tr class="header"><th>参数</th><th>描述</th></tr></thead><tbody><tr class="odd"><td>--bootstrap-server &lt;String: server toconnect to&gt;</td><td>链接的kafka broker主机名和短端口号</td></tr><tr class="even"><td>--topic</td><td>操作的topic名称</td></tr><tr class="odd"><td>--from-beginning</td><td>从头开始消费</td></tr><tr class="even"><td>--group &lt;String: consumer group id&gt;</td><td>指定消费者组名称</td></tr></tbody></table><h3 id="消费消息">消费消息</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">bin/kafka-console-consumer.sh --bootstrap-server hadoop102:9092 --topic first</span><br><span class="line"><span class="comment"># 从主题的开始消费消息</span></span><br><span class="line">bin/kafka-console-consumer.sh --bootstrap-server hadoop102:9092 --from-beginning --topic first</span><br></pre></td></tr></table></figure>]]></content>
    
    
    <summary type="html">&lt;p&gt;业务需要，最近在看尚硅谷的教程，本着好记性不如烂笔头的原则，有了这一系列的文章。&lt;/p&gt;
&lt;p&gt;关键词：&lt;strong&gt;kafaka&lt;/strong&gt;&lt;/p&gt;</summary>
    
    
    
    
    <category term="kafaka" scheme="https://fly97.cn/tags/kafaka/"/>
    
  </entry>
  
  <entry>
    <title>kafaka学习笔记之绪论</title>
    <link href="https://fly97.cn/p/introduction-to-kafaka-study-notes/"/>
    <id>https://fly97.cn/p/introduction-to-kafaka-study-notes/</id>
    <published>2022-04-18T02:03:00.000Z</published>
    <updated>2022-04-18T02:03:00.000Z</updated>
    
    <content type="html"><![CDATA[<p>业务需要，最近在看尚硅谷的教程，本着好记性不如烂笔头的原则，有了这一系列的文章。</p><p>关键词：<strong>kafaka</strong></p><a id="more"></a><h2 id="定义">定义</h2><h3 id="传统定义">传统定义</h3><p>kafka是一个<strong>分布式</strong>的基于<strong>发布/订阅模式</strong>的消息队列（Message Queue），主要应用于大数据实时处理。</p><h4 id="发布订阅">发布/订阅</h4><p>消息的发布者不会将数据直接发送给特定的订阅者，而是<strong>将发布的消息分为不同的类别</strong>，订阅者<strong>只接受感兴趣的消息</strong>。</p><h3 id="kafka最新定义">kafka最新定义</h3><p>kafka是一个<strong>开源的分布式事件流平台</strong>，用于高性能数据管道，流分析、数据集成、和关键任务应用。</p><h2 id="应用场景">应用场景</h2><h3 id="流量缓冲削峰">流量缓冲、削峰</h3><ul><li>有助于控制和优化数据流经过系统的速度，解决生产消息和消费消息处理速度不一致的情况。</li></ul><p><img data-src="image-20220419165347641.png" /></p><h3 id="解耦">解耦</h3><ul><li>允许独立的扩展和修改两边的处理过程，只需确保他们遵守同样的接口约束。</li></ul><p><img data-src="image-20220419165511301.png" /></p><h3 id="异步通信">异步通信</h3><ul><li>允许用户把一个消息放入队列，但是不立刻处理它，需要的时候再去处理。</li></ul><p><img data-src="image-20220419165706773.png" /></p><h2 id="消息队列的两种方式">消息队列的两种方式</h2><h3 id="点对点方式">点对点方式</h3><ul><li>消费者主动拉取数据，消息收到后清除消息</li></ul><p><img data-src="image-20220419165858779.png" /></p><h3 id="发布订阅方式">发布订阅方式</h3><ul><li>可以有多个topic主题（浏览、点赞、收藏、评论）</li><li>消费者消费以后不删除数据</li><li>每个消费者相互独立，都可以消费到数据</li></ul><p><img data-src="image-20220419170024390.png" /></p><h2 id="基础架构">基础架构</h2><p><img data-src="image-20220419170301160.png" /></p><ol type="1"><li>producer：消息生产者，就是向kafka broker发消息的客户端。</li><li>consumer：消息消费者，就是向kafka broker取消息的客户端。</li><li>consumer group（CG）：消费者组，多个consumer组成。<strong>消费者组内每个消费者负责消费不同分区的数据，一个分区只能由一个组内消费者消费；消费者组之间互不影响</strong>。所有消费者都属于某个消费者组。<strong>消费者组属于逻辑上的一个订阅者。</strong></li><li>broker：一台kafka服务器就是一个broker，一个集群由多个broker组成。一个broker可以容纳多个topic。</li><li>topic：生产者和消费者逻辑上操作的对象，即生产者向某个topic写数据，消费者从从某个topic读取数据。</li><li>partition：每个topic有多个partition，可以提高broker的负载（生产者可以向同一个topic发送多条数据，数据可以存储在不同的partition）。每个topic在每个partition上的数据是不重复的。partition表现为一个个的文件夹。</li><li>replica：副本。一个topic的每个partition都有若干个副本，副本分为leader和follower，leader是生产者和消费者物理上操作的对象，follower主动从leader拉取更新的内容。当某个分区的leader挂掉后，通过某种算法，将某一个follower升级为leader。</li></ol>]]></content>
    
    
    <summary type="html">&lt;p&gt;业务需要，最近在看尚硅谷的教程，本着好记性不如烂笔头的原则，有了这一系列的文章。&lt;/p&gt;
&lt;p&gt;关键词：&lt;strong&gt;kafaka&lt;/strong&gt;&lt;/p&gt;</summary>
    
    
    
    
    <category term="kafaka" scheme="https://fly97.cn/tags/kafaka/"/>
    
  </entry>
  
</feed>
