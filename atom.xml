<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>个人随想</title>
  
  <subtitle>fly97&#39;s Blogs</subtitle>
  <link href="https://fly97.cn/atom.xml" rel="self"/>
  
  <link href="https://fly97.cn/"/>
  <updated>2022-01-27T07:12:00.000Z</updated>
  <id>https://fly97.cn/</id>
  
  <author>
    <name>fly97</name>
    
  </author>
  
  <generator uri="https://hexo.io/">Hexo</generator>
  
  <entry>
    <title>golang实现简易的tcp客户端和服务端</title>
    <link href="https://fly97.cn/p/socket-in-golang/"/>
    <id>https://fly97.cn/p/socket-in-golang/</id>
    <published>2022-01-27T07:12:00.000Z</published>
    <updated>2022-01-27T07:12:00.000Z</updated>
    
    <content type="html"><![CDATA[<p>在计算机通信领域，socket 被翻译为“套接字”，它是计算机之间进行<strong>通信</strong>的<strong>一种约定</strong>或一种方式。通过 socket 这种约定，一台计算机可以接收其他计算机的数据，也可以向其他计算机发送数据。</p><p>本文介绍如何使用golang实现socket通信。</p><a id="more"></a><h2 id="tcp">TCP</h2><h3 id="tcp服务端">TCP服务端</h3><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">package</span> main</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> (</span><br><span class="line"><span class="string">&quot;bytes&quot;</span></span><br><span class="line"><span class="string">&quot;fmt&quot;</span></span><br><span class="line"><span class="string">&quot;net&quot;</span></span><br><span class="line">)</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">listener, err := net.Listen(<span class="string">&quot;tcp&quot;</span>, <span class="string">&quot;127.0.0.1:7890&quot;</span>)</span><br><span class="line"><span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line"><span class="built_in">panic</span>(err)</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">defer</span> listener.Close()</span><br><span class="line"><span class="keyword">for</span> &#123;</span><br><span class="line">conn, err := listener.Accept()</span><br><span class="line"><span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line"><span class="keyword">return</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 启用并发</span></span><br><span class="line"><span class="keyword">go</span> <span class="function"><span class="keyword">func</span><span class="params">(conn net.Conn)</span></span> &#123;</span><br><span class="line"><span class="keyword">var</span> buffer bytes.Buffer</span><br><span class="line"><span class="keyword">for</span> &#123;</span><br><span class="line"><span class="keyword">var</span> buffarr = <span class="built_in">make</span>([]<span class="keyword">byte</span>, <span class="number">1</span>)</span><br><span class="line">_, err := conn.Read(buffarr)</span><br><span class="line"><span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line"><span class="built_in">panic</span>(err)</span><br><span class="line">&#125;</span><br><span class="line">b := buffarr[<span class="number">0</span>]</span><br><span class="line"><span class="comment">// 检测到消息终止符</span></span><br><span class="line"><span class="keyword">if</span> b == <span class="string">&#x27;\t&#x27;</span> &#123;</span><br><span class="line"><span class="keyword">break</span></span><br><span class="line">&#125;</span><br><span class="line">buffer.WriteByte(b)</span><br><span class="line"></span><br><span class="line">&#125;</span><br><span class="line">fmt.Println(<span class="string">&quot;get msg from client:&quot;</span>, buffer.String())</span><br><span class="line"></span><br><span class="line">&#125;(conn)</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="tcp客户端">TCP客户端</h3><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">package</span> main</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> (</span><br><span class="line"><span class="string">&quot;bytes&quot;</span></span><br><span class="line"><span class="string">&quot;net&quot;</span></span><br><span class="line">)</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">conn, err := net.Dial(<span class="string">&quot;tcp&quot;</span>, <span class="string">&quot;127.0.0.1:7890&quot;</span>)</span><br><span class="line"><span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line"><span class="built_in">panic</span>(err)</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">var</span> buffer bytes.Buffer</span><br><span class="line">buffer.WriteString(<span class="string">&quot;Hi, I am a client&quot;</span>)</span><br><span class="line"><span class="comment">// 终止符</span></span><br><span class="line">buffer.WriteByte(<span class="string">&#x27;\t&#x27;</span>)</span><br><span class="line">_, err = conn.Write(buffer.Bytes())</span><br><span class="line"><span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line"><span class="built_in">panic</span>(err)</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="udp">UDP</h2><h3 id="udp服务端">UDP服务端</h3><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">package</span> main</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> (</span><br><span class="line"><span class="string">&quot;fmt&quot;</span></span><br><span class="line"><span class="string">&quot;net&quot;</span></span><br><span class="line">)</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">conn, err := net.ListenUDP(</span><br><span class="line"><span class="string">&quot;udp&quot;</span>, &amp;net.UDPAddr&#123;</span><br><span class="line">IP:   net.ParseIP(<span class="string">&quot;127.0.0.1&quot;</span>),</span><br><span class="line">Port: <span class="number">7890</span>,</span><br><span class="line">&#125;,</span><br><span class="line">)</span><br><span class="line"><span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line"><span class="built_in">panic</span>(err)</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">defer</span> conn.Close()</span><br><span class="line">bytearray := <span class="built_in">make</span>([]<span class="keyword">byte</span>, <span class="number">1024</span>)</span><br><span class="line"><span class="keyword">for</span> &#123;</span><br><span class="line">n, _, err := conn.ReadFrom(bytearray)</span><br><span class="line"><span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line"><span class="built_in">panic</span>(err)</span><br><span class="line">&#125;</span><br><span class="line">fmt.Println(<span class="string">&quot;get msg from client:&quot;</span>, <span class="keyword">string</span>(bytearray[:n]))</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="udp客户端">UDP客户端</h3><p>```go package main</p><p>import ( "bytes" "net" )</p><p>func main() { conn, err := net.Dial("udp", "127.0.0.1:7890") if err != nil { panic(err) } defer conn.Close() var bytebuffer bytes.Buffer bytebuffer.WriteString("hello, I am a udp client") _, err = conn.Write(bytebuffer.Bytes()) if err != nil { panic(err) }<br />}</p>]]></content>
    
    
    <summary type="html">&lt;p&gt;在计算机通信领域，socket 被翻译为“套接字”，它是计算机之间进行&lt;strong&gt;通信&lt;/strong&gt;的&lt;strong&gt;一种约定&lt;/strong&gt;或一种方式。通过 socket 这种约定，一台计算机可以接收其他计算机的数据，也可以向其他计算机发送数据。&lt;/p&gt;
&lt;p&gt;本文介绍如何使用golang实现socket通信。&lt;/p&gt;</summary>
    
    
    
    
    <category term="golang" scheme="https://fly97.cn/tags/golang/"/>
    
    <category term="socket" scheme="https://fly97.cn/tags/socket/"/>
    
  </entry>
  
  <entry>
    <title>Docker安装MySQL5.7</title>
    <link href="https://fly97.cn/p/install-mysql5.7-in-docker/"/>
    <id>https://fly97.cn/p/install-mysql5.7-in-docker/</id>
    <published>2022-01-16T10:17:00.000Z</published>
    <updated>2022-01-16T10:17:00.000Z</updated>
    
    <content type="html"><![CDATA[<p>为什么要在docker中安装mysql？</p><a id="more"></a><p>在docker中安装mysql可以随时启动另外的mysql服务端用于测试或者其他用途，缺点就是比较消耗资源，不过资源随时都能申请得到，毕竟世界加钱可及。</p><h3 id="安装docker">安装docker</h3><p>可以参考我的这一篇文章：<a href="https://fly97.cn/p/docker-tutorial-01/">初探docker(一) | 个人随想 (fly97.cn)</a></p><h3 id="安装mysql容器">安装MySQL容器</h3><p>以官方的MySQL5.7为例：<a href="https://hub.docker.com/_/mysql">Mysql - Official Image | Docker Hub</a></p><p>首先确保docker服务已经正常启动：</p><p>linux执行：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">sudo service docker start</span><br></pre></td></tr></table></figure><p>笔者的系统是mac，因此打开<strong>Docker Destop</strong>即可。</p><h4 id="拉取镜像">拉取镜像</h4><p>拉取镜像前需要使用<code>docker login</code>登陆docker hub，这里以MySQL5.7为例：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">docker pull mysql:5.7</span><br></pre></td></tr></table></figure><h4 id="运行容器">运行容器</h4><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line">docker run --name mysql-5.7 \</span><br><span class="line">-v /Users/wf09/develop/mysql/data:/var/lib/mysql \</span><br><span class="line">-v /Users/wf09/develop/mysql/conf:/etc/mysql/conf.d \</span><br><span class="line">  -p 3306:3306 \</span><br><span class="line">  -d -e MYSQL_ROOT_PASSWORD=adminadmin \</span><br><span class="line">  mysql:5 \</span><br><span class="line">  --character-set-server=utf8mb4 \</span><br><span class="line">  --character_set_connection=utf8mb4 \</span><br><span class="line">  --character_set_client=utf8mb4 \</span><br><span class="line">  --character_set_results=character_set_results \</span><br><span class="line">  --collation-server=utf8mb4_unicode_ci \</span><br><span class="line">  --collation_connection=utf8mb4_unicode_ci</span><br><span class="line"></span><br><span class="line">  </span><br></pre></td></tr></table></figure><p>其中：</p><ul><li><code>-v</code>参数是挂载本地文件到容器内部，<code>/var/lib/mysql</code>是mysql默认的数据文件保存路径；<code>/etc/mysql/conf.d</code>是附加配置目录，该目录下的.cnf文件都会被读取并覆盖原有的配置；</li><li><code>-e</code>参数是为容器指定环境变量，<code>MYSQL_ROOT_PASSWORD</code>为容器指定密码，容器预设的环境变量可以在这里查看：<a href="https://hub.docker.com/_/mysql">Docker Hub</a>；</li><li>最后的参数是指定字符集为<strong>utf8mb4</strong>，避免遇到mysql无法存放中文字符的问题。<ul><li>查看当前数据库支持的字符集：<code>show variables like '%char%';</code></li></ul></li></ul><h3 id="设置mysql容器">设置MySQL容器</h3><h4 id="配置文件">配置文件</h4><figure class="highlight ini"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="section">[client]</span></span><br><span class="line"><span class="attr">default-character-set</span>=utf8mb4</span><br><span class="line"></span><br><span class="line"><span class="section">[mysql]</span></span><br><span class="line"><span class="attr">default-character-set</span>=utf8mb4</span><br><span class="line"></span><br><span class="line"><span class="section">[mysqld]</span></span><br><span class="line"><span class="attr">character-set-server</span>=utf8mb4</span><br></pre></td></tr></table></figure><h4 id="允许外网访问">允许外网访问</h4><p>先进入容器</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">docker <span class="built_in">exec</span> -ti mysql-5.7 /bin/bash</span><br></pre></td></tr></table></figure><p>输入密码，连接mysql</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">mysql -uroot -p</span><br></pre></td></tr></table></figure><p>修改表项</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">update mysql.user set host&#x3D;&#39;%&#39; where user&#x3D;&#39;root&#39;;</span><br><span class="line">flush privileges;</span><br></pre></td></tr></table></figure><p>查看修改后的结果</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">SELECT USER,HOST FROM mysql.user;</span><br></pre></td></tr></table></figure><p>结果如下所示，说明修改成功</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">mysql&gt; SELECT USER,HOST FROM mysql.user;</span><br><span class="line">+---------------+-----------+</span><br><span class="line">| USER          | HOST      |</span><br><span class="line">+---------------+-----------+</span><br><span class="line">| root          | %         |</span><br><span class="line">| mysql.session | localhost |</span><br><span class="line">| mysql.sys     | localhost |</span><br><span class="line">| root          | localhost |</span><br><span class="line">+---------------+-----------+</span><br><span class="line">4 rows <span class="keyword">in</span> <span class="built_in">set</span> (0.00 sec)</span><br></pre></td></tr></table></figure><h4 id="设置日志路径">设置日志路径</h4><figure class="highlight ini"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="section">[mysqld]</span></span><br><span class="line"><span class="comment"># 错误日志</span></span><br><span class="line"><span class="attr">log_error</span> = /var/log/mysql/error.log</span><br><span class="line"><span class="comment"># 查询日志</span></span><br><span class="line"><span class="attr">general_log</span> = <span class="number">1</span></span><br><span class="line"><span class="attr">general_log_file</span> = /var/log/mysql/general.log</span><br><span class="line"><span class="comment"># 慢查询日志</span></span><br><span class="line"><span class="attr">slow_query_log</span> = <span class="number">1</span></span><br><span class="line"><span class="attr">slow_query_log_file</span> = /var/log/mysql/slow.log</span><br><span class="line"><span class="comment"># 同步写binlog</span></span><br><span class="line"><span class="attr">log_bin_index</span> = /var/log/mysql/bin</span><br><span class="line"><span class="attr">max_binlog_size</span> = <span class="number">4</span>M</span><br><span class="line"><span class="attr">sync_binlog</span> = <span class="number">1</span></span><br></pre></td></tr></table></figure><h3 id="使用docker-compose.yml">使用docker-compose.yml</h3><p>废话不多说直接上干货</p><figure class="highlight yml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">version:</span> <span class="string">&#x27;1&#x27;</span></span><br><span class="line"><span class="attr">services:</span></span><br><span class="line">  <span class="attr">mysql:</span></span><br><span class="line">    <span class="attr">image:</span> <span class="string">mysql:5</span></span><br><span class="line">    <span class="attr">container_name:</span> <span class="string">mysql</span></span><br><span class="line">    <span class="attr">restart:</span> <span class="string">always</span></span><br><span class="line">    <span class="attr">ports:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">&quot;3306:3306&quot;</span></span><br><span class="line">    <span class="attr">environment:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">MYSQL_ROOT_PASSWORD=ff143253-f1dd-266f-2a4b-cdb747c31150</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">TZ=Asia/Shanghai</span></span><br><span class="line">    <span class="attr">volumes:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">/home/deepsoft01/mysql/conf:/etc/mysql/conf.d</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">/home/deepsoft01/mysql/data:/var/lib/mysql</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">/home/deepsoft01/mysql/sql:/root/sqls</span></span><br></pre></td></tr></table></figure>]]></content>
    
    
    <summary type="html">&lt;p&gt;为什么要在docker中安装mysql？&lt;/p&gt;</summary>
    
    
    
    
    <category term="docker" scheme="https://fly97.cn/tags/docker/"/>
    
    <category term="mysql" scheme="https://fly97.cn/tags/mysql/"/>
    
  </entry>
  
  <entry>
    <title>力扣每日一题之完美数</title>
    <link href="https://fly97.cn/p/perfect-number/"/>
    <id>https://fly97.cn/p/perfect-number/</id>
    <published>2021-12-31T06:33:00.000Z</published>
    <updated>2021-12-31T06:33:00.000Z</updated>
    
    <content type="html"><![CDATA[<p>对于一个正整数，如果除了它和它自身以外的所有<strong>正因子</strong>相等，我们称他为完美数。</p><p>给定一个整数，如果是完美数，返回<code>true</code>，否则返回<code>false</code></p><a id="more"></a><p>示例 1：</p><p>输入：num = 28 输出：true 解释：28 = 1 + 2 + 4 + 7 + 14 1, 2, 4, 7, 和 14 是 28 的所有正因子。</p><p>示例 2：</p><p>输入：num = 6 输出：true</p><p>示例 3：</p><p>输入：num = 496 输出：true</p><h2 id="方法一枚举">方法一：枚举</h2><p>枚举<code>num</code> 的所有真因子，累加所有真因子之和，记作<code>sum</code>。若<code>sum=num</code>则返回<code>true</code>，否则返回<code>false</code>.</p><p>在枚举时，只需要枚举不超过$  <span class="math inline">\(的数。这是因为如果\)</span>num<span class="math inline">\(有一个大于\)</span>  <span class="math inline">\(的因数\)</span>d<span class="math inline">\(，那么它一定有一个小于\)</span>  $的因数 <span class="math inline">\(frac{num}{d}\)</span>.</p><p>在枚举时，若能找到一个因数<span class="math inline">\(d\)</span>，那么就找到了因数<span class="math inline">\(frac{num}{d}\)</span>.</p><p>当<span class="math inline">\(d \\times d=num\)</span>时，这两个因数相同，此时不能重复计算。</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">checkPerfectNumber</span><span class="params">(num <span class="keyword">int</span>)</span> <span class="title">bool</span></span> &#123;</span><br><span class="line">    <span class="keyword">if</span> num == <span class="number">1</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">false</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">var</span> sum <span class="keyword">int</span> = <span class="number">1</span> <span class="comment">// 和从1开始</span></span><br><span class="line">    <span class="keyword">for</span> d := <span class="number">2</span>; d * d &lt;= num; d ++ &#123;</span><br><span class="line">        <span class="keyword">if</span> num % d == <span class="number">0</span>&#123;</span><br><span class="line">            sum += d</span><br><span class="line">            <span class="keyword">if</span> d * d &lt; num &#123;</span><br><span class="line">                sum += num /d</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> sum == num</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
    
    
    <summary type="html">&lt;p&gt;对于一个正整数，如果除了它和它自身以外的所有&lt;strong&gt;正因子&lt;/strong&gt;相等，我们称他为完美数。&lt;/p&gt;
&lt;p&gt;给定一个整数，如果是完美数，返回&lt;code&gt;true&lt;/code&gt;，否则返回&lt;code&gt;false&lt;/code&gt;&lt;/p&gt;</summary>
    
    
    
    
    <category term="leetcode" scheme="https://fly97.cn/tags/leetcode/"/>
    
  </entry>
  
  <entry>
    <title>力扣每日一题之适龄的朋友</title>
    <link href="https://fly97.cn/p/friends-of-appropriate-ages/"/>
    <id>https://fly97.cn/p/friends-of-appropriate-ages/</id>
    <published>2021-12-28T05:09:00.000Z</published>
    <updated>2021-12-28T05:09:00.000Z</updated>
    
    <content type="html"><![CDATA[<p>在社交媒体网站上有 n 个用户。给你一个整数数组 ages ，其中 ages[i] 是第 i 个用户的年龄。</p><p>如果下述任意一个条件为真，那么用户 x 将不会向用户 y（x != y）发送好友请求：</p><ul><li>age[y] &lt;= 0.5 * age[x] + 7</li><li>age[y] &gt; age[x]</li><li>age[y] &gt; 100 &amp;&amp; age[x] &lt; 100 否则，x 将会向 y 发送一条好友请求。</li></ul><p>注意，如果 x 向 y 发送一条好友请求，y 不必也向 x 发送一条好友请求。另外，用户不会向自己发送好友请求。</p><p>返回在该社交媒体网站上产生的好友请求总数。</p><a id="more"></a><p>示例 1：</p><p>输入：ages = [16,16] 输出：2 解释：2 人互发好友请求。</p><p>示例 2：</p><p>输入：ages = [16,17,18] 输出：2 解释：产生的好友请求为 17 -&gt; 16 ，18 -&gt; 17</p><p>示例 3：</p><p>输入：ages = [20,30,100,110,120] 输出：3 解释：产生的好友请求为 110 -&gt; 100 ，120 -&gt; 110 ，120 -&gt; 100</p><h2 id="方法一排序双指针">方法一：排序+双指针</h2><p>观察题目中给出的三个条件：</p><p><img src="image-20211228131642378.png" /></p><p>可知条件3是蕴含条件2中的，即如果满足条件3那么也就满足条件2。因此当条件12均不满足时，用户x就会向用户y发送好友请求。</p><p>用户y需要满足： <span class="math display">\[0.5 \times ages[x]+7 &lt; ages[y] \leq age[x]\]</span> 当<span class="math inline">\(age[x] \leq 14\)</span>时，不存在满足要求的ages[y]，因此我们只需要考虑<span class="math inline">\(ages[y] \geq 15\)</span>的情况，此时满足要求的<span class="math inline">\(ages[y]\)</span>的范围：<span class="math inline">\((0.5 \times ages[x]+7, age[x]]\)</span>。</p><p>当<span class="math inline">\(age[x]\)</span>增加时，上述区间的左右边界均<strong>单调递增</strong>，因此如果我们将数组的<span class="math inline">\(ages\)</span>进行升序排序，那么就可以在遍历<span class="math inline">\(ages[x]\)</span>的同时，使用两个指针left和right维护满足要求的左右边界。当x向后移动一个位置时：</p><ul><li>如果左边界指针left指向的元素<strong>不满足</strong><span class="math inline">\(ages[left] &gt; 0.5 \ times ages[x] + 7\)</span>，那么就将左边界向右移动一个位置；</li><li>如果右边界指针right指向的下一个元素<strong>满足</strong><span class="math inline">\(ages[right+1] \geq ages[x]\)</span>，那么就将右边界向右移动一个位置；</li></ul><p>这样一来，<span class="math inline">\([left, right]\)</span>就是满足要求的y的下标。需要注意的是，<span class="math inline">\(x\)</span>本身一定在<span class="math inline">\([left, right]\)</span>区间内，因此<span class="math inline">\(x\)</span>发送的好友请求数，即为<span class="math inline">\([left, right]\)</span>区间的长度-1.</p><p>我们将每一个x对应的<span class="math inline">\([left, right]\)</span>区间的长度减去1进行累加，就可以得到最后的答案。</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">numFriendRequests</span><span class="params">(ages []<span class="keyword">int</span>)</span> <span class="params">(ans <span class="keyword">int</span>)</span></span> &#123;</span><br><span class="line">        sort.Ints(ages)</span><br><span class="line">        left, right := <span class="number">0</span>, <span class="number">0</span></span><br><span class="line">        <span class="keyword">for</span> _, age := <span class="keyword">range</span> ages &#123;</span><br><span class="line">            <span class="keyword">if</span> age &lt; <span class="number">15</span> &#123;</span><br><span class="line">                <span class="keyword">continue</span></span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">for</span> ages[left]*<span class="number">2</span> &lt;= age+<span class="number">14</span> &#123;</span><br><span class="line">                left ++</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">for</span> right+<span class="number">1</span> &lt; <span class="built_in">len</span>(ages) &amp;&amp; ages[right+<span class="number">1</span>] &lt;= age &#123;</span><br><span class="line">                right ++</span><br><span class="line">            &#125;</span><br><span class="line">            ans += right - left</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span></span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>]]></content>
    
    
    <summary type="html">&lt;p&gt;在社交媒体网站上有 n 个用户。给你一个整数数组 ages ，其中 ages[i] 是第 i 个用户的年龄。&lt;/p&gt;
&lt;p&gt;如果下述任意一个条件为真，那么用户 x 将不会向用户 y（x != y）发送好友请求：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;age[y] &amp;lt;= 0.5 * age[x] + 7&lt;/li&gt;
&lt;li&gt;age[y] &amp;gt; age[x]&lt;/li&gt;
&lt;li&gt;age[y] &amp;gt; 100 &amp;amp;&amp;amp; age[x] &amp;lt; 100 否则，x 将会向 y 发送一条好友请求。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;注意，如果 x 向 y 发送一条好友请求，y 不必也向 x 发送一条好友请求。另外，用户不会向自己发送好友请求。&lt;/p&gt;
&lt;p&gt;返回在该社交媒体网站上产生的好友请求总数。&lt;/p&gt;</summary>
    
    
    
    
    <category term="leetcode" scheme="https://fly97.cn/tags/leetcode/"/>
    
  </entry>
  
  <entry>
    <title>力扣每日一题之课程表Ⅲ</title>
    <link href="https://fly97.cn/p/course-schedule-iii/"/>
    <id>https://fly97.cn/p/course-schedule-iii/</id>
    <published>2021-12-14T13:18:00.000Z</published>
    <updated>2021-12-14T13:18:00.000Z</updated>
    
    <content type="html"><![CDATA[<p>这里有<code>n</code>门不同的在线课程，按<code>1</code>到<code>n</code>编号。给你一个数组<code>courses</code>，其中<code>courses[i] = [durationi, lastDayi]</code>表示第<code>i</code>门课将会持续上<code>durationi</code>天课，并且必须不晚于<code>lastDayi</code>的时候完成。</p><p>你的学期从第一天开始，且不能同时修读两门及两门以上的课程。</p><p>返回你最多可以修读的课程数目。</p><a id="more"></a><p><strong>示例1：</strong></p><p>输入：<code>courses = [[100, 200], [200, 1300], [1000, 1250], [2000, 3200]]</code> 输出：<code>3</code> 解释： 这里一共有 4 门课程，但是你最多可以修 3 门： 首先，修第 1 门课，耗费 100 天，在第 100 天完成，在第 101 天开始下门课。 第二，修第 3 门课，耗费 1000 天，在第 1100 天完成，在第 1101 天开始下门课程。 第三，修第 2 门课，耗时 200 天，在第 1300 天完成。 第 4 门课现在不能修，因为将会在第 3300 天完成它，这已经超出了关闭日期。</p><p><strong>示例2：</strong></p><p>输入：<code>courses = [[1, 2]]</code> 输出：<code>1</code></p><p><strong>示例3：</strong></p><p>输入：<code>courses = [[3, 2],[4,3]]</code> 输出：<code>0</code></p><h2 id="方法一优先队列贪心">方法一：优先队列+贪心</h2><p><strong>思路</strong></p><p>对于两门课<span class="math inline">\((t1, d1)\)</span>和<span class="math inline">\((t2, d2)\)</span>，如果后者关闭的时间较晚，即<span class="math inline">\(d1 \le d2\)</span>，那先学习前者，再学习后者，总是最优的。这是因为：</p><ul><li><p>假设最开始学习的时间点为<span class="math inline">\(x\)</span>，如果先学习前者，再学习后者，那么需要满足： <span class="math display">\[\begin{cases}x+t_1 \le d_1 \\x+t_1+t_2\le d_2 \\\end{cases}\]</span></p></li><li><p>如果先学习后者，再学习前者，那么需要满足：</p></li></ul><p><span class="math display">\[\begin{cases}x+t_2 \le d_2 \\x+t_1+t_2\le d_1 \\\end{cases}\]</span></p><p>如果 <span class="math inline">\(x+t_1+t_2 \le d_1\)</span>成立，由于<span class="math inline">\(d_1 \le d_2\)</span>，那么<span class="math inline">\(x+t_1+t_2 \le d_2\)</span>同时成立。这说明，如果能【先学习后者，再学习前者】那么一定能【先学习前者，再学习后者】。如果<span class="math inline">\(x+t_1+t_2 \le d_2\)</span>成立，则不能推出<span class="math inline">\(x+t_2+t_1 \le d_1\)</span>成立。<strong>虽然能【先学习前者，再学习后者】，但不能【先学习后者，再学习前者】。</strong></p><p>因此，我们可以将所有的课程按照关闭时间<span class="math inline">\(d\)</span>进行升序排序，再依次挑选课程并按照顺序进行学习。</p><p>在遍历的过程中，假设我们当前遍历到了第<span class="math inline">\(i\)</span>门课<span class="math inline">\((t_i, d_i)\)</span>，而在前<span class="math inline">\(i-1\)</span>门课程中我们选择了<span class="math inline">\(k\)</span>门课<span class="math inline">\((t_{x1}, d_{x1}),(t_{x2}, d_{x2}), ... ,(t_{xk}, d_{xk})\)</span>，满足<span class="math inline">\(x_1&lt;x_2&lt; \cdots x_k\)</span>，那么有： <span class="math display">\[\begin{cases}t_{x1} \le d_{x1} \\t_{x1}+t_{x2} \le d_{x2} \\\cdots \\t_{x1}+t_{x2}+ \cdots +\le t_{x2} d_{xk}\\\end{cases}\]</span> 如果上述选择方案是前<span class="math inline">\(i-1\)</span>门课程的【最优方案】：即不存在能选择<span class="math inline">\(k+1\)</span>门课程的方法，也不存在能选择<span class="math inline">\(k\)</span>门课程，且总时长更短的方案，那么我们可以基于该方案与第<span class="math inline">\(i\)</span>门课程<span class="math inline">\((t_i, d_i)\)</span>，构造出前<span class="math inline">\(i\)</span>门课程的最优方案：</p><p><img src="image-20211214221952979.png" /></p><p>这样一来，当我们遍历完所有的<span class="math inline">\(n\)</span>门课程后，就可以得到最终答案。</p><p><strong>算法</strong></p><p>我们需要一个数据结构支持【取出<span class="math inline">\(t\)</span>值最大的那门课程】，因此我们可以使用优先队列。</p><p>依次遍历每一门课程，当遍历到<span class="math inline">\((t_i, d_i)\)</span>时：</p><ul><li>如果当前优先队列中所有课程的总时间与<span class="math inline">\(t_i\)</span>之和小于等于<span class="math inline">\(d_i\)</span>，那么我们就把<span class="math inline">\(t_i\)</span>加入优先队列中；</li><li>如果当前优先队列中所有课程的总时间与<span class="math inline">\(t_i\)</span>之和大于<span class="math inline">\(d_i\)</span>，那么我们找到优先队列的最大元素<span class="math inline">\(t_{xj}\)</span>。如果<span class="math inline">\(t_{xj} &gt; t_{i}\)</span>，则将它移出优先队列，并把<span class="math inline">\(t_i\)</span>加入优先队列中。</li></ul><p>在遍历完成之后，优先队列中包含的元素个数即为答案。</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">scheduleCourse</span><span class="params">(courses [][]<span class="keyword">int</span>)</span> <span class="title">int</span></span> &#123;</span><br><span class="line">    sort.Slice(courses, <span class="function"><span class="keyword">func</span><span class="params">(i, j <span class="keyword">int</span>)</span> <span class="title">bool</span></span> &#123;</span><br><span class="line">        <span class="keyword">return</span> courses[i][<span class="number">1</span>] &lt; courses[j][<span class="number">1</span>]</span><br><span class="line">    &#125;)</span><br><span class="line"></span><br><span class="line">    h := &amp;Heap&#123;&#125;</span><br><span class="line">    total := <span class="number">0</span> <span class="comment">// 优先队列中所有课程的总时间</span></span><br><span class="line">    <span class="keyword">for</span> _, course := <span class="keyword">range</span> courses &#123;</span><br><span class="line">        <span class="keyword">if</span> t := course[<span class="number">0</span>]; total+t &lt;= course[<span class="number">1</span>] &#123;</span><br><span class="line">            total += t</span><br><span class="line">            heap.Push(h, t)</span><br><span class="line">        &#125; <span class="keyword">else</span> <span class="keyword">if</span> h.Len() &gt; <span class="number">0</span> &amp;&amp; t &lt; h.IntSlice[<span class="number">0</span>] &#123;</span><br><span class="line">            total += t - h.IntSlice[<span class="number">0</span>]</span><br><span class="line">            h.IntSlice[<span class="number">0</span>] = t</span><br><span class="line">            heap.Fix(h, <span class="number">0</span>)</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> h.Len()</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">type</span> Heap <span class="keyword">struct</span> &#123;</span><br><span class="line">    sort.IntSlice</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(h Heap)</span> <span class="title">Less</span><span class="params">(i, j <span class="keyword">int</span>)</span> <span class="title">bool</span></span> &#123;</span><br><span class="line">    <span class="keyword">return</span> h.IntSlice[i] &gt; h.IntSlice[j]</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(h *Heap)</span> <span class="title">Push</span><span class="params">(x <span class="keyword">interface</span>&#123;&#125;)</span></span> &#123;</span><br><span class="line">    h.IntSlice = <span class="built_in">append</span>(h.IntSlice, x.(<span class="keyword">int</span>))</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(h *Heap)</span> <span class="title">Pop</span><span class="params">()</span> <span class="title">interface</span></span>&#123;&#125; &#123;</span><br><span class="line">    a := h.IntSlice</span><br><span class="line">    x := a[<span class="built_in">len</span>(a)<span class="number">-1</span>]</span><br><span class="line">    h.IntSlice = a[:<span class="built_in">len</span>(a)<span class="number">-1</span>]</span><br><span class="line">    <span class="keyword">return</span> x</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
    
    
    <summary type="html">&lt;p&gt;这里有&lt;code&gt;n&lt;/code&gt;门不同的在线课程，按&lt;code&gt;1&lt;/code&gt;到&lt;code&gt;n&lt;/code&gt;编号。给你一个数组&lt;code&gt;courses&lt;/code&gt;，其中&lt;code&gt;courses[i] = [durationi, lastDayi]&lt;/code&gt;表示第&lt;code&gt;i&lt;/code&gt;门课将会持续上&lt;code&gt;durationi&lt;/code&gt;天课，并且必须不晚于&lt;code&gt;lastDayi&lt;/code&gt;的时候完成。&lt;/p&gt;
&lt;p&gt;你的学期从第一天开始，且不能同时修读两门及两门以上的课程。&lt;/p&gt;
&lt;p&gt;返回你最多可以修读的课程数目。&lt;/p&gt;</summary>
    
    
    
    
    <category term="leetcode" scheme="https://fly97.cn/tags/leetcode/"/>
    
  </entry>
  
  <entry>
    <title>力扣题目类型之回溯法（一）</title>
    <link href="https://fly97.cn/p/back-tracking-one/"/>
    <id>https://fly97.cn/p/back-tracking-one/</id>
    <published>2021-12-14T13:06:00.000Z</published>
    <updated>2021-12-14T13:06:00.000Z</updated>
    
    <content type="html"><![CDATA[<p>回溯法也可以叫做回溯搜索法，它是一种搜索的方式。</p><p>回溯是递归的副产品，只要有递归就会有回溯。</p><a id="more"></a><h2 id="回溯法的效率">回溯法的效率</h2><p>回溯的本质是穷举，穷举所有可能。如果还想让回溯法高效一些，可以加一些剪枝的操作，但其本质仍是穷举。</p><h2 id="回溯法可以解决的问题">回溯法可以解决的问题</h2><p>回溯法，一般用于解决以下问题：</p><ul><li>组合问题：N个数里面按一定规则找出k个数的集合；</li><li>切割问题：一个字符串按一定规则有几种切割方式；</li><li>子集问题：一个N个数的集合里有多少符合条件的子集；</li><li>排列问题：N个数按一定规则全排列，有几种排列方式；</li><li>棋盘问题：N皇后，解数独等。</li></ul><h2 id="如何理解回溯法">如何理解回溯法</h2><p>回溯法解决的问题都可以抽象成树形结构，因为回溯法解决的都是在集合中递归查找子集：</p><ul><li>集合的大小是树的宽度</li><li>递归的深度是树的深度</li></ul><p>递归要有终止条件，因此必然是一颗高度有限的树（N叉树）</p><h2 id="回溯法模板">回溯法模板</h2><p>回溯三部曲</p><ul><li><strong>返回值以及参数</strong>：一般是先写逻辑，然后需要什么参数填写什么参数。</li><li><strong>终止条件</strong>：什么时候达到了终止条件，树中就可以看出，一般来说搜到叶子节点了，也就找到了满足条件的一条答案，然后把整个答案存起来，并结束本层递归。</li><li><strong>遍历过程</strong>：回溯法是在集合中递归搜索，集合的大小构成了树的宽度，递归的深度构成了树的深度。</li></ul><p><img src="20210130173631174.png" /></p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">func</span><span class="params">(参数)</span></span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (终止条件) &#123;</span><br><span class="line">        存放结果;</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">for</span> (选择; 本层集合中元素(树中节点孩子的大小就是集合的大小)) &#123; <span class="comment">// for循环是横向遍历</span></span><br><span class="line">        处理节点;</span><br><span class="line">        func(路径, 选择列表); <span class="comment">// 递归是纵向遍历</span></span><br><span class="line">        回溯，撤销处理结果</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
    
    
    <summary type="html">&lt;p&gt;回溯法也可以叫做回溯搜索法，它是一种搜索的方式。&lt;/p&gt;
&lt;p&gt;回溯是递归的副产品，只要有递归就会有回溯。&lt;/p&gt;</summary>
    
    
    
    
    <category term="leetcode" scheme="https://fly97.cn/tags/leetcode/"/>
    
  </entry>
  
  <entry>
    <title>力扣每日一题之保持城市天际线</title>
    <link href="https://fly97.cn/p/max-increase-to-keep-city-skyline/"/>
    <id>https://fly97.cn/p/max-increase-to-keep-city-skyline/</id>
    <published>2021-12-13T10:51:00.000Z</published>
    <updated>2021-12-13T10:51:00.000Z</updated>
    
    <content type="html"><![CDATA[<p>给你一座<code>nxn</code>个街区组成的城市，每个截取都包含一座立方体建筑。给你一个坐标从0开始的<code>nxn</code>整数矩阵<code>grid</code>，其中<code>grid[r][c]</code>表示坐落于<code>r</code>行c列的建筑物的高度。</p><p>城市的天际线是从远处观察城市时，所有建筑物形成的外部轮廓。从东、南、西、北四个方向观测到的天际线可能不同。</p><p>我们被允许为任意数量的建筑物的高度增加任意增量（不同的建筑物的增量可能不同）。高度为0的建筑物的高度也可以增加。然而，增加的建筑物高度不能影响从任何主要方向观察城市的得到的天际线。</p><p>在 <strong>不改变</strong> 从任何主要方向观测到的城市的天际线的前提下，返回建筑物可以增加的最大高度增量总和。</p><a id="more"></a><p><strong>示例1</strong></p><p><img src="807-ex1.png" /></p><p><strong>输入</strong>：<code>grid = [[3,0,8,4],[2,4,5,7],[9,2,6,3],[0,3,1,0]]</code> <strong>输出</strong>：<code>35</code> <strong>解释</strong>：建筑物的高度如上图中心所示。 用红色绘制从不同方向观看得到的天际线。 在不影响天际线的情况下，增加建筑物的高度： <code>gridNew = [ [8, 4, 8, 7],             [7, 4, 7, 7],             [9, 4, 8, 7],             [3, 3, 3, 3] ]</code></p><p><strong>示例2</strong></p><p><strong>输入</strong>：<code>grid=[[0,0,0],[0,0,0],[0,0,0]]</code> <strong>输出</strong>：0 <strong>解释</strong>：增加任何建筑物的高度都会导致天际线的变化。</p><h3 id="方法一贪心算法">方法一：贪心算法</h3><p>从左侧和右侧看，城市天际线等于<strong>矩阵grid每一行的建筑物的最大值</strong>；从顶部和底部看，城市天际线等于<strong>矩阵grid的每一列的建筑物的最大值</strong>。只要<strong>不改变每一行和每一列的建筑物的高度的最大值</strong>，就能保持城市天际线，因此可以使用贪心的思想计算高度建筑可以增加的最大高度。</p><p>由于矩阵grid的行数和列数都是n，因此创建一个两个长度为n的数组rowMax和colMax分别记录建筑物grid的每一行的最大值和每一列的最大值。遍历矩阵grid填入两个数组之后，再次遍历矩阵，计算每个建筑物可以增加的最大值。</p><p>对于<code>0&lt;=i, j&lt;n</code>时，对于第i行和第j列的建筑物，其所在行的建筑物高度的最大值是<code>rowMax[i]</code>，其所在列的建筑物的高度的最大值是<code>colMax[j]</code>。为了保持城市天际线，该建筑物增加后的高度不能超过其所在行和所在列的建筑物高度最大值，即该建筑物增加后的最大高度是<code>min(rowMax[i], colMax[j])</code>。由于该建筑物的原始高度是<code>grid[j][j]</code>，因此该建筑物增加后的最大高度是<code>min(rowMax[i], colMax[j]) - grid[i, j]</code>.</p><p>对于矩阵grid中每个元素可以增加的最大值，即可得到建筑物高度可以增加的最大总和。</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">min</span><span class="params">(a, b <span class="keyword">int</span>)</span> <span class="title">int</span></span> &#123;</span><br><span class="line"><span class="keyword">if</span> a &lt; b &#123;</span><br><span class="line"><span class="keyword">return</span> a</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">return</span> b</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">maxIncreaseKeepingSkyline</span><span class="params">(grid [][]<span class="keyword">int</span>)</span> <span class="title">int</span></span> &#123;</span><br><span class="line"><span class="comment">//计算每一行的最大值和每一列的最大值</span></span><br><span class="line">m, n := <span class="built_in">len</span>(grid), <span class="built_in">len</span>(grid[<span class="number">0</span>])</span><br><span class="line">rowMax := <span class="built_in">make</span>([]<span class="keyword">int</span>, m)</span><br><span class="line">colMax := <span class="built_in">make</span>([]<span class="keyword">int</span>, n)</span><br><span class="line"><span class="keyword">for</span> i := <span class="number">0</span>; i &lt; m; i++ &#123;</span><br><span class="line"><span class="keyword">for</span> j := <span class="number">0</span>; j &lt; n; j++ &#123;</span><br><span class="line"><span class="keyword">if</span> rowMax[i] &lt; grid[i][j] &#123;</span><br><span class="line">rowMax[i] = grid[i][j]</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">if</span> colMax[j] &lt; grid[i][j] &#123;</span><br><span class="line">colMax[j] = grid[i][j]</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 每一个栅格上的建筑物不能超过所在行最大值、所在列最大值两者的最小值</span></span><br><span class="line">    <span class="comment">// 能增加的最大值是这两者的差</span></span><br><span class="line"><span class="keyword">var</span> ret <span class="keyword">int</span></span><br><span class="line"><span class="keyword">for</span> i := <span class="number">0</span>; i &lt; m; i++ &#123;</span><br><span class="line"><span class="keyword">for</span> j := <span class="number">0</span>; j &lt; n; j++ &#123;</span><br><span class="line">ret += min(rowMax[i], colMax[j]) - grid[i][j]</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">return</span> ret</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
    
    
    <summary type="html">&lt;p&gt;给你一座&lt;code&gt;nxn&lt;/code&gt;个街区组成的城市，每个截取都包含一座立方体建筑。给你一个坐标从0开始的&lt;code&gt;nxn&lt;/code&gt;整数矩阵&lt;code&gt;grid&lt;/code&gt;，其中&lt;code&gt;grid[r][c]&lt;/code&gt;表示坐落于&lt;code&gt;r&lt;/code&gt;行c列的建筑物的高度。&lt;/p&gt;
&lt;p&gt;城市的天际线是从远处观察城市时，所有建筑物形成的外部轮廓。从东、南、西、北四个方向观测到的天际线可能不同。&lt;/p&gt;
&lt;p&gt;我们被允许为任意数量的建筑物的高度增加任意增量（不同的建筑物的增量可能不同）。高度为0的建筑物的高度也可以增加。然而，增加的建筑物高度不能影响从任何主要方向观察城市的得到的天际线。&lt;/p&gt;
&lt;p&gt;在 &lt;strong&gt;不改变&lt;/strong&gt; 从任何主要方向观测到的城市的天际线的前提下，返回建筑物可以增加的最大高度增量总和。&lt;/p&gt;</summary>
    
    
    
    
    <category term="leetcode" scheme="https://fly97.cn/tags/leetcode/"/>
    
  </entry>
  
  <entry>
    <title>力扣每日一题之位运算的应用</title>
    <link href="https://fly97.cn/p/to-lower-case/"/>
    <id>https://fly97.cn/p/to-lower-case/</id>
    <published>2021-12-12T09:20:00.000Z</published>
    <updated>2021-12-12T09:20:00.000Z</updated>
    
    <content type="html"><![CDATA[<p>给你一个字符串 <code>s</code> ，将该字符串中的大写字母转换成相同的小写字母，返回新的字符串。</p><p>当然我们可以用库函数解决这个问题，但是如果这个问题出现在面试里，那肯定是不能用库函数的。</p><p>这里我们使用<strong>位运算</strong>解决这个大小写转换问题。</p><a id="more"></a><p>我们知道</p><ol type="1"><li><p>大写字母A-Z的ASCII码是65到90，小写字母的ASCII码是97到112，大小写字母的ASCII码相差32；</p></li><li><p>大写A(65)的二进制编码为<em>1000001</em>，小写a(97)的二进制编码位<em>1100001</em>。</p></li></ol><p>从上面我们可以发现一个规律：</p><ul><li>大小写是由<strong>第五位是1还是0</strong>来区分的。</li></ul><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">fmt.Println(<span class="string">&#x27;a&#x27;</span>) <span class="comment">// 97, 1100001</span></span><br><span class="line">fmt.Println(<span class="string">&#x27;z&#x27;</span>) <span class="comment">// 122, 1111010</span></span><br><span class="line">fmt.Println(<span class="string">&#x27;A&#x27;</span>) <span class="comment">// 65, 1000001</span></span><br><span class="line">fmt.Println(<span class="string">&#x27;Z&#x27;</span>) <span class="comment">// 90, 1011010</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>问题转化为：如何让一个二进制的某一位的值发生改变？</p><ul><li>如果需要从0变成1，只需要和这个二进制数进行<strong>按位或运算</strong>，其中需要变化的位是1，其他位是0；</li><li>如果需要从1变成0，只需要和这个二进制数进行<strong>按位与运算</strong>，其中需要变化的位是0，其他位是1；</li></ul><h3 id="大写转小写">大写转小写</h3><p>在本题中，将二进制数的第5位从0变成1，那么只需要将该二进制数和二进制数<code>0100000</code><strong>按位或</strong>即可：</p><p><img src="image-20211212174229278.png" /></p><p>而当该字母本身就是小写字母时，则二进制数不发生改变，即<strong>小写字母还是原来的小写字母</strong></p><p>而二进制数00100000转换后得到十进制数32，因此如果需要将<strong>大写字母转换成小写字母，同时小写字母不变</strong>，只需要把这个字符按位或32即可：</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">b := <span class="string">&#x27;A&#x27;</span></span><br><span class="line">b |= <span class="number">32</span> <span class="comment">// &#x27;a&#x27;</span></span><br></pre></td></tr></table></figure><h3 id="小写转大写">小写转大写</h3><p>如果需要将小写转换成大写字母，需要将小写字母的二进制编码的第五位从1变成0，则需要将二进制数和二进制数<code>11011111</code><strong>按位与</strong>即可。</p><p><img src="image-20211212174546766.png" /></p><p>而当该字母本身就是大写字母时，则二进制数不发生改变，即<strong>大写字母还是原来的大写字母</strong></p><p>如何将<code>110111111</code>转换成十进制数呢？</p><p>二进制中最高位表示符号位，最高位是1时是负数，0是负数。</p><p>负数的计算方式：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">-64+16+8+4+2+1 = -33</span><br></pre></td></tr></table></figure><p>如果需要将<strong>小写字母转换成大写字母，同时大写字母不变</strong>，只需要把这个字符按位与-33即可：</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">b := <span class="string">&#x27;A&#x27;</span></span><br><span class="line">b &amp;= <span class="number">-33</span></span><br></pre></td></tr></table></figure><h3 id="大写转小写小写转大写">大写转小写，小写转大写</h3><p>也就是二进制的<strong>第5位1变为0，同时0变为1</strong>.</p><p>小写转大写：</p><p><img src="image-20211212184452762.png" /></p><p>大写转小写：</p><p><img src="image-20211212184320992.png" /></p><p>将00100000转换成十进制是32.</p><p>如果需要将<strong>小写字母转换成大写字母，同时大写字母转换成小写字母</strong>，只需要把这个字符按位异或32即可：</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">b := <span class="string">&#x27;A&#x27;</span></span><br><span class="line">b ~= <span class="number">32</span></span><br></pre></td></tr></table></figure><h3 id="总结">总结</h3><ul><li>大写转小写、小写转大写：字符^=32；</li><li>大写转小写、小写转小写：字符|=32；</li><li>小写转大写、大写转小写：字符&amp;=-33；</li></ul><p>完。</p>]]></content>
    
    
    <summary type="html">&lt;p&gt;给你一个字符串 &lt;code&gt;s&lt;/code&gt; ，将该字符串中的大写字母转换成相同的小写字母，返回新的字符串。&lt;/p&gt;
&lt;p&gt;当然我们可以用库函数解决这个问题，但是如果这个问题出现在面试里，那肯定是不能用库函数的。&lt;/p&gt;
&lt;p&gt;这里我们使用&lt;strong&gt;位运算&lt;/strong&gt;解决这个大小写转换问题。&lt;/p&gt;</summary>
    
    
    
    
    <category term="leetcode" scheme="https://fly97.cn/tags/leetcode/"/>
    
  </entry>
  
  <entry>
    <title>力扣每日一题之在线选举</title>
    <link href="https://fly97.cn/p/online-election/"/>
    <id>https://fly97.cn/p/online-election/</id>
    <published>2021-12-11T09:20:00.000Z</published>
    <updated>2021-12-11T09:20:00.000Z</updated>
    
    <content type="html"><![CDATA[<p>给你两个证书数组<code>persons</code>和<code>times</code>。在选举中，第<code>i</code>张选票是在时刻为<code>times[i]</code>时投给选举人<code>persons[i]</code>的。</p><p>对于发生在时刻<code>t</code>的每个查询，需要找出在<code>t</code>时刻中领先的候选人的编号。</p><p>在<code>t</code>时刻投出的选票也被我们计入在我们的查询之中。在平局的情况下，最近获得选票的候选人将会获胜。</p><p>你的任务：实现<code>ToVotedCandidate</code>类：</p><ul><li><code>TopVotedCandidate(int[] persons, int[] times)</code>使用<code>persons</code>和<code>times</code>数组初始化对象。</li><li><code>int q(int t)</code>根据前面描述的规则，返回在时刻<code>t</code>在选举中领先的候选人的编号。</li></ul><a id="more"></a><p>输入： <code>["TopVotedCandidate", "q", "q", "q", "q", "q", "q"]</code> <code>[[[0, 1, 1, 0, 0, 1, 0], [0, 5, 10, 15, 20, 25, 30]], [3], [12], [25], [15], [24], [8]]</code> 输出： <code>[null, 0, 1, 1, 0, 0, 1]</code></p><p>解释：</p><ul><li>时刻为0时第0张票投给了0号；<ul><li>时刻为3时此时都没有票，所以返回0；</li></ul></li><li>时刻为5时第1张票投给了1号；<ul><li>时刻为8时此时票数分布是[0, 1]，因为1是最近获得选票的候选人，因此返回1；</li></ul></li><li>时刻为10时第2张票投给了1号；<ul><li>时刻为12时此时票数分布是[0, 1, 1]，此时返回1；</li></ul></li><li>时刻为15时第3张票投给了0号；</li><li>时刻为20时第4张票投给了0号；</li><li>时刻为25时第4张票投给了1号；<ul><li>时刻为25时此时票数分布为[0, 1, 1, 0, 0, 1]，又因为1是最近获得票的候选人，因此返回1；</li></ul></li><li>时刻为30时第5张票投给了0号。</li></ul><h2 id="方法一预计算二分查找">方法一：预计算+二分查找</h2><p>记<code>persons</code>的长度为<em>N</em>。我们对输入进行预计算，用一个长度为<em>N</em>的数组<code>tops</code>记录各时间段得票数领先的候选人。具体来说，<code>tops[i]</code>表示 <span class="math display">\[\begin{cases}times[i]≤t&lt;times[i+1],&amp; 0≤i&lt;N−1 \\t≥times[i],&amp; i=N-1\end{cases}\]</span> 的时间段的领先的候选人，这样的预计算可以通过对<em>persons</em>在<em>time</em>上的计数完成。</p><p>具体实现方法是：</p><ul><li>用一个哈希表<code>voteCounts</code>记录不同的候选人的得票数，用一个变量<code>top</code>表示当前领先的候选人。按时间从小到大遍历<code>persons</code>和<code>times</code>，并更新<code>voteCounts</code>和<code>top</code>，把<code>top</code>放入<code>tops</code>。遍历结束后，我们可以得到一个长度为N的tops，表示各个时间段的票领先的候选人。</li><li>每次查询时，我们在times中找出不大于t且离t最近的元素的下标，这部操作可以通过二分查找完成。到tops索引相同的下标即可返回结果。</li></ul><h3 id="顺序查找">顺序查找</h3><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">type</span> TopVotedCandidate <span class="keyword">struct</span> &#123;</span><br><span class="line">tops        []<span class="keyword">int</span></span><br><span class="line">times       []<span class="keyword">int</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">Constructor</span><span class="params">(persons, times []<span class="keyword">int</span>)</span> <span class="title">TopVotedCandidate</span></span> &#123;</span><br><span class="line">tops := []<span class="keyword">int</span>&#123;&#125;</span><br><span class="line">top := <span class="number">-1</span></span><br><span class="line">voteCounts := <span class="keyword">map</span>[<span class="keyword">int</span>]<span class="keyword">int</span>&#123;<span class="number">-1</span>: <span class="number">-1</span>&#125;</span><br><span class="line"><span class="keyword">for</span> _, p := <span class="keyword">range</span> persons &#123;</span><br><span class="line">voteCounts[p]++</span><br><span class="line"><span class="keyword">if</span> voteCounts[p] &gt;= voteCounts[top] &#123;</span><br><span class="line">top = p <span class="comment">// 记录上一个p</span></span><br><span class="line">&#125;</span><br><span class="line">tops = <span class="built_in">append</span>(tops, top)</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">return</span> TopVotedCandidate&#123;tops, times&#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(t *TopVotedCandidate)</span> <span class="title">Q</span><span class="params">(i <span class="keyword">int</span>)</span> <span class="title">int</span></span> &#123;</span><br><span class="line"><span class="comment">// 顺序查找</span></span><br><span class="line">gap := <span class="number">10000000</span></span><br><span class="line">ret := <span class="number">0</span></span><br><span class="line"><span class="keyword">for</span> index, value := <span class="keyword">range</span> t.times &#123;</span><br><span class="line"><span class="keyword">if</span> i-value &lt; gap &amp;&amp; i &gt;= value &#123;</span><br><span class="line">gap = i - value</span><br><span class="line">ret = index</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br><span class="line">fmt.Println(t.tops[ret])</span><br><span class="line"><span class="keyword">return</span> t.tops[ret]</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="手写二分查找">手写二分查找</h3><ul><li>区间<code>[left,right]</code>, <strong>left &lt;= right</strong>，i &gt; mid(left=mid+1)，i&lt;mid(right=mid-1)</li></ul><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(t *TopVotedCandidate)</span> <span class="title">Q</span><span class="params">(i <span class="keyword">int</span>)</span> <span class="title">int</span></span> &#123;</span><br><span class="line"><span class="comment">// 二分查找</span></span><br><span class="line">mid := <span class="number">0</span></span><br><span class="line">left := <span class="number">0</span></span><br><span class="line">right := <span class="built_in">len</span>(t.times) - <span class="number">1</span></span><br><span class="line"><span class="keyword">for</span> left &lt;= right &#123;</span><br><span class="line">mid = left + (right-left)/<span class="number">2</span></span><br><span class="line"><span class="keyword">if</span> i &gt; t.times[mid] &#123;</span><br><span class="line">left = mid + <span class="number">1</span></span><br><span class="line">&#125; <span class="keyword">else</span> <span class="keyword">if</span> i &lt; t.times[mid] &#123;</span><br><span class="line">right = mid - <span class="number">1</span></span><br><span class="line">&#125; <span class="keyword">else</span> &#123;</span><br><span class="line"><span class="keyword">return</span> t.tops[mid]</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">return</span> t.tops[right]</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="库函数二分查找">库函数二分查找</h3><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(c *TopVotedCandidate)</span> <span class="title">Q</span><span class="params">(t <span class="keyword">int</span>)</span> <span class="title">int</span></span> &#123;</span><br><span class="line">    <span class="keyword">return</span> c.tops[sort.SearchInts(c.times, t+<span class="number">1</span>)<span class="number">-1</span>]</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
    
    
    <summary type="html">&lt;p&gt;给你两个证书数组&lt;code&gt;persons&lt;/code&gt;和&lt;code&gt;times&lt;/code&gt;。在选举中，第&lt;code&gt;i&lt;/code&gt;张选票是在时刻为&lt;code&gt;times[i]&lt;/code&gt;时投给选举人&lt;code&gt;persons[i]&lt;/code&gt;的。&lt;/p&gt;
&lt;p&gt;对于发生在时刻&lt;code&gt;t&lt;/code&gt;的每个查询，需要找出在&lt;code&gt;t&lt;/code&gt;时刻中领先的候选人的编号。&lt;/p&gt;
&lt;p&gt;在&lt;code&gt;t&lt;/code&gt;时刻投出的选票也被我们计入在我们的查询之中。在平局的情况下，最近获得选票的候选人将会获胜。&lt;/p&gt;
&lt;p&gt;你的任务：实现&lt;code&gt;ToVotedCandidate&lt;/code&gt;类：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;TopVotedCandidate(int[] persons, int[] times)&lt;/code&gt;使用&lt;code&gt;persons&lt;/code&gt;和&lt;code&gt;times&lt;/code&gt;数组初始化对象。&lt;/li&gt;
&lt;li&gt;&lt;code&gt;int q(int t)&lt;/code&gt;根据前面描述的规则，返回在时刻&lt;code&gt;t&lt;/code&gt;在选举中领先的候选人的编号。&lt;/li&gt;
&lt;/ul&gt;</summary>
    
    
    
    
    <category term="leetcode" scheme="https://fly97.cn/tags/leetcode/"/>
    
  </entry>
  
  <entry>
    <title>力扣每日一题之最短补全词</title>
    <link href="https://fly97.cn/p/shortest-completing-word/"/>
    <id>https://fly97.cn/p/shortest-completing-word/</id>
    <published>2021-12-10T11:40:00.000Z</published>
    <updated>2021-12-10T11:40:00.000Z</updated>
    
    <content type="html"><![CDATA[<p>给你一个字符串<code>licensePlate</code>和一个字符串数组<code>words</code>，请你找出并返回<code>words</code>中最短补全词。</p><p>补全词是一个包含<code>licensePlate</code>中所有字母的单词，在所有补全词中，最短的那个就是最短补全词。</p><ul><li>在匹配<code>licensePlate</code>中的数字或空格。</li><li>不区分大小写</li><li>如果某个字母在<code>licensePlace</code>中出现不止一次，那么该字母在补全词中的出现的次数也应当一致或者更多。</li></ul><p>例如：<code>licensePlate</code>=<code>aBc 12c</code>, 那么他的补全词应当包含字母<code>a</code>、<code>b</code>忽略大小写和两个<code>c</code>，可能的补全词有<code>abccdef</code>、<code>caaacab</code>以及<code>cbca</code>。</p><p>请你找出并返回<code>words</code>中的最短补全词。题目数据中保证一定存在一个最短补全词。当有多个单词都符合最短补全词的匹配条件时区<code>words</code>中<strong>最靠前</strong>的那个。</p><a id="more"></a><h2 id="方法一统计字符出现次数">方法一：统计字符出现次数</h2><p>根据题意，先统计<code>licesePlate</code>中每个字母的出现次数（忽略大小写），然后遍历<code>words</code>中的每个单词，若26个英文字母在该单词中初选次数均小于<code>licesePlate</code>中才出现的次数，则该单词是一个补全词。返回最短且最靠前的补全词。</p><figure class="highlight golang"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">package</span> main</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> <span class="string">&quot;unicode&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">shortestCompletingWord</span><span class="params">(licensePlate <span class="keyword">string</span>, words []<span class="keyword">string</span>)</span> <span class="params">(ans <span class="keyword">string</span>)</span></span> &#123;</span><br><span class="line">cnt := [<span class="number">26</span>]<span class="keyword">int</span>&#123;&#125;</span><br><span class="line"><span class="keyword">for</span> _, ch := <span class="keyword">range</span> licensePlate &#123;</span><br><span class="line"><span class="keyword">if</span> unicode.IsLetter(ch) &#123; <span class="comment">// 如果是字母</span></span><br><span class="line">cnt[unicode.ToLower(ch)-<span class="string">&#x27;a&#x27;</span>] += <span class="number">1</span> <span class="comment">// 就让他变成小写</span></span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br><span class="line">next:</span><br><span class="line"><span class="keyword">for</span> _, word := <span class="keyword">range</span> words &#123;</span><br><span class="line">c := [<span class="number">26</span>]<span class="keyword">int</span>&#123;&#125;</span><br><span class="line"><span class="keyword">for</span> _, ch := <span class="keyword">range</span> word &#123;</span><br><span class="line">c[ch-<span class="string">&#x27;a&#x27;</span>] += <span class="number">1</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">for</span> i := <span class="number">0</span>; i &lt; <span class="number">26</span>; i++ &#123;</span><br><span class="line"><span class="keyword">if</span> c[i] &lt; cnt[i] &#123;</span><br><span class="line"><span class="keyword">continue</span> next</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">if</span> ans == <span class="string">&quot;&quot;</span> || <span class="built_in">len</span>(word) &lt; <span class="built_in">len</span>(ans) &#123;</span><br><span class="line">ans = word</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">return</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
    
    
    <summary type="html">&lt;p&gt;给你一个字符串&lt;code&gt;licensePlate&lt;/code&gt;和一个字符串数组&lt;code&gt;words&lt;/code&gt;，请你找出并返回&lt;code&gt;words&lt;/code&gt;中最短补全词。&lt;/p&gt;
&lt;p&gt;补全词是一个包含&lt;code&gt;licensePlate&lt;/code&gt;中所有字母的单词，在所有补全词中，最短的那个就是最短补全词。&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;在匹配&lt;code&gt;licensePlate&lt;/code&gt;中的数字或空格。&lt;/li&gt;
&lt;li&gt;不区分大小写&lt;/li&gt;
&lt;li&gt;如果某个字母在&lt;code&gt;licensePlace&lt;/code&gt;中出现不止一次，那么该字母在补全词中的出现的次数也应当一致或者更多。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;例如：&lt;code&gt;licensePlate&lt;/code&gt;=&lt;code&gt;aBc 12c&lt;/code&gt;, 那么他的补全词应当包含字母&lt;code&gt;a&lt;/code&gt;、&lt;code&gt;b&lt;/code&gt;忽略大小写和两个&lt;code&gt;c&lt;/code&gt;，可能的补全词有&lt;code&gt;abccdef&lt;/code&gt;、&lt;code&gt;caaacab&lt;/code&gt;以及&lt;code&gt;cbca&lt;/code&gt;。&lt;/p&gt;
&lt;p&gt;请你找出并返回&lt;code&gt;words&lt;/code&gt;中的最短补全词。题目数据中保证一定存在一个最短补全词。当有多个单词都符合最短补全词的匹配条件时区&lt;code&gt;words&lt;/code&gt;中&lt;strong&gt;最靠前&lt;/strong&gt;的那个。&lt;/p&gt;</summary>
    
    
    
    
    <category term="leetcode" scheme="https://fly97.cn/tags/leetcode/"/>
    
  </entry>
  
  <entry>
    <title>ubuntu2004安装最新版本MySQL并设置外网访问</title>
    <link href="https://fly97.cn/p/install-newest-mysql-in-ubuntu2004/"/>
    <id>https://fly97.cn/p/install-newest-mysql-in-ubuntu2004/</id>
    <published>2021-12-03T07:39:00.000Z</published>
    <updated>2021-12-03T07:39:00.000Z</updated>
    
    <content type="html"><![CDATA[<p><strong>本文主要介绍了如何在Ubuntu2004上安装最新版本的MySQL以及如何设置MySQL外网访问。</strong></p><p><strong>关键词：MySQL，Linux</strong></p><a id="more"></a><h3 id="更新仓库">更新仓库</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">sudo apt update</span><br></pre></td></tr></table></figure><h3 id="用官方仓库安装-mysql">用官方仓库安装 MySQL</h3><h4 id="下载mysql仓库工具包">下载mysql仓库工具包</h4><p><a href="https%3A//dev.mysql.com/downloads/repo/apt/">下载页面</a></p><p><img src="image-20211203152648123.png" /></p><p>点击Download，选择<strong>No thanks, just start my download.</strong></p><p><img src="image-20211203152704181.png" /></p><h4 id="安装">安装</h4><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">sudo dpkg -i mysql-apt-config*</span><br></pre></td></tr></table></figure><h4 id="更新仓库-1">更新仓库</h4><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">sudo apt update</span><br></pre></td></tr></table></figure><h4 id="安装mysql-server">安装mysql-server</h4><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">sudo apt install mysql-server -y</span><br></pre></td></tr></table></figure><p>这样做会在你的终端中打开包配置的提示。使用向下箭头选择“Ok”选项。</p><p><img src="v2-b09d5038434b3b540638c382677300bb_1440w.jpg" /></p><p>点击回车。这应该会提示你输入密码：这是在为 MySQL 设置 root 密码。</p><p><img src="v2-b22e27255b02ea03066a398e2b90acb8_1440w.jpg" /></p><p>输入密码然后点击 Tab 键去选择“Ok“。点击回车键，你将重新输入密码。</p><p><img src="v2-191abdf08ed88a03f4842b715f229649_1440w.jpg" /></p><p>将会展示一些关于 MySQL Server 的配置信息。再次按下 Tab 去选择 “Ok” 和按下回车键：</p><p><img src="/v2-c950f3613406d1b62dfa22904be045bb_1440w.jpg" /></p><p>第一种是最新的密码加密方法，第二种是sql5版本之前的密码加密方法，这里选择第二个。</p><h3 id="验证安装">验证安装</h3><p>要验证 MySQL 已经正确安装，使用下列命令：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">sudo systemctl status mysql.service</span><br></pre></td></tr></table></figure><p><img src="v2-f506966fb17a1b8a145c84a8f5add612_1440w.jpg" /></p><p>检查到显示running即安装成功</p><h3 id="设置自动启动">设置自动启动</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">sudo systemctl <span class="built_in">enable</span> mysql.service</span><br></pre></td></tr></table></figure><h3 id="设置外网访问">设置外网访问</h3><p>链接mysql以后，在mysql命令行输入以下命令就可以允许外网访问：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">update mysql.user set host&#x3D;&#96;%&#96; where user&#x3D;&#39;root&#39;;</span><br><span class="line">flush privileges;</span><br></pre></td></tr></table></figure><h4 id="原理">原理</h4><p>mysql默认是不允许外网访问的，只允许<code>localhost</code>或者<code>127.0.0.1</code>访问。</p><p>查询默认的mysql.user表，如下表示不允许外网访问，只允许本机访问：</p><p><img src="image-20211203153346215.png" /></p><p>更新访问权限</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">update mysql.user <span class="built_in">set</span> host=`%` <span class="built_in">where</span> user=<span class="string">&#x27;root&#x27;</span>;</span><br></pre></td></tr></table></figure><p><img src="image-20211203153437479.png" /></p><p>更新权限</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">flush privileges;</span><br></pre></td></tr></table></figure>]]></content>
    
    
    <summary type="html">&lt;p&gt;&lt;strong&gt;本文主要介绍了如何在Ubuntu2004上安装最新版本的MySQL以及如何设置MySQL外网访问。&lt;/strong&gt;&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;关键词：MySQL，Linux&lt;/strong&gt;&lt;/p&gt;</summary>
    
    
    
    
    <category term="mysql" scheme="https://fly97.cn/tags/mysql/"/>
    
    <category term="linux" scheme="https://fly97.cn/tags/linux/"/>
    
  </entry>
  
  <entry>
    <title>ubuntu2004安装最新版本Redis并设置开机启动</title>
    <link href="https://fly97.cn/p/install-newest-redis-in-ubuntu2004/"/>
    <id>https://fly97.cn/p/install-newest-redis-in-ubuntu2004/</id>
    <published>2021-12-02T09:11:00.000Z</published>
    <updated>2021-12-02T09:11:00.000Z</updated>
    
    <content type="html"><![CDATA[<p><strong>本文主要介绍了如何在Ubuntu2004上安装最新版本的Redis以及如何设置Redis开机自动启动。</strong></p><p><strong>关键词：Redis，Linux</strong></p><a id="more"></a><h3 id="官方网站">官方网站</h3><p><a href="https://redis.io/download">Redis</a></p><p><img src="image-20211024202247242.png" /></p><h3 id="中文网">中文网</h3><p><a href="http://www.redis.cn/">redis中文官方网站</a></p><blockquote><p>注意，中文网的版本要比官方的版本要低</p></blockquote><p><img src="image-20211024202614937.png" /></p><h3 id="下载并安装">下载并安装</h3><p>这里选择的官网下载的版本。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">wget https://download.redis.io/releases/redis-6.2.6.tar.gz</span><br></pre></td></tr></table></figure><h3 id="直接安装编译好的版本">直接安装编译好的版本</h3><p><a href="https://fly97.coding.net/s/ab90cc76-374d-49d4-a79f-edf6ec5e7d79">jemalloc_5.2.1-1_amd64.deb</a></p><p><a href="https://fly97.coding.net/s/b45f9603-dc1d-4c13-9e32-b94a7e236fa4">redis_6.2.6-1_amd64.deb</a></p><h4 id="解压">解压</h4><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">tar -zxvf redis-6.2.6.tar.gz</span><br></pre></td></tr></table></figure><h4 id="编译安装">编译安装</h4><h5 id="首先查看gcc版本">首先查看gcc版本</h5><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">gcc -v</span><br></pre></td></tr></table></figure><p><img src="image-20211024202923989.png" /></p><p>太旧的版本可能需要升级才能安装。这里用的是<strong>ubuntu20 04 LTS</strong>。</p><h5 id="尝试编译">尝试编译</h5><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">make </span><br></pre></td></tr></table></figure><h6 id="有可能出现的问题">有可能出现的问题</h6><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">zmalloc.h:50:10: fatal error: jemalloc/jemalloc.h:</span><br></pre></td></tr></table></figure><h6 id="解决方法">解决方法</h6><ol type="1"><li>使用以下命令进行编译</li></ol><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">make MALLOC=libc</span><br></pre></td></tr></table></figure><ol start="2" type="1"><li><p>安装jemalloc</p><p><a href="https://blog.csdn.net/SweeNeil/article/details/94648313">jemalloc安装与使用</a></p></li></ol><h4 id="打包成deb包">打包成deb包</h4><p>为什么要打包成deb包呢？可以方便后续卸载等其他操作。</p><h5 id="首先安装checkinstall">首先安装checkinstall</h5><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">sudo apt install checkinstall -y</span><br></pre></td></tr></table></figure><h5 id="执行打包命令">执行打包命令</h5><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">sudo checkinstall -y</span><br></pre></td></tr></table></figure><h5 id="打包完成">打包完成</h5><p><img src="image-20211024204914879.png" /></p><p>如果想卸载就执行执行</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">sudo dpkg -r redis</span><br></pre></td></tr></table></figure><h4 id="检测是否安装完毕">检测是否安装完毕</h4><p>运行</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">redis-server</span><br></pre></td></tr></table></figure><p><img src="image-20211024211634295.png" /></p><p>如有上图显示则说明运行成功</p><h3 id="后续的配置">后续的配置</h3><h4 id="配置文件">配置文件</h4><p>将配置文件移动到系统目录下</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">mkdir /etc/redis </span><br></pre></td></tr></table></figure><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">mv redis.conf /etc/redis/</span><br></pre></td></tr></table></figure><h5 id="修改监听ip">修改监听IP</h5><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">vim redis.conf</span><br></pre></td></tr></table></figure><p><img src="image-20211203163721863.png" /></p><h5 id="修改端口">修改端口</h5><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">vim redis.conf</span><br></pre></td></tr></table></figure><p><img src="image-20211203163751891.png" /></p><h4 id="设置开机自启动">设置开机自启动</h4><p>将redis使用service进行管理</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">touch redis.service</span><br></pre></td></tr></table></figure><p><code>redis.service</code>文件的格式如下：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line">[Unit]</span><br><span class="line">Description=Redis data structure server</span><br><span class="line">Documentation=https://redis.io</span><br><span class="line">After=network.target</span><br><span class="line"></span><br><span class="line">[Service]</span><br><span class="line">User=root</span><br><span class="line">Type=simple</span><br><span class="line">ExecStart=/usr/<span class="built_in">local</span>/bin/redis-server /etc/redis/redis.conf</span><br><span class="line">ExecStop=/usr/<span class="built_in">local</span>/bin/redis-cli -p 6379 shutdown</span><br><span class="line"></span><br><span class="line">[Install]</span><br><span class="line">WantedBy=multi-user.target</span><br></pre></td></tr></table></figure><p>将redis.service移动到系统目录下</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">mv redis.service /etc/systemd/system/redis.service</span><br></pre></td></tr></table></figure><p>设置自启动</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">sudo systemctl <span class="built_in">enable</span> redis.service</span><br></pre></td></tr></table></figure><p>查看是否启动成功</p><p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">sudo systemctl status redis.service</span><br></pre></td></tr></table></figure></p><p><img src="image-20211024220501733.png" /></p><p>完。</p>]]></content>
    
    
    <summary type="html">&lt;p&gt;&lt;strong&gt;本文主要介绍了如何在Ubuntu2004上安装最新版本的Redis以及如何设置Redis开机自动启动。&lt;/strong&gt;&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;关键词：Redis，Linux&lt;/strong&gt;&lt;/p&gt;</summary>
    
    
    
    
    <category term="linux" scheme="https://fly97.cn/tags/linux/"/>
    
    <category term="redis" scheme="https://fly97.cn/tags/redis/"/>
    
  </entry>
  
  <entry>
    <title>go语言设计与实现之反射</title>
    <link href="https://fly97.cn/p/Reflections-in-go-language-base/"/>
    <id>https://fly97.cn/p/Reflections-in-go-language-base/</id>
    <published>2021-12-02T09:11:00.000Z</published>
    <updated>2021-12-02T09:11:00.000Z</updated>
    
    <content type="html"><![CDATA[<p>写日志收集项目的时候遇到了一个关于<code>reflect</code>的问题，于是有了这一篇文章。</p><p><strong>本文主要介绍go语言中reflect的基本用法，以及在使用反射特性时遇到的一些坑。</strong></p><p><strong>关键词：反射</strong></p><a id="more"></a><h2 id="问题引入">问题引入</h2><p>使用<a href="https://ini.unknwon.io/">go-ini</a>时遇到了一个问题：</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">var</span> cfg *conf.AppConf</span><br><span class="line">err := ini.MapTo(cfg, <span class="string">&quot;./conf/config.ini&quot;</span>)</span><br></pre></td></tr></table></figure><p>此时会报错：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">panic: reflect: call of reflect.Value.Type on zero Value</span><br></pre></td></tr></table></figure><p>修改成以下内容错误就会消失了</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">var</span> cfg = <span class="built_in">new</span>(conf.AppConf) <span class="comment">// 要为cfg创建一个地址</span></span><br><span class="line"><span class="keyword">var</span> cfg *conf.AppConf</span><br><span class="line">err := ini.MapTo(cfg, <span class="string">&quot;./conf/config.ini&quot;</span>)</span><br></pre></td></tr></table></figure><p>以下是两种声明方式的区别：</p><ul><li>使用<code>var</code>声明一个变量的指针，默认值是<code>nil</code>，此时gc并没有为<code>cfg</code>分配内存；</li><li>使用<code>new()</code>创建一个<code>conf.AppConf</code>的对象（此时gc已经为<code>cfg</code>开辟了内存），同时获得这个对象的指针；</li></ul><p>这让我对<strong>反射</strong>的用法产生了疑问。下面引出本文的核心：<strong>反射是什么？</strong></p><h2 id="反射是什么">反射是什么</h2><p>反射是指程序运行期对程序本身进行访问和修改的能力，程序在编译时，变量被转换为内存地址，变量名不会被编译器写入到可执行部分。在运行程序时，程序无法获取自身的信息。</p><p>支持反射的语言可以在程序编译期将变量的反射信息如<strong>字段名称、类型信息、结构体信息</strong>等整合到可执行文件中，并给程序提供<strong>接口</strong>访问反射信息，这样就可以在程序运行期间获取类型的反射信息，并且有能力修改他们。</p><p>Go程序在运行期间使用<strong>reflect包</strong>访问程序的反射信息。</p><p>Go语言中的空接口可以存储任意类型的变量，反射可以在程序运行时动态的获取一个变量的<strong>类型信息和值信息</strong>。</p><h3 id="reflect包">reflect包</h3><p>Go语言的反射机制中，任何接口值都是<code>一个具体类型</code>和<code>一个具体类型的值</code> 两部分组成。</p><p>Go语言中反射的相关功能是由内置的reflect包提供，任意接口值在反射中都可以理解为<code>reflect.Type</code>和<code>reflect.Value</code>两部分组成，并且reflect包里提供了以下两个函数来获取任意对象的Value和Type：</p><ul><li>reflect.TypeOf：能获取类型信息</li><li>reflect.ValueOf：能获取数据的运行时表示</li></ul><p>两个类型是<code>reflect.Type</code>和<code>reflect.Value</code>，他们和函数是一一对应的关系：</p><p><img src="golang-reflection.png" /></p><h4 id="reflect.type">reflect.Type</h4><p>类型<code>reflect.Type</code>是反射包定义的一个<strong>接口</strong>，可以使用<code>reflect.TypeOf</code>函数获取任意变量的类型， <code>reflect.Type</code>接口中定义了一些有趣的方法：<code>MethodByName</code>可以获取当前类型对应方法的引用、<code>Implements</code>可以判断当前类型是否实现了某个接口：</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">type</span> Type <span class="keyword">interface</span> &#123;</span><br><span class="line">        Align() <span class="keyword">int</span></span><br><span class="line">        FieldAlign() <span class="keyword">int</span></span><br><span class="line">        Method(<span class="keyword">int</span>) Method</span><br><span class="line">        MethodByName(<span class="keyword">string</span>) (Method, <span class="keyword">bool</span>)</span><br><span class="line">        NumMethod() <span class="keyword">int</span></span><br><span class="line">        ...</span><br><span class="line">        Implements(u Type) <span class="keyword">bool</span></span><br><span class="line">        ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="reflect.value">reflect.Value</h4><p>反射包中<code>reflect.Value</code>的类型与<code>reflect.Type</code>不同，他被声明成了<strong>结构体</strong>。这个结构体没有对外暴露的字段，但是提供了获取或者写入数据的方法。</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">type</span> Value <span class="keyword">struct</span>&#123;</span><br><span class="line">    <span class="comment">// 包含过滤的或者未导出的字段</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(v Value)</span> <span class="title">Addr</span><span class="params">()</span> <span class="title">Value</span></span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(v Value)</span> <span class="title">Bool</span><span class="params">()</span> <span class="title">bool</span></span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(v Value)</span> <span class="title">Bytes</span><span class="params">()</span> []<span class="title">byte</span></span></span><br><span class="line">... </span><br></pre></td></tr></table></figure><p>反射包中所有方法都是围绕<code>reflect.Type</code>和<code>reflect.Value</code>两个类型设计的。</p><p>通过<code>reflect.TypeOf</code>、<code>reflect.ValueOf</code>可以将一个普通变量转换成<code>reflect.Type</code>和<code>reflect.Value</code>，随后就可以使用反射包中的方法对他们进行复杂的操作。</p><h3 id="三大法则">三大法则</h3><p>运行时反射时程序在运行期间检查自身结构的一种方式，反射带来的灵活性是一把双刃剑，<strong>反射作为一种元编程方式可以减少重复代码</strong>，但是过量的使用反射会使我们的程序逻辑难以理解并且运行缓慢。使用反射，一般要遵守<strong>反射三原则</strong>：</p><ol type="1"><li><p>从<code>interface&#123;&#125;</code>变量可以反射出反射对象；</p></li><li><p>从反射对象可以获取<code>interface&#123;&#125;</code>变量；</p></li><li><p>要修改反射对象，其值必须可设置；</p></li></ol><h4 id="第一法则">第一法则</h4><p>为什么是从<code>interface&#123;&#125;</code>变量到反射对象？当执行<code>reflect.ValueOf(1)</code>是，虽然看起来是获取了基本类型<code>int</code>对应的反射类型，但是由于<code>reflect.TypeOf</code>、<code>reflect.ValueOf</code>的两个方法的入参都是<code>interface&#123;&#125;</code>类型，所以在方法执行的过程中发生了类型转换。</p><p>由于Go语言的<strong>函数调用都是值传递</strong>的，那么变量在函数调用时会进行类型转换，基本类型会转换成<code>interface&#123;&#125;</code>类型，这就是为什么第一条原则是<strong>从接口到反射对象</strong>。</p><p>上面提到的<code>reflect.TypeOf</code>和<code>reflect.ValueOf</code>就能完成这里的<strong>转换</strong>。Go语言的类型和反射类型处于两个不同的世界，这两个函数是链接这两个函数的桥梁。</p><p><img src="golang-interface-to-reflection.png" /></p><p>通过以下例子介绍他们的作用，<code>reflect.TypeOf</code>获取了变量author的类型，<code>reflect.ValueOf</code>获取了变量的值draven。如果我们知道了一个变量的<strong>值和类型</strong>，那么就意味着我们知道了这个变量的<strong>全部信息</strong>。</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">    author := <span class="string">&quot;draven&quot;</span></span><br><span class="line">    fmt.Println(<span class="string">&quot;TypeOf author:&quot;</span>, reflect.TypeOf(author))</span><br><span class="line">    fmt.Println(<span class="string">&quot;ValueOf author:&quot;</span>, reflect.ValueOf(author))</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">$ go run main.go</span><br><span class="line">TypeOf author: string</span><br><span class="line">ValueOf author: draven</span><br></pre></td></tr></table></figure><p>拿到变量的类型以后，可以通过Method方法获得类型实现的方法，通过Field获取类型包含的全部字段，对于不同的类型， 可以通过调用不同的方法来获取相关信息：</p><ul><li>结构体：获取字段的数量并通过下标和字段名获取字段名StructField；</li><li>哈希表：获取哈希表的Key类型；</li><li>函数或方法：获取入参和返回值的类型；</li><li>...</li></ul><p>总而言之，使用<code>reflect.TypeOf</code>和<code>reflect.ValueOf</code>能够获取</p><p>Go语言中的<strong>变量对应的反射对象</strong>，我们就能获得当前的数据类型和操作，并可以使用这些<strong>运行时</strong>获取结构执行方法。</p><h4 id="第二法则">第二法则</h4><p>反射的第二法则是可以从反射对象获取<code>interface&#123;&#125;</code>变量，reflect中的<code>reflect.Value.Interface()</code>可以将反射对象还原成接口类型的变量：</p><p><img src="golang-reflection-to-interface.png" /></p><p>调用<code>reflect.Value.Interface</code>方法只能获得<code>interface&#123;&#125;</code>类型的变量，如果还想将其还原成最原始的状态还需要对其进行显式类型转换：</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">v := reflect.ValueOf(<span class="number">1</span>)</span><br><span class="line">v.Interface().(<span class="keyword">int</span>)</span><br></pre></td></tr></table></figure><p>从反射对象到接口值的过程是从接口值到反射对象的镜面过程，两个过程都需要经历两次转换：</p><ul><li>从接口值到反射对象：<ul><li>从基本类型到接口类型的类型转换；</li><li>从接口类型到反射对象的转换；</li></ul></li><li>从反射对象到接口值：<ul><li>从反射对象转换成接口类型；</li><li>通过<strong>显式类型转换</strong>成原始类型；</li></ul></li></ul><p><img src="golang-bidirectional-reflection.png" /></p><p>不是所有的变量都需要类型转换这一过程。如果变量本身就是<code>interface&#123;&#125;</code>类型的，那么它不需要类型转换。只由<strong>将反射对象转换成基本类型</strong>时才需要显式的转换操作。</p><h4 id="第三法则">第三法则</h4><p>Go语言反射的最后一条法则是与值是否可以被更改有关，如果我们想要更新一个<code>reflect.Value</code>，那么它持有的值一定是可以被更新的，假设由以下代码：</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">    i := <span class="number">1</span></span><br><span class="line">    v := reflect.ValueOf(i)</span><br><span class="line">    v.SetInt(<span class="number">10</span>)</span><br><span class="line">    fmt.Println(i)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="variable">$go</span> run main.go</span><br><span class="line">panic: reflect: reflect.flag.mustBeAssignable using unaddressable value</span><br></pre></td></tr></table></figure><p>上述代码出错的原因是：由于<strong>Go语言的函数调用都是传值的</strong>，所以我们得到的反射跟最开始的变量没有任何关系，那么<strong>直接修改反射对象无法改变原始变量</strong>，程序就会报错。</p><p>想要修改原变量只能使用如下的方法：</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">    i := <span class="number">1</span></span><br><span class="line">    v := reflect.ValueOf(&amp;i)</span><br><span class="line">    v.Elem().SetInt(<span class="number">10</span>)</span><br><span class="line">    fmt.Println(i)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="variable">$go</span> run main.go</span><br><span class="line">10</span><br></pre></td></tr></table></figure><ol type="1"><li>调用<code>reflect.ValueOf</code>获取变量指针（传指针）；</li><li>调用<code>reflect.Value.Elem</code>获取指针指向的变量；</li><li>调用<code>reflect.Value.SetInt</code>更新变量的值；</li></ol><p>由于Go语言的函数调用都是值传递的，所以我们只能用迂回的方式改变原变量：先获取指针对应的<code>reflect.Value</code>，再通过<code>reflect.Value.Elem</code>方法得到可以被设置的变量，通过以下代码理解这个过程：</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">    i := <span class="number">1</span></span><br><span class="line">    v := &amp;<span class="number">1</span></span><br><span class="line">    &amp;v = <span class="number">10</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>如果不能直接操作<code>i</code>变量修改其持有的值，就只能获取<code>i</code>变量所在的地址并使用<code>*v</code>修改所在的地址中存储的整数。</p><h3 id="类型和值">类型和值</h3><p>Go语言的<code>interface&#123;&#125;</code>类型在语言内部是通过<code>reflect.emptyInterface</code>结构体表示的，其中的<code>typ</code>字段用于表示变量的类型，另一个<code>word</code>字段表示指向内部封装的数据：</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">type</span> emptyInterface <span class="keyword">struct</span> &#123;</span><br><span class="line">typ  *rtype</span><br><span class="line">word unsafe.Pointer</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="reflect.typeof实现">reflect.TypeOf实现</h4><p>用于获取变量类型的<code>reflect.TypeOf</code>函数将传入的变量隐式转换成<code>reflect.emptyInterface</code>类型并<strong>返回</strong>其中存储的类型信息<code>reflect.rtype</code>：</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">TypeOf</span><span class="params">(i <span class="keyword">interface</span>&#123;&#125;)</span> <span class="title">Type</span></span> &#123;</span><br><span class="line">    eface := *(*emptyInterface)(unsafe.Pointer(&amp;i))</span><br><span class="line">    <span class="keyword">return</span> toType(eface.typ)</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">toType</span><span class="params">(t *rtype)</span> <span class="title">Type</span></span>&#123;</span><br><span class="line">    <span class="keyword">if</span> t == <span class="literal">nil</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">nil</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> t</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><code>reflect.rtype</code>是一个实现了<code>reflect.Type</code>接口的结构体，该结构体实现的<code>reflect.rtype.String</code>方法可以帮助我们获取当前类型的名称：</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(t *rtype)</span> <span class="title">String</span><span class="params">()</span> <span class="title">string</span></span> &#123;</span><br><span class="line">    s := t.nameOff(t.str).name()</span><br><span class="line">    <span class="keyword">if</span> t.tflag&amp;tflagExtraStar != <span class="number">0</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> s[<span class="number">1</span>:]</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> s</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>reflect.TypeOf的实现原理并不复杂，它只是将一个<code>interface&#123;&#125;</code>变量转换成了内部的<code>reflect.emptyInterface</code> 表示，然后从中获取相应的类型信息。</p><h4 id="reflect.valueof实现">reflect.ValueOf实现</h4><p>用于接口值的<code>reflect.Value</code>的函数实现<code>reflect.ValuesOf</code>实现也非常简单，在该函数中先调用<code>reflect.escapes</code>保证当前值逃逸到堆上，然后通过<code>reflect.unpackEface</code>从接口中获取<code>reflect.Value</code>结构体：</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">ValueOf</span><span class="params">(i <span class="keyword">interface</span>&#123;&#125;)</span> <span class="title">Value</span></span> &#123;</span><br><span class="line">    <span class="keyword">if</span> i == <span class="literal">nil</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> Value&#123;&#125;</span><br><span class="line">    &#125;</span><br><span class="line">    escapes(i)</span><br><span class="line">    <span class="keyword">return</span> unpackEface(is)</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">unpackEface</span><span class="params">(i <span class="keyword">interface</span>&#123;&#125;)</span> <span class="title">Value</span></span> &#123;</span><br><span class="line">    e := *(emptyInterface)(unsafe.Pointer(&amp;i))</span><br><span class="line">    t := e.typ</span><br><span class="line">    <span class="keyword">if</span> t == <span class="literal">nil</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> Value&#123;&#125;</span><br><span class="line">    &#125;</span><br><span class="line">    f := flag(t.Kind())</span><br><span class="line">    <span class="keyword">if</span> ifaceIndir(t) &#123;</span><br><span class="line">        f |= flagIndir</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> Value&#123;t, e.Word, f&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><code>reflect.unpackEface</code>会将传入的接口转换成<code>reflect.emptyInterface</code>，然后将具体类型和指针包装成<code>reflect.Value</code>结构体后返回。</p><h4 id="编译调用">编译调用</h4><p><code>reflect.TypeOf</code>和<code>reflect.ValueOf</code>的实现都很简单，我们已经分析了这两个函数的实现，现在需要了解编译器在调用函数之前做了哪些工作：</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">    i := <span class="number">20</span></span><br><span class="line">    _ = reflect.TypeOf(i)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="variable">$go</span> build -gcflags=<span class="string">&quot;-S -N&quot;</span> main.go</span><br><span class="line">...</span><br><span class="line">MOVQ<span class="variable">$20</span>, <span class="string">&quot;&quot;</span>..autotmp_20+56(SP) // autotmp = 20</span><br><span class="line">LEAQtype.int(SB), AX           // AX = type.int(SB)</span><br><span class="line">MOVQAX, <span class="string">&quot;&quot;</span>..autotmp_19+280(SP) // autotmp_19+280(SP) = type.int(SB)</span><br><span class="line">LEAQ<span class="string">&quot;&quot;</span>..autotmp_20+56(SP), CX  // CX = 20</span><br><span class="line">MOVQCX, <span class="string">&quot;&quot;</span>..autotmp_19+288(SP) // autotmp_19+288(SP) = 20</span><br><span class="line">...</span><br></pre></td></tr></table></figure><p>从上面这段截取的汇编语言，我们可以发现在函数调用之前就已经发生了类型转换，上述指令将int类型的变量转换成了占用16字节的<code>autotmp_19+280(SP) ~ autotmp_19+288(SP)</code> 接口，两个<code>LEAQ</code>指令分别获取了类型的指针<code>type.int(SB)</code>以及变量i所在的地址。</p><p>当我们想要将一个变量转换成反射对象时，Go语言会在<strong>编译期间</strong>完成类型转换，将变量的类型和值转换成了<code>interface&#123;&#125;</code>并等待<strong>运行期间使用</strong><code>reflect</code>包获取接口中存储的信息。</p><h3 id="更新变量">更新变量</h3><p>当我们想要更新<code>reflect.Value</code>时，就需要调用<code>reflect.Value.Set</code>更新反射对象，该方法会调用</p><ul><li><strong>reflect.flag.mustBeAssignable</strong>：用于检查当前反射对象是否可以被设置</li><li><strong>reflect.flag.mustBeExported</strong>：用于检查字段是否对外公开</li></ul><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(v Value)</span> <span class="title">Set</span><span class="params">(x Value)</span></span> &#123;</span><br><span class="line">    v.mustBeAssignable()</span><br><span class="line">    v.mustBeExported()</span><br><span class="line">    <span class="keyword">var</span> target unsafe.Pointer</span><br><span class="line">    <span class="keyword">if</span> v.Kind() == Interface&#123;</span><br><span class="line">        target = v.ptr</span><br><span class="line">    &#125;</span><br><span class="line">    x = x.assignTo(<span class="string">&quot;reflect.Set&quot;</span>, v.typ, target)</span><br><span class="line">    typedmemmove(v.typ, v.ptr, x.ptr)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><code>reflect.Value.Set</code>会调用<code>reflect.Value.assignTo</code>并返回一个新的反射对象，这个反射对象指针会直接覆盖原反射变量。</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(v Value)</span> <span class="title">assignTo</span><span class="params">(context <span class="keyword">string</span>, dst *rtype, target unsafe.Pointer)</span> <span class="title">Value</span></span> &#123;</span><br><span class="line">    ...</span><br><span class="line">    <span class="keyword">switch</span> &#123;</span><br><span class="line">    <span class="keyword">case</span> directlyAssignable(dst, v.typ):</span><br><span class="line">        ...</span><br><span class="line">        <span class="keyword">return</span> Value&#123;dst, v.ptr. f1&#125;</span><br><span class="line">    <span class="keyword">case</span> implements(dst, v.typ):</span><br><span class="line">        <span class="keyword">if</span> v.kind() == Interface &amp;&amp; v.IsNil() &#123;</span><br><span class="line">            <span class="keyword">return</span> Value&#123;dst, <span class="literal">nil</span>, flag(Interface)&#125;</span><br><span class="line">        &#125;</span><br><span class="line">        x := valueInterface(v, <span class="literal">false</span>)</span><br><span class="line">        <span class="keyword">if</span> dst.NumMethod() == <span class="number">0</span> &#123;</span><br><span class="line">            *(*<span class="keyword">interface</span>&#123;&#125;)(target) = x</span><br><span class="line">        &#125;<span class="keyword">else</span> &#123;</span><br><span class="line">            ifaceE2I(dst, x, target)</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> Value&#123;dst, target, flagIndir | flag(Interface)&#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">panic</span>(context+ <span class="string">&quot;:value of type &quot;</span> + v.typ.String() + <span class="string">&quot; is not assignable to type &quot;</span>+ dst.String())</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><code>reflect.Value.assignTo</code> 会根据当前和被设置的反射对象类型创建一个新的<code>reflect.Value</code>结构体：</p><ul><li>如果反射对象的类型是可以被替换的，就会直接返回目标对象；</li><li>如果当前接口对象是接口并且目标对象实现了接口，就会把目标对象简单包装成接口值；</li></ul><p>在变量更新的过程中，<code>reflect.Value.assignTo</code>返回的 <code>reflect.Value</code>中的指针会覆盖当前反射对象中的指针实现变量的更新。</p><h3 id="实现协议">实现协议</h3><p><code>reflect</code>包提供了<code>reflect.rtype.Implements</code>方法可以用于判断某些类型是否遵循<strong>特定的接口</strong>。在Go语言中获取结构体的反射类型<code>reflect.Type</code>还是比较容易的，但是想要获得接口类型就需要通过以下方式：</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">reflect.TypeOf((*&lt;<span class="keyword">interface</span>&gt;)(<span class="literal">nil</span>)).Elem()</span><br></pre></td></tr></table></figure><p>通过一个例子来介绍如何判断一个类型是否实现了某个接口。</p><p>我们通过一个例子来介绍如何判断一个类型是否实现了某个接口。假设我们需要判断如下代码中的<code>CustomError</code>是否实现了Go语言标准库中的<code>error</code>接口：</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">type</span> CustomError <span class="keyword">struct</span>&#123;&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(*CustomError)</span> <span class="title">Error</span><span class="params">()</span> <span class="title">string</span></span> &#123; <span class="comment">// 实现了error接口</span></span><br><span class="line"><span class="keyword">return</span> <span class="string">&quot;&quot;</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">typeOfError := reflect.TypeOf(((*error)(<span class="literal">nil</span>))).Elem() <span class="comment">// 把nil强制转换成error接口类型</span></span><br><span class="line">customErrorPtr := reflect.TypeOf(&amp;CustomError&#123;&#125;)</span><br><span class="line">customError := reflect.TypeOf(CustomError&#123;&#125;)</span><br><span class="line"></span><br><span class="line">fmt.Println(customErrorPtr.Implements(typeOfError)) <span class="comment">// true</span></span><br><span class="line">fmt.Println(customError.Implements(typeOfError))  <span class="comment">// false</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>上述代码的运行结果说明了：</p><ul><li><code>CustomError</code>类型<strong>没有实现</strong><code>error</code>接口；</li><li><code>*CustomError</code>类型<strong>实现了</strong><code>error</code>接口；</li></ul><p>分析一下<code>reflect.rtype.Implements</code>方法的工作原理：</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(t *rtype)</span> <span class="title">Implements</span><span class="params">(u Type)</span> <span class="title">bool</span></span> &#123;</span><br><span class="line">    <span class="keyword">if</span> u == <span class="literal">nil</span> &#123;</span><br><span class="line">        <span class="built_in">panic</span>(<span class="string">&quot;reflect: nil type passed to Type.Implements&quot;</span>)</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> u.Kind() != Interface &#123;</span><br><span class="line">        <span class="built_in">panic</span>(<span class="string">&quot;reflect: non-interface type passed to Type.Implements&quot;</span>)</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> implements(u.(*rtype), t)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><code>reflect.rtype.Implements</code>会检查传入的类型是不是接口，如果不是接口或者是空值就会直接<strong>panic</strong>。</p><p>在参数没有问题的情况下，上述方法会调用私有函数<code>reflect.implements</code>判断类型之间<strong>是否有实现关系</strong>：</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">implements</span><span class="params">(T, V *rtype)</span> <span class="title">bool</span></span> &#123;</span><br><span class="line">t := (*interfaceType)(unsafe.Pointer(T))</span><br><span class="line"><span class="keyword">if</span> <span class="built_in">len</span>(t.methods) == <span class="number">0</span> &#123;</span><br><span class="line"><span class="keyword">return</span> <span class="literal">true</span></span><br><span class="line">&#125;</span><br><span class="line">...</span><br><span class="line">v := V.uncommon()</span><br><span class="line">i := <span class="number">0</span></span><br><span class="line">vmethods := v.methods()</span><br><span class="line"><span class="keyword">for</span> j := <span class="number">0</span>; j &lt; <span class="keyword">int</span>(v.mcount); j++ &#123;</span><br><span class="line">tm := &amp;t.methods[i]</span><br><span class="line">tmName := t.nameOff(tm.name)</span><br><span class="line">vm := vmethods[j]</span><br><span class="line">vmName := V.nameOff(vm.name)</span><br><span class="line"><span class="keyword">if</span> vmName.name() == tmName.name() &amp;&amp; V.typeOff(vm.mtyp) == t.typeOff(tm.typ) &#123;</span><br><span class="line"><span class="keyword">if</span> i++; i &gt;= <span class="built_in">len</span>(t.methods) &#123;</span><br><span class="line"><span class="keyword">return</span> <span class="literal">true</span></span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">return</span> <span class="literal">false</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>如果接口中不包含任何方法，就意味着这是一个空的接口，任意类型都自动实现该接口，此时会直接返回<code>true</code>。</p><p><img src="golang-type-implements-interface.png" /></p><p>在其他情况下，由于方法都是按照<strong>字母序</strong>存储的，<code>reflect.implements</code>会维护两个用于遍历接口和类型方法的索引<code>i</code>和 <code>j</code>判断类型是否实现了接口，因为最多只会进行<code>n</code>次比较（类型的方法数量），所以整个过程的时间复杂度是O(n)。</p><h3 id="方法调用">方法调用</h3><p>作为一门静态语言，如果想要通过<code>reflect</code>包利用反射在运行期间执行方法不是一件容易的事情，下面的代码使用反射来执行<code>Add(0, 1)</code>函数：</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">Add</span><span class="params">(a, b <span class="keyword">int</span>)</span> <span class="title">int</span></span> &#123;<span class="keyword">return</span> a + b&#125;</span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">    v := reflect.ValueOf(Add) <span class="comment">// 1. 获取函数反射对象</span></span><br><span class="line">    <span class="keyword">if</span> v.Kind() != reflect.Func &#123;</span><br><span class="line">        <span class="keyword">return</span> </span><br><span class="line">    &#125;</span><br><span class="line">    t := v.Type()</span><br><span class="line">    argv := <span class="built_in">make</span>([]reflect.Value, t.NumIn()) <span class="comment">// 2. 获取函数入参个数，并根据数目创建[]reflect.Value切片</span></span><br><span class="line">    <span class="keyword">for</span> i := <span class="keyword">range</span> argv &#123;</span><br><span class="line">        <span class="keyword">if</span> t.In(i).Kind() != reflect.Int &#123;</span><br><span class="line">            <span class="keyword">return</span></span><br><span class="line">        &#125;</span><br><span class="line">        argv[i] = reflect.ValueOf(i) <span class="comment">// 3. 设置入参数组的各个参数</span></span><br><span class="line">    &#125;</span><br><span class="line">    result := v.Call(argv) <span class="comment">// 4. 调用方法并传入参数列表</span></span><br><span class="line">    <span class="keyword">if</span> <span class="built_in">len</span>(result) != <span class="number">1</span> || result[<span class="number">0</span>].Kind() != reflect.Int&#123; <span class="comment">// 5.获取返回值数组，验证数组的长度以及类型；</span></span><br><span class="line">        <span class="keyword">return</span> </span><br><span class="line">    &#125;</span><br><span class="line">    fmt.Println(result[<span class="number">0</span>].Int())  <span class="comment">// 1 // 5. 打印返回值数组中的数据</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>可以概括为以下几个步骤：</p><ol type="1"><li>通过调用<code>reflect.ValueOf</code>获取函数<code>Add</code>对应的反射对象；</li><li>通过调用<code>reflect.rtype.NumIn</code>获取函数的入参个数；</li><li>多次调用<code>reflect.ValueOf</code>函数逐一设置<code>argv数组</code>的各个参数；</li><li>调用反射对象<code>Add</code>的<code>reflect.Value.Call</code>方法并传入参数列表；</li><li>获取返回值数组、验证数组的长度以及类型并打印其中的数据；</li></ol><p>使用反射来调用方法非常复杂，原本只需要一行代码就能完成的工作，现在需要十几行代码才能完成，但这也是在静态语言中使用动态特性所需要付出的成本。</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(v Value)</span> <span class="title">Call</span><span class="params">(in []Value)</span> []<span class="title">Value</span></span> &#123;</span><br><span class="line">    v.mustBe(Func)</span><br><span class="line">    v.mustBeExported()</span><br><span class="line">    <span class="keyword">return</span> v.call(<span class="string">&quot;Call&quot;</span>, in)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>reflect.Value.Call是运行时调用方法的入口，它通过两个<strong>mustBe</strong>开头的方法确定了当前反射对象的类型是函数以及可见性，随后调用reflect.Value.call完成方法调用，这个私有方法的执行过程会分成以下的几个部分：</p><ol type="1"><li>检查输入参数以及类型的合法性；</li><li>将传入的 <code>reflect.Value</code>参数数组设置到栈上；</li><li>通过函数指针和输入参数调用函数；</li><li>从栈上获取函数的返回值；</li></ol><p>下面将按照以上的顺序分析<code>reflect</code>进行函数调用的过程。</p><h4 id="参数检查">参数检查</h4><p>参数检查是通过反射调用方法的第一步，在参数检查期间会从反射对象取出当前的函数指针<code>unsafe.Pointer</code>，如果该函数指针是方法，那么会通过<code>reflect.methodReceiver</code>获取方法的接收者和函数指针。</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(v Value)</span> <span class="title">call</span><span class="params">(op <span class="keyword">string</span>, in []Value)</span> []<span class="title">Value</span></span> &#123;</span><br><span class="line">t := (*funcType)(unsafe.Pointer(v.typ)) <span class="comment">// 强制转换成函数指针</span></span><br><span class="line">...</span><br><span class="line"><span class="keyword">if</span> v.flag&amp;flagMethod != <span class="number">0</span> &#123;  <span class="comment">// 如果该函数指针是方法</span></span><br><span class="line">rcvr = v</span><br><span class="line">rcvrtype, t, fn = methodReceiver(op, v, <span class="keyword">int</span>(v.flag)&gt;&gt;flagMethodShift) <span class="comment">// 获取方法指针的接收者和函数指针</span></span><br><span class="line">&#125; <span class="keyword">else</span> &#123;</span><br><span class="line">...</span><br><span class="line">&#125;</span><br><span class="line">n := t.NumIn()</span><br><span class="line"><span class="keyword">if</span> <span class="built_in">len</span>(in) &lt; n &#123; <span class="comment">// 检查入参个数是否匹配</span></span><br><span class="line"><span class="built_in">panic</span>(<span class="string">&quot;reflect: Call with too few input arguments&quot;</span>)</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">if</span> <span class="built_in">len</span>(in) &gt; n &#123; <span class="comment">// 检查入参个数是否匹配</span></span><br><span class="line"><span class="built_in">panic</span>(<span class="string">&quot;reflect: Call with too many input arguments&quot;</span>)</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">for</span> i := <span class="number">0</span>; i &lt; n; i++ &#123;</span><br><span class="line"><span class="keyword">if</span> xt, targ := in[i].Type(), t.In(i); !xt.AssignableTo(targ) &#123;  <span class="comment">// 检查函数入参类型是否和函数签名匹配</span></span><br><span class="line"><span class="built_in">panic</span>(<span class="string">&quot;reflect: &quot;</span> + op + <span class="string">&quot; using &quot;</span> + xt.String() + <span class="string">&quot; as type &quot;</span> + targ.String())</span><br><span class="line">&#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>上述方法会检查入参个数的个数以及参数的类型与签名中的类型是否可以匹配，任何参数的不匹配都导致整个程序的崩溃中止。</p><h4 id="准备参数">准备参数</h4><p>当我们已经对当前方法的参数完成验证后，就会进入函数调用的下一个阶段，为函数调用准备参数。<strong>Go语言的函数调用惯例是函数或者方法在调用时，所有的参数都会被以依次放到栈上。</strong></p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br></pre></td><td class="code"><pre><span class="line">nout := t.NumOut()</span><br><span class="line">frametype, _, retOffset, _, framePool := funcLayout(t, rcvrtype) <span class="comment">// 计算当前函数需要的参数和返回值的栈布局，也就是每一个参数和返回值所占的空间大小</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> args unsafe.Pointer</span><br><span class="line"><span class="keyword">if</span> nout == <span class="number">0</span> &#123; <span class="comment">// 若当前函数有返回值</span></span><br><span class="line">args = framePool.Get().(unsafe.Pointer) <span class="comment">// 那么就为当前函数的参数和返回值分配内存空间</span></span><br><span class="line">&#125; <span class="keyword">else</span> &#123; </span><br><span class="line">args = unsafe_New(frametype)  <span class="comment">// 否则就全新开辟一块内存空间</span></span><br><span class="line">&#125;</span><br><span class="line">off := <span class="keyword">uintptr</span>(<span class="number">0</span>)</span><br><span class="line"><span class="keyword">if</span> rcvrtype != <span class="literal">nil</span> &#123; <span class="comment">// 如果当前函数是方法</span></span><br><span class="line">storeRcvr(rcvr, args) <span class="comment">// 将方法的接收者拷贝到内存中</span></span><br><span class="line">off = ptrSize</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">for</span> i, v := <span class="keyword">range</span> in &#123; <span class="comment">// 将所有函数入参按照顺序依次拷贝到args内存中</span></span><br><span class="line">targ := t.In(i).(*rtype)</span><br><span class="line">a := <span class="keyword">uintptr</span>(targ.align)</span><br><span class="line">off = (off + a - <span class="number">1</span>) &amp;^ (a - <span class="number">1</span>)</span><br><span class="line">n := targ.size</span><br><span class="line">...</span><br><span class="line">addr := add(args, off, <span class="string">&quot;n &gt; 0&quot;</span>)</span><br><span class="line">v = v.assignTo(<span class="string">&quot;reflect.Value.Call&quot;</span>, targ, addr)</span><br><span class="line">*(*unsafe.Pointer)(addr) = v.ptr</span><br><span class="line">off += n</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><ol type="1"><li><p>通过 <a href="https://draveness.me/golang/tree/reflect.funcLayout"><code>reflect.funcLayout</code></a> 计算当前函数需要的参数和返回值的栈布局，也就是每一个参数和返回值所占的空间大小；</p></li><li><p>如果当前函数有返回值，需要为当前函数的参数和返回值分配一片内存空间 <code>args</code>；</p></li><li><p>如果当前函数是方法，需要向将方法的接收接收者者拷贝到 <code>args</code> 内存中；</p></li><li><p>将所有函数的参数按照顺序依次拷贝到对应<code>args</code>内存中</p><ol type="1"><li><p>使用<strong><code>reflect.funLayout</code>返回的参数</strong>计算参数在内存中的位置；</p><blockquote><p>这个返回的参数是什么意思鸭？</p></blockquote></li><li><p>将参数拷贝到内存空间中；</p></li></ol></li></ol><h4 id="调用函数">调用函数</h4><p>准备好调用函数需要的全部参数后，就会通过下面的代码执行函数指针了。函数入参是<strong>栈类型、函数指针、参数、和返回值的内存空间、栈的大小，以及返回值的偏移量：</strong></p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">call(frametype, fn, args, <span class="keyword">uint32</span>(frametype.size), <span class="keyword">uint32</span>(retOffset))</span><br></pre></td></tr></table></figure><p>上述函数实际上并不存在，他会在编译期间链接到<code>reflect.reflectcall</code>这个用汇编语言实现的函数上。</p><h4 id="处理返回值">处理返回值</h4><p>当函数调用结束以后，就会开始处理函数的返回值：</p><ul><li>如果函数没有任何返回值，会直接清空args中的全部内容来释放内存空间；</li><li>如果当前函数有返回值：<ol type="1"><li>将<code>args</code>中于输入参数有关的内存空间清空；</li><li>创建一个<code>nout</code>长度的切片用于保存由反射对象构成的返回值数组；</li><li>从函数对象中获取返回值的类型和内存大小，将<code>args</code>内存中的数据转换成<code>reflect.Value</code>类型并存储到切片中；</li></ol></li></ul><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">var</span> ret []Value</span><br><span class="line"><span class="keyword">if</span> nout == <span class="number">0</span> &#123; <span class="comment">//  如果函数没有返回值</span></span><br><span class="line">typedmemclr(frametype, args)   <span class="comment">// 清空args释放空间</span></span><br><span class="line">framePool.Put(args)</span><br><span class="line">&#125; <span class="keyword">else</span> &#123; <span class="comment">// 如果函数有返回值</span></span><br><span class="line">typedmemclrpartial(frametype, args, <span class="number">0</span>, retOffset) <span class="comment">// 将args中输入参数有关的内存空间清空</span></span><br><span class="line">ret = <span class="built_in">make</span>([]Value, nout)   <span class="comment">// 新建一个切片来保存返回值数组</span></span><br><span class="line">off = retOffset</span><br><span class="line"><span class="keyword">for</span> i := <span class="number">0</span>; i &lt; nout; i++ &#123; </span><br><span class="line">tv := t.Out(i)</span><br><span class="line">a := <span class="keyword">uintptr</span>(tv.Align())</span><br><span class="line">off = (off + a - <span class="number">1</span>) &amp;^ (a - <span class="number">1</span>)</span><br><span class="line"><span class="keyword">if</span> tv.Size() != <span class="number">0</span> &#123;</span><br><span class="line">fl := flagIndir | flag(tv.Kind())</span><br><span class="line">ret[i] = Value&#123;tv.common(), add(args, off, <span class="string">&quot;tv.Size() != 0&quot;</span>), fl&#125; <span class="comment">// 从函数对象中获取返回值的类型和内存大小，将 args 内存中的数据转换成 reflect.Value 类型并存储到切片中</span></span><br><span class="line">&#125; <span class="keyword">else</span> &#123;</span><br><span class="line">ret[i] = Zero(tv)</span><br><span class="line">&#125;</span><br><span class="line">off += tv.Size()</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">return</span> ret</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>由<code>reflect.Value</code>构成的ret数组会被返回到调用方，到此使用反射实现函数调用的过程就结束了，</p><h2 id="小结">小结</h2><p>Go 语言的 <a href="https://golang.org/pkg/reflect/"><code>reflect</code></a> 包为我们提供了多种能力，包括如何使用反射来动态修改变量、判断类型是否实现了某些接口以及动态调用方法等功能，通过分析反射包中方法的原理能帮助我们理解之前看起来比较怪异、令人困惑的现象。</p><h2 id="鸣谢">鸣谢</h2><p>https://draveness.me/golang/docs/part2-foundation/ch04-basic/golang-reflect</p>]]></content>
    
    
    <summary type="html">&lt;p&gt;写日志收集项目的时候遇到了一个关于&lt;code&gt;reflect&lt;/code&gt;的问题，于是有了这一篇文章。&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;本文主要介绍go语言中reflect的基本用法，以及在使用反射特性时遇到的一些坑。&lt;/strong&gt;&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;关键词：反射&lt;/strong&gt;&lt;/p&gt;</summary>
    
    
    
    
    <category term="golang" scheme="https://fly97.cn/tags/golang/"/>
    
  </entry>
  
  <entry>
    <title>golang LogAgent 项目实战（三）</title>
    <link href="https://fly97.cn/p/LogAgent-project-action-in-golang-3/"/>
    <id>https://fly97.cn/p/LogAgent-project-action-in-golang-3/</id>
    <published>2021-12-01T06:00:00.000Z</published>
    <updated>2021-12-01T06:00:00.000Z</updated>
    
    <content type="html"><![CDATA[<p>本文主要记录了学习golang时做的一个项目：日志收集项目。</p><p><strong>关键词：日志收集项目</strong></p><a id="more"></a><h2 id="架构图">架构图</h2><p><img src="image-20211130214306450.png" /></p><h3 id="logagent工作流程">LogAgent工作流程</h3><h4 id="读日志tailf第三方库">读日志（<code>tailf</code>第三方库）</h4><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">fileName := <span class="string">&quot;./my.log&quot;</span></span><br><span class="line">config := tail.Config&#123;</span><br><span class="line">ReOpen:    <span class="literal">true</span>,                                 <span class="comment">// 重新打开重新创建的文件(失败了会尝试重新读取)</span></span><br><span class="line">Follow:    <span class="literal">true</span>,                                 <span class="comment">// 继续寻找新行 (tail -f)</span></span><br><span class="line">Location:  &amp;tail.SeekInfo&#123;Offset: <span class="number">0</span>, Whence: <span class="number">2</span>&#125;, <span class="comment">// 从哪个地方开始读取</span></span><br><span class="line">MustExist: <span class="literal">false</span>,                                <span class="comment">// 日志文件是否必须存在</span></span><br><span class="line">Poll:      <span class="literal">true</span>,                                 <span class="comment">// 轮询文件更改而不是使用inotify</span></span><br><span class="line">&#125;</span><br><span class="line">tails, err := tail.TailFile(fileName, config)</span><br><span class="line"><span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">fmt.Printf(<span class="string">&quot;tail file failed, err: %v&quot;</span>, err)</span><br><span class="line"><span class="keyword">return</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">var</span> (</span><br><span class="line">line *tail.Line</span><br><span class="line">ok   <span class="keyword">bool</span></span><br><span class="line">)</span><br><span class="line"><span class="keyword">for</span> &#123;</span><br><span class="line">line, ok = &lt;-tails.Lines</span><br><span class="line"><span class="keyword">if</span> !ok &#123;</span><br><span class="line">fmt.Printf(<span class="string">&quot;tail file close reopen, finename :%s\n&quot;</span>, tails.Filename)</span><br><span class="line">time.Sleep(time.Second)</span><br><span class="line"><span class="keyword">continue</span></span><br><span class="line">&#125;</span><br><span class="line">fmt.Println(<span class="string">&quot;line: &quot;</span>, line.Text)</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><h4 id="往kafka里写日志">往Kafka里写日志</h4><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 基于sarama第三方库开发kafka client</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line"></span><br><span class="line">config := sarama.NewConfig()</span><br><span class="line"><span class="comment">// tailf包使用</span></span><br><span class="line">config.Producer.RequiredAcks = sarama.WaitForAll          <span class="comment">//发送完数据需要等待follow回复ack</span></span><br><span class="line">config.Producer.Partitioner = sarama.NewRandomPartitioner <span class="comment">// 先选出一个partition</span></span><br><span class="line">config.Producer.Return.Successes = <span class="literal">true</span>                   <span class="comment">// 成功交付的消息将在success channel中返回</span></span><br><span class="line"><span class="comment">// 构造一个消息</span></span><br><span class="line">msg := &amp;sarama.ProducerMessage&#123;&#125;</span><br><span class="line">msg.Topic = <span class="string">&quot;web_log&quot;</span></span><br><span class="line">msg.Value = sarama.StringEncoder(<span class="string">&quot;this is a test log&quot;</span>)</span><br><span class="line"><span class="comment">// 链接kafka</span></span><br><span class="line">client, err := sarama.NewSyncProducer([]<span class="keyword">string</span>&#123;<span class="string">&quot;192.168.31.103:9092&quot;</span>&#125;, config)</span><br><span class="line"><span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">fmt.Println(<span class="string">&quot;producer closed, err :&quot;</span>, err)</span><br><span class="line"><span class="keyword">return</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">defer</span> client.Close()</span><br><span class="line"><span class="comment">// 发送消息</span></span><br><span class="line">pid, offset, err := client.SendMessage(msg)</span><br><span class="line"><span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">fmt.Println(<span class="string">&quot;send msg failed, err:&quot;</span>, err)</span><br><span class="line"><span class="keyword">return</span></span><br><span class="line">&#125;</span><br><span class="line">fmt.Printf(<span class="string">&quot;pid: %v, offset:     %v\n&quot;</span>, pid, offset)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="kafka和zookeeper">Kafka和zookeeper</h4><h4 id="tail介绍">tail介绍</h4><h3 id="第一版程序">第一版程序</h3><p><a href="https://github.com/flyzstu/logAgent/commit/4bd7e5ba6ba138958ae8dd2b030a7d96810709f9">第一版代码</a></p><p><img src="image-20211201205643407.png" /></p><p>设计包的入口函数时，要考虑上面的设计模式。</p><h3 id="第二版程序">第二版程序</h3><p><a href="https://github.com/flyzstu/logAgent/commit/a70343ffd668c059b4aabdecbaf518a64b9e22fa">第二版代码</a></p><p>读取配置文件版本的logAgent</p><h4 id="go-ini库的使用">go-ini库的使用</h4><p><a href="https://github.com/go-ini/ini">go-ini</a></p>]]></content>
    
    
    <summary type="html">&lt;p&gt;本文主要记录了学习golang时做的一个项目：日志收集项目。&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;关键词：日志收集项目&lt;/strong&gt;&lt;/p&gt;</summary>
    
    
    
    
    <category term="golang" scheme="https://fly97.cn/tags/golang/"/>
    
  </entry>
  
  <entry>
    <title>golang LogAgent 项目实战（四）</title>
    <link href="https://fly97.cn/p/LogAgent-project-action-in-golang-4/"/>
    <id>https://fly97.cn/p/LogAgent-project-action-in-golang-4/</id>
    <published>2021-12-01T06:00:00.000Z</published>
    <updated>2021-12-01T06:00:00.000Z</updated>
    
    <content type="html"><![CDATA[<p>本文主要记录了学习golang时做的一个项目：日志收集项目。</p><p><strong>关键词：日志收集项目</strong></p><a id="more"></a><h2 id="日志收集项目">日志收集项目</h2><h3 id="上次进度">上次进度</h3><ol type="1"><li>kafka：消息队列</li><li>tailf：从文件里读取文件</li><li>go-ini：解析配置文件</li></ol><h3 id="为什么不用elk">为什么不用ELK？</h3><p>ELK：部署的时候比较麻烦，每一个filebeat都需要配置一个配置文件</p><p>使用etcd来管理被收集的日志项。</p><ul><li>每次logagent都会主动从etcd中拉取配置，免去了手动写配置项的麻烦；</li></ul><figure><img src="LogAgent%20项目实战（四）/image-20211130214306450.png" alt="image-20211130214306450" /><figcaption aria-hidden="true">image-20211130214306450</figcaption></figure><h2 id="etcd">etcd</h2><h3 id="etcd介绍">etcd介绍</h3><p>etcd是使用Go语言开发的一个开源的、高可用的分布式key-value存储系统，可以用于<strong>配置共享</strong>和<strong>服务注册发现</strong>。</p><p>类似的项目由zookeeper和consul。</p><p>etcd具有以下特点：</p><ul><li>完全复制：集群中的每个节点都可以使用完整的存档</li><li>高可用性：etcd可用于避免硬件的单点故障或者网络问题</li><li>一致性：每次读取都会返回跨多主机的最新写入</li><li>简单：包括了一个定义良好、面向用户的API（gRPC）</li><li>安全：实现了带有可选的客户端证书身份验证的自动化TLS</li><li>快速：每秒10000次写入的基准速度</li><li>可靠：使用Raft算法实现了强一致、高可用的服务存储目录<ul><li><strong>Raft协议</strong><ol type="1"><li>选举</li><li>日志复制机制</li><li>异常处理（脑裂）</li><li>zookeeper的zad协议的区别</li></ol></li><li><strong>etcd的watch是怎么实现的？</strong><ol type="1"><li>etcd底层如何实现watch给客户端发通知的</li></ol></li></ul></li></ul><h3 id="etcd应用场景">etcd应用场景</h3><h4 id="服务注册发现">服务注册发现</h4><p>服务发现要解决的也是分布式系统常见的问题，即在同一个分布式集群中的进程或服务，要如何才能找到对方建立连接。</p><p>本质上说，服务发现就是想要了解集群中<strong>是否有进程在监听tcp或者udp端口，并且通过名字就可以查找和连接</strong>。</p><p><img src="LogAgent%20项目实战（四）/image-20211203193827699.png" /></p><h4 id="配置中心">配置中心</h4><p>将一些配置信息放到etcd上进行集中管理。</p><p>通常的应用场景：应用在启动的时候主动从etcd获取一次配置消息，同时，在etcd节点上注册一个Watcher并等待，以后每次配置有更新的时候，etcd都会实时通知订阅者，以此达到获取最新配置信息的目的。</p><h4 id="分布式锁">分布式锁</h4><p>因为etcd使用Raft算法保持了数据的强一致性，某次操作存储到集群中的值必然是全局一致的，所有很容易实现分布式锁。锁服务有两种使用方式：一是保持独占，二是保持时序。</p><ul><li><strong>保持独占即所有获取锁的用户最终只有一个可以得到</strong>。etcd提供了一套实现分布式锁原子操作CAS( CompareAndSwap)的API。通过设置<code>prevExist</code>值，可以保证在多个节点同时创建某个目录时，只有一个成功。创建成功的用户就可以认为是获得了锁。</li><li>控制时序，即所有获得锁的用户都会被安排执行。但是<strong>获得锁的顺序全局唯一，同时决定了执行顺序</strong>。etcd提供了一套API（自动创建有序键），对一个目录建值时指定为POST动作，这样etcd会自动在目录下生成一个当前最大的值为键，存储这个新的值（客户端编号）。同时还可以使用API按顺序列出当前目录下的键值。此时这些键的值就是客户的时序，而这些键存储的值可以是代表客户端的编号。</li></ul><p><img src="LogAgent%20项目实战（四）/image-20211203195339151.png" /></p><h3 id="为什么用etcd而不用zookeeper">为什么用etcd而不用zookeeper？</h3><p>etcd实现的这些功能，zookeeper都能实现，那为什么要用etcd而不是直接使用zookeeper呢？</p><p>相较之下，zookeeper有以下缺点：</p><ol type="1"><li>复杂。zookeeper的<strong>部署维护复杂</strong>，管理员需要掌握一系列的知识和技能；Paxos强一致性<strong>算法素来以复杂难懂而闻名于世</strong>；zookeeper的使用也比较复杂，需要安装客户端，而且只提供了<strong>Java和C语言</strong>的两种接口。</li><li>Java编写。Java本身偏向于<strong>重型应用</strong>，引入大量的依赖。而运维人员则普遍希望保持强一致、高可用的机器集群尽可能简单，维护起来不容易出错。</li><li><strong>发展缓慢</strong>。Apache基金会特有的"Apache Way"在开源界饱受争议，其中一大原因就是由于基金会庞大的结构以及松散的管理导致项目发展缓慢。</li></ol><p>etcd作为一个后起之秀，其优点也很明显：</p><ol type="1"><li>简单。使用go语言编写<strong>部署简单</strong>；使用HTTP作为<strong>接口使用简单</strong>；<strong>使用Raft算法保证强一致性便于用户理解。</strong></li><li><strong>数据持久化</strong>。etcd默认数据已更新就进行持久化。</li><li><strong>安全</strong>。etcd支持SSL客户端安全验证。</li></ol><p>etcd作为一个年轻的项目，既是一个优点也是一个缺点。优点是未来无限可能，缺点是无法的发哦大项目长时间使用的检验。</p><p>目前CoreOS、Kubernetes和CloudFoundry等知名项目均在生产环境使用了etcd，etcd值得去尝试。</p><h3 id="etcd架构">etcd架构</h3><p><img src="LogAgent%20项目实战（四）/image-20211203201212153.png" /></p><figure><img src="LogAgent%20项目实战（四）/image-20211203201343566.png" alt="image-20211203201343566" /><figcaption aria-hidden="true">image-20211203201343566</figcaption></figure><h3 id="etcd集群">etcd集群</h3><p>etcd作为一个高可以键值存储系统，天生就是为集群化而设计的。由于Raft算法在做决策时需要多数节点的投票，所有etcd一般部署集群推荐奇数节点，推荐3，5，7个节点构成一个集群。</p><h4 id="搭建一个3节点集群实例">搭建一个3节点集群实例</h4><p><img src="LogAgent%20项目实战（四）/image-20211203201817418.png" /></p><p><img src="LogAgent%20项目实战（四）/image-20211203201903787.png" /></p><p><img src="LogAgent%20项目实战（四）/image-20211203202157587.png" /></p><p><img src="LogAgent%20项目实战（四）/image-20211203202209008.png" /></p><h3 id="etcd服务端">etcd服务端</h3><h4 id="文档">文档</h4><p>https://doczhcn.gitbook.io/etcd/index/index-1/configuration</p><h4 id="允许外网访问">允许外网访问</h4><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">export</span> ETCD_LISTEN_CLIENT_URLS=<span class="string">&quot;http://0.0.0.0:2379&quot;</span></span><br><span class="line"><span class="built_in">export</span> ETCD_ADVERTISE_CLIENT_URLS=<span class="string">&quot;http://0.0.0.0:2379&quot;</span></span><br></pre></td></tr></table></figure><h3 id="etcd客户端">etcd客户端</h3><p>官方客户端：https://github.com/etcd-io/etcd</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">go get go.etcd.io/etcd/client/v3</span><br></pre></td></tr></table></figure><h4 id="实例">实例</h4><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">package</span> main</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> (</span><br><span class="line"><span class="string">&quot;context&quot;</span></span><br><span class="line"><span class="string">&quot;fmt&quot;</span></span><br><span class="line"><span class="string">&quot;time&quot;</span></span><br><span class="line"></span><br><span class="line">clientv3 <span class="string">&quot;go.etcd.io/etcd/client/v3&quot;</span></span><br><span class="line">)</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line"><span class="keyword">var</span> (</span><br><span class="line">config clientv3.Config</span><br><span class="line">client *clientv3.Client</span><br><span class="line">err    error</span><br><span class="line">)</span><br><span class="line"></span><br><span class="line"><span class="comment">// 客户端配置</span></span><br><span class="line">config = clientv3.Config&#123;</span><br><span class="line">Endpoints:   []<span class="keyword">string</span>&#123;<span class="string">&quot;192.168.31.103:2379&quot;</span>&#125;,</span><br><span class="line">DialTimeout: <span class="number">5</span> * time.Second,</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 建立连接</span></span><br><span class="line">client, err = clientv3.New(config)</span><br><span class="line"><span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">fmt.Println(err)</span><br><span class="line"><span class="keyword">return</span></span><br><span class="line">&#125;</span><br><span class="line">fmt.Println(<span class="string">&quot;connent to etcd succeed&quot;</span>)</span><br><span class="line"><span class="keyword">defer</span> client.Close()</span><br><span class="line"></span><br><span class="line"><span class="comment">// 存</span></span><br><span class="line">ctx, cancel := context.WithTimeout(context.Background(), time.Second)</span><br><span class="line">_, err = client.Put(ctx, <span class="string">&quot;q1mi&quot;</span>, <span class="string">&quot;dsb&quot;</span>)</span><br><span class="line">cancel() <span class="comment">// 立即释放资源</span></span><br><span class="line"><span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line"><span class="built_in">panic</span>(err)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 取</span></span><br><span class="line">ctx, cancel = context.WithTimeout(context.Background(), time.Second)</span><br><span class="line">resp, err := client.Get(ctx, <span class="string">&quot;q1mi&quot;</span>)</span><br><span class="line">cancel() <span class="comment">// 立即释放资源</span></span><br><span class="line"><span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line"><span class="built_in">panic</span>(err)</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">for</span> _, kv := <span class="keyword">range</span> resp.Kvs &#123;</span><br><span class="line">fmt.Printf(<span class="string">&quot;key:%s value:%s\n&quot;</span>, <span class="keyword">string</span>(kv.Key), <span class="keyword">string</span>(kv.Value)) <span class="comment">// 索引键值对</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 哨兵：监视键的变化</span></span><br><span class="line">rch := client.Watch(context.Background(), <span class="string">&quot;q1mi&quot;</span>) <span class="comment">// &lt;-chan watchResponse</span></span><br><span class="line"><span class="keyword">for</span> wresp := <span class="keyword">range</span> rch &#123;</span><br><span class="line"><span class="keyword">for</span> _, kv := <span class="keyword">range</span> wresp.Events &#123;</span><br><span class="line">fmt.Printf(<span class="string">&quot;Type:%s Key:%s Value:%s\n&quot;</span>, kv.Type, kv.Kv.Key, kv.Kv.Value)</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">$ ./etcd.exe</span><br><span class="line">connent to etcd succeed</span><br><span class="line">key:q1mi value:dsb</span><br><span class="line">Type:PUT Key:q1mi Value:111 <span class="comment"># 得到了键的变化</span></span><br></pre></td></tr></table></figure><h2 id="使用etcd优化项目代码">使用etcd优化项目代码</h2><figure><img src="LogAgent%20项目实战（四）/image-20211205131214769.png" alt="image-20211205131214769" /><figcaption aria-hidden="true">image-20211205131214769</figcaption></figure><figure><img src="LogAgent%20项目实战（四）/image-20211205131939536.png" alt="image-20211205131939536" /><figcaption aria-hidden="true">image-20211205131939536</figcaption></figure><figure><img src="LogAgent%20项目实战（四）/image-20211205134219893.png" alt="image-20211205134219893" /><figcaption aria-hidden="true">image-20211205134219893</figcaption></figure><figure><img src="LogAgent%20项目实战（四）/image-20211205153501064.png" alt="image-20211205153501064" /><figcaption aria-hidden="true">image-20211205153501064</figcaption></figure>]]></content>
    
    
    <summary type="html">&lt;p&gt;本文主要记录了学习golang时做的一个项目：日志收集项目。&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;关键词：日志收集项目&lt;/strong&gt;&lt;/p&gt;</summary>
    
    
    
    
    <category term="golang" scheme="https://fly97.cn/tags/golang/"/>
    
  </entry>
  
  <entry>
    <title>golang LogAgent 项目实战（二）</title>
    <link href="https://fly97.cn/p/LogAgent-project-action-in-golang-2/"/>
    <id>https://fly97.cn/p/LogAgent-project-action-in-golang-2/</id>
    <published>2021-11-30T14:54:48.000Z</published>
    <updated>2021-11-30T14:54:48.000Z</updated>
    
    <content type="html"><![CDATA[<p>本文主要记录了学习golang时做的一个项目：日志收集项目。</p><p><strong>关键词：日志收集项目</strong></p><a id="more"></a><h2 id="消息队列的通信模型">消息队列的通信模型</h2><h3 id="点对点模式">点对点模式</h3><p>消息生产者生产消息发送到queue里，然后消息消费者从queue中取出并且消费消息。一条消息被消费以后，queue中就没有了，不存在重复消费。</p><h3 id="发布订阅模式">发布订阅模式</h3><p>消息发布者（发布）将消息发布到topic中，同时有多个消费消息者（订阅）消费该消息。</p><p>和点对点方式不同，发布到topic的消息被所有订阅者消费（类似于关注了微信公众号的人都能收到推送的文章）。</p><p>补充：发布订阅模式下，放发布者消息量很大时，显然单个订阅者的处理能力是不足的，实际上现实场景中是<strong>多个订阅者节点组成一个订阅组</strong>负载均衡消费topic消息即<strong>分组订阅</strong>，这样订阅者很容易实现消费能力线性扩展。</p><p>对于不同的消息订阅者，可以看作是一个topic下有多个Queue，每个Queue是点对点的方式，Queue之间是发布订阅方式。</p><h2 id="kafka">kafka</h2><p>Apache Kafka由著名社交公司LinkdIn开发， 最初是被设计用来解决Linkedin公司内部海量日志传输等问题。</p><p>kafka实验Scala语言编写，于2011年开源并进入Apache孵化器，于2012年10月正式毕业，现为Apach顶级项目。</p><h3 id="介绍">介绍</h3><p>Kafka是一个分布式数据流平台，可以运行在单个服务器上，也可以在多台服务器上部署行程集群。它提供了发布和订阅功能，使用者可以发送数据到Kafka，也可以从Kafka中读取数据（以便进行后续的处理）。</p><p>Kafka 具有高吞吐、低延迟、高容错等特点。</p><p><img src="LogAgent%20项目实战（二）/image-20211201141121142.png" /></p><ul><li><strong>Producer</strong>：Producer即生产者，消息的生产者，是消息的入口。</li><li><strong>kafka cluster</strong>：kafka集群，一台或者多台服务器组成<ul><li><strong>Broker</strong>：Broker是指部署了Kafka实例的服务器节点。每个服务器上由一个或者多个Kafka的实例。姑且将每个broker对应一台服务器，每个Kafka集群里的broker都有一个不重复的编号，如图中的broker-0、broker-1等。</li><li><strong>Topic</strong>：消息的主题，可以理解为消息的分类，kafka的数据就保存在topic。每个broker上都可以创建多个topic。实际应用中通常是一个业务线创建一个topic。</li><li><strong>Partition</strong>：Topic的分区，每个topic可以由多个分区，分区的作用是做负载，提高kafka的吞吐量。同一个topic在不同的分区的数据是不重复的，partition的表现形式就是一个个的文件夹。</li><li><strong>Replication</strong>：每一个分区在不同的Broker上都有副本，副本的作用是做备胎。当主分区（Leader）宕机时会重新选择一个备胎（Follower）上位，成为Leader。在Kafka中绝对副本的最大数量是10个，且副本的数量不能大于Broker的数量，follower和leader<strong>绝对是在不同的机器</strong>，<strong>同一机器对同一个分区也只能存放一个副本</strong>（包括自己）。</li></ul></li></ul><h3 id="工作流程">工作流程</h3><p>Producer是生产者，是数据的入口，Producer在写入数据的时候会把数据写入到<strong>leader</strong>中，不会将数据直接写入follower！</p><p><img src="LogAgent%20项目实战（二）/image-20211201144116325.png" /></p><ol type="1"><li>生产者从Kafka集群中获取分区leader信息</li><li>生产者将消息发送给leader</li><li>leader将消息写入本地磁盘</li><li>follower从leader拉取数据 ：<strong>生产者把数据写入Leader之后，Leader其他的副本主动从Leader中拉取数据</strong></li><li>follower将消息写入本地磁盘以后向leader发送ACK（回复确认）</li><li>leader收到所有的follower的ACK后向生产者发送ACK（回复确认）</li></ol><h3 id="选择partition的原则">选择Partition的原则</h3><p>如果某个topic有多个partition，producer怎么知道应该将数据发给那个partition呢？</p><p>Kafka中有以下几个原则：</p><ul><li>partition在写入的时候可以指定写入的partition，如果有指定，则写入对应的partition；</li><li>如果在写入的时候没有指定partition，但是又设置了数据的key，则会根据key的值hash出一个partition；</li><li>如果既没有指定partition，有没有设置key，则会采用轮询方式，即每次取一小段时间的数据写入某个partition，下一小段的时间写入下一个partition。</li></ul><h3 id="ack应答机制">ACK应答机制</h3><p>producer在Kafka写入消息的时候，可以设置参数来确定是否确认kafka接收到数据，这个参数可设置为<strong>0、1、all</strong>。</p><ul><li>0代表producer往集群发送数据不需要等到集群返回，不确保消息发送成功，安全性最低但是效率最高；</li><li>1代表producer往集群发送数据只要leader应答就可以发送下一条，只确保leader发送成功。</li><li>all代表producer往集群发送数据需要所有的follower都完成从leader的同步才会发送下一条，确保leader发送成功和所有的副本都完成备份。安全性最高，但是效率最低。</li></ul><p><strong>注意：若往不存在的topic写数据，kafka会自动创建topic，partition和replication的数量的默认配置都是1。</strong></p><h3 id="topic和数据日志">Topic和数据日志</h3><p><code>topic</code>是统一类别的消息记录（record）的集合。 在Kafka中，一个主题通常有多个订阅者，对于每个主题，kafka集群维护了一个分区数据日志文件结构如下：</p><p><img src="LogAgent 项目实战（二）/image-20211201152943721.png" style="zoom: 33%;" /></p><p>每个partition都是一个有序而且不可变的消息记录集合，当新的数据写入时，就被追加到partition的末尾。在每个partition中，每条消息都会被分配一个顺序的唯一标识，这个表示被称为offset，<strong>即偏移量</strong> 。</p><ul><li><strong>Kafka只保证在同一个partition内部消息是有序的，在不同partition之间，不能保证消息有序。</strong></li></ul><p>顺序读取，所以读取更快。</p><p>Kafka可以配置一个保留期限，用来标识日志会在Kafka集群中保留多长时间，Kafka集群回保留在保留期限内所以被发布的消息，不管这些消息是否被消费过。比如保留期限设置为两天，那么数据被发布到Kafka两天以内，所有的数据都可以被消费。当超过两天，这些数据都会被清空，一边为后续的数据腾出空间。由于kafka会将数据进行持久化存储（即写入到硬盘上），所以保留的数据大小可以设置为一个比较大的值。</p><h3 id="partition结构">Partition结构</h3><p>Partition在服务器上的表现形式就是一个一个的文件夹，每个partition的文件夹下会有多组segment文件，每组segment文件又包含<code>.index</code>文件，<code>.log</code>文件和<code>.timeindex</code>文件三个文件，其中<code>.log</code>文件是实际存储message的地方，而<code>.index</code>和<code>.timeindex</code>文件为索引文件，用于检索消息。</p><h3 id="消费数据">消费数据</h3><p>多个消费者实例可以组成一个消费者组，并用一个便签来标识这个消费者组。一个消费者组的不同消费者实例可以运行在不同的进程甚至不同的个服务器上。</p><ul><li>如果所有的消费者实例都在同一个消费者组中，那么消息记录会被很好的均衡发送到每个消费者实例。</li><li>如果所有的消费者实例都在不同的消费者组，那么每一条纤细记录会被广播到每一个消费者实例。</li></ul><p><img src="LogAgent%20项目实战（二）/image-20211201154851552.png" /></p><ul><li>同一消费者组中，每个消费者实例可以消费多个分区；</li><li>每个分区最多只能被消费者组中的一个实例消费；</li></ul><h3 id="总结关键点">总结关键点</h3><ol type="1"><li>Kafka集群的架构<ol type="1"><li>broker</li><li>topic</li><li>partition：分区，把同一个topic分成不同的分区，提高负载<ol type="1"><li>leader：分区的主节点</li><li>follower：分区的从节点</li></ol></li><li>Consumer Group：消费组</li></ol></li><li>生产者往Kafka发送数据的流程（6步）</li><li>选择分区的模式（三种）<ol type="1"><li>指定分区写</li><li>指定key，kafka根据key做hash决定写分区</li><li>轮询方式</li></ol></li><li>生产者往kafka发送数据的模式<ol type="1"><li>0：把数据发哦是那个给leader就成功，效率最高，安全性最低</li><li>1：把数据发送给leader，等待leader回ack</li><li>all：把数据发送给leader，follow从leader拉取数据成功后b回复ack给leader，leader收到所有的follower都回复ack给leader以后，生产者才会发送下一条数据</li></ol></li><li>分区存储文件的原理</li><li>为什么Kafka快</li><li>消费者组消费数据的原理<strong>（参考官方文档）</strong></li></ol><h2 id="zookeeper">Zookeeper</h2><p>zookeeper是一个分布式的，开放源码的分布式应用程序协调服务，是Google的Chubby的一个开源实现，是集群的管理者，监视着集群中各个系欸但的状态，并根据节点提交的反馈进行下一步合理操作。最终将简单易用的接口和性能高效、功能稳定的系统提供给用户。</p><p><img src="LogAgent%20项目实战（二）/image-20211201190723113.png" /></p>]]></content>
    
    
    <summary type="html">&lt;p&gt;本文主要记录了学习golang时做的一个项目：日志收集项目。&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;关键词：日志收集项目&lt;/strong&gt;&lt;/p&gt;</summary>
    
    
    
    
    <category term="golang" scheme="https://fly97.cn/tags/golang/"/>
    
  </entry>
  
  <entry>
    <title>golang LogAgent 项目实战（一）</title>
    <link href="https://fly97.cn/p/LogAgent-project-action-in-golang-1/"/>
    <id>https://fly97.cn/p/LogAgent-project-action-in-golang-1/</id>
    <published>2021-11-29T14:54:48.000Z</published>
    <updated>2021-11-29T14:54:48.000Z</updated>
    
    <content type="html"><![CDATA[<p>本文主要记录了学习golang时做的一个项目：日志收集项目。</p><p><strong>关键词：日志收集项目</strong></p><a id="more"></a><h2 id="项目背景">项目背景</h2><p>每个业务系统都有日志，当系统出现问题时， 需要通过日志信息来定位和解决问题。当系统机器比较少时，登录到服务器上查看即可满足；当系统机器规模巨大，登录到服务器上查看几乎不现实（分布式系统，一个系统部署在是十几台服务器上）</p><h2 id="解决方案">解决方案</h2><p>把机器上的日志<strong>实时收集</strong>，<strong>统一存储</strong>到中心系统。再对这些日志<strong>建立索引</strong>，通过搜索即可<strong>快速找到</strong>对应的日志记录。</p><p>通过提供一个<strong>界面友好</strong>的web页面实现日志的展示和检索。</p><h2 id="面临的问题">面临的问题</h2><p>实时日志量非常大，每条处理几十亿条，日志准实时收集，延迟控制在分钟级别。</p><p>系统的架构设计能够支持<strong>水平扩展</strong>。</p><h2 id="业界方案">业界方案</h2><h3 id="elk">ELK</h3><p><img src="image-20211130213650745.png" /></p><p><img src="image-20211130213933474.png" /></p><h3 id="elk方案的问题">ELK方案的问题</h3><ul><li>运维成本高，每增加一个日志收集项，都需要手动配置；</li><li>监控（Beats，现在有了）缺失，无法准确获取<strong>logstash</strong>的状态；</li><li>无法做到定制化开发和维护。</li></ul><h2 id="日志收集系统架构设计">日志收集系统架构设计</h2><p><img src="image-20211130214306450.png" /></p><h3 id="优势">优势</h3><ul><li>基于etcd实现了配置的自动发现</li></ul><h3 id="组件介绍">组件介绍</h3><ul><li>LogAgent：日志收集客户端，用来收集服务器上的日志</li><li>Kafka：高吞吐量的分布式队列（Linkin开源，Apache顶级开源项目）</li><li>ElasticSearch：开源的搜索引擎，提供基于HTTP RESTful的web接口</li><li>Kibaa：开源的ES数据引起分析和可视化工具</li><li>Hadoop：分布式计算框架，能够对大量数据进行分布式处理的平台</li><li>Storm：一个免费并开源的分布式实时计算系统</li></ul><h3 id="将学到的技能">将学到的技能</h3><ul><li>服务端Agent开发</li><li>后端服务组件开发</li><li>ES和Kibana的使用</li><li>Kafka和zookeeper的使用</li><li>etcd的使用</li></ul>]]></content>
    
    
    <summary type="html">&lt;p&gt;本文主要记录了学习golang时做的一个项目：日志收集项目。&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;关键词：日志收集项目&lt;/strong&gt;&lt;/p&gt;</summary>
    
    
    
    
    <category term="golang" scheme="https://fly97.cn/tags/golang/"/>
    
  </entry>
  
  <entry>
    <title>Ubuntu配置jdk-12环境</title>
    <link href="https://fly97.cn/p/install-jdk12-on-ubuntu2004/"/>
    <id>https://fly97.cn/p/install-jdk12-on-ubuntu2004/</id>
    <published>2021-11-29T14:54:48.000Z</published>
    <updated>2021-11-29T14:54:48.000Z</updated>
    
    <content type="html"><![CDATA[<p>本文介绍如何在Ubuntu 20.04 LTS系统上配置jdk开发环境。</p><p><strong>关键词：jdk，ubuntu，linux</strong></p><a id="more"></a><h3 id="下载jdk">下载jdk</h3><p>下载相应版本的jdk，这里以jdk-12为例：</p><p>https://www.oracle.com/java/technologies/javase/jdk12-archive-downloads.html</p><p><img src="image-20211130153041694.png" /></p><p>此处需要登录Oracle账号。</p><p>根据自己的系统类型选择即可，这里选择打包好的<strong>二进制文件.tar.gz</strong>。</p><p>下载完上传到服务器即可。</p><h3 id="安装jdk">安装jdk</h3><h4 id="解压文件">解压文件</h4><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">tar xvfz jdk-12.0.2_linux-x64_bin.tar.gz</span><br></pre></td></tr></table></figure><p>移动到目录</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">sudo mv jdk-12.0.2 /usr/<span class="built_in">local</span>/</span><br></pre></td></tr></table></figure><h4 id="设置环境变量">设置环境变量</h4><p>设置环境变量一般有三种：</p><ul><li><code>/etc/profile</code>：对全部用户生效，对所有shell解释器生效；</li><li><code>/etc/bash.bashrc</code>：对全部用户生效，对bash解释器生效；</li><li><code>/home/&lt;user_name&gt;/.bashrc</code>：仅对当前用户名生效，对bash解释器生效。</li></ul><p>这里选择第二种。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">export</span> JAVA_HOME=/usr/<span class="built_in">local</span>/jdk-12.0.2</span><br><span class="line"><span class="built_in">export</span> PATH=<span class="variable">$JAVA_HOME</span>/bin:<span class="variable">$PATH</span></span><br><span class="line"><span class="comment"># export CLASSPATH=.:$JAVA_HOME/lib/dt.jar:$JAVA_HOME/lib/tools.jar</span></span><br></pre></td></tr></table></figure><h3 id="检查jdk是否安装成功">检查jdk是否安装成功</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">java -version</span><br></pre></td></tr></table></figure><p><img src="image-20211130155722694.png" /></p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">javac -version</span><br></pre></td></tr></table></figure><p><img src="image-20211130155802514.png" /></p>]]></content>
    
    
    <summary type="html">&lt;p&gt;本文介绍如何在Ubuntu 20.04 LTS系统上配置jdk开发环境。&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;关键词：jdk，ubuntu，linux&lt;/strong&gt;&lt;/p&gt;</summary>
    
    
    
    
    <category term="linux" scheme="https://fly97.cn/tags/linux/"/>
    
    <category term="jdk" scheme="https://fly97.cn/tags/jdk/"/>
    
  </entry>
  
  <entry>
    <title>Ubuntu安装kafka和zookeeper并设置开机自动启动</title>
    <link href="https://fly97.cn/p/install-kafka-on-ubuntu2004/"/>
    <id>https://fly97.cn/p/install-kafka-on-ubuntu2004/</id>
    <published>2021-11-29T12:18:00.000Z</published>
    <updated>2021-11-29T12:18:00.000Z</updated>
    
    <content type="html"><![CDATA[<p>kafka是一个分布式消息队列。具有高性能、持久化、多副本备份、横向扩展能力。生产者往队列里写消息，消费者从队列里取消息进行业务逻辑。一般在架构设计中起到解耦、削峰、异步处理的作用。kafka对外使用topic的概念，生产者往topic里写消息，消费者从读消息。为了做到水平扩展，一个topic实际是由多个partition组成的，遇到瓶颈时，可以通过增加partition的数量来进行横向扩容。单个parition内是保证消息有序。每新写一条消息，kafka就是在对应的文件append写，所以性能非常高。</p><p>本文介绍如何在ubuntu上安装kafka并设置开机自动启动。</p><p><strong>关键词：kafka，ubuntu，linux</strong></p><a id="more"></a><h3 id="安装kafka">安装kafka</h3><h4 id="下载">下载</h4><p>下载地址：https://kafka.apache.org/downloads</p><p>这里以 <strong><a href="https://www.apache.org/dyn/closer.cgi?path=/kafka/3.0.0/kafka_2.12-3.0.0.tgz">kafka_2.12-3.0.0.tgz</a> </strong>为例。</p><h4 id="解压">解压</h4><p>下载完毕以后上传到服务器并将其解压：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">tar -xvzf kafka_2.12-3.0.0.tgz</span><br></pre></td></tr></table></figure><h4 id="修改配置">修改配置</h4><p>默认配置 <code>advertised.listeners=PLAINTEXT://your.host.name:9092</code>修改为</p><p><code>advertised.listeners=PLAINTEXT://&lt;ip&gt;:9092</code></p><p>其中ip是服务器ip，hostname和端口是用来建议给生产者和消费者使用的，如果没有设置，将会使用<strong>listener</strong>的设置；</p><p>如果<strong>listener</strong>也没有设置，将使用<code>java.net.InetAddress.getCanonicalHostName()</code>来获取hostname和port，通常这个获取的值就是<code>localhost</code>.</p><p>"PLAINTEXT"表示协议，可选的值有PLAINTEXT和SSL，hostname可以指定IP地址，<strong>不可以用"0.0.0.0"</strong>，一般用<strong>公网ip</strong>或者<strong>局域网内的ip</strong>。</p><p>如果hostname为空表示只对默认的网络接口(localhost)有效；如果没有配置advertised.listeners，就使用listeners的配置通告给消息的生产者和消费者，这个过程是在生产者和消费者获取源数据(metadata)。</p><h4 id="其他配置">其他配置</h4><p>见文末。</p><h3 id="运行kafka">运行kafka</h3><h4 id="安装jdk">安装JDK</h4><p>运行kafka之前还需要安装JDK，具体可参考笔者的这篇文章：</p><p><a href="https://fly97.cn/p/install-jdk12-on-ubuntu2004/">Ubuntu配置jdk-12环境</a></p><h4 id="运行zookeeper">运行zookeeper</h4><p>运行kafka之前需要先打开zookeeper，否则会报错。</p><p><img src="image-20211130195707407.png" /></p><p>可以在zookeeper.properties修改存放数据的文件地址，默认是<code>/tmp/zookeeper</code>。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">cd</span> kafka_2.12-3.0.0</span><br><span class="line">./bin/zookeeper-server-start.sh config/zookeeper.properties</span><br></pre></td></tr></table></figure><p>运行成功：</p><p><img src="image-20211130195517170.png" /></p><h4 id="运行kafka-1">运行kafka</h4><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">cd</span> kafka_2.12-3.0.0</span><br><span class="line">./bin/kafka-server-start.sh config/server.properties</span><br></pre></td></tr></table></figure><p>运行成功：</p><p><img src="image-20211130195908228.png" /></p><h3 id="设置自动启动">设置自动启动</h3><h4 id="编写启动脚本">编写启动脚本</h4><p>将上述两个命令封装成bash脚本：</p><ul><li>init-zookeeper.sh：</li></ul><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#!/bin/sh</span></span><br><span class="line"><span class="built_in">trap</span> <span class="string">&quot;echo &#x27;Zookeeper Stopped.&#x27;; exit 0;&quot;</span> INT TERM</span><br><span class="line"><span class="built_in">export</span> JAVA_HOME=/usr/<span class="built_in">local</span>/jdk-12.0.2</span><br><span class="line"><span class="built_in">export</span> PATH=<span class="variable">$JAVA_HOME</span>/bin:<span class="variable">$PATH</span></span><br><span class="line"><span class="built_in">echo</span> <span class="string">&quot;Init Zookeeper Service&quot;</span></span><br><span class="line"><span class="built_in">cd</span> /home/wf09/kafka</span><br><span class="line">./bin/zookeeper-server-start.sh config/zookeeper.properties</span><br><span class="line"><span class="built_in">exit</span> 255</span><br></pre></td></tr></table></figure><ul><li>init-kafka.sh</li></ul><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#!/bin/sh</span></span><br><span class="line"><span class="built_in">trap</span> <span class="string">&quot;echo &#x27;Kafka Stopped.&#x27;; exit 0;&quot;</span> INT TERM</span><br><span class="line"><span class="built_in">export</span> JAVA_HOME=/usr/<span class="built_in">local</span>/jdk-12.0.2</span><br><span class="line"><span class="built_in">export</span> PATH=<span class="variable">$JAVA_HOME</span>/bin:<span class="variable">$PATH</span></span><br><span class="line"><span class="built_in">echo</span> <span class="string">&quot;Init Kafka Service&quot;</span></span><br><span class="line"><span class="built_in">cd</span> /home/wf09/kafka</span><br><span class="line">./bin/kafka-server-start.sh config/server.properties</span><br><span class="line"><span class="built_in">exit</span> 255</span><br></pre></td></tr></table></figure><h4 id="编写服务文件">编写服务文件</h4><ul><li>zerokeeper.service</li></ul><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line">[Unit]</span><br><span class="line">Description=zerokeeper-service</span><br><span class="line">After=network.target</span><br><span class="line"></span><br><span class="line">[Service]</span><br><span class="line"><span class="comment">#KillMode=mixed</span></span><br><span class="line">User=wf09</span><br><span class="line">Type=simple</span><br><span class="line">ExecStart=/bin/bash /home/wf09/init-zookeeper.sh</span><br><span class="line">ExecStop=/bin/<span class="built_in">kill</span> -s SIGTERM <span class="variable">$MAINPID</span></span><br><span class="line"></span><br><span class="line">[Install]</span><br><span class="line">WantedBy=multi-user.target</span><br></pre></td></tr></table></figure><ul><li>kafka.service</li></ul><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line">[Unit]</span><br><span class="line">Description=Kafka-service</span><br><span class="line">After=network.target</span><br><span class="line">Requires=zookeeper.service</span><br><span class="line"></span><br><span class="line">[Service]</span><br><span class="line"><span class="comment">#KillMode=mixed</span></span><br><span class="line">User=wf09</span><br><span class="line">Type=simple</span><br><span class="line">ExecStart=/bin/bash /home/wf09/init-kafka.sh</span><br><span class="line">ExecStop=/bin/<span class="built_in">kill</span> -s SIGTERM <span class="variable">$MAINPID</span></span><br><span class="line"></span><br><span class="line">[Install]</span><br><span class="line">WantedBy=multi-user.target</span><br></pre></td></tr></table></figure><h4 id="设置自动启动-1">设置自动启动</h4><p>设置自动启动可以参考笔者的这一篇文章</p><p><a href="https://fly97.cn/p/How-to-manage-services-on-ubuntu/">Ubuntu服务管理</a></p><h3 id="kafka其他配置">kafka其他配置</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br><span class="line">84</span><br><span class="line">85</span><br><span class="line">86</span><br><span class="line">87</span><br><span class="line">88</span><br><span class="line">89</span><br><span class="line">90</span><br><span class="line">91</span><br><span class="line">92</span><br><span class="line">93</span><br><span class="line">94</span><br><span class="line">95</span><br><span class="line">96</span><br><span class="line">97</span><br><span class="line">98</span><br><span class="line">99</span><br><span class="line">100</span><br><span class="line">101</span><br><span class="line">102</span><br><span class="line">103</span><br><span class="line">104</span><br><span class="line">105</span><br><span class="line">106</span><br><span class="line">107</span><br><span class="line">108</span><br><span class="line">109</span><br><span class="line">110</span><br><span class="line">111</span><br><span class="line">112</span><br><span class="line">113</span><br><span class="line">114</span><br><span class="line">115</span><br><span class="line">116</span><br><span class="line">117</span><br><span class="line">118</span><br><span class="line">119</span><br><span class="line">120</span><br><span class="line">121</span><br><span class="line">122</span><br><span class="line">123</span><br><span class="line">124</span><br><span class="line">125</span><br><span class="line">126</span><br><span class="line">127</span><br><span class="line">128</span><br><span class="line">129</span><br><span class="line">130</span><br><span class="line">131</span><br><span class="line">132</span><br><span class="line">133</span><br><span class="line">134</span><br><span class="line">135</span><br><span class="line">136</span><br><span class="line">137</span><br><span class="line">138</span><br><span class="line">139</span><br><span class="line">140</span><br><span class="line">141</span><br><span class="line">142</span><br><span class="line">143</span><br><span class="line">144</span><br><span class="line">145</span><br><span class="line">146</span><br><span class="line">147</span><br><span class="line">148</span><br><span class="line">149</span><br><span class="line">150</span><br><span class="line">151</span><br><span class="line">152</span><br><span class="line">153</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># Licensed to the Apache Software Foundation (ASF) under one or more</span></span><br><span class="line"><span class="comment"># contributor license agreements.  See the NOTICE file distributed with</span></span><br><span class="line"><span class="comment"># this work for additional information regarding copyright ownership.</span></span><br><span class="line"><span class="comment"># The ASF licenses this file to You under the Apache License, Version 2.0</span></span><br><span class="line"><span class="comment"># (the &quot;License&quot;); you may not use this file except in compliance with</span></span><br><span class="line"><span class="comment"># the License.  You may obtain a copy of the License at</span></span><br><span class="line"><span class="comment">#</span></span><br><span class="line"><span class="comment">#    http://www.apache.org/licenses/LICENSE-2.0</span></span><br><span class="line"><span class="comment">#</span></span><br><span class="line"><span class="comment"># Unless required by applicable law or agreed to in writing, software</span></span><br><span class="line"><span class="comment"># distributed under the License is distributed on an &quot;AS IS&quot; BASIS,</span></span><br><span class="line"><span class="comment"># WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.</span></span><br><span class="line"><span class="comment"># See the License for the specific language governing permissions and</span></span><br><span class="line"><span class="comment"># limitations under the License.</span></span><br><span class="line"><span class="comment"># see kafka.server.KafkaConfig for additional details and defaults</span></span><br><span class="line"></span><br><span class="line"><span class="comment">############################# Server Basics #############################</span></span><br><span class="line"></span><br><span class="line"><span class="comment">##################################################################################</span></span><br><span class="line"><span class="comment">#  broker就是一个kafka的部署实例，在一个kafka集群中，每一台kafka都要有一个broker.id</span></span><br><span class="line"><span class="comment">#  并且，该id唯一，且必须为整数</span></span><br><span class="line"><span class="comment">##################################################################################</span></span><br><span class="line">broker.id=10</span><br><span class="line"></span><br><span class="line"><span class="comment">############################# Socket Server Settings #############################</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># The address the socket server listens on. It will get the value returned from </span></span><br><span class="line"><span class="comment"># java.net.InetAddress.getCanonicalHostName() if not configured.</span></span><br><span class="line"><span class="comment">#   FORMAT:</span></span><br><span class="line"><span class="comment">#     listeners = security_protocol://host_name:port</span></span><br><span class="line"><span class="comment">#   EXAMPLE:</span></span><br><span class="line"><span class="comment">#     listeners = PLAINTEXT://your.host.name:9092</span></span><br><span class="line"><span class="comment">#listeners=PLAINTEXT://:9092</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Hostname and port the broker will advertise to producers and consumers. If not set, </span></span><br><span class="line"><span class="comment"># it uses the value for &quot;listeners&quot; if configured.  Otherwise, it will use the value</span></span><br><span class="line"><span class="comment"># returned from java.net.InetAddress.getCanonicalHostName().</span></span><br><span class="line"><span class="comment">#advertised.listeners=PLAINTEXT://your.host.name:9092</span></span><br><span class="line"></span><br><span class="line"><span class="comment">##################################################################################</span></span><br><span class="line"><span class="comment">#The number of threads handling network requests</span></span><br><span class="line"><span class="comment"># 默认处理网络请求的线程个数 3个</span></span><br><span class="line"><span class="comment">##################################################################################</span></span><br><span class="line">num.network.threads=3</span><br><span class="line"><span class="comment">##################################################################################</span></span><br><span class="line"><span class="comment"># The number of threads doing disk I/O</span></span><br><span class="line"><span class="comment"># 执行磁盘IO操作的默认线程个数 8</span></span><br><span class="line"><span class="comment">##################################################################################</span></span><br><span class="line">num.io.threads=8</span><br><span class="line"></span><br><span class="line"><span class="comment">##################################################################################</span></span><br><span class="line"><span class="comment"># The send buffer (SO_SNDBUF) used by the socket server</span></span><br><span class="line"><span class="comment"># socket服务使用的进行发送数据的缓冲区大小，默认100kb</span></span><br><span class="line"><span class="comment">##################################################################################</span></span><br><span class="line">socket.send.buffer.bytes=102400</span><br><span class="line"></span><br><span class="line"><span class="comment">##################################################################################</span></span><br><span class="line"><span class="comment"># The receive buffer (SO_SNDBUF) used by the socket server</span></span><br><span class="line"><span class="comment"># socket服务使用的进行接受数据的缓冲区大小，默认100kb</span></span><br><span class="line"><span class="comment">##################################################################################</span></span><br><span class="line">socket.receive.buffer.bytes=102400</span><br><span class="line"></span><br><span class="line"><span class="comment">##################################################################################</span></span><br><span class="line"><span class="comment"># The maximum size of a request that the socket server will accept (protection against OOM)</span></span><br><span class="line"><span class="comment"># socket服务所能够接受的最大的请求量，防止出现OOM(Out of memory)内存溢出，默认值为：100m</span></span><br><span class="line"><span class="comment"># （应该是socker server所能接受的一个请求的最大大小，默认为100M）</span></span><br><span class="line"><span class="comment">##################################################################################</span></span><br><span class="line">socket.request.max.bytes=104857600</span><br><span class="line"></span><br><span class="line"><span class="comment">############################# Log Basics （数据相关部分，kafka的数据称为log）#############################</span></span><br><span class="line"></span><br><span class="line"><span class="comment">##################################################################################</span></span><br><span class="line"><span class="comment"># A comma seperated list of directories under which to store log files</span></span><br><span class="line"><span class="comment"># 一个用逗号分隔的目录列表，用于存储kafka接受到的数据</span></span><br><span class="line"><span class="comment">##################################################################################</span></span><br><span class="line">log.dirs=/home/uplooking/data/kafka</span><br><span class="line"></span><br><span class="line"><span class="comment">##################################################################################</span></span><br><span class="line"><span class="comment"># The default number of log partitions per topic. More partitions allow greater</span></span><br><span class="line"><span class="comment"># parallelism for consumption, but this will also result in more files across</span></span><br><span class="line"><span class="comment"># the brokers.</span></span><br><span class="line"><span class="comment"># 每一个topic所对应的log的partition分区数目，默认1个。更多的partition数目会提高消费</span></span><br><span class="line"><span class="comment"># 并行度，但是也会导致在kafka集群中有更多的文件进行传输</span></span><br><span class="line"><span class="comment"># （partition就是分布式存储，相当于是把一份数据分开几份来进行存储，即划分块、划分分区的意思）</span></span><br><span class="line"><span class="comment">##################################################################################</span></span><br><span class="line">num.partitions=1</span><br><span class="line"></span><br><span class="line"><span class="comment">##################################################################################</span></span><br><span class="line"><span class="comment"># The number of threads per data directory to be used for log recovery at startup and flushing at shutdown.</span></span><br><span class="line"><span class="comment"># This value is recommended to be increased for installations with data dirs located in RAID array.</span></span><br><span class="line"><span class="comment"># 每一个数据目录用于在启动kafka时恢复数据和在关闭时刷新数据的线程个数。如果kafka数据存储在磁盘阵列中</span></span><br><span class="line"><span class="comment"># 建议此值可以调整更大。</span></span><br><span class="line"><span class="comment">##################################################################################</span></span><br><span class="line">num.recovery.threads.per.data.dir=1</span><br><span class="line"></span><br><span class="line"><span class="comment">############################# Log Flush Policy （数据刷新策略）#############################</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Messages are immediately written to the filesystem but by default we only fsync() to sync</span></span><br><span class="line"><span class="comment"># the OS cache lazily. The following configurations control the flush of data to disk.</span></span><br><span class="line"><span class="comment"># There are a few important trade-offs（平衡） here:</span></span><br><span class="line"><span class="comment">#    1. Durability 持久性: Unflushed data may be lost if you are not using replication.</span></span><br><span class="line"><span class="comment">#    2. Latency 延时性: Very large flush intervals may lead to latency spikes when the flush does occur as there will be a lot of data to flush.</span></span><br><span class="line"><span class="comment">#    3. Throughput 吞吐量: The flush is generally the most expensive operation, and a small flush interval may lead to exceessive seeks.</span></span><br><span class="line"><span class="comment"># The settings below allow one to configure the flush policy to flush data after a period of time or</span></span><br><span class="line"><span class="comment"># every N messages (or both). This can be done globally and overridden on a per-topic basis.</span></span><br><span class="line"><span class="comment"># kafka中只有基于消息条数和时间间隔数来制定数据刷新策略，而没有大小的选项，这两个选项可以选择配置一个</span></span><br><span class="line"><span class="comment"># 当然也可以两个都配置，默认情况下两个都配置，配置如下。</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># The number of messages to accept before forcing a flush of data to disk</span></span><br><span class="line"><span class="comment"># 消息刷新到磁盘中的消息条数阈值</span></span><br><span class="line"><span class="comment">#log.flush.interval.messages=10000</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># The maximum amount of time a message can sit in a log before we force a flush</span></span><br><span class="line"><span class="comment"># 消息刷新到磁盘生成一个log数据文件的时间间隔</span></span><br><span class="line"><span class="comment">#log.flush.interval.ms=1000</span></span><br><span class="line"></span><br><span class="line"><span class="comment">############################# Log Retention Policy（数据保留策略） #############################</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># The following configurations control the disposal（清理） of log segments（分片）. The policy can</span></span><br><span class="line"><span class="comment"># be set to delete segments after a period of time, or after a given size has accumulated（累积）.</span></span><br><span class="line"><span class="comment"># A segment will be deleted whenever（无论什么时间） *either* of these criteria（标准） are met. Deletion always happens</span></span><br><span class="line"><span class="comment"># from the end of the log.</span></span><br><span class="line"><span class="comment"># 下面的配置用于控制数据片段的清理，只要满足其中一个策略（基于时间或基于大小），分片就会被删除</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># The minimum age of a log file to be eligible for deletion</span></span><br><span class="line"><span class="comment"># 基于时间的策略，删除日志数据的时间，默认保存7天</span></span><br><span class="line">log.retention.hours=168</span><br><span class="line"></span><br><span class="line"><span class="comment"># A size-based retention policy for logs. Segments are pruned from the log as long as the remaining</span></span><br><span class="line"><span class="comment"># segments don&#x27;t drop below log.retention.bytes. 1G</span></span><br><span class="line"><span class="comment"># 基于大小的策略，1G</span></span><br><span class="line"><span class="comment">#log.retention.bytes=1073741824</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># The maximum size of a log segment file. When this size is reached a new log segment will be created.</span></span><br><span class="line"><span class="comment"># 数据分片策略</span></span><br><span class="line">log.segment.bytes=1073741824</span><br><span class="line"></span><br><span class="line"><span class="comment"># The interval at which log segments are checked to see if they can be deleted according</span></span><br><span class="line"><span class="comment"># to the retention policies 5分钟</span></span><br><span class="line"><span class="comment"># 每隔多长时间检测数据是否达到删除条件</span></span><br><span class="line">log.retention.check.interval.ms=300000</span><br><span class="line"></span><br><span class="line"><span class="comment">############################# Zookeeper #############################</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Zookeeper connection string (see zookeeper docs for details).</span></span><br><span class="line"><span class="comment"># This is a comma separated host:port pairs, each corresponding to a zk</span></span><br><span class="line"><span class="comment"># server. e.g. &quot;127.0.0.1:3000,127.0.0.1:3001,127.0.0.1:3002&quot;.</span></span><br><span class="line"><span class="comment"># You can also append an optional chroot string to the urls to specify the</span></span><br><span class="line"><span class="comment"># root directory for all kafka znodes.</span></span><br><span class="line">zookeeper.connect=uplooking01:2181,uplooking02:2181,uplooking03:2181</span><br><span class="line"></span><br><span class="line"><span class="comment"># Timeout in ms for connecting to zookeeper</span></span><br><span class="line">zookeeper.connection.timeout.ms=6000</span><br></pre></td></tr></table></figure><h3 id="参考链接">参考链接</h3><ul><li>https://www.cnblogs.com/toutou/p/linux_install_kafka.html</li></ul>]]></content>
    
    
    <summary type="html">&lt;p&gt;kafka是一个分布式消息队列。具有高性能、持久化、多副本备份、横向扩展能力。生产者往队列里写消息，消费者从队列里取消息进行业务逻辑。一般在架构设计中起到解耦、削峰、异步处理的作用。kafka对外使用topic的概念，生产者往topic里写消息，消费者从读消息。为了做到水平扩展，一个topic实际是由多个partition组成的，遇到瓶颈时，可以通过增加partition的数量来进行横向扩容。单个parition内是保证消息有序。每新写一条消息，kafka就是在对应的文件append写，所以性能非常高。&lt;/p&gt;
&lt;p&gt;本文介绍如何在ubuntu上安装kafka并设置开机自动启动。&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;关键词：kafka，ubuntu，linux&lt;/strong&gt;&lt;/p&gt;</summary>
    
    
    
    
    <category term="linux" scheme="https://fly97.cn/tags/linux/"/>
    
    <category term="kafka" scheme="https://fly97.cn/tags/kafka/"/>
    
  </entry>
  
  <entry>
    <title>golang中的生产者消费者模型</title>
    <link href="https://fly97.cn/p/producer-consumer-model-in-golang/"/>
    <id>https://fly97.cn/p/producer-consumer-model-in-golang/</id>
    <published>2021-11-28T14:54:48.000Z</published>
    <updated>2021-11-28T14:54:48.000Z</updated>
    
    <content type="html"><![CDATA[<p>使用goroutine和channel构建生产者消费者模型，指定启动的goroutine数量。</p><p><strong>关键词</strong>：生产者消费者模型，golang</p><a id="more"></a><p>Go语言使用并发很方便，但是goroutine并不是越多越好。通常使用<code>workpool</code>模式，控制<code>goroutine</code>的数量，防止goroutine泄露和暴涨。</p><h2 id="简易生产者模型">简易生产者模型</h2><p>一个简易的<strong>生产者消费者模型</strong>代码如下：</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">worker</span><span class="params">(id <span class="keyword">int</span>, jobs &lt;-<span class="keyword">chan</span> <span class="keyword">int</span>, results <span class="keyword">chan</span>&lt;- <span class="keyword">int</span>)</span></span> &#123; <span class="comment">// jobs:只读通道，result:只写通道</span></span><br><span class="line">    <span class="keyword">for</span> j := <span class="keyword">range</span> jobs &#123;</span><br><span class="line">        fmt.Printf(<span class="string">&quot;worker:%d start jobs:%d\n&quot;</span>, id, j)</span><br><span class="line">        time.Sleep(time.Second)</span><br><span class="line">        fmt.Printf(<span class="string">&quot;worker:%d end jobs:%d\n&quot;</span>, id, j)</span><br><span class="line">        results &lt;- j * <span class="number">2</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">    jobs := <span class="built_in">make</span>(<span class="keyword">chan</span> <span class="keyword">int</span>, <span class="number">100</span>)</span><br><span class="line">    results := <span class="built_in">make</span>(<span class="keyword">chan</span> <span class="keyword">int</span>, <span class="number">100</span>)</span><br><span class="line">    <span class="comment">// 开启3个goroutine</span></span><br><span class="line">    <span class="keyword">for</span> w := <span class="number">1</span>; w &lt; <span class="number">3</span>; w++ &#123;</span><br><span class="line">        <span class="keyword">go</span> worker(w, jobs, results)</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 分配5个任务</span></span><br><span class="line">    <span class="keyword">for</span> j := <span class="number">1</span>; j &lt;= <span class="number">5</span>; j++ &#123;</span><br><span class="line">        jobs &lt;- j</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">close</span>(jobs) <span class="comment">// 关闭通道</span></span><br><span class="line">    <span class="comment">// 输出结果</span></span><br><span class="line">    <span class="keyword">for</span> a := <span class="number">1</span>; a &lt;= <span class="number">5</span>; a++ &#123;</span><br><span class="line">        &lt;-results <span class="comment">// 取出的过程是阻塞的，要等待每一个worker执行完</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
    
    
    <summary type="html">&lt;p&gt;使用goroutine和channel构建生产者消费者模型，指定启动的goroutine数量。&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;关键词&lt;/strong&gt;：生产者消费者模型，golang&lt;/p&gt;</summary>
    
    
    
    
    <category term="golang" scheme="https://fly97.cn/tags/golang/"/>
    
  </entry>
  
</feed>
